# Agent Handoff

## 当前目标

只修复 Phase 4A Evaluator 复核残留 HIGH-03：exchange 回滚预检必须识别
`trading_calendar_versions` 下游依赖。本单本地完成并提交，不部署；等待 hosted CI
额度恢复后再执行 CI、镜像、Deploy、E2E，并由独立 Evaluator 对该单项终验。

## 当前阶段

Phase 4A 尚未 PASS。修复候选已形成，生产仍运行此前验收候选；不得启动 Phase 4B。

## 已完成

- 修复提交 `23e679db3b7fa3a384e764235e6ea3066d18766f` 已在本地创建，提交信息为
  `fix(high-03): include calendar version dependency in exchange precheck`。
- `projection_dependencies()` 的 `exchange` 分支新增
  `trading_calendar_versions` 查询，按 workspace/exchange 精确匹配并使用 `for update`。
- 新增 PostgreSQL 集成测试，构造首次创建 exchange 的 v2 批次及后续日历版本依赖，
  断言预检只返回 `downstream_dependency / trading_calendar_version` 冲突。
- CI validate job 新增 PostgreSQL service 和显式 ignored-test 执行步骤；正常 workspace
  测试仍可在无 PostgreSQL 的本机运行。
- Phase 4A E2E 新增“首次创建 exchange → 后续日历版本引用 → 预检拒绝 → enqueue
  409 → 两张正式表指纹零变更”的场景；随后按公开回滚 API 先回滚日历批次、再回滚
  catalog 批次，正式夹具归零，仅保留不可变审计链。
- futures VPS 的 self-hosted runner 在空闲时把 systemd `MemoryMax` 从 256 MiB 调整为
  384 MiB，已执行 daemon-reload 和服务重启；实态 `active/running`，生效值
  `402653184` bytes。`docs/DEPLOYMENT.md` 已记录。

### Phase 4A 正式投影 FK 全量对照

| 父投影 | 子表 FK | 预检依赖类型 | 结果 |
| --- | --- | --- | --- |
| exchanges | instruments.exchange_id | instrument | 已覆盖 |
| exchanges | trading_calendar_versions.exchange_id | trading_calendar_version | 本次补齐 |
| instruments | contracts.instrument_id | contract | 已覆盖 |
| trading_calendar_versions | trading_calendar_days.calendar_version_id | trading_calendar_day | 已覆盖 |
| trading_calendar_versions | market_prices.calendar_version_id | market_price | 已覆盖 |
| contracts | market_prices.contract_id | market_price | 已覆盖 |
| contracts | seat_positions.contract_id | seat_position | 已覆盖 |
| seat_entities | seat_positions.seat_id | seat_position | 已覆盖 |

迁移全量检索未发现第二条正式投影反向依赖遗漏。指向 workspace、user、data_source、
import_batch、imported_record 等非正式投影父表的 FK 不属于该回滚依赖图。

## 正在进行

- 编写并提交本交接和 `LATEST.md`；完成后推送两个本地提交。

## 尚未完成

- hosted CI 因计费额度限制尚未执行；新 PostgreSQL 集成测试尚未获得真实 PostgreSQL
  运行结果。
- container-images、Deploy 和 Phase 4A E2E 尚未对本候选执行，本单明确不部署。
- 独立 Evaluator 对 HIGH-03 的单项终验尚未执行。
- 不得在上述链路完成前宣告 Phase 4A PASS，不得启动 Phase 4B。

## 关键决策

- 修复范围严格限于 HIGH-03：一条遗漏依赖边、对应 PostgreSQL/E2E 回归、CI 测试
  执行条件，以及用户授权的 runner 内存上限调整。
- E2E 使用两个完整 change-log v2 夹具批次，确保 dependency 不是由同一批次创建；
  冲突验证后通过现有 Worker 回滚链清除正式夹具，不修改既有手动导入流程。
- CI 和镜像继续使用 GitHub-hosted runner；1 GB VPS 仅运行 deploy job，不承担编译。

## 修改过的文件

- `.github/workflows/ci.yml`
- `rust/crates/database/src/imports.rs`
- `rust/tests/phase_4a_e2e.sh`
- `docs/DEPLOYMENT.md`
- `docs/handoffs/HANDOFF_20260804_1527.md`
- `docs/handoffs/LATEST.md`

## 未提交修改

创建本交接时仅 handoff 与 `LATEST.md` 待单独 docs 提交；业务修复文件已提交为
`23e679d`。

## Git 分支与提交

- 分支：`phase/04-akshare-collection`。
- 本单基线：`c866ff047efe05a06970ca10020a34a3c4cd1b78`。
- 修复候选：`23e679db3b7fa3a384e764235e6ea3066d18766f`。
- 创建交接时本地比 origin ahead 1；推送后必须核对 origin 包含修复与 docs 交接提交。
- 不合并 main、不打标签。

## 本地测试结果

已运行并通过：

- `cargo +stable fmt --all -- --check`。
- `cargo +stable clippy --workspace --all-targets -- -D warnings`。
- `cargo +stable test --workspace`：135 passed，0 failed，1 ignored；ignored 项为新
  PostgreSQL 集成用例。
- `cargo +stable test -p database --no-run`：新测试编译成功。
- `pnpm lint`；`pnpm test`：6 files / 18 tests passed；`pnpm build` 成功。
- 锁定依赖下 `ruff check`、`ruff format --check`：15 files passed。
- collector `pytest`：35 passed。
- `bash -n rust/tests/phase_4a_e2e.sh` 与 `git diff --check`。

未运行：

- 新 PostgreSQL 集成测试的实际数据库执行：本机没有 PostgreSQL/Docker；CI 已设置
  独立强制步骤，待 hosted 额度恢复后执行。
- Phase 4A E2E：需要新镜像和授权部署链，本单明确延后。

前端 test/build 首次在 Codex Windows 沙箱内因 esbuild 无权读取父目录失败；在正常
用户权限下原命令重跑通过，不是代码或测试失败。

## futures VPS 状态

- 已实际核对，未部署。
- API 运行版本仍为 `82cec44184ffb6ae4bf700afd0210193a081ad0a`。
- self-hosted runner 为 `active/running`，`MemoryMax=402653184` bytes（384 MiB）。
- runner 调整期间没有运行 job；未触发 workflow、Deploy 或 E2E。

## 数据库迁移状态

- 本单没有新增或执行迁移，也没有连接或修改生产数据库。
- 生产迁移状态沿用上一份已验证交接至 `202608030003`；本单未重新取证。

## 已知问题和风险

- GitHub-hosted Actions 计费拒绝仍是发布链外部阻塞；不得把 0-step 平台拒绝写成
  测试失败，也不得把 CI/镜像编译迁到 VPS。
- runner 新上限 384 MiB 尚未经历下一次 Deploy/E2E 叠加峰值；后续部署要记录
  peak、OOM 与 cgroup 限流事件。
- E2E 新场景已通过 shell 语法检查但尚未在生产 Compose/数据库上执行。

## 下一步第一条操作

hosted CI 额度恢复后，先核对修复候选 HEAD 的 CI validate 实际执行并成功，尤其是
`PostgreSQL rollback dependency integration test`；再按顺序执行镜像、Deploy、
Phase 4A E2E，最后交独立 Evaluator 单项终验。

## 新 AI 必读文件

- `AGENTS.md`
- `PLANS.md`
- `docs/DECISIONS.md`
- `docs/reviews/PHASE_04A_EVALUATION.md` 的复核章节
- `docs/handoffs/LATEST.md`
- 本交接全文
- `docs/DEPLOYMENT.md`

## 禁止重复或禁止修改事项

- 不得重复实现已关闭的 HIGH-01/02/04/05、MEDIUM-01~04、LOW-01。
- 不得修改原始设计方案范围、手动导入流程或清理 VPS 既有数据。
- 不得部署本单之外的代码，不得合并 main、打标签或启动 Phase 4B。
- 不得输出或记录任何密码、token、Cookie、主密钥或数据库明文凭据。
