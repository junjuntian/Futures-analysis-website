# Agent Handoff

## 当前目标

完成 Phase 3D 独立 Evaluator 报告 `docs/reviews/PHASE_03D_EVALUATION.md` 中钉死的 HIGH-01、HIGH-02、MEDIUM-01 至 MEDIUM-04、LOW-01，并走完本地回归、CI、三镜像发布和 futures VPS 部署验收。本 Generator 单不执行独立 Evaluator 复核，不合并 `main`，不打标签，不修改 `PLANS.md`。

## 当前阶段

Generator 缺陷修复与发布链验收已完成。代码候选为 `45ee8028647a1b8e4b8cda043e8012b4e281d739`，已部署到 futures VPS 并通过 Phase 3C E2E、Phase 3D E2E、Phase 3D schema invariants。下一阶段应由新的独立 Evaluator 复核，不能由本 Generator 自行宣告 Phase 3D PASS。

## 已完成

- HIGH-01：`bf5baab fix: keep compensation replays from leaking objects (HIGH-01)`
  - 补偿创建在临时上传阶段完成原批次可见性、允许状态和幂等决议。
  - 同参重放、同键异参、不可见原批次、不允许状态和并发同键均不会登记或遗留新对象。
  - VPS 证据：`compensation_replay_objects_before=13:13 after=13:13 rejection_and_concurrency_snapshot=14:14 unchanged=PASS`。
- HIGH-02：`1b4c06b fix: restore soft-deleted rows during rollback (HIGH-02)`；补充验收修正 `45ee802 test: count stale fingerprint in soft-delete conflict (HIGH-02)`。
  - 回滚预检接受受控 `soft_delete` snapshot；Worker 在 generation fence 和单事务内恢复 before snapshot。
  - VPS 成功恢复证据：`soft_delete_restore=30:<baseline-import-id>`。
  - VPS 冲突证据：`worker_conflict=worker_conflict:3 zero_business_change=PASS`；计数包含两个行级冲突和一个陈旧预检指纹冲突。
- MEDIUM-01：`f13b17d fix: bound compensation lineage depth (MEDIUM-01)`，两个递归 lineage 查询和补偿创建均执行 32 层上限，超限返回稳定错误。
- MEDIUM-02：`3aa200c test: prove PostgreSQL worker fairness (MEDIUM-02)`，VPS E2E 覆盖两个并发 Worker、持续跨 Workspace 入队、`SKIP LOCKED` 和不饥饿。
- MEDIUM-03：`5b67393 test: cover object governance fault matrix (MEDIUM-03)`，覆盖 rename 后数据库失败、commit 响应不确定、记录缺对象、哈希错误、过期临时文件和重复 scan；VPS 输出 `governance_physical_delete_count=0`。
- MEDIUM-04：`1c32ab4 test: cover import center recovery flows (MEDIUM-04)`，新增 5 个 `ImportCenterView` 组件级测试。
- LOW-01：`49fc930 docs: remove evaluator trailing whitespace (LOW-01)`，清除 Phase 3B/3C Evaluator 报告 6 处行尾双空格。
- DEC-026 发布兼容：`c87b26f ci: stop requiring one-time bootstrap token after initialization`。
  - 仅删除工作流对 bootstrap-token 的 legacy 重建调用，并从 required 列表移除该一次性文件。
  - `database-url`、`import-idempotency-pepper`、`master-key-v1`、`postgres-password` 仍强制存在且权限为 `0400`。
  - 未修改 Compose 的 `BOOTSTRAP_TOKEN_FILE` 配置，未生成、恢复或迁移任何 bootstrap token。
- 发布链：
  - 最终 CI Run `30688920855`：validate、API/Worker/Frontend 三镜像验证全部 success。
  - 最终 Container images Run `30689138347`：三镜像发布全部 success，`headSha` 精确匹配代码候选。
  - 最终 Deploy Run `30689392268`：success；部署、迁移、Phase 3C/3D、schema invariants、registry 凭据清理、运行 digest 核验和日志秘密扫描全部通过。

## 正在进行

无。Generator 范围内工作已完成并已部署。

## 尚未完成

- 尚未由独立 Evaluator 复核本轮修复；不得把 Generator 结果等同于 Evaluator PASS。
- 未合并 `main`，未打标签，未修改 `PLANS.md`。
- MEDIUM-05 的 VPS 127 个测试批次按用户裁定不属于本单，留待项目完工时生产库归零重置。

## 关键决策

- 唯一缺陷范围仍是 `docs/reviews/PHASE_03D_EVALUATION.md` 的 HIGH-01、HIGH-02、MEDIUM-01 至 MEDIUM-04、LOW-01；另有用户明确追加的 DEC-026 部署工作流两行兼容修改。
- HIGH-02 Worker 冲突计数为 3：目标行版本变化产生两个行级冲突，预检指纹变化再产生一个全局冲突。事务分支在任何目标写入前持久化冲突并返回。
- Phase 3D 已 commit 对象禁止物理删除；临时上传文件的 abort 清理不受该限制。
- VPS E2E 只操作 UUID 隔离 fixture，并在 PASS 前清理；部署后既有 `import_batches=127` 保持不变。
- 工作流不再参与一次性 bootstrap token 的生命周期；系统已初始化时该文件必须不存在。

## 未来生产归零重置的 bootstrap 流程

未来生产库归零重置时，bootstrap 流程固定为：人工在宿主机放置 token 文件（`root:root`、`0400`）→ 完成首次初始化 → 人工删除 token 文件。部署工作流不创建、不恢复、不迁移、不删除该 token，也不把它列为日常部署 required secret。Compose 中 `BOOTSTRAP_TOKEN_FILE` 保留，供未来重置流程人工放置文件时使用。

## 修改过的文件

- `.github/workflows/deploy-futures.yml`：按 DEC-026 停止日常部署重建和强制要求一次性 bootstrap token。
- `rust/apps/api/src/imports.rs`：补偿上传在对象 commit 前完成可见性、状态和幂等决议。
- `rust/crates/database/src/compensations.rs`：补偿上传事务准备/完成、幂等锁和 lineage 深度上限。
- `rust/crates/database/src/imports.rs`：`soft_delete` 回滚预检与受控 snapshot 校验。
- `rust/crates/database/src/rollback_jobs.rs`：Worker 原子恢复 `soft_delete` before snapshot。
- `rust/crates/domain/src/import.rs`：Phase 3D 稳定错误与 `soft_delete` 领域契约测试。
- `rust/crates/infrastructure/src/object_storage.rs`：对象 rename/登记结果不确定故障测试。
- `rust/tests/phase_3d_e2e.sh`：HIGH-01/HIGH-02、lineage、公平性、对象治理的 VPS 场景与明确证据输出。
- `frontend/src/views/ImportCenterView.test.ts`：确认面板、冲突分页和恢复流组件测试。
- `docs/reviews/PHASE_03B_EVALUATION.md`、`docs/reviews/PHASE_03C_EVALUATION.md`：清除行尾空白并保持 Markdown 渲染语义。
- `docs/handoffs/HANDOFF_20260801_1522.md`、`docs/handoffs/LATEST.md`：本次交接与最新指针。

## 未提交修改

交接提交完成后应无未提交修改。创建交接前 `git status` 为 clean，且本轮业务与验收提交均已推送至 `origin/phase/03-import-foundation`。

## Git 分支与提交

- 分支：`phase/03-import-foundation`。
- 交接前代码 HEAD：`45ee8028647a1b8e4b8cda043e8012b4e281d739`，与 `origin/phase/03-import-foundation` 一致。
- 缺陷提交顺序：`bf5baab`、`1b4c06b`、`f13b17d`、`3aa200c`、`5b67393`、`1c32ab4`、`49fc930`、`c87b26f`、`45ee802`。
- 本文档随后以独立 `docs: add agent handoff checkpoint` 提交，最终远端 HEAD 会是该 docs 提交；已部署代码候选仍为 `45ee802…`。
- `git diff --check 4155102..45ee802`、`git diff --check 6dfea78..45ee802` 和工作树检查均为 exit 0。
- `PLANS.md` 相对基线无 diff；没有 merge 或 tag 操作。

## 本地测试结果

已运行并通过：

- `rust/ cargo +stable fmt --check`：exit 0。
- `rust/ cargo +stable clippy --workspace --all-targets -- -D warnings`：exit 0。
- `rust/ cargo +stable test --workspace`：119 passed（20 + 12 + 61 + 13 + 8 + 5），0 failed。
- 根目录 `pnpm lint`：exit 0。
- 根目录 `pnpm test`：6 files、18 tests passed。
- 根目录 `pnpm build`：1468 modules transformed，build success；仅保留既有大 chunk warning。
- `bash -n rust/tests/phase_3d_e2e.sh`：exit 0。
- 全库 tracked trailing whitespace 扫描：0；候选 diff 秘密模式扫描：0。

已运行但失败后重跑通过：

- 一次沙箱内 `pnpm test` 在 Vitest 启动前因 Windows 上级目录读取被拒绝而失败，未执行测试；以受控权限原命令重跑后 18/18 通过，不是产品测试失败。
- Deploy Run `30687881017` 在服务切换前因旧工作流强制要求已销毁 bootstrap-token 而失败；无备份、迁移或数据变更。
- Deploy Run `30688601091` 在 HIGH-02 E2E 误期望冲突计数 2 时失败，工作流 `ROLLBACK_PASS`；修正为包含陈旧指纹冲突的 3 后，最终 Run `30689392268` 通过。

未运行：无验收清单遗漏。

## futures VPS 状态

已通过 MCP SSH 实际核对：

- 最新部署报告 `status=PASS`，Run `30689392268`，部署版本 `45ee8028647a1b8e4b8cda043e8012b4e281d739`。
- API digest：`sha256:e4b3dae73713759bae3cbf2acea6b42a5147c7d5fa968820b978c4eaad7fdacf`。
- Worker digest：`sha256:ae19760fed45fc4b4f64d381fca9f320b33bcfad24bdf78b1add4e22acb2d716`。
- Frontend digest：`sha256:ee15b17dc20bbdd667c961808c8c479d42d3c4ea01ce639b320e4e70b827078b`。
- `phase3c_e2e=PASS`、`phase3d_e2e=PASS`、`PHASE3D_SCHEMA_INVARIANTS_PASS`。
- `runtime_digest_match=PASS`、`registry_auth_cleanup=PASS`、`service_log_secret_scan=PASS`。
- 数据实态：`users=31`、`import_batches=127`；与本单部署前一致，未删除或修改既有测试批次。
- bootstrap-token 文件实态：`absent`，符合 DEC-026。

## 数据库迁移状态

- `202607260001_phase_3d_rollback_and_object_governance.sql` 已执行。
- `202607260002_phase_3d_worker_fairness.sql` 已执行。
- 最终部署报告记录 `migrations=202607260001,202607260002`，schema invariants 为 PASS。

## 已知问题和风险

- 独立 Evaluator 尚未复核，Phase 3D 最终结论仍待 Evaluator。
- `origin/main` 与 phase 分支的 CI 提交同名不同 hash、工作流内容存在分叉；后续 merge 必须显式审查 `.github/workflows` 的冲突和最终采用版本，不能按提交标题自动判同。
- MEDIUM-05 的 127 个 VPS 测试批次按用户裁定延后到项目完工的生产库归零重置；本单没有清理。
- 生产归零重置的 bootstrap 流程仍需人工安全操作，不能由日常部署工作流代办。

## 下一步第一条操作

由全新独立 Evaluator 读取 `docs/reviews/PHASE_03D_EVALUATION.md`、本交接和 Phase 3D 契约，对 `4155102..45ee802` 的修复及最终 VPS 证据进行只读复核；不要由 Generator 自评 PASS。

## 新 AI 必读文件

- 工作区与项目 `AGENTS.md`、`.agents/Evaluator.md`。
- `docs/handoffs/LATEST.md` 与本交接。
- `docs/reviews/PHASE_03D_EVALUATION.md`。
- `docs/phases/PHASE_03D_IMPORT_FINALIZATION.md`。
- `docs/phases/PHASE_03_IMPORT_FOUNDATION.md`。
- `docs/DECISIONS.md` 中 DEC-026、DEC-036。
- `PLANS.md` 仅用于读取上级边界，不得信任其过时状态陈述。

## 禁止重复或禁止修改事项

- 不得修改原始 Word 设计方案。
- 不得读取、输出、提交或记录任何密码、token、密钥、Cookie、数据库明文凭据或私钥。
- 不得重复实现或重跑已完成的 Generator 工作；Evaluator 只做独立取证复核。
- 未经新授权不得合并 `main`、打标签或修改 `PLANS.md`。
- 不得清理、改写或删除 VPS 现有 127 个测试批次；MEDIUM-05 已明确延后。
- 不得恢复、生成或由部署工作流管理 bootstrap token；未来归零重置严格采用人工放置 `0400 root` 文件、初始化、人工删除的流程。
