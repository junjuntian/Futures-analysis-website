# Agent Handoff

## 当前目标

Phase 3C 已完成实现、本地测试、`futures` VPS Docker/E2E 和独立 Evaluator 收口。下一会话不得直接进入 Phase 3D；如需继续，必须先由 Planner 获得并细化新的明确授权。

## 当前阶段

- Phase 3A：已完成并 PASS。
- Phase 3B：已完成并 PASS。
- Phase 3C：已完成并 PASS，`BLOCKER=0`、`HIGH=0`。
- Phase 3D：未授权、未实现。

## 已完成

- 数据校验、文件内重复、数据库业务唯一性和校验后并发冲突检测。
- `skip`、`overwrite`、`keep_conflict`、`abort` 四种策略。
- 确认冻结、Workspace 级幂等、唯一入队、通用 `imported_records` 正式入库和冲突候选保存。
- PostgreSQL `job_queue`、双 Worker `SKIP LOCKED`、30 秒租约/10 秒续租、`lease_generation` fence、重试和第五次 dead-letter。
- 持久化 SSE、`Last-Event-ID` 精确重放、非法/跨批次/跨 Workspace 拒绝。
- 五张 Phase 3C 新表的 RLS/FORCE RLS、最小授权和成功/失败/拒绝审计。
- 前端冲突策略选择、确认导入、进度/SSE 重连和终态展示。
- Phase 3B 五项 MEDIUM 已在 Phase 3C 范围内收口。
- 独立 Evaluator 最终 PASS；报告为 `docs/reviews/PHASE_03C_EVALUATION.md`。

## 正在进行

无业务实现正在进行。本 handoff 与计划状态更新将作为独立文档 checkpoint 提交。

## 尚未完成

- Phase 3D 未授权：cancel、人工 dead-letter replay、回滚、补偿批次、`import_row_changes`、冲突人工解决和完整导入前端均未实现。
- Evaluator 记录的 4 项非阻断 MEDIUM 尚未排期：
  1. SSE OpenAPI 逐帧 schema 与 `event_not_visible` 契约。
  2. Worker 跨 Workspace 公平轮询。
  3. 长 SSE 连接周期性重验会话/权限。
  4. 确认面板与断线重连的组件级交互测试。
- 生产 HTTPS、TLS 入口和 `AUTH_COOKIE_SECURE=true` 验证仍未完成。

## 关键决策

- Phase 3C 正式目标仅为导入域通用 `imported_records`，不代表行情或交易模型获授权。
- `keep_conflict` 只保存 `import_conflict_candidates`，不是绕过唯一约束的 `keep_both`。
- confirm 使用 Workspace + 哈希幂等键事务级 advisory lock，锁序为 advisory key → batch。
- Worker 长事务不锁 job/batch；提交边界按 job → batch 锁序并校验 owner、generation、lease 和冻结快照。
- 原始 `Idempotency-Key` 不入库；生产 pepper 来自 `/run/secrets/import-idempotency-pepper`。
- 不新增第三个迁移修补已执行的 009；VPS 已执行兼容 ALTER 补齐 `lease_generation`，仓库中新环境仍由 009 一次性创建最终结构。

## 修改过的文件

- 计划与部署：`.env.example`、`PLANS.md`、`docker-compose.yml`、`docs/phases/PHASE_03_IMPORT_FOUNDATION.md`。
- 评审：`docs/reviews/PHASE_03C_EVALUATION.md`。
- 前端：`frontend/src/api.ts`、`frontend/src/api.test.ts`、`frontend/src/views/ImportCenterView.vue`、`importPreviewState.ts` 及测试。
- API/Worker：`rust/apps/api/`、`rust/apps/worker/` 的 Phase 3C 路由、执行器和依赖。
- application/database/domain：校验、transform、确认、任务队列、租约 fence、审计和仓储实现。
- 迁移：`202607250008_phase_3c_validation_and_imported_records.sql`、`202607250009_phase_3c_job_queue_and_events.sql`。
- 测试：Phase 3B 两个回归脚本、`phase_3c_runtime_grants.sql`、`phase_3c_e2e.sh`。

## 未提交修改

创建本 handoff 时，Phase 3C 实现已提交为 `04011ed`；仅 `PLANS.md`、Phase 3 计划、本 handoff 和 `LATEST.md` 等收口文档等待 checkpoint 提交。checkpoint 后预期工作树干净，实际状态必须以 `git status --short` 为准。

## Git 分支与提交

- 分支：`phase/03-import-foundation`。
- Phase 3C 实现提交：`04011ed feat: complete phase 3c validation and async import`。
- 基线提交：`4cc17223bc22d4f124218a8576066bd9d44dc572`。
- 本 handoff 随后创建独立文档 checkpoint；实际最终 HEAD 和最近提交必须通过 `git log -5 --oneline` 核验。
- 仓库未配置 remote，未执行 push。

## 本地测试结果

已运行并通过：

- `cargo +stable fmt --all -- --check`。
- `cargo +stable test --workspace`：47 passed。
- `cargo +stable clippy --workspace --all-targets -- -D warnings`。
- `pnpm lint`。
- `pnpm test`：3 files / 5 tests passed。
- `pnpm build`：PASS，仅有既有的大 chunk warning。
- `git diff --check`。
- `bash -n rust/tests/phase_3c_e2e.sh`：由 Generator 与 VPS Bash 验证通过。

没有未解释的失败。本地 Windows 主 shell 没有 `bash` 命令，因此主 Agent 依赖 Generator 和 VPS 执行脚本语法门禁；完整脚本已在 VPS exit 0。

## futures VPS 状态

- 已实际核验，访问地址：`http://172.238.11.174:8088`。
- PostgreSQL、API、Worker、Frontend、Nginx 五个容器运行；API/PostgreSQL healthy，ready/live 返回 200。
- 最终业务源码归档 SHA-256：`65addb91e0cc7dff0852d3245c2e29d3341bac6549c7f1ae9e79098af5d1c417`。
- 最终 E2E 脚本 SHA-256：`434c291ec01f1fb2813b1c2c2477a4735a493c42351e85db0ab589b4f8988a00`。
- 最终 E2E：exit 0，输出 `PHASE3C_E2E_PASS`；证据目录 `/tmp/phase3c-e2e-559842`。
- 覆盖 75 行、20 路确认、幂等四组合/跨批次、12/12 冲突矩阵、双 Worker、续租、SIGKILL 恢复、generation fence、重试/dead-letter、SSE、5/5 RLS 读写、审计与秘密扫描。
- 根分区约 25G，尚余约 7.1G；临时 E2E trigger 为 0。
- 部署前备份：`/opt/futures-platform-backups/source-phase3b-before-phase3c-20260725-1502` 和 `/opt/futures-platform-backups/database-before-phase3c-20260725-1502.dump`。

## 数据库迁移状态

- Phase 3C 迁移 `202607250008`、`202607250009` 已在 `futures` VPS 执行并记录。
- 009 已执行后增加 `lease_generation` 的兼容 ALTER：`bigint NOT NULL DEFAULT 0`，非负约束有效，既有行无 NULL/负值。
- 五张新表 `imported_records`、`import_conflict_candidates`、`import_confirmations`、`job_queue`、`import_job_events` 均启用并强制 RLS。
- Phase 3B mapping SQL、模板冻结并发脚本和 Phase 3C runtime grants 脚本在最终 VPS 上通过。

## 已知问题和风险

- 4 项非阻断 MEDIUM 见“尚未完成”和 `docs/reviews/PHASE_03C_EVALUATION.md`；不得无授权擅自扩展处理。
- VPS 资源有限，根分区当前约 70% 使用率；后续冷构建前应先核验磁盘。
- E2E 使用随机隔离 Workspace 并保留不可变验收证据；不要通过禁用触发器、约束或删除审计证据来“清理”。
- 当前仅 HTTP 开发入口，不是生产 TLS 部署。

## 下一步第一条操作

先阅读本 handoff 和 Phase 3C 评审报告，核验 Git/VPS 实际状态；如用户要求进入 Phase 3D，先由 Planner 独立细化授权边界，不得直接开始 Generator 实现。

## 新 AI 必读文件

1. `AGENTS.md`
2. `PLANS.md`
3. `docs/DECISIONS.md`
4. `docs/handoffs/LATEST.md`
5. `docs/handoffs/HANDOFF_20260726_1040.md`
6. `docs/phases/PHASE_03_IMPORT_FOUNDATION.md`
7. `docs/reviews/PHASE_03C_EVALUATION.md`

## 禁止重复或禁止修改事项

- 不得修改原始 Word 设计方案作为实现捷径。
- 不得在文档、日志或提交中写入密码、Cookie、Token、API Key、pepper、私钥或数据库明文凭据。
- 不得重复已经通过的 Phase 3C 验收，除非代码、迁移或部署状态发生变化。
- 不得禁用 RLS、触发器、唯一约束或审计以通过测试。
- 未获 Phase 3D 授权前，不得实现 cancel、replay、回滚、补偿、`import_row_changes`、冲突人工解决或完整前端。
- 不得扩展到套利、交易、图表、OCR、AI 或外部网站采集。
