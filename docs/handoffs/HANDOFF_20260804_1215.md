# Agent Handoff

## 当前目标

完成 Phase 4A 独立 Evaluator 报告 `docs/reviews/PHASE_04A_EVALUATION.md`（`d30b3e3`）所列 HIGH-01～05、MEDIUM-01～04、LOW-01 的 Generator 缺陷修复、完整发布链和 futures VPS E2E；交单后由全新独立 Evaluator 复核，不在本单合并 main、打标签或启动 Phase 4B。

## 当前阶段

- Phase 4A 缺陷修复已实现并推送到 `phase/04-akshare-collection`。
- 固定业务运行候选为 `82cec44184ffb6ae4bf700afd0210193a081ad0a`；最终验收脚本/部署修订为分支 HEAD `579017bdfb15bc67112ea437b014ab94ab8ab2ae`。
- CI Run `30813165664`、Container images Run `30813606780`、Deploy Run `30873823206` 均为 `success`。
- 本文只记录 Generator 实证，不自行宣告独立 Evaluator PASS。

## 已完成

### 十项缺陷闭环

| 缺陷 | 主要修复提交 | Generator 侧闭环证据 |
| --- | --- | --- |
| HIGH-01 | `f884584` | 禁止自动重定向；逐跳 scheme/精确域名/DNS 地址/连接目标复核和跳数上限；覆盖私网、非白名单、多跳、DNS 前后不一致及“被禁目标未收到请求”。 |
| HIGH-02 | `e236539`、`6e8f2d8`、`7062cd3`、`6cd86cb`、`6750635` | 自动导入 business key 纳入受控来源身份；official/fallback 同键并存；优先视图按 `data_sources.priority` 选 official；相同值/不同值恢复场景 E2E 通过。 |
| HIGH-03 | `9f05029`、`d04fd1a`、`496a2cb`、`9c4cb95`、`730f26d`、`06c296e`、`d85f217` | Phase 4A 全投影进入版本化 change log/预检；新增迁移 `202608030003` 精确授予 8 张正式投影 DELETE；四类批次成功回滚、依赖冲突零变更及精确投影差集 E2E 通过。 |
| HIGH-04 | `baced5f`、`96b351f`、`846d83a` | 以 DCE catalog 闭合，失败整体隔离，不引入 partial；未发布与真正失败分流，结构化计数、有限重试和请求超时生效。最终真实采集曾实际触发完整性重试并成功，无 partial 提交。 |
| HIGH-05 | `442c1a5`、`f8b3f39`、`6750635` | collector/manual 双向隔离；automatic-confirm 总是执行服务端固定流水线；身份×mode×endpoint HTTP 拒绝矩阵 E2E 通过。 |
| MEDIUM-01 | `9584eef` | host 调度从受控交易日历解析最近交易日并显式传参；周末、法定休市与节后边界有测试。 |
| MEDIUM-02 | `29d3932`、`7062cd3` | `observed_at` 使用单次采集开始真实 UTC，全批一致并处于批次窗口内。 |
| MEDIUM-03 | `9f05029` | 目录投影受控 upsert，补全空字段，带 row_version、完整 before/after change log，并进入回滚链。 |
| MEDIUM-04 | `6750635`、`06c296e`、`d84eeed`、`579017b` | 五所×来源×数据集 exact expected-set、身份矩阵和回滚闭环；live replay 保存首轮业务键集合，要求全部保留且全表重复键为零，并审计上游新增键。最终实态 `market_new=0`、`seat_new=0`。 |
| LOW-01 | `2368c06` | compose 增加 `/work` 128 MiB tmpfs，collector 明确以其为临时根；512 MiB 容器限额下实测峰值 135,401,472 bytes。 |

### 发布链与 runner

- `28b9633`：部署 workflow 改用仓库级 self-hosted runner `[self-hosted, futures-vps]`；CI 和 container-images 仍使用 hosted runner。
- runner：官方 Actions Runner `2.336.0`，systemd 常驻，`MemoryMax=256M`；当前 active/running、重启 0、OOM 0。部署叠加期间峰值达到 256 MiB，最终 `memory.events max=105`、`sock_throttled=23`，应作为资源风险保留。
- `346d542`：严格分离 runtime SHA 与 acceptance SHA；只允许部署 workflow、Phase 4A E2E 和精确迁移 `202608030003` 出现在两 SHA 差异中。
- runtime CI：Run `30813165664` success，SHA `82cec44`。
- 四镜像：Run `30813606780` success，SHA `82cec44`。
- 最终部署：Run `30873823206` success，runtime `82cec44`，acceptance `579017b`。
- 早期失败 Run `30815709446`、`30820373258`、`30826679525`、`30867850495`、`30870331570` 均执行生产自动回滚并得到 `ROLLBACK_PASS`；最后两次分别暴露 numeric 字符串格式脆弱断言和 live 数据源在长时 E2E 中增长，已按证据修正验收断言。

### 最终 E2E

- `PHASE4A_E2E_BASELINE manual_batches=144 automatic_batches=90 users=32`。
- 五交易所四类数据集 first run 与 replay run 完成；DCE 真实使用 `akshare_sina_dce_fallback`，其余四所只使用官方来源。
- replay：`market_before=830 market_after=830 market_new=0 seat_before=17806 seat_after=17806 seat_new=0`；业务唯一键重复均为 0。
- `authorization_matrix_passed`、`catalog_upsert_passed`、`projection_rollback_passed`、`dce_source_recovery_passed`。
- 故障注入中 DCE 行情数据集 failed 隔离，SHFE/CZCE/GFEX/CFFEX 同轮 succeeded；正式行情计数保持不变。
- collector 峰值 `135401472` bytes，限额 `536870912` bytes。
- 最终标记 `PHASE4A_E2E_PASS`。

## 正在进行

- 无运行中的开发或部署操作。
- 等待全新独立 Evaluator 按 `docs/reviews/PHASE_04A_EVALUATION.md` 逐项复核。

## 尚未完成

- 独立 Evaluator 最终判定尚未执行。
- 不合并 main、不打标签、不启动 Phase 4B；这些均不属于本单。
- hosted CI 对 `82cec44` 之后仅含部署验收/迁移的提交仍在 GitHub 计费层 0 steps 失败；用户已明确裁定不充值。不得把该平台拒绝误写成测试失败，亦不得把 self-hosted VPS 用于编译。

## 关键决策

- DCE 继续 official-first，412 等失败自动回退到经审核 Sina 聚合源；fallback 真实标注 `akshare_sina_dce_fallback`，不得伪装 DCE official。
- official 和 fallback 同日同合约允许并存；不删除 fallback 历史，默认读取按 `data_sources.priority`（official 100、fallback 50）。
- DCE 必要数据完整性失败时整个数据集 failed；不引入 partial。
- runtime 镜像固定在经过 hosted CI/镜像发布的 `82cec44`；后续验收脚本和授权迁移通过受控 acceptance SHA 提供。
- live 数据源两次取样可能增长；幂等验收保存首轮完整业务键集合、要求逐键不丢失且全表重复键为零，同时记录新增键。最终成功 Run 中新增键为 0。

## 修改过的文件

- workflow/部署：`.github/workflows/deploy-futures.yml`、`deploy/collector/run-collector.sh`、`docker-compose.yml`。
- collector：`collector/src/futures_collector/{api,cli,normalize,runner,sources,trading_calendar}.py` 及对应测试。
- Rust/API/数据库：`rust/apps/api/src/imports.rs`、`rust/crates/application/src/import_jobs.rs`、`rust/crates/database/src/{imports,job_queue,lib,rollback_jobs}.rs`。
- 迁移：`202608030001`、`202608030002`、`202608030003`。
- E2E：`rust/tests/phase_4a_e2e.sh`。
- 阶段契约：`docs/phases/PHASE_04_AKSHARE_COLLECTION.md`。
- 本交接及 `docs/handoffs/LATEST.md`。

## 未提交修改

- 写交接前工作树干净；本交接与 `LATEST.md` 将作为单独 docs 提交。
- 无业务代码未提交修改。

## Git 分支与提交

- 分支：`phase/04-akshare-collection`。
- 交接前 HEAD：`579017bdfb15bc67112ea437b014ab94ab8ab2ae`（`test: assert replay business-key preservation`）。
- 远端同步：`origin/phase/04-akshare-collection...HEAD = 0/0`。
- 运行候选：`82cec44184ffb6ae4bf700afd0210193a081ad0a`（`test: align seat fixtures with rank payload contract`）。
- 最近关键提交：`730f26d` DELETE grant、`d84eeed` numeric 断言、`579017b` replay 业务键保存。
- `git status --porcelain`：交接写入前为空。

## 本地测试结果

已运行并通过：

- `cargo +stable fmt --all -- --check`。
- `cargo +stable clippy --workspace --all-targets -- -D warnings`。
- `cargo +stable test --workspace`：135 项通过。
- `pnpm lint`。
- `pnpm test`：6 个文件、18 项通过（沙箱首次因上级目录访问限制无法启动，宿主权限复跑通过）。
- `pnpm build`：通过；仅 Vite chunk size warning。
- Python 锁定环境 ruff 与单元测试：候选 `82cec44` 上 35 项通过；之后无 Python 代码变化。
- `bash -n rust/tests/phase_4a_e2e.sh`。
- `git diff --check d30b3e3..HEAD`。

已运行但失败且已解释：

- 当前 Windows 裸 `python` 不在 PATH；不能用于最终复跑，未下载或改写依赖。Python 候选结果以上述锁定环境/CI 为准。
- HEAD 自动 hosted CI 在所有 job 0 steps 时即失败，属于 GitHub 计费拒绝；固定业务候选 CI `30813165664` 为 success。

## futures VPS 状态

- 已实际核对，部署报告 `status=PASS`，Run `30873823206`。
- `/api/v1/version`：`git_sha=82cec44184ffb6ae4bf700afd0210193a081ad0a`，version `0.1.0`。
- api/worker/frontend/collector digest：
  - api `sha256:ead8f733e704412ccebdbb31a83c78ebff76304140fdaf07d588aed752906587`
  - worker `sha256:95170cdb58a0977a13c2342d598ec5169523e986e738bfa6d10faa19b654bd2a`
  - frontend `sha256:77c93db6cca3deefbcf9932010f03cbfa11fee63775c582f77ee778b413d483e`
  - collector `sha256:3bc5ce53760de7ba3e77ce1450aef2a561695b9d612fc526f070e81d02e16544`
- api/postgres healthy；worker/frontend/nginx 运行中。
- cron：工作日 Asia/Shanghai 17:30、21:30 各一次。
- collector 凭据仅核对权限：`0400 root:root`，未读取内容；bootstrap-token absent。
- 手动批次 144，原 127 基线仍保留且 E2E fingerprint 未变；users 32 且身份 fingerprint 未变。
- 自动批次 159；这是成功采集、故障隔离和验收批次，不清理。
- 正式表：market_prices 830、seat_positions 17806；重复键 0；缺失 source/source_import_batch lineage 0。
- DCE 2026-07-30：market 212、seat 5340，真实来源为 Sina fallback；其余四所均为 official。
- runner active/running，MemoryMax/peak 256 MiB，OOM/OOM kill 0，重启 0；发生过 cgroup 限流，后续部署需继续观察。

## 数据库迁移状态

- 已执行：`202607260001`、`202607260002`、`202608020001`、`202608020002`、`202608030001`、`202608030002`、`202608030003`。
- `schema_versions` 中 `202608030003` 精确 1 条。
- `futures_runtime` DELETE 只对 `exchanges`、`instruments`、`contracts`、`trading_calendar_versions`、`trading_calendar_days`、`market_prices`、`seat_entities`、`seat_positions` 授予。
- 上述 8 表均 `RLS=true` 且 `FORCE RLS=true`。

## 已知问题和风险

- DCE Sina fallback 在长时真实采集中会出现目录增长和请求波动；完整性策略会增加 E2E 时长并可能触发重试，这是预期保护而非 partial 成功。
- self-hosted runner 达到 256 MiB MemoryMax 并发生限流，虽无 OOM/重启，未来部署需关注 runner 与 Docker 操作叠加。
- hosted Actions 计费拒绝尚未解除；用户已决定不充值。不得擅自把 CI/镜像编译迁到 1GB VPS。
- Generator 未做独立 Evaluator 结论；最终缺陷 CLOSED/NOT-CLOSED 以新 Evaluator 为准。

## 下一步第一条操作

由全新独立 Evaluator 读取本交接和 `docs/reviews/PHASE_04A_EVALUATION.md`，从 Git 范围 `d30b3e3..HEAD`、CI/镜像 Run、Deploy Run `30873823206` 与 futures VPS 实态开始只读复核十项缺陷。

## 新 AI 必读文件

- `AGENTS.md`
- `PLANS.md`
- `docs/DECISIONS.md`（特别是 DEC-031、DEC-038、DEC-039、DEC-041）
- `docs/reviews/PHASE_04A_EVALUATION.md`
- `docs/phases/PHASE_04_AKSHARE_COLLECTION.md`
- `docs/handoffs/LATEST.md`
- 本交接文档
- `.github/workflows/deploy-futures.yml`
- `rust/tests/phase_4a_e2e.sh`
- `rust/migrations/202608030003_phase_4a_projection_rollback_delete_grants.sql`

## 禁止重复或禁止修改事项

- 不得修改原始 Word 设计方案。
- 不得写入或输出密码、token、Cookie、数据库凭据、主密钥或 SSH 私钥。
- 不得重复已通过的生产部署；Evaluator 只读复核时不得重部署或改数据。
- 不得清理原 127 测试批次或其他 VPS 数据。
- 不得恢复已废止的 Chromium/Playwright/noVNC/OCR/渲染沙箱基础设施。
- 不得给 DCE 之外四所引入聚合 fallback。
- 不得合并 main、打标签或启动 Phase 4B。
