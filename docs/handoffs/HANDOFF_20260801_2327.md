# Agent Handoff

## 当前目标

完成总方案重审修订纯文档单：把用户确认的采集域裁剪与 akshare 方案落入决策、路线和设计文档，并按“决策 → 设计 → 交接”三个主题提交到 `main`；不写业务代码、不部署。

## 当前阶段

Phase 1 至 Phase 3 已完成并收口。2026-08-02 总方案重审决策已落档，后续路线固定为 Phase 4 至 Phase 9；Phase 4 尚未建立实施契约，也未开始实现。

## 已完成

- 第一主题提交 `7f2e7e623a1bcf49030fba8cf5e938a7e877fef7 docs: revise collection decisions` 已推送，main CI Run `30705219493` success。
- 第二主题提交 `decbadced149491b1b1c590ab72ce57804fb9c56 docs: align designs with akshare collection` 已推送，main CI Run `30705776449` success。
- DEC 变更完整清单：废止 `DEC-023`、`DEC-030`、`DEC-034`；修订 `DEC-025`、`DEC-031`；新增 `DEC-038`、`DEC-039`、`DEC-040`。
- 已把五交易所 akshare 按需采集器、自动批次免确认、全历史回填、80% 磁盘扩容水位和 ECharts 前端导出同步到产品、架构、模块、安全、导入、API、数据库和验收文档。
- 已把开发路线重排为 Phase 4 akshare 采集与全历史回填、Phase 5 多腿套利与图表、Phase 6 成交/持仓/绩效、Phase 7 席位分析、Phase 8 AI、Phase 9 运维加固与完工事项。
- Phase 9 已明确包含生产库归零重置、TLS+域名+`AUTH_COOKIE_SECURE`、备份自动化+master-key 离线副本。
- 已同步清理非评审/非交接历史文档中的旧基础设施活性措辞；既有 `docs/reviews/` 和 `docs/handoffs/` 历史文件未修改。

## 正在进行

本交接与 `docs/handoffs/LATEST.md` 组成最后一个独立 docs 提交。提交推送后必须等待对应 main CI success，再做远端与文件范围终检。

## 尚未完成

- Phase 4 Planner 详细契约尚未创建；实现仍需用户单独授权。
- Phase 9 的生产库归零重置、TLS/域名/安全 Cookie、备份自动化和 master-key 离线副本仍是完工时事项，本单未执行。
- 席位全历史实际磁盘增长需在 Phase 4 运行后观测；当前按已确认的 5–15GB 估算和 80% 扩盘水位执行。

## 关键决策

- 五交易所清单为 DCE、SHFE、CZCE、GFEX、CFFEX；akshare 第一批一次覆盖，东方财富等二手数据源不进入第一批。
- 采集器为独立 Python 容器，每交易日盘后按需拉起、跑完退出；产出标准化 CSV 后调用导入 API。
- 自动采集批次免人工确认、免提取预览；解析/校验失败隔离为 `failed` 且正式表零写入，质量警告仅记录、不拦截；手动文件导入流程不变。
- 历史范围为全市场日行情、全市场席位龙虎榜、交易日历、合约参数；行情全量一次拉取，席位先近 5 年再向前补拉，不可得年份留记录、不阻塞。
- 不建设服务端静态图渲染能力；PNG/SVG 由 ECharts 前端导出，保留 SVG 安全清洗验收。
- 裁剪后现有 1GB/25G VPS 支撑全部阶段；五核心容器实测约 310MB，席位全历史预计 5–15GB，磁盘达到 80% 时扩盘、不换机器。

## 修改过的文件

第一主题：

- `docs/DECISIONS.md`：保留旧决策审计正文，完成废止/修订/新增 DEC。

第二主题：

- `PLANS.md`：记录重审完成、Phase 4–9 路线和 Phase 9 完工事项。
- `README.md`：同步技术基线、项目边界、路线和当前状态。
- `docs/PRODUCT_REQUIREMENTS.md`：更新 3.3、3.7，重写 3.8。
- `docs/ARCHITECTURE.md`：更新上下文图、导入/采集数据流、冲突修正表和资源结论。
- `docs/MODULE_DESIGN.md`：更新 connector 职责/端口，删除服务端图表渲染端口和旧依赖规则。
- `docs/SECURITY_DESIGN.md`：更新信任边界和采集出口约束，删除旧远程会话整节。
- `docs/IMPORT_DESIGN.md`：加入自动批次跳过人工步骤和固定模板状态流，删除旧提取口径。
- `docs/API_DESIGN.md`：删除旧远程会话与服务端静态图接口，把 `extraction-jobs` 改为采集任务元数据接口。
- `docs/DATABASE_DESIGN.md`：移除 `browser_sessions`，保留并修订 `extraction_jobs`，保留 `data_sources` 白名单表。
- `docs/ACCEPTANCE_CRITERIA.md`：重写自动采集验收并更新前端图表导出验收。
- `docs/OPEN_QUESTIONS.md`：关闭并重定向残留 OPEN-COL/浏览器项到新 DEC。
- `docs/DEVELOPMENT_PLAN.md`：重排 Phase 4–9，加入资源结论和 Phase 9 完工三件事。
- `docs/AI_DESIGN.md`：把不可信外部内容口径同步为自动采集内容。
- `docs/phases/PHASE_01_FOUNDATION.md`、`docs/phases/PHASE_02_IDENTITY_WORKSPACE_SECURITY.md`、`docs/phases/PHASE_03_IMPORT_FOUNDATION.md`、`docs/phases/PHASE_03D_IMPORT_FINALIZATION.md`：消除旧基础设施的活性措辞，不改变已验收业务事实。
- `docs/deployments/FUTURES_VPS_PHASE_01.md`：把 Phase 1 历史部署边界改为中性的“未实现后续阶段业务功能”。

第三主题：

- `docs/handoffs/HANDOFF_20260801_2327.md`：本交接。
- `docs/handoffs/LATEST.md`：指向并概括本交接。

## 未提交修改

编写本交接时，仅本交接与 `docs/handoffs/LATEST.md` 构成待提交的第三主题文档变更。提交后工作树应干净；不包含任何业务代码、部署文件、工作流或秘密。

## Git 分支与提交

- 当前分支：`main`。
- 本交接编写前 HEAD：`decbadced149491b1b1c590ab72ce57804fb9c56`，与 `origin/main` 一致。
- 最近提交：`decbadc docs: align designs with akshare collection`、`7f2e7e6 docs: revise collection decisions`、`6626b69 docs: record phase 3 closure`。
- 本交接将以第三个独立 docs 提交推进 `main`；其 CI Run 只能在提交推送后取得，因此不在自身正文回写，由最终任务报告给出。

## 本地测试结果

已运行并通过：

- `git diff --check`。
- 本地 Markdown 相对链接完整性检查：`MARKDOWN_LOCAL_LINKS_OK`。
- 旧关键词检索：排除既有 `reviews/`、`handoffs/` 后，剩余命中仅为已废止 DEC 的审计原文、已关闭 OPEN 项或明确“不建设/已移除”的裁剪说明；无活性旧接口、表、模块端口或阶段要求。
- 文件范围检查：第一主题仅 `docs/DECISIONS.md`；第二主题仅 `docs/`、`README.md`、`PLANS.md`。
- main CI Run `30705219493` 与 `30705776449` 均为 success，validate/API/Worker/Frontend 四个 job 全绿。

已运行但失败：无。

未运行及原因：未重复运行本地业务测试；本单只修改 Markdown，GitHub Actions 已按仓库统一门禁完整运行 Rust、前端、Compose config 和三镜像 CI build。未运行部署/E2E，因为本单明确禁止部署。

## futures VPS 状态

本单未连接 VPS 做运行态核对，也未执行部署、重启、数据清理或文件修改。上一份已验收交接记录的运行版本为 `45ee8028647a1b8e4b8cda043e8012b4e281d739`，本交接不把该历史记录冒充本次实时核验结果。

## 数据库迁移状态

本单没有创建、修改或执行任何数据库迁移，也没有连接数据库。Phase 3 已执行迁移的历史证据见上一份交接和 Phase 3D Evaluator 报告。

## 已知问题和风险

- akshare 的实际端点、字段差异、限流和不可得年份分布需在 Phase 4 详细契约与实现中用真实数据验证，不能由本次文档修订替代。
- 自动批次免确认提高了幂等、固定模板版本、失败隔离和来源追溯的重要性；上述均已写入验收门槛。
- 25G 磁盘的主要增长风险为席位全历史；必须监控 80% 水位，不能等磁盘写满后再处理。
- 旧方案关键词仍会出现在已废止 DEC 审计正文、已关闭 OPEN 项以及不修改的历史评审/交接中；这些不是活性要求，不得据此恢复旧架构。

## 下一步第一条操作

为 Phase 4“akshare 采集与全历史回填”创建 Planner 详细契约，等待用户单独授权实施。

## 新 AI 必读文件

- `AGENTS.md`
- `PLANS.md`
- `docs/DECISIONS.md`
- `docs/DEVELOPMENT_PLAN.md`
- `docs/PRODUCT_REQUIREMENTS.md`
- `docs/ARCHITECTURE.md`
- `docs/IMPORT_DESIGN.md`
- `docs/ACCEPTANCE_CRITERIA.md`
- `docs/handoffs/LATEST.md`
- `docs/handoffs/HANDOFF_20260801_2327.md`

## 禁止重复或禁止修改事项

- 原始 Word 设计方案不得修改。
- 不得写入、输出、复制或重新生成任何密码、Token、Cookie、私钥、主密钥或数据库明文凭据。
- 不得把本次纯文档决策同步误作 Phase 4 实施授权；未经用户明确授权，不写采集业务代码、不改容器或部署。
- 不得恢复已废止的远程登录、OCR 或服务端图表渲染基础设施。
- 不得修改既有历史评审与交接文档来掩盖当时事实。
- 不得重复 Phase 3 已完成的部署、Evaluator 或版本收口工作。
