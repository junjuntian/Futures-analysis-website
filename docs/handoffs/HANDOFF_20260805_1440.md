# Agent Handoff

## 当前目标

完成 MEDIUM-05 workflow 修复取证、Phase 4A 收尾，并以既有 Phase 4B-1 驱动恢复
2021-08-04..2026-08-03 连续五年回填。连续任务在确认两个日期批次稳定完成后交由宿主脚本
自治运行，不再由本任务持续观察。

## 当前阶段

Phase 4A 已按用户裁定完成主验收并判定 PASS；下一独立 Evaluator 只需复确认
MEDIUM-05。Phase 4B-1 连续回填正在后台运行，Phase 4B-2 未启动。仍不得合并 main
或打标签；Phase 4 整体完成后一次收口。

## 已完成

- `8018f32` 关闭 MEDIUM-05：CI PostgreSQL service 只发布到
  `127.0.0.1:5432:5432`，以一次性测试认证取代 `trust`，并同步测试数据库连接串。
- CI Run `30977655724`（SHA `8018f32`）success；validate、PostgreSQL 专项集成
  测试及 API/Worker/Frontend/Collector 四个非发布镜像 job 均 success。
- CI service 存活期间，VPS `ss -tlnp '( sport = :5432 )'` 的唯一结果为：
  `LISTEN 0 4096 127.0.0.1:5432 0.0.0.0:* users:(("docker-proxy",pid=224915,fd=7))`；同刻 Docker 映射为
  `127.0.0.1:5432->5432/tcp`，没有 `0.0.0.0:5432` 或 `[::]:5432`。
- `docs/reviews/PHASE_04A_EVALUATION.md` 已在 HIGH-03 终验之后追加 MEDIUM-05
  关闭记录。Phase 4A 缺陷时间线为：首轮 FAIL（HIGH 5 / MEDIUM 4 / LOW 1）→
  Generator 修复 → 复核 9/10 → HIGH-03 终验 CLOSED → 新增 MEDIUM-05 →
  `8018f32` 修复并由用户授权主验收关闭；当前剩余计数为 0。
- 10 日试跑此前已完成并通过试跑验收；手动批次及整行指纹保持不变，正式投影增长，
  两类业务唯一键重复组为 0。
- `8ce831a` 把回填驱动的精确运行候选护栏从旧镜像候选更新到 Phase 4A 最终运行
  候选 `e627ab8`；只改一行 SHA 白名单，未绕过运行版本检查。本地 `bash -n` 和该
  提交的 CI validate 均通过。
- 2026-08-05 13:37:50 CST（05:37:50Z）以 `nohup` 启动连续回填，PID `250167`，
  参数为 `--from 2021-08-04 --to 2026-08-03 --daily-limit 80 --sleep-seconds 60
  --continuous`，复用既有 collector/driver 锁、保护窗、磁盘水位和 512 MiB 限额。
- 启动后的两个日期批次均完整结束：
  - `2026-08-03` 于 06:02:54Z 完成，五所成功，`failed_sources=0`；
  - `2026-07-31` 于 06:37:01Z 完成；DCE 在 30 分钟单源护栏超时并以
    `exit_code=124, streak=1` 隔离，SHFE/CZCE/GFEX/CFFEX 均成功，
    `failed_sources=1`。该失败低于连续失败 5 日暂停阈值，未见 412/429 或持续空数据
    迹象，驱动按契约继续运行。

## 正在进行

Phase 4B-1 连续回填由 `/usr/local/sbin/run-futures-backfill` 自治运行。收尾快照时
`driver_running=yes`，连续 PID 存活；后续不需要本任务持续观察。

## 尚未完成

- Phase 4B-1 剩余日期回填及失败日期后续重试。
- 由下一独立 Evaluator 对 MEDIUM-05 单项复确认。
- Phase 4B-2 老年份适配与 Phase 4 整体收口，均须另单执行。

## 关键决策

- MEDIUM-05 按用户裁定由主验收人依据代码、CI 与 VPS 实态关闭；下一独立 Evaluator
  仅复确认，不重新解释已授权的 self-hosted runner 与 Docker 组边界。
- 4B-1 继续遵守每天最多 80 个交易日、单日后至少等待 60 秒、上海时间
  16:30-22:30 保护窗、磁盘达到 80% 停止、来源连续失败达到 5 日暂停，以及与每日
  cron 互斥的既有契约。
- DCE 历史失败如实保留清单，不因单次 30 分钟超时阻断其他来源或整个连续任务。
- Phase 4A 已 PASS，但 Phase 4 分支仍不合并 main、不打标签；待 4B 完成后一次收口。

## 修改过的文件

- `.github/workflows/ci.yml`：MEDIUM-05 的 loopback 端口与测试认证修复。
- `deploy/collector/run-backfill.sh`：精确接受最终 Phase 4A 运行候选 `e627ab8`。
- `docs/reviews/PHASE_04A_EVALUATION.md`：追加 MEDIUM-05 关闭记录与 Phase 4A PASS。
- 本交接与 `docs/handoffs/LATEST.md`。

## 未提交修改

创建本交接时仅有评审追加、本交接和 `LATEST.md` 文档修改待提交；业务代码、collector、
deploy 脚本与 workflows 均无未提交修改。docs 提交后应为干净状态。

## Git 分支与提交

- 分支：`phase/04-akshare-collection`。
- 写交接前 HEAD：`8ce831ab429b4ad937367dbcb59657f69e6e73ae`。
- 关键提交：`8018f32` MEDIUM-05；`8ce831a` 回填运行候选精确更新；
  `24e9308` HIGH-03 最终评审；运行候选仍为 `e627ab8`。
- `origin/main` 未合并本阶段；未创建新标签。

## 本地测试结果

- `bash -n deploy/collector/run-backfill.sh`：PASS。
- `git diff --check`：PASS（提交前复跑）。
- CI Run `30977655724`：完整 CI success，包含 PostgreSQL 专项集成测试。
- `8ce831a` 对应 CI Run `30977900965` 的 validate success；其无关镜像 job 为避免与
  数据操作争用资源而取消，驱动脚本不进入生产镜像。

## futures VPS 状态

- 已实际核对；生产运行候选仍为 `e627ab8`，本单未部署、未构建新镜像。
- 回填收尾快照：`processed_date_count=19`、`waterline_date=2026-07-16`、
  `newest_processed_date=2026-08-03`、`failed_pair_count=5`、根分区使用率 20%、
  `driver_running=yes`。
- 当前失败清单共 5 个 DCE 日期对；本次新增 `2026-07-31` 的 30 分钟超时，当前
  连续失败 streak 为 1，未达到暂停阈值。
- 启动后 collector 观测内存约 79 MiB / 512 MiB，未触碰资源上限。
- 预计完成窗口：80 日/自然日仅是理论上限，约 1200 个交易日的数学下界为 15+ 日；
  结合本次 DCE 单日约 22-30 分钟和保护窗，保守预计为 2026 年 9 月上中旬，来源
  超时、失败重试或磁盘事件会顺延。监控入口为 `--status`、`state.env`、
  `failed_dates.tsv`、`events.log`、`/var/log/futures-backfill.log` 与磁盘水位。

## 数据库迁移状态

- 本单不部署、不执行迁移；生产迁移仍为 18 条，最大版本 `202608030003`。
- 两批完成后的只读快照：手动批次 144、自动批次 520、
  `market_prices=10676`、`seat_positions=216171`；行情与席位业务唯一键重复组均为 0。
- 127 历史手动批次及后续既有数据未清理；本单只通过既有自动导入链追加回填数据。

## 已知问题和风险

- DCE 历史采集耗时明显高于其他来源，`2026-07-31` 已触发单源超时；失败记录按
  DEC-041 与 4B-1 契约保留，不计为本单阻塞，但会延长整体完成时间。
- 若某来源连续失败达到 5 日、出现持续 412/429/空数据、磁盘达到 80% 或进程异常
  退出，应由后续运维任务按现有护栏暂停并记录，不得高频重试。
- MEDIUM-05 尚待下一独立 Evaluator 单项复确认；主验收结论已为 Phase 4A PASS。

## 下一步第一条操作

无需立即操作；让 Phase 4B-1 脚本自治运行。下一单如需检查，先只读执行
`/usr/local/sbin/run-futures-backfill --status` 并查看失败清单与磁盘水位；Phase 4B-2
另单启动。

## 新 AI 必读文件

- `AGENTS.md`
- `PLANS.md`
- `docs/DECISIONS.md`
- `docs/phases/PHASE_04_AKSHARE_COLLECTION.md`
- `docs/reviews/PHASE_04A_EVALUATION.md`
- `deploy/collector/run-backfill.sh`
- 本交接与 `docs/handoffs/LATEST.md`

## 禁止重复或禁止修改事项

- 原始 Word 设计方案不得修改。
- 不得输出或提交密码、Token、Cookie、数据库凭据、collector 凭据或主密钥。
- 不得重复启动第二个连续回填进程，不得绕过锁、限速、保护窗、失败或磁盘护栏。
- 不得合并 main、打标签、启动 Phase 4B-2、清理 127 历史手动批次或其他既有数据。
- 不得为本回填构建镜像、重新部署或修改 Rust/collector 业务代码。
