# Agent Handoff

## 当前目标

完成 Phase 4A——akshare 采集链路（目录自动建档、每日行情、席位、日历、正式业务表入库）并交给全新的独立 Evaluator。Phase 4B 全历史回填不在本单。

## 当前阶段

Phase 4A 实现、CI、四镜像发布、VPS 部署和完整 E2E 已完成；候选 `944a4defe578d5922b9f1ea83f951ddbd6fb005e` 等待独立 Evaluator。没有合并 `main`、没有打标签。Phase 4B 未实施。

## 已完成

- 先行 Planner 契约：`docs/phases/PHASE_04_AKSHARE_COLLECTION.md`，明确 4A/4B 边界、四类固定数据集和字段映射。
- Rust 正式数据集处理器与迁移：交易所/品种/合约、交易日历、行情、席位实体与席位事实表；Workspace/RLS、正式业务唯一键、来源与批次链路、自动批次固定模板、failed 隔离和回滚机制。
- 独立 Python collector：锁定 Python/akshare/传递依赖，单日期参数、标准化 CSV、平台登录/上传/自动确认、交易所级隔离、专用服务账号和日志脱敏。
- DEC-041：DCE 官方直连优先，412 等失败时仅 DCE 自动 fallback 到审核后的新浪期货接口；真实 `data_source` 与事实表 `source_id` 均记录聚合来源，其他四家没有聚合 fallback。
- 官方源瞬态响应有限重试：DCE 官方路径不重试而立即进入 DEC-041 fallback；SHFE/CZCE/GFEX/CFFEX 仅在各自官方来源内最多三次重试，最终失败仍隔离。
- CI / 镜像 / 部署：collector 纳入 Compose、CI、GHCR 四镜像发布、不可变 digest 部署、受控账号 provision、cron/logrotate 和失败回滚。
- VPS E2E：指定 `2026-07-30` 完成五交易所真实采集、同日完整重跑、业务唯一键、故障隔离、RLS、来源链、目录建档、旧数据保护、日志秘密扫描和内存峰值验收。
- 当前文档已更新 Phase 4A 状态、部署实证、README、PLANS 和本交接。

## 正在进行

仅剩本 docs 收尾提交的推送后 CI 核验；业务实现和 VPS 部署没有进行中的变更。

## 尚未完成

- 全新的独立 Evaluator 尚未执行 Phase 4A 审查。
- Phase 4B 全历史回填未实施、未执行。
- 未合并 `main`、未打 Phase 4 标签；版本收口必须另单授权。

## 关键决策

- `DEC-031`：五交易所由按需运行的独立 akshare collector 覆盖。
- `DEC-038`：白名单结构化自动批次免人工预览/确认，失败隔离，质量警告只记录。
- `DEC-039`：范围与历史回填策略；本单仅完成单日期能力，不执行历史回填。
- `DEC-041`：DCE 官方优先、Sina fallback，仅限 DCE且来源如实落库。
- collector 不直接连接数据库；必须经平台 API 与专用 analyst 服务账号。
- Phase 4A 不新增前端页面，批次仍在现有导入中心查看。

## 修改过的文件

- Collector：`collector/` 下 Dockerfile、锁文件、`futures_collector` 全部模块与 15 个单测。
- Rust：API auth/import 路由、application 自动数据集校验、database import/job handler、domain 契约、两份 Phase 4A 迁移、`rust/tests/phase_4a_e2e.sh`。
- 发布运维：四份 GitHub Actions 工作流、两份 Compose、`deploy/collector/` cron/runner/logrotate。
- 文档：`docs/DECISIONS.md`、Phase 4 契约、`docs/DEPLOYMENT.md`、`README.md`、`PLANS.md` 与 handoff。

完整文件清单以 `git diff --name-only 2a6e6d5..944a4de` 及本 docs 收尾提交为准。

## 未提交修改

编写本交接时只有 Phase 4A 状态/部署/交接文档待组成单独 docs 提交；业务代码工作区干净。提交后应再次执行 `git status --short` 确认无残留。

## Git 分支与提交

- 分支：`phase/04-akshare-collection`。
- 实现基线：`main@2a6e6d5`。
- 业务候选 HEAD：`944a4defe578d5922b9f1ea83f951ddbd6fb005e`。
- 提交范围：`2a6e6d5..944a4de`，共 31 个提交。
- 关键提交：`c503f29` Planner 契约；`9836485` schema；`8c98f32` 自动正式入库；`0960d8d` collector；`c733565` CI/部署；`9f0bb44` E2E；`ede2f3c` DEC-041；`90eb603` DCE fallback；`66f90d1` 官方源有限重试；`944a4de` 稳定用户身份字段保护。
- 本地分支已推送到 `origin/phase/04-akshare-collection`；没有 merge/rebase/squash 到 main。

## 本地测试结果

最终候选复跑并通过：

- `cargo +stable fmt --check`：PASS。
- `cargo +stable clippy --workspace --all-targets -- -D warnings`：PASS。
- `cargo +stable test --workspace`：127 passed，0 failed。
- `pnpm lint`：PASS。
- `pnpm test`：6 files / 18 tests passed。
- `pnpm build`：PASS，1468 modules；仅既存大 chunk warning。
- `ruff check collector`：PASS。
- `ruff format --check collector`：13 files already formatted。
- `pytest collector/tests -q`：15 passed。
- `git diff --check`：PASS。
- 最终候选 CI Run `30753685223`：success。
- Container images Run `30753724067`：success。

## futures VPS 状态

- 已通过 Deploy Run `30754021926` 实际核对并部署，不是沿用旧交接推测。
- 运行版本：`944a4defe578d5922b9f1ea83f951ddbd6fb005e`。
- 运行镜像 digest：API `3ee25c7f…60abda`、Worker `960173e9…8d414e3`、Frontend `deaa22ce…582875`、Collector `bcb8d75d…d5ab7d`；完整值见 `docs/DEPLOYMENT.md`。
- `PHASE4A_E2E_PASS`、`DEPLOYMENT_PASS`、runtime digest match、collector image verify、registry auth cleanup 全部 PASS。
- 真实日期 `2026-07-30` 五交易所目录/日历/行情/席位批次成功；DCE fallback 来源追溯 PASS，其余四家官方来源不变。
- 完整同日重跑后 `market_prices`/`seat_positions` 行数不变，唯一键重复为 0。
- 故障注入：DCE 行情 failed；其他四家 succeeded 且正式行情行数不变。
- collector cgroup 峰值 `130641920` bytes，小于 512 MiB。
- cron：工作日 17:30、21:30；`flock` 防重入。
- 部署前数据保护基线：手动批次 144、自动批次 25、users 32；手动批次整行指纹与用户稳定身份字段指纹前后相同。服务账号登录只允许更新登录元数据。
- 未清理 VPS 数据；原 127 个历史测试批次包含于受保护的 144 个手动批次中。

## 数据库迁移状态

- 已执行且部署报告核验：`202607260001`、`202607260002`、`202608020001`、`202608020002`。
- `202608020001` 创建 Phase 4A 业务表、自动批次元数据、固定模板与 RLS。
- `202608020002` 登记 DCE Sina fallback 独立数据源与白名单。
- 迁移部署前已备份数据库；成功发布后未执行数据清理或历史回填。

## 已知问题和风险

- DCE 官方接口仍受全局 WAF 412 挑战；当前按 DEC-041 自动 fallback，akshare 上游修复后优先级逻辑会自动回到官方来源。
- GFEX 席位官方接口曾瞬态返回空/异常结构；已增加仅官方源内有限重试，VPS 后续多轮成功。持续失败会形成 failed 批次，不会切聚合源。
- VPS 手动批次基线已从历史记录的 127 变为 144，原因未在本单清理或追溯；本单只做全量数量/指纹保护，生产归零仍延后到项目完工。
- Phase 4A 多次真实验收产生自动批次；这些批次有完整来源与状态，不得冒充手动历史批次或擅自清理。

## 下一步第一条操作

以全新独立 Evaluator 身份读取 `.agents/Evaluator.md`、DEC-036、Phase 4 契约、本交接和 Git 范围 `2a6e6d5..docs收尾HEAD`，对 Phase 4A 做只读独立审查与 VPS 实态复核。

## 新 AI 必读文件

- `AGENTS.md`
- `.agents/Evaluator.md`
- `PLANS.md`
- `docs/DECISIONS.md`（重点 DEC-031/038/039/041）
- `docs/phases/PHASE_04_AKSHARE_COLLECTION.md`
- `docs/DEPLOYMENT.md`
- `docs/handoffs/LATEST.md`
- 本文件

## 禁止重复或禁止修改事项

- 不得修改原始 Word 方案或历史 review/handoff 文档。
- 不得输出、读取或复制 collector 密码、Cookie、CSRF、数据库凭据、GHCR 凭据、主密钥或 SSH 私钥。
- 不得清理 144 个手动批次或其他 VPS 数据。
- 不得把 Phase 4B 历史回填混入 Phase 4A 审查。
- 不得为 SHFE/CZCE/GFEX/CFFEX 引入聚合 fallback；DEC-041 例外仅限 DCE。
- 不得重复本单已经通过的部署修复或以旧失败 Run 否定最终成功 Run；审查必须以 Git、当前测试和 VPS 实态独立取证。
- 未经另单授权不得合并 main、打标签或执行版本收口。
