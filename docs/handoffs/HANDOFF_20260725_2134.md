# Agent Handoff

交接时间：2026-07-25 21:34 +08:00

## 当前目标

Phase 3B 已完成实现、验证、独立评审和业务提交；当前目标是保持收口状态，等待用户单独授权 Phase 3C 或明确安排 Phase 3B 的非阻断 MEDIUM。

## 当前阶段

Phase 3：导入基础。

- Phase 3A：已完成、已提交、Evaluator PASS。
- Phase 3B：已完成、已提交、独立顶层 Evaluator PASS。
- Phase 3C：未授权、未实现。
- Phase 3D：未授权、未实现。

## 已完成

- TXT、CSV、XLS、XLSX 安全解析。
- 编码和分隔符识别及人工覆盖。
- Excel 工作表和表头选择。
- 前 50 行预览和解析错误展示。
- 服务端数据集字段/转换定义、字段映射、映射模板及版本冻结。
- OpenAPI multipart 必填 binary 文件契约修复。
- 预览失效、模板绑定、映射身份和模板数据集的数据库不变量。
- 本地 Rust/前端测试、`futures` VPS Docker 构建部署、迁移、数据库回归、并发测试和 Phase 3B E2E。
- 独立顶层 Evaluator 最终 PASS，`BLOCKER=0`、`HIGH=0`。
- Phase 3B 实现提交：`150194c feat: complete phase 3b import parsing preview mapping`。

## 正在进行

仅剩本交接和收口文档 checkpoint 提交；没有业务实现正在进行。

## 尚未完成

- Phase 3C：校验、去重、冲突策略、确认导入、PostgreSQL 任务队列、SSE。
- Phase 3D：原子回滚、补偿批次、完整前端流程和最终部署验收。
- 独立 Evaluator 的非阻断 MEDIUM 尚未实施，详见 `docs/reviews/PHASE_03B_EVALUATION.md`。

## 关键决策

- 本地 Git 是唯一源码源头；Codex Cloud 仅作辅助开发、测试和独立审查；`futures` VPS 是最终 Docker、数据库、RLS、文件持久化和部署验收环境。
- Phase 3B 只实现 inspect/mapping/preview/templates/datasets/errors；不得包含 confirm/events/rollback/cancel、jobs、SSE 或正式入库。
- 已绑定模板版本不可变；版本自身冻结 `dataset_type` 和字段配置。
- 映射不得移动 Workspace 或批次；预览失效与映射修改必须保持同一事务。
- Evaluator 必须独立，主 Agent 不得冒充独立 Evaluator。

## 修改过的文件

- 计划与流程：`AGENTS.md`、`PLANS.md`、`docs/DEVELOPMENT_PLAN.md`、`docs/phases/PHASE_03_IMPORT_FOUNDATION.md`。
- 评审：`docs/reviews/PHASE_03B_EVALUATION.md`。
- 前端：`frontend/src/App.vue`、`frontend/src/api.ts`、`frontend/src/router.ts`、`frontend/src/views/ImportCenterView.vue`、预览状态 helper/test。
- Rust：import 相关 domain/application/database/infrastructure/API 模块及 Cargo 依赖。
- 数据库：迁移 `202607250003` 至 `202607250007`。
- 数据库测试：`rust/tests/phase_3b_mapping_invariants.sql`、`rust/tests/phase_3b_template_binding_concurrency.sh`。

## 未提交修改

创建本交接时，Phase 3B 业务实现已提交；仅 `PLANS.md`、Phase 3 计划、Phase 3B 评审报告、本交接和 `LATEST.md` 等收口文档等待 checkpoint 提交。

## Git 分支与提交

- 当前分支：`phase/03-import-foundation`。
- 创建交接时 HEAD：`150194c feat: complete phase 3b import parsing preview mapping`。
- 最近提交：
  - `150194c feat: complete phase 3b import parsing preview mapping`
  - `84dc7be docs: record phase 3a completion`
  - `1b089f7 feat: complete phase 3a import upload foundation`
  - `cde16ee docs: plan phase three import foundation`
  - `6dfea78 feat: complete phase two identity foundation`
- checkpoint 提交后实际 HEAD 以前述分支的 `git log` 为准。

## 本地测试结果

已运行并通过：

- `git diff --check`
- `cargo +stable fmt --check`
- `cargo +stable test --workspace`：25 项测试通过
- `cargo +stable clippy --workspace --all-targets -- -D warnings`
- `pnpm lint`
- `pnpm test`
- `pnpm build`

本机未运行 Docker；Docker 门禁在 `futures` VPS 完成。

## futures VPS 状态

- 已实际核对，访问地址：`http://172.238.11.174:8088`。
- PostgreSQL、API、Worker、Frontend、Nginx 均运行；API/PostgreSQL healthy。
- 最新 Phase 3B API 镜像构建和部署通过。
- `/api/v1/health/ready` 返回 ready。
- Phase 3B HTTP E2E 返回 `PHASE3B_E2E_PASS`。
- 数据库不变量 SQL 测试和双连接模板绑定并发测试通过。
- 根分区最终使用率 45%，约 13G 可用。
- PostgreSQL 数据卷、对象存储卷和当前运行镜像均保留。

## 数据库迁移状态

- Phase 3A：`202607250001`、`202607250002` 已执行。
- Phase 3B：`202607250003` 至 `202607250007` 已执行。
- `import_template_versions.dataset_type` 为 NOT NULL。
- 映射和模板的四个最终不变量触发器存在。
- 固定测试 UUID 残留计数为 0。

## 已知问题和风险

独立 Evaluator 记录以下非阻断 MEDIUM：

1. 同参数 inspect 返回 `preview_invalidated=false` 时，前端仍会清空 preview/errors。
2. errors API 固定 `LIMIT 500`，尚无分页契约。
3. 数据库测试未直接覆盖预览失效后映射写入失败的整事务回滚。
4. 并发脚本未覆盖父模板 `dataset_type` 冻结竞争。
5. 两份数据库测试脚本的迁移前置注释仍止于 006，实际依赖 007。

这些项目不回退 Phase 3B PASS，但后续必须显式排期。

## 下一步第一条操作

先确认用户是否单独授权 Phase 3C；若授权，必须先调用 Planner 固化 3C 文件范围、API/DB/Worker/SSE 边界和验收标准。不得直接开始实现。

## 新 AI 必读文件

1. `AGENTS.md`
2. `PLANS.md`
3. `docs/DECISIONS.md`
4. `docs/phases/PHASE_03_IMPORT_FOUNDATION.md`
5. `docs/reviews/PHASE_03B_EVALUATION.md`
6. `docs/handoffs/LATEST.md`
7. `docs/handoffs/HANDOFF_20260725_2134.md`

## 禁止重复或禁止修改事项

- 不重做已通过独立评审和 VPS 验收的 Phase 3A/3B。
- 不在未授权时实现 Phase 3C/3D。
- 不新增 confirm/events/rollback/cancel、任务队列、SSE、正式入库、冲突策略、回滚或补偿批次。
- 不扩展到套利、交易、AI、OCR、外部采集或自动回测。
- 不直接在 VPS 编辑业务源码。
- 不删除 PostgreSQL 数据卷、对象存储卷或当前运行镜像。
- 不修改原始 Word 设计方案，不写入生产密钥、Cookie、数据库备份、用户数据或其他秘密。
