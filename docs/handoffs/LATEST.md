# 最新交接状态

最后更新:2026-08-22(**鸡蛋切机构成本进场信号 DEC-112**;纯碱组合实验立项;原 2026-08-21:**修掉回撤判据跨合约污染 DEC-111**;纯碱回退不要 dip;展示价改收盘价;原 2026-08-20:套利监控加平台位/卖点/止损;持仓跟合约;原 2026-08-19:**机构资金三品种改次日开盘成交**;套利监控进场/合格方向 BUG 修复;FG-SA 资金流向;交割纪律)。**接手三步:读本文 → 读 `docs/PITFALLS.md`(全部实抓教训)
→ 有疑问再查 `docs/DECISIONS.md` 对应条目。** 逐次变更的完整历史在
`git log -- docs/handoffs/LATEST.md` 与 DECISIONS.md,本文不再堆历史条目
(保留策略见 `docs/handoffs/README.md`:LATEST 只描述当前状态)。

## 当前生产状态

- **生产 = `24b2efa`**(2026-08-22),站点 `https://shejimao.trade`,
  服务器别名 `qh`。发布流程照 `docs/RELEASE_PROCESS.md`,部署前跑
  `ops/preflight-deploy.sh`。
  **这一行写的是「上一次成功部署的 SHA」,部署完之后用一个纯文档提交更新它**
  ——不是「即将部署的那个」:一个文档提交没法在自己被部署之前写出自己的 SHA。
  preflight 有门禁盯着这条等式(DEC-100),对不上就红。

### 2026-08-21

- **DEC-108 修掉机构资金引擎的回榜前视(生产 bug,已修已上线)**。
  `clean_seat` 把 `reboard_inferred` 与官方行一视同仁,而那些行是**用回榜日的增减
  倒推**的,实测可见滞后**恒为 1 个交易日**。第 D 日收盘算信号、D+1 开盘成交,
  两个时点上 D 日的反推值都还不存在,而信号 `net.diff(5)` 一头正好踩在这格上。
  修法:`clean_seat` 多一列 `net_off`(只用官方行),**当天用可见口径、5 天前的
  被减数用全量**。**代价很大**:玻璃 +442%→**+34.9%**(夏普 0.65→**0.21**,
  回撤 −45.9%→**−67.9%**)、纯碱 +256%→**+47.0%**(0.84→**0.36**)、
  鸡蛋 −27%、焦煤 −29%;**生猪反而略好**(它只有 2,520 条反推行)。
  **策略没变差,是记分牌一直读错** —— 实盘成交盈亏一直是这个水平。
  `VARIETIES.backtest` 五串全部重算。
- **DEC-112 鸡蛋进场信号换成机构成本;纯碱组合实验立项**(2026-08-22)。
  运营者诊断「流量信号慢了」被两个实验证实;成本状态信号
  (机构在场 + 价不劣于其成本 + 本轮卸仓 ≤30%)在鸡蛋五道闸门全过
  (t+1.91,夏普/回撤首次胜过恒定做空),已上引擎:`signal_source: "cost"`
  按品种注入,**出场四件套一字不动**;引擎与研究实现逐笔对拍一致,
  其余四品种逐笔零变化。纯碱组合实验预注册在 `research/PLAN_SA_COMBO_v1.md`
  (未执行);散户单路尚未走闸门,排在其后。挡单原因带数字直出页面。
- **DEC-111 修掉回撤判据的跨合约污染;纯碱回退;展示价改收盘价**(线上 bug)。
  `past`(做多要求的「先有回撤」)是在**混合主连**上 `pct_change` 的,
  换月跳空被当成真涨 —— 纯碱 8/13 由 SA2609 换 SA2701,990 → 1031 全是合约价差,
  于是 8/20 真实近 20 日 **−3.29%** 被算成 **+2.38%**,一个真做多信号被挡掉。
  判据符号被改写:鸡蛋 12.2%、生猪 10.4%、纯碱 8.6%、玻璃 7.5%、焦煤 5.6%。
  修成复用已处理换月的逐日 `ret` 连乘,加两条测试(含反证)。
  **纯碱 `long_needs_dip` 回退 True → False**:DEC-110 开它的唯一理由
  「两档夏普打平」在干净数据上不成立(开−dip 0.36/+47.0% vs 开+dip 0.30/+31.5%);
  修完 past 后那笔做多在两档下都会回来,回退只是让配置站在成立的理由上。
  鸡蛋排序也翻了(关 0.69 > 开+dip 0.59)但**有意不跟** —— 26 笔样本,噪音量级。
  **页面价格改显示收盘价**(运营者指定),盈亏与成本仍一律走结算价。
- **DEC-109 触发信号显示做多/做空 + 做多开关在新口径重扫**。
  进场方向抽成共用判据 `entry_side`,`replay` 与 payload 共用(前端自己推正是
  DEC-104 的病根);payload 加 `signal.entry_side` / `entry_blocked`,阻塞原因分清
  强度未到 / 做多已关 / 要求回撤 / **机构与散户背离** / 临近交割。
  做多开关重扫:当时结论是「五个品种现行配置全部仍是最优」;玻璃三档 t 全 <1、
  逐年 6/13 为正基本是随机。
  **⚠️ 其中「纯碱开+dip 与开−dip 夏普相同」这一句已被 DEC-111 证伪** ——
  那次比较用的 past 混了换月跳空,修完是 0.36 vs 0.30,不是平局。
  当前三档实数见 `engine/hog_money.py` VARIETIES 上方的表(那里是唯一现行来源)。
- **运营者拍板:玻璃纯碱继续上**(修前视后夏普 0.21 / 0.36、回撤 −67.9% / −61.8%,
  赢基准但很薄;他知情后决定保留)。

- **DEC-107 机构资金页加「已卸掉 X%」,只摆不用**。运营者提出「和机构反向要等机构
  出货出得差不多」,这个数此前页面上没有。两轮研究的结论是:作为**判据**它只在
  纯碱 5 日窗口上过关(玻璃样本外翻号、焦煤否、生猪鸡蛋验不了),
  **横截面无支持且与现行 20 日以上持仓口径不兼容** —— 所以只显示,
  `replay` 一个字不读它,测试扫 replay 函数体钉住边界。
  三处重置(换组/翻向/掉榜冻结)与掉榜混淆提示见 DEC-107。
- **研究留档(未动生产)**:`REPORT_INST_COST_v1`(规则 1 只在玻璃成立、
  规则 3 是动量伪装)、`REPORT_INST_UNLOAD_v1`(五品种过一个)、
  `REPORT_SA_UNLOAD_DEEP_v1`(纯碱 5 日四关全过,置换第 0.2 百分位)。

- **DEC-105 勘误:净持仓的日期只改摘要那行,序列不截**。运营者验收后否掉我的取舍
  ——他要的是一边看某天的各家明细、一边保留完整的图(K 线/净持仓曲线/累计盈亏
  都是上下文)。cut_at 换成 as_of_day:只挑出「摘要与分腿看哪一天」,序列一天不动;
  摘要改为跟后端定的 as-of 日走(此前摘要取序列末日、分腿取 latest_members,
  是两个来源)。域层留了一条「不动序列本身」钉住范围。

### 2026-08-21(DEC-106)

- **全仓复核第二轮**。三栈机械检查全绿(Rust 153 / 前端 123 / 引擎 22+ruff),
  编号无缺号、代码注释的文件引用零失效、LATEST 与实际部署一致。
  找到的五处**全部是同一类:同一件事两处描述或两处实现,其中一处过期或缺失**
  —— DEC-103/104 两个线上问题的共同根因。
  最要紧的一条:**`run-smart-money` 是整条链上唯一不加锁的作业**,cron 与部署的
  ENGINE_REFRESH 两个调用方共用 `$TMP`,叠在一起会把信号算错或算没,而
  ENGINE_REFRESH 失败有意不阻断部署、没人当场发现。已加自己的锁(等 900 秒,
  不共用维护锁——部署跑重算时仍握着那把,共用会自己挡自己)。
  其余:引擎注释「不参与进出场」与方案 C 打架(DEC-104 的上游)、方案 C 的
  「只做空」对 FG/SA/JD 不成立、preflight 窗口提醒改为从 cron 现读并按作业分别
  措辞(新增 cron 未登记会当场 fail)、DEPLOYMENT.md 的 cron 描述停在两轮、
  `REACH_MIN_DAYS` 两语言各一份已加跨文件断言。

### 2026-08-20(DEC-095~105)

- **DEC-104 「进场条件」显示错了数**。运营者问「玻璃为什么没有触发多单进场条件」:
  页面写「需达 1(现 2.09)」却又显示无持仓。方案 C 下引擎比的是**共振后的散户
  信号**(0.92),页面显示的是**机构合计流向**(2.09)。**两个方向都会错**——同一天
  纯碱页面显示 0.52 像是没信号,实际散户 1.19、共振成立,**条件其实满足了**。
  根源是前端类型注释「散户当前只作展示,不参与进出场」切到方案 C 之后就过期了。
  判据抽进 `frontend/src/entry-gate.ts`(10 条测试)。
- **DEC-105 净持仓子页接上交易日**。选 8.19 仍显示 8.20 —— 不是联动坏了,是
  `SeatNetPositionQuery` **从来没有这个字段**,永远取序列最后一天。
  域层加 `cut_at`,整条序列一起截(摘要/分腿/K线/累计盈亏必须停在同一天)。
- **运维**:当天连部署三次,其中一次压在 09:30 UTC 采集轮上,大商所(只有新浪
  一个源)当天缺一半合约,生猪最新蜡烛画不出来。已补采并加了 preflight 的
  采集窗口提醒(±20 分钟,黄字不拦)。采集轮 = 08:00/09:30/13:30 UTC。

- **DEC-103 净持仓页日期轴改由行情日历驱动**。运营者第二次发现「高盛黄金
  3.25-4.27 减仓过程没有蜡烛图」。根因:序列只由席位行喂养,连续掉榜的交易日
  不进序列,前端 category 轴**整列消失**(不是留空),K 线跟着没——行情数据完整。
  **不是旧修复失效**:同一缺陷 2026-08-16 在建仓过程汇总档修过(157a8d4),
  DEC-080 换页面形态后新路径又造了一遍轴。实测高盛(旧名乾坤期货)在黄金上
  738 个交易日里 **71 天被静默吞掉**,最长一段正是 3/25→4/27 的 21 天。
  连带把 cumulative_pnl 的 "0" 兜底改成沿用前值——否则补轴后累计曲线会砸到零,
  画出一根不存在的巨亏。

- **DEC-102 全仓审计九条整改**。运营者要求查代码与文档、列清单、全部修掉。
  最要紧的一条:**引擎终于进了 CI** —— `hog_money.py` + `smart_money.py` 共 2594 行
  决定进场出场,此前零测试、CI 一行不跑(只覆盖 `collector/`)。新增
  `engine/test_hog_money.py`(22 条),并做了**变异验证**:注入三个历史 bug,
  其中一个 21 个用例全部照过 —— 那条分支没人覆盖,于是补了第 22 条。
  **绿色不等于测试有牙,这条以后每次都要做。**

  其余八条:到达概率曲线现在能用 `research/run_shelf_prob.py` 复现(此前代码里
  那句「重估跑本脚本」是空头支票,脚本根本不产出常量;现已验证 124 个数最大差
  0.00);**剩余不足 5 天不再给到达概率**(拟合样本本就是 T≥5,再往下是外推,
  页面显示「—」并写明原因);套利监控拆进 `spread_analytics/monitor.rs`
  (4984→3613+1417 行,**在生产全量数据上跑新旧两版引擎对拍 7 个产出文件,
  剔除指纹与时间戳后完全相同**);DECISIONS 文首写明 `DEC-013`~`DEC-086` 有
  73 个编号没有正文、该用 `git log -S` 查;`research/PITFALLS.md` 写进接手路径;
  另清理游离文件、PITFALLS 归类、四处失效引用。

- **DEC-101 信号文件权限写死 644**。DEC-099 加的「部署自动重算」把机构资金全站
  打成了 403:部署脚本 `umask 077`,它调起来的引擎用 `cp` 落盘就是 600,容器里
  非 root 的 nginx 读不到。cron 的 umask 是 022 所以以前一直没事。
  现在 `run-smart-money.sh` 两处落盘一律 `install -m 644`,收尾再扫一遍
  `$WEB/*.json` 的权限,不对就吼并纠正。**给别的进程读的文件,模式要写死。**
  前端兜底文案也改了:原文一律「请确认信号引擎已运行」,而那天引擎跑得好好的,
  这句话把排查引偏 —— 现在按状态码分流(`frontend/src/fetch-hint.ts`)。


- **DEC-099 部署自动重算信号 + 页面在引擎指纹对不上时露馅**。
  **这是本轮最该记住的运维事实**:机构资金几个页面读的是**每日定时任务产出的静态
  JSON**,不是接口实时算的 —— **部署只换代码、不重算 JSON**,会出现「代码已上线、
  页面还是旧引擎算的数」而且**看不出来**(DEC-096 上线后就这样过了一夜)。
  现在两道保险:①部署在 `stable.env` 落定后自动跑 `run-smart-money`
  (日志 `ENGINE_REFRESH ok`,失败不阻断);②引擎把自己源码指纹写进 payload、
  部署把装到位那份写进 `web/engine.json`,前端不一致就挂「这份信号是旧引擎算的」。
  **用文件指纹不用 git sha**:`/api/v1/version` 给的是**镜像构建时**的 sha,
  镜像复用是常态,拿它判断发布是否生效会误判(我当天误判过一次)。
  这道兜底上线当天自己先坏过一次(YAML 里换行写成字面 `
` → engine.json 非法 JSON
  → 前端 catch 吞掉 → 指纹恒为 null),已加「写完立刻按读取方解析器验一次」。

### 2026-08-20 早些时候(DEC-095~097)

- **DEC-095 套利监控加平台位、到达概率、卖点、止损**。平台位 = 价差自己走出来的
  横盘转折位(前后各 3 日的收盘极值,50 点内并档,存 lo/hi 区间不存单点)。
  **因果**:t 的判定到 t+3 才成立,最近三日的转折当天不算数。
  到达概率是**合并品种与方向**的经验曲线(逐品种/逐方向样本外崩了,那是漂移不是
  品种特性),**固化成 Rust 常量不每晚重算**,逐年 17%~53% 必须写在界面上。
  卖点/止损按 DEC-088 的 trade_side 派。运营者在 A/B 设计稿里选了 A(写百分比)。
- **DEC-096 持仓留在自己的合约里**。原来交割纪律查「今天主力还剩几天」,主力一换
  检查对象就变远月,老仓免费滚过去(FG2609 那笔就是)。改成查**持仓所在合约**。
  **修掉反而更好**:生猪 +79.8→+85.0%、玻璃 +385→+442%、纯碱 +244→+256%。
  `临近交割` 这个出场原因**第一次真的触发**(生猪 1 笔、玻璃 18 笔),以前一直是 0。
  鸡蛋顺势打开做多开关(运营者拍板,**属样本内选择,别写成实测最优**)。
- **DEC-098 并档阈值改按品种波动(0.5σ)+ 行内双向盈亏比 + 撤掉「曾回归」大号数字**。
  ① 并档写死 50 点是在生猪(σ≈94)上试的,套到焦煤跨月(σ≈16)就是 3.2σ,把 31 个转折位
  并成跨 150 点的一档。改 0.5σ 后焦煤 1→7 档,**生猪 6 档不变**(0.5σ=47≈原来的 50)。
  ② 行内并排给两个方向的第一目标/止损/盈亏比 —— 同一位置两方向可以差十几倍
  (实测 3.64 vs 0.27),此前只画信号那一侧。
  ③ 「曾回归 100%」两侧会同时成立(全表 41% 两侧同时 100%、84% 两侧都过 80% 门槛),
  它测的是「动没动过」。大号数字撤掉,`isQualified` 只留 `drift > 0`(合格行 +2.7%)。
  **盈亏比只作显示,不进合格判据**:DEC-067 否决过一次(跨起点的历年中位数),
  换成「到目标 ÷ 到止损」重测**依然否决**——一开始那条完美单调、t=+19 的曲线
  是**收盘价判触碰的离散化偏差**造出来的,把日变动打乱后曲线一点没少、真实减随机
  还是负的(>5 档 t=−3.21)。详见 DEC-098 附录,探针 `research/run_rr_test.py`。
  **教训:拿离散数据检验连续模型,对照组必须同样离散。**
- **DEC-097 四个历年点数重排**。修掉一处硬矛盾:「风险预留 −4965」与「止损 −885」
  (距现价 50 点)并存,而仓位口径写着按前者算,差 100 倍。
  **仓位改按到止损的距离算**;最有利/持到期点数/风险预留收进悬停。

### 2026-08-19 这一天上了七件事(按重要性)

0''. **DEC-093 机构资金加鸡蛋 JD、焦煤 JM**(运营者要求「类似生猪聪明钱跟随」)。
   两者席位数据均自 **2023-08-11** 起(大商所,与生猪同起点),样本同为三年、一种市况。
   **先验再接**,两个品种成色差很远:
   · **焦煤 JM 站得住**:22 笔 +89.7%/回撤 −14.7%/夏普 1.11/胜率 68.2%/**t=+2.61**,
     大幅跑赢基准(+18.2%/0.34/−45.2%),风险条 **0 条**。
   · **鸡蛋 JD 没验出来**:13 笔/夏普 1.04/胜率 38.5%/**t=+1.08**,且**风险调整后
     还不如躺着满仓做空**(1.04 vs 1.14)。上线了,但页面挂 **3 条**风险标识。
   两个都**关做多**(开着一律更差)。三个大商所品种一致,但它们共用同一段行情,
   只能算一次观察,且属样本内选择(DEC-090)。
   **加品种现在只改三处**:引擎 `VARIETIES`、`run-smart-money.sh`(导出/FLOW_CODES/
   拷贝清单)、前端 `FLOW` 列表一行。交割纪律/风险条/跳空说明全部自动继承。
   现有五品种 = 生猪 / 鸡蛋 / 焦煤 / 玻璃 / 纯碱,外加金银那套独立信号。

0'. **DEC-092 玻璃/纯碱保留 + 顶部数据驱动风险条**(运营者裁定,生猪不动)。
   条目由引擎 `risk_flags()` 按**写死的门槛、实算的数字**产出,**不按品种硬编码**
   ——当前生猪 0 条、玻璃 3 条、纯碱 5 条;品种变好条目自己消失。摆在收益数字**之前**。
   三个品种成色留档见 DEC-092:生猪可跟(但 18 笔/3 年/只有熊市)、玻璃边际真但薄
   (夏普 0.62/回撤 −43%/13 年 4 年亏)、纯碱 t=1.94 **已不显著**。
   **别再重推的两条**:① 玻纯的原始信号比生猪**还强**,问题在跳空不在指标;
   ② 玻纯行情来自 czce_official(交易日口径),`open` 就是夜盘 21:00 开盘价,
   16:26 公布 → 21:00 成交**已是最快**,没有更早的窗口。

0. **DEC-091 讲清了「散户反向很准 vs 回撤 −43%」为什么同时成立** —— 信号后第一天
   的超额有 **83~86% 落在隔夜跳空里**(玻璃 86%、纯碱 83%、生猪 69%)。席位排名
   16:26 才公布,所有人同一时刻看到,价格在夜盘/次日开盘一步跳过去。能吃到的日内
   只有 +0.07%~+0.14%。**规则没改(压短持有、延迟进场都试过,更差)**,改成引擎每天
   实算 `edge_split` 写进 caveats。生猪受影响最小是因为它还有 D+5→D+20 的余温。
   **新做任何以盘后数据为输入的信号,都要先做这个拆分。**

1. **DEC-090 合计流向引擎改「次日开盘成交」** —— 审查时查出的未来函数:原来进出场
   都按**信号日结算价**成交,而席位排名是**收盘后**才公布的。改口径之后玻璃从
   +4264% 降到 **+385%**、纯碱 +1522% → **+244%**、生猪 +88.4% → **+79.8%**;
   夏普玻纯从 1.63/2.02 掉到 **0.61/0.80**,回撤翻倍到 −43%/−53%。
   **收益几乎全部集中在信号后第一天,而那一天拿不到。**
   `replay` 现在自带「逐日连乘 == 逐笔连乘」断言,对不上就保留上一版 JSON。
   **新口径下纯碱 t=1.94 已经不显著**,玻璃 t=2.31,生猪 t=2.52。
2. **DEC-088 套利监控:进场标与合格标此前指的不是同一笔交易** ——
   统计取侧由 `alert.or(turn)` 改 `turn.or(alert)`;`monitor_turn` 两侧并存的平局
   判据由「离门槛更远」改「离门槛更近(刚穿线)」;徽标写方向(⚡ 进场 · 做空价差);
   两侧相反时用 `revert_alt` 摊开另一侧 + ⚠ 两侧方向相反。
   全量回放 88,473 行:⚡ 692 → 799(消失 10、新增 117)。
3. **DEC-089 交割纪律**:LH/FG/SA 主力进入窗口止点前 10 个交易日**强制平仓且禁止
   进场**。写在共用 RULES 里,鸡蛋焦煤将自动继承;**贵金属不加**(运营者拍板)。
   回测数字未变(主力总在撞线前换月),是前瞻性安全网。
4. **DEC-087 FG−SA 相对资金流向**进套利监控,只作背景不作进场信号。
- **套利监控现行规则(读时判定,存事实不存结论)**:
  - 报警:当年轨/历年轨位置进两端带,页面阈值可调(默认 5%);「新」=段首日。
  - ✓合格:留一法历年触及率 ≥80% 且持到期中位 >0(DEC-062/063)。
  - 已拐头:近 20 日触过 3% 带且自极值回撤超**品种档位**——**JM 20%、JD 5%、
    FG-SA 8%、其余(含 AP)10%**(DEC-070 定档,**DEC-075 用干净数据重测后把 AP
    退回默认档**);标永远带 ·高位/·低位。
  - ⚡进场 = 今天刚穿线 × **本轮首次**(turn_crosses==1)× 合格 × 未进红线;
    ⚠信号差 = 20 日内穿线 ≥2 次;临近交割 ≤15 日压制 ⚡,16~39 日衰减区(DEC-067)。
  - 组合圈定:六品种,持仓量前 6;玻纯两腿都要主力月(1/5/9),**焦煤至少一腿
    主力月即成对**(DEC-072,JM 12 对);历年已到期实例全量入库供历史视图
    (DEC-071,2013 起 8.8 万行/618 实例,过期灰标,历史信号翻页)。
  - 手工产业备注挂**月份模板**(品种+月+品种+月),跨年共享,行内可编辑(DEC-069)。
  - **现货与基差**(DEC-074):行内显示该品种现货价、主力基差与历年分位。
    **是产业背景不是信号**——跨期两条腿相对同一个现货,基差之差 ≡ 价差本身;
    增量在现货序列与基差水平/分位。苹果无现货报价。数据源生意社经 akshare,
    日更管线里回看 8 天,失败不阻断后续步骤。
  - 回归率/MAE:按月-日历年对齐到 5A 窗口止点,触及即回归;补仓参考=MAE 中位,
    风险预留=MAE 最大(DEC-062/067)。
  - **取价口径**:`coalesce(nullif(close_price,0), settlement_price)`——郑商所对
    无成交合约写收盘价 0,直接用会造出假极值(DEC-073)。
- **机构资金引擎**:**两个品种、两套独立引擎、两份产物**,同一个 cron
  (`run-smart-money`,工作日 18:10/22:10 北京)里先后跑;engine/ 随发布包自动装。
  - **金银(AU/AG)**:纯席位跟随 v4.2 + 中继 + 主力跑路警报 → `signals.json`。
  - **合计流向三品种**(生猪 LH / 玻璃 FG / 纯碱 SA,DEC-083→086):一个引擎按品种
    逐个跑、各写各的 JSON(`hog_/fg_/sa_signals.json`),一个品种挂了不影响其余。
    现行策略是**方案 C:共振进场(聪明钱与散户反向同号且散户 z 过门槛)/ 散户出场**。
    席位组**每年**按历史 alpha 滚动重选前 5;**信号用品种合计、计价逐合约**。
    **规则按品种实测,不许照抄**(DEC-086):生猪只做空、玻璃纯碱双向;
    `long_needs_dip` 只有生猪留着(玻璃带它收益差 3.5 倍)。
    回测:LH 18 笔 +88.4%/夏普 2.80、FG 206 笔 +4264%/1.63、SA 100 笔 +1522%/2.02。
    **FG 毛糙得多**(夏普 1.63、回撤 −19.4%、逐年有 3 年为负),别只看累计收益。
  - 生猪那段失败只告警、不拖垮金银(`if docker run …; then` 包着)。
  - **做多支路已关闭**(`long_enabled=False`,DEC-084):它跟的是「机构减空」不是
    「机构转多」;而机构真转多的那 14 天(2025-07)之后 20 日主力仍平均跌 1.18%。
    关掉后 21 笔全空头、胜率 71.4%、夏普 1.96→2.39。**页面仍显示机构转多状态
    并醒目提示,只是不进场**——开关留着,样本攒够再议。
  - **绝对收益没跑赢「恒定满仓做空」**(+79.7% vs +99.2%),赢的是回撤
    (−9.4% vs −14.8%)与夏普(2.39 vs 1.65)。界面 `compare` 必须摆着这个对比。
  - **散户反向维度**(DEC-085,**只展示不交易**,`resonance_trades=False`):
    东方财富、平安期货、徽商期货三家**跨品种固定名单**,反向取用;与机构流向同号
    即「共振」。**不交易的理由是「与主信号表现相当」,不是「它不够好」**:同一时间轴
    (2023-08 起)主信号 21 笔 +79.7%/夏普 2.39、散户反向 +91.4%/2.42、共振进场
    +88.4%/2.80,但**单笔均值差的 t 只有 0.22~0.49,分不出高下**。
    它的定位是**独立的第二意见**(与主信号相关 0.59)——看两者一致还是背离比看它
    自己的方向更有用;共振时回撤小一半(−4.1% vs −9.4%),尚不显著,继续观察。
    名单**不许逐品种重选**(会丢掉「无挑人过拟合」这个最大优势),也**不要加人**
    (实测四家不如三家)。
  - 改引擎或研究回测任一边,都要跑 `research/run_lh_parity.py` 逐笔对拍(现 36 笔全等)。
- **数据链**(北京时间):**16:00 → 16:25 → 16:40**(抢早轮,DEC-076 新增)、
  17:30 → 17:55 → 18:10、21:30 → 21:55 → 22:10;每轮都是采集 → 官方席位 → 引擎,
  顺序是硬要求。**16:xx 那轮是加出来的,原轮次保留兜底——交易所 15:30~16:30 才
  陆续发布结算与持仓排名,16:00 能否采全待观察,几天后看日志再决定是否撤旧轮。**
  改了 `compute-spread-monitor.sql` 的部署,完成后必须手动重算
  (见 RELEASE_PROCESS §八),脚本只从 `stable.env` 的 `$previous_release_dir` 取。
- **会话 30 天**(DEC-076):绝对与闲置都是 30 天(原来是 7 天/**4 小时**,每天重登
  的元凶是闲置那条)。改配置只影响新登录——运营者已于 2026-08-18 重登,**此事已闭环**。
- **会员名归一**(读时归一,不落库,改别名**不需要重算**):别名表只在
  `spread_analytics.rs` 的 `MEMBER_ALIASES` 与 `engine/smart_money.py` 两处,必须同集。
  **格林三名归一到「格林大华」**(DEC-082):郑商所写「格林大华期货」、上期所大商所
  写「格林大华」(品种零重叠),「格林期货」是 2014-03-27 前的旧名(交接日持仓逐笔
  相同)。**「大华期货」是另一家公司,不许并**——它与「格林期货」同日同合约并存
  266 次,2013 年才被吸收合并,已有断言钉死。
- **席位页**(DEC-080,推翻 DEC-077 的页面形态):顶部一组选择——**席位多选**
  (上限 10 家,带拼音搜索与收藏)+ 交易日,**两个子页共用**:
  - **席位持仓**:按家分段罗列,勾几家列几段(名次与增减是逐家公布的,不合并);
    筛选器只出一次。走单席位接口,前端并发取 N 份。
  - **净持仓**:原「建仓过程」那四张图(行情/净持仓/当日盈亏/合约累计盈亏),
    按所选那几家合计,读 `seats/net-position`。常驻摘要带多空手数与两腿成本
    (手数紧跟自己那条腿的成本),推算日有单独一条更淡的底色带(DEC-079);
    **摘要下面还有一排逐家的手数与均价**(DEC-082,`latest_members`,与合计同源、
    分项必然对得上合计;掉榜那家报「当日掉榜,持仓未知」不报 0 手)。
  - 单席位用法 = 多选框里只勾一家。多选框**不折叠**——这组选择两个子页共用,
    最该常驻可见。独立净持仓页面已删,旧地址重定向。
  - **一家不选是允许的状态**(DEC-082):清空不再自动补名录第一家,页面出
    「先在上面选几个席位」。拼音搜索在**中文输入法**下也可用(监听 el-select 上
    冒泡的 `compositionupdate`;el-select 自带的 filter-method 在 IME 预编辑期间
    根本不触发,这是那次「输入 gs 完全出不来」的根因)。
- **掉榜反推**(DEC-064/048):反推行由 `infer-offboard-seats.sql` 写库,**两条链
  各跑一次**——collector 里一次(大商所席位它自采)、官方席位脚本末尾一次
  (上期所郑商所席位那时才入库)。后者是 DEC-079 补的:少了它,官方源品种的
  反推永远滞后一整轮,报告表(读时反推)与净持仓页(读库)会对不上。

## 最近三批变更(更早的看 git log)

- **DEC-086**(生产=66cedac):三件事。①**套利监控 BUG**(运营者报「品种下拉只剩
  鸡蛋」):`compute-spread-monitor.sql` 的 delete 无条件执行,16:00 抢早轮在只有
  大商所到货时把三天快照删光重写成残缺的。已加数据完整性门(判据=大商所与郑商所
  是否都到),部署后三天全恢复 56 组六品种。**这同时回答了 DEC-076 的待办:
  16:00 采不齐,原轮次必须保留。**②生猪改**方案 C**(共振进场/散户出场),
  18 笔 +88.4%/回撤 −4.1%/夏普 2.80——但三个候选统计上分不出高下(t=0.22~0.49),
  选它是判断不是证明。③**玻璃、纯碱上线**,引擎泛化为多品种;规则按品种实测,
  生猪只做空而玻璃纯碱双向。

- **DEC-085**(生产=1642c5f):**散户反向维度**上线(生猪,只展示不交易)。
  缘起是运营者一句「散户确实是反向指标」,四轮研究验下来成立:东方财富、平安期货、
  徽商期货三家**跨品种固定名单**,反着跟,在 AU/LH/FG/SA 四个品种样本外都有效
  (AG 除外——那三家在白银上没有一致方向)。**它相对「找聪明钱」的优势是没有挑人的
  过拟合**,新品种可直接套用。运营者按「散户天然站多头、一致净空的是套保」剔掉
  格林大华,剔干净后生猪 t 从 2.84 翻到 5.96。共振(与机构流向同号)时信号最强、
  背离时基本消失。**顺带修正 DEC-083 对玻璃的判决**:玻璃跟机构不行,反着跟散户行。

- **DEC-084**(生产=6522bff):生猪按运营者当天四点反馈修订。①选人 3 个月→**一年**
  (换组 10→3 组,且后两组只差排序;胜率 58.3%→63.9%、回撤 −10.2%→−8.6%,代价是
  累计 +104.5%→+86.5%;三档不单调,差异多半是噪音)。②**关闭做多支路**:它跟的是
  「机构减空」不是「转多」(15 笔进场时机构全部仍净空);而机构真转多的 14 天之后
  20 日主力仍跌 1.18%。关掉后 21 笔全空、胜率 71.4%、夏普 2.39。③界面摆出与
  **恒定满仓做空**的对比——策略 +79.7% 其实**没跑赢**基准 +99.2%,赢的是回撤与夏普。
  ④翻页统一 el-pagination。否决了「最短持有期」这个改进(3/5/7/10 日不单调=噪音)。

- **DEC-075**(生产=2eaac3f):运营者问「SA 修了,FG-SA 8% 要不要重测」——要,
  且不止 FG-SA:回测数据当初与生产用同一个有缺陷的取价条件,郑商所 FG/SA/AP 全被
  污染。干净数据重测:**AP 的 8/10/12 三档结果完全相同、逐年一致性 8/8→5/8、
  样本外挑参收益归零**(+16.5% vs 固定 10% 的 +16.5%),AP 已退回默认档;
  JM/JD 数据未污染、机制+数据双证,保留。
- **DEC-073 + DEC-074**(生产=e7781ed):①做基差探针时挖出**收盘价 0 被当价格用**
  (9,795 行,污染 79 组合 200 行,假极值把 AP2603−AP2605 区间下界钉在 −10753 vs
  真实 −1313),取价改结算价兜底并全量重算;**连带撤回我对 −8020「真实观测」的
  误判**,护栏已由 202608180004 收回 ±10(重算后实际值域 −2.85~5.01)。
  ②接入现货价与基差(新表 + 采集脚本 + 日更接入 + 行内展示)。

- **DEC-072**(生产=6d87299):焦煤组对放宽 either 模式,JM 3→12 对;engine
  clean_seat 去重键补剥「(代客)」括号(防御性,详见 PITFALLS 四)。
- **DEC-071**(37654f7):历史信号跨年全量——过期历年实例入库、打灰标、翻页。
  (当时为回填放宽了 sane 护栏,理由后被 DEC-073 推翻,护栏已收回 ±10。)
- **DEC-069/070**(fe46f71/172b9ce/355dacf):手工产业备注(47 模板已按两份交易
  文档预填)+ 回撤档分品种 + ⚡只认首次 + 历史信号视图。

## 待办与观察项

**~~下一个交易日(2026-08-19)要做的一件事~~ 已有答案(DEC-086)**:16:00 抢早轮
**采不齐**——实测大商所 16:03 到、郑商所与上期所 16:26 才到。原轮次(17:30)
必须保留,不要撤。抢早轮的价值只剩「早一点拿到大商所数据」,而它一度把套利监控
写残(已加完整性门挡住)。以下为原文,留作背景:

**下一个交易日(2026-08-19)要做的一件事**:16:00 抢早轮是当天首次真实运行,
盘后核对它到底采到了什么——三条链的日志在
`/var/log/futures-collector.log`、`futures-official-seats.log`、
`futures-smart-money.log`;数据侧看
`select exchange, min(updated_at) from price_history where trade_date='2026-08-19' group by 1`
(转北京时间)。**四家交易所若在 16:0x 就齐了,再考虑撤掉 17:30 那轮;不齐就保持
现状**——原轮次本来就是为此保留的兜底,不要先撤后验。

- **待验收:席位页新形态**(席位多选 + 收藏 + 席位持仓按家分段 + 净持仓合计)。
  重点看勾多家时:席位持仓是否每家一段、净持仓四张图是否按合计算、掉榜与推算
  两条底色带是否分得开。**建仓过程的代码不再是死代码**——它就是现在的净持仓
  子页,不要再按 DEC-077 那条待办去清理它。
- **待办(DEC-085)**:散户反向维度跑一段实盘后再评估。**判据不是"它是不是更好"**
  ——回测里三个信号统计上分不出高下(t=0.22~0.49);要看的是**共振时回撤更小
  (−4.1% vs −9.4%)这个方向能不能在实盘里延续**,那才是它可能的真价值。
  **别急着改规则**,现有规则有 DEC-084 的完整论证。
- **未落地但已验证的两条**(研究报告在 research/):①纯碱 SA 两条路都行
  (聪明钱 6 正 1 负、散户反向 t=+4.70);②FG-SA 价差信号(两品种流向之差,
  t=+5.43、滚动样本外全正),平台已有套利监控在盯这个组合,可考虑接进去。
- **待验收:生猪页**(机构资金 → 品种选择器切「生猪」)。当前状态是**观察中**
  (关掉做多后,那笔 08-04 的多单不再存在)。看五项:今日信号三张卡(含「机构方向」
  行)、历史 **21 笔**表与新翻页、**与恒定满仓做空的对比表**、席位组重选历史
  (应只有 3 组)、策略方案里的边界说明。
- 运营者视觉验收:席位页四项、监控页新徽标/备注/历史视图。
- DEC-067 备选:⑥基差数据源**已做**(DEC-074);未立项的剩 ⑤仓单分位旗、
  ③移仓换月标记(建议不做,与红线重复)。
- 观察:鸡蛋限仓 120 手公告;LH2611 净空历年 2.6~3 倍(套保结构,分析时要看,
  DEC-072 对话有完整数据);拐头质量口径收紧选项(回带后再穿才计第二次)未拍板。
- DCE 东财 change 历史回填(08-06 前)按需再做。
- 基差历史已回填完成:**9,134 行 / 2018-01-02 起**(FG/JD/JM 2090 天、SA 1507、
  LH 1357),历年分位已全量生效(2026-08-18 实测:鸡蛋基差处历史 3.4% 分位=8 年
  最贴水,生猪 80.3%=期货升水偏高)。
