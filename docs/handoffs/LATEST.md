# 最新交接状态

- **回归率换口径已上线(2026-08-17,生产=`7976424`,deploy 32003349815,`DEC-062` 修订)**:
  运营者指出上一版算错——固定 20 个交易日与合约剩余寿命无关(鸡蛋 09 合约 8 月末就
  退出散户可交易区间),而且只看第 20 天那一个点(中途回落过又涨回去的算「没回归」,
  可套利仓那期间随时能平)。同一模板从 6-01 起算,旧口径 45%,**新口径 12 年 12 次
  全都曾跌破起点**。新口径:可交易窗口照 **5A 窗口引擎**(止点 = 先到期那条腿的
  `last_weekday_before_delivery`),历年按**月-日**对齐看到各自窗口止点,**曾经触及**
  即算回归,只用已走完的年份;幅度用点数,不设样本年数门槛(均运营者定)。
  **存四个数不是一个比率**——单看回归率会骗人:剩余期一长就趋近 100%(苹果剩 128 天
  的组合全是 100%),而 **JD2612/JD2701 回归率 100% 却「持到期」中位 −166 点,方向
  是反的**。所以并列给 hit/n、move(最有利)、drift(持到期,正数=朝回归走)、days。
  三档 3/5/10 全删(新口径与阈值无关)。**上线前与 Python 参考实现逐行对拍(381 行
  交集、10 个字段全等)**;部署后已用发布包脚本实跑 window_days=45(1137 行,10.7 秒),
  生产数字与参考实现一致(JD2609/JD2701 高位 11/12 年、最有利 +102、持到期 +81、
  剩 12 天)。顺带修掉 `legs` 取数的来源优先级瑕疵:原写 `like '%\_official'`,大商所的
  `dce_official_history` 匹配不上、官方数据反掉到最后一档(实测影响为零,两种写法
  结果逐字节相同)。**视觉验收仍未做**(要登录,不代输凭据)。

- **⚠️ 两个当天实抓的教训(都已闭环)**:
  ①**只读验证截掉写操作,就检验不到约束**。为了绝不写生产,验证脚本把 insert 之后
  整段截掉,于是只验了计算没验落库——`move >= 0` 这条写错的约束一直到部署后跑全量
  重算才被 check 拦下(LH2609/LH2701 低位 move=−45:低位段的 move = 后续最高 − 起点,
  历年后续最高仍低于起点时它就是负的,而且这是有效信息)。已由 `202608170003` 修掉。
  **下次做只读验证要另配一遍值域检查。**
  ②**迁移必须能在已处于目标状态的库上重跑**(第二、三次重复的教训):全部加列改
  `add column if not exists`、约束改 `drop constraint if exists` + `add`,并加测试钉住
  数量。已在生产库上实测重跑通过。

- **套利监控加「新触发」标记与历史回归率已上线(2026-08-17,生产=`4ab7f10`,
  deploy 31998277987,`DEC-062`)**:起因是「今年鸡蛋焦煤都是历史级价差,3%
  触发时有多少套利席位进场」——按监控脚本口径重算全部历史后做事件研究,
  三条结论与页面原有读法直接冲突:①触发日席位同步反向建仓的活跃度只有平常日
  的 1.14~1.16 倍,**极值并没有把套利资金吸引进来**;②**价差在极值处不回归**
  (段首日起 20 日按回归方向计:鸡蛋 445 段仅 45%、焦煤 68 段仅 **29%**,
  这条只用价格数据,最稳);③焦煤 2026 年 **64.2%** 的交易日都在 3% 触发,
  阈值在单边突破行情里失去筛选力。对应改动:表存**前一交易日两轨位置**(不存
  「是不是段首日」,读时套同一阈值重判)、按**月份组合模板**存 3/5/10 三档 ×
  低高两侧的回归命中数与样本数、页头文案改中性。实测同为低位触发,鸡蛋
  JD2611/JD2612 回归率 45/54=**83%**、JD2609/JD2702 只有 15/37=**41%**,
  差一倍以上而页面此前画成同一个红条。**回归率按当年轨划分极值段是工程折中**
  (合成轨要逐行 percentile_cont,推到全历史成本从百来次变几万次),页面已标口径。
  部署后已用发布包脚本实跑 `window_days=45`(1137 行,6.8 秒),md5 与仓库
  逐字节一致。**视觉验收未做**(页面要登录,不代输凭据),请运营者亲眼看一次
  `/spread-analytics/monitor`。

- **⚠️ 开发期 DDL 误落生产(2026-08-17,已闭环,值得记住)**:做「事务里跑一遍
  再 rollback」的只读验证时,拼接脚本的 grep 转义没生效,compute 脚本自带的
  `commit;` 没被剥掉,整套加列 DDL 与一次窗口重算**当场落进生产库**。后果本身
  可控(列可空无默认、当时的生产代码不读新列、日更不失败),真正的代价是让迁移
  在正式部署时必然撞「列已存在」。已把 14 个加列改 `add column if not exists`、
  约束改 `drop constraint if exists` + `add`,并加测试钉住数量。**规则:迁移必须
  能在已处于目标状态的库上再跑一遍而不报错**——同类教训仓库里已有一次
  (concurrently 预建索引),这是第二次。**另:想「跑一遍再回滚」时,别用 grep
  剥 `commit;` 这种脆办法,要么复制一份改好的脚本,要么就别在生产库上试。**

- **采集源到达时刻画像已上线(2026-08-16 深夜,生产=`69ffaff`,deploy
  31953109836)**:seat/price_history 的 `loaded_at` 自此固定为「首次入库
  时刻」,任何 upsert/修复 update 都不许碰——`min(loaded_at)` 按(交易日,
  交易所)聚合即数据源到达画像(首页交易所行 + cron 自检 3.5 节展示,例
  08-14 席位 DCE 21:31/CZCE 21:34/SHFE 21:34);「最近装载触碰」语义整体
  搬去新列 `updated_at`(迁移 `202608160001`,可空无默认,全部 8 条写入
  路径同批统一,4A E2E「刚写过」断言改看它)。**教训:首版(4287059)只改
  装载不加列,让画像挤掉了 E2E 的验收语义,当晚部署被
  `seat_rows_actually_written` 静默 test 拦下整轮回滚(31951652344,拦得
  对)——写入侧改时间戳语义,先搜谁在消费它**。报告表 15 分钟缓存
  (`8255e93`,方案 A)同批已在生产。迟发告警阈值按约攒两周画像再定,
  不拍脑袋。

- **引擎新规则:高盛持有闸已上线(2026-08-16 深夜,`DEC-061`)**:仅黄金,
  消退满 7 日时高盛净多 >2000 手则不卖继续持有,高盛离场次日恢复消退卖出。
  AU 全期 +159.4→+179.7(四笔主升段大单),掉榜=按「低于第 20 名门槛」保守
  判定。**查席位数据必须按 source 过滤,反推行(reboard_inferred)不得进
  规则与榜单统计**(本轮实抓教训)。DEC-060 的"消退即上限"已被此条部分
  突破并就地修正。待办:建仓页掉榜日显示「<门槛」上界。

- **派发段攻坚已收官并留档(2026-08-16 深夜,`DEC-060`)**:四层 12 个变体
  全量回测的最终裁定——**卖出端无信号是结构性的**(第七次独立验证:机构
  卖出=锁利润不含观点,判顶天花板=期望归零),引擎零改动、派发警示不做。
  出场规则六路全灭,消退 7 日即出场就是上限;强撤退态反而统计看涨
  (t=+4.79,备查不规则化)。**再想动出场规则或做判顶警示,先读 DEC-060**。

- **引擎新规则:中继共振质量闸已上线(2026-08-16 深夜,`DEC-059`)**:5 日窗
  触发席位 <2 家不做中继(单家擦线不算共振,2025-10-15 案例驱动)。发布
  记录见 DEC-059 末尾(生产=`b67bbf9`,引擎已重跑,成熟期 49 笔/67.3%/
  +138.9 三项全升)。

- **引擎新规则:中继 MA200 趋势闸已上线(2026-08-16 晚,`DEC-058`)**:收盘在
  200 日均线下方不做中继再进场,首进场/重挫共振不受影响。来历=运营者问
  "中继在 2006-2011 有没有用",2008 起全量回测发现 2011-18 八年中继净拖累
  -11.5%、全部价值在 2019 后牛市;四方案对照 T1 胜出(证据表全在 DEC-058)。
  改引擎前先读它。

- **前端已换 TradingView 风格双主题(2026-08-16 晚,`HANDOFF_20260816_2200.md`)**:
  全站颜色收进 token(CSS 用 `--tv-*`,ECharts 用 `chartTheme.ts`,双份同源
  改一边必须同步另一边);红涨绿跌不许翻转;顶栏可切深浅色,缺省深色;
  **改前端样式先读那份交接的第 0 节**,EP 深色特异性坑与 `:key` 重挂载机制
  都在里面。本地视觉验收用 `npm run dev:proddata`(生产数据代理,DNS 污染
  与 `/smart-money/` 尾斜杠两个坑见 vite.config.ts 注释)。引擎、业务逻辑、
  布局零改动。

- **(引擎与平台功能的最新权威交接)`docs/handoffs/HANDOFF_20260816_0230.md`**。它覆盖 08-15 白天
  至 08-16 凌晨的全部工作(平台六批功能 + 引擎两条规则变更 + 席位海选收官),
  收口时**生产=GitHub=本地=`5535478`**。四条最要紧的:
  - **引擎买点规则变了**:v2 区间条件当日采纳次日撤销(`DEC-055`);新增
    **重挫共振买点**(仅黄金,伦敦金美元 ±10% 分轮口径,重挫态共振→免起点
    条件市价买入,`DEC-056`)。改引擎前先读 `RULES["crash_dd"]` 注释块。
  - **席位海选收官**(`DEC-057`):信号组七家为尽头,国贸/中泰永久排除,
    新增「当前持仓照妖镜」筛选维度。
  - **引擎多了一条外部数据链**:伦敦金基线 `engine/data/london_xau.csv`
    随发布包走,GC=F 增量缓存 `gold_usd.csv`,重叠区基线为准。
  - **引擎改动部署后必须手动 run-smart-money 一次**,deploy 只装文件不重跑。
    **判断跑没跑过看 `signals.json` 的 `generated_at` 与 `rules.buy` 串,别看
    文件 mtime**——引擎文件会被二次部署刷新 mtime,08-16 已因此差点误判(实为
    已跑;当日 01:23 复核重跑,新旧产物除 `generated_at` 外逐字节相等)。
  - 待办:08-17(周一)收盘后 `futures-cron-selfcheck` 验新规则首次 cron 实跑。

- **(历史)权威交接:`docs/handoffs/HANDOFF_20260815_0150.md`**。它覆盖 08-11 之后的全部
  工作(更早的交接文件已清理,见文末保留策略)。三条最要紧的变化:
  - **生产已换机**：`qh`（172.238.18.206，`shejimao.trade`），老机 172.238.11.174
    实例已于 2026-08-15 删除。**没有热备机器了**，退路只剩每日异地 dump（推 ssp）
    与部署链回滚，见 `DEC-050`。
  - **导入通道整体不存在了**（`DEC-049`）：27 张表、后端 16,000 余行、`worker`
    服务与前端导入中心全删，采集走直灌，库表 57 → 30。
  - **引擎规则改过**：信号组 7 家（去国投，`DEC-051`）、消退 7 日（`DEC-052`）；
    机构成本统一逐合约口径并有对拍守卫盯着（`DEC-053`）。**动引擎前先读
    `research/PITFALLS.md`**，六轮系统性寻优全部负结果，别重复。
  - 生产运行 **`ac152a2`**（2026-08-15 部署成功，已核 `/api/v1/version`）。


- **不得动 `research/` 下任何文件**,那是运营者另一个会话的席位因子预测模型,会长期处于改动状态。

- 2026-08-16 起交接文件只保留最近三份(0150/0230/2200)+ 本文件,更早的已从工作树删除,需要时从 git 历史找回。

接手须以 Git、Actions、VPS 实态复核,不盲信摘要;不得输出密钥、恢复回填、清理数据或未经授权合 main/打标签。
