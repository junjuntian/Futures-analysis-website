# 最新交接状态

- **前端已换 TradingView 风格双主题(2026-08-16 晚,`HANDOFF_20260816_2200.md`)**:
  全站颜色收进 token(CSS 用 `--tv-*`,ECharts 用 `chartTheme.ts`,双份同源
  改一边必须同步另一边);红涨绿跌不许翻转;顶栏可切深浅色,缺省深色;
  **改前端样式先读那份交接的第 0 节**,EP 深色特异性坑与 `:key` 重挂载机制
  都在里面。本地视觉验收用 `npm run dev:proddata`(生产数据代理,DNS 污染
  与 `/smart-money/` 尾斜杠两个坑见 vite.config.ts 注释)。引擎、业务逻辑、
  布局零改动。

- **(引擎与平台功能的最新权威交接)`docs/handoffs/HANDOFF_20260816_0230.md`**。它覆盖 08-15 白天
  至 08-16 凌晨的全部工作(平台六批功能 + 引擎两条规则变更 + 席位海选收官),
  收口时**生产=GitHub=本地=`5535478`**。四条最要紧的:
  - **引擎买点规则变了**:v2 区间条件当日采纳次日撤销(`DEC-055`);新增
    **重挫共振买点**(仅黄金,伦敦金美元 ±10% 分轮口径,重挫态共振→免起点
    条件市价买入,`DEC-056`)。改引擎前先读 `RULES["crash_dd"]` 注释块。
  - **席位海选收官**(`DEC-057`):信号组七家为尽头,国贸/中泰永久排除,
    新增「当前持仓照妖镜」筛选维度。
  - **引擎多了一条外部数据链**:伦敦金基线 `engine/data/london_xau.csv`
    随发布包走,GC=F 增量缓存 `gold_usd.csv`,重叠区基线为准。
  - **引擎改动部署后必须手动 run-smart-money 一次**,deploy 只装文件不重跑。
    **判断跑没跑过看 `signals.json` 的 `generated_at` 与 `rules.buy` 串,别看
    文件 mtime**——引擎文件会被二次部署刷新 mtime,08-16 已因此差点误判(实为
    已跑;当日 01:23 复核重跑,新旧产物除 `generated_at` 外逐字节相等)。
  - 待办:08-17(周一)收盘后 `futures-cron-selfcheck` 验新规则首次 cron 实跑。

- **(历史)权威交接:`docs/handoffs/HANDOFF_20260815_0150.md`**。它覆盖 08-11 之后的全部
  工作，**下面 08-11 及更早条目里凡涉及机器、生产版本、导入通道、`research/` 目录的
  描述一律作废**（技术红线仍然有效，历史条目按约定不删）。三条最要紧的变化：
  - **生产已换机**：`qh`（172.238.18.206，`shejimao.trade`），老机 172.238.11.174
    实例已于 2026-08-15 删除。**没有热备机器了**，退路只剩每日异地 dump（推 ssp）
    与部署链回滚，见 `DEC-050`。
  - **导入通道整体不存在了**（`DEC-049`）：27 张表、后端 16,000 余行、`worker`
    服务与前端导入中心全删，采集走直灌，库表 57 → 30。
  - **引擎规则改过**：信号组 7 家（去国投，`DEC-051`）、消退 7 日（`DEC-052`）；
    机构成本统一逐合约口径并有对拍守卫盯着（`DEC-053`）。**动引擎前先读
    `research/PITFALLS.md`**，六轮系统性寻优全部负结果，别重复。
  - 生产运行 **`ac152a2`**（2026-08-15 部署成功，已核 `/api/v1/version`）。

- **（历史）最新权威交接：`docs/handoffs/HANDOFF_20260811_1320.md`**（当日下午场），与同日 `HANDOFF_20260811_0310.md` 配合读，其余历史条目不变。
- **两张历史表已全部灌满（先采数据阶段完成，机构资金 M0 前置就位）**：八品种价格 + 席位 + 品种汇总，全部到 2026-08-10，全在运营者 workspace，汇总零重复。三禾 783/783 交易日、0 失败。大商所/上期所**从来没有**官方品种汇总行，已自算补齐 143 万行（`variety_total_is_computed` 与官方分清，只补官方缺的组合）。
- **RLS 性能红线（本轮事故）**：`regexp_replace` 等非 leakproof 函数放进 seat_history 的 WHERE 过滤，RLS 下索引全废——futures_app 121ms 的查询在 futures_runtime 下 60 秒超时，席位页会员下拉整个空掉。**性能验证必须 `set role futures_runtime` + `set_config` 后做**，futures_app 绕过 RLS 测的不是生产路径。过滤一律原始列，归一化在 Rust；有测试盯着。
- **psql 脚本红线**：文件内 `\set` 会覆盖命令行 `-v`，带参数的脚本必须 `\if :{?var}` 守护默认值——compute-seat-totals 的日更小窗口曾因此从未生效。
- **构建提速已上线**：变更感知（输入路径没变→重打标签复用，等价性 deploy 三层实核）+ api/worker 合并编译。全量 25→10~17.5 分钟；复用路径因缺 `actions: read` 首跑空转已修，实战首验见 1320 交接的「下一步」。构建侧 `paths` 与 deploy 侧 `image_paths` 必须同步改。

- **最新权威交接：`docs/handoffs/HANDOFF_20260811_0310.md`。** 之前的 `20260809_2130` 及同日两份仍然有效，其中被本轮取代的地方在下面逐条标注，历史结论未删改。
- **本轮最重要的一条：数据曾整体落在错误的 workspace，且不报任何错。** 生产上有 31 个 workspace，绝大多数是历次验收留下的 E2E 空间；回填装载脚本用 `select id from workspaces order by id limit 1` 取到的是 `Phase 3C E2E 1`。于是 23.5 万行价格、380 万行席位落在测试空间，运营者的 `019f94f3-c26a-7391-bbee-2d2fd4f8abb4` 只有每日采集写的最近八天——页面上是「几乎没有数据」，而库里躺着 380 万行。**此前每次验证都用「行数最多的那个 workspace」取数，所以一直在验运营者看不见的那一份。** 已搬迁完成并备份。**今后判据：有 `market_prices` 的那个 workspace。** 见 `backfill/README.md` 置顶段落。
- **部署前置条件已经脚本化，不再靠记性：先跑 `ops/preflight-deploy.sh`，全绿再读 `docs/DEPLOY_PREFLIGHT.md` 第二节。** 它会打印出可直接执行的 dispatch 命令，digest 由它从构建产物取，不要手抄。这份东西是 2026-08-10 连续三次部署失败之后立的（漏 `schema_versions`、漏入口校验、绑定错位），之后两次部署均一次通过。**`1111` 第 0 节的部署铁律仍然有效，脚本把其中能机械查的都覆盖了。**
- 生产运行 **`87f3c26`**（2026-08-11T02:15Z 部署成功）。其后仅有一个提交 `ea29701`（前置检查修误报 + 备好三禾装载 SQL），**不含 Rust/前端改动，可与下批一起发**。
- **自由价差已切自建引擎**（`DEC-046`）：同一交易日两腿收盘价相减、时间早的减时间晚的。生产实测焦煤 09-01 在运营者 workspace 切出 **2202 个点，2013-03-22 → 2026-08-10**。三禾降为可回退备选，`DEC-042` 的三个只读端点维持不变。**2130 里「自由价差走三禾」的口径到此为止。**
- **`DEC-045`「大商所全量改走东方财富」中关于行情的那一半已失效并被 `DEC-047` 取代。** 2026-08-10 实证：大商所官网全站 412（含首页）；akshare 的 `get_dce_daily` 打同一个站拿回挑战页；东财 `push2his` K 线**第一个请求就被断开**、`push2` 行情 502——**不是限频**。东财 `futsse-static` 目录接口仍 200 且当日更新，用它取在市合约清单。**大商所行情改由新浪供给，带四道到货检查；席位仍走东财，那条是通的。**
- **新浪不是新源**：`price_history` 里大商所 2025-01-02 之后的行情本来就是它的（回填补缺口 11,262 行）。与交易所年度文件 843 个重叠日逐字段对照：收盘/结算/开盘/最高各 837 天完全相同，持仓量恰好 2.0000 倍（交易所双边、新浪单边）。剩下 6 天：2024-09-25 一天新浪把六个合约结算价全写成 `0.0`；另三天是成交 1–2 手的合约日收盘价偏。**四道检查按这两处真实故障写的测试在 `collector/tests/test_sina_dce_daily.py`。**
- **两张历史表此前没有任何写入方**，只有一次性手工装载。日更投影已接上（`deploy/collector/project-history.sql`，挂在 `run-collector.sh` 里采集之后，顺序是硬要求）。
- 待办：**三禾大商所席位历史采集中（约 6–8 小时，三进程，0 失败）→ 用 `backfill/load_sanhe_seats.sql` 装载** → 服务端段边界语义 → Phase 5A 独立 Evaluator → 4B-2 → collector 凭据轮换。Phase 5 尚未合 main。
- **不得动 `research/` 下任何文件**，那是运营者另一个会话的席位因子预测模型，会长期处于改动状态。

- **最新权威交接：`docs/handoffs/HANDOFF_20260809_2130.md`。先读它第 0 节三条。** 同日的 `HANDOFF_20260809_1111.md`（部署铁律、分支拓扑、腿序规则）与 `HANDOFF_20260809_1740.md`（自由价差前端修复）仍然有效，其中已过时或口径打架的地方都在原文就地标注并指回 2130，历史结论未删改。
- **接手前必读 `1111` 第 0 节「部署铁律」**（四条，每条都是踩出来的），再看第 2 节分支拓扑，再补 2130 第 5 节的第五条。
- 生产运行 **`f7374e4`**（2026-08-09 部署成功，见 2130 第 10 节）。`retained_points` 2069→2071 是日历修复的直接量化证据。**其后仍有未部署提交**（workflow 打包修复等），需再跑一轮镜像构建+部署；含 Rust/采集器改动的批次**四个镜像都要重编，不是 15 分钟路径**。
- **两个当天查出的生产故障已修并已部署**（`IMAGE_TAG` 修复的真实效果待周一 cron 验证）：①定时采集自安装以来一次都没成功过（`run-collector.sh` 缺 `export IMAGE_TAG`，compose 逐文件插值时 `:?` 先炸），导致行情与交易日历停在 2026-08-05、08-04 整天为空；②交易日历只采半个月时，散户截止日被算成「已采到的最后一天」，把自由价差图的最后两天误剔。两者串联，详见 2130 第 2 节。
- **部署铁律第 0 条（新增，2026-08-10，最常被忽略）：构建镜像之前先看 `gh run list --workflow=ci.yml --limit 1`，红的不准往下走。** `ci.yml` 在每次 push 自动跑，含本地 `cargo test` 会跳过的 PostgreSQL 集成测试与 ruff/前端闸门。**2026-08-09 它连续红了 8 次以上没人看**，本地只跑 `cargo test`（跳过 `#[ignore]`）当作通过，结果一路构建镜像、部署、失败。而且 lint 步骤靠后，它一红，后面的前端 lint/测试/构建/compose 校验**全部被跳过**。
- 部署铁律：①`run_live_collection` 与采集无关时传 `false`（本批相关，见 2130 第 6 节）；②传了还要 `grep -c PHASE4A_RUN_LIVE_COLLECTION rust/tests/phase_4a_e2e.sh` 确认 ≥1，否则开关空转白跑一小时；③镜像必须本链新构建；④验收 Origin 与 `PUBLIC_ORIGIN` 一致（workflow 已自动传，勿删）；⑤**镜像构建完成后、部署跑完前不要再往 `deploy/phase-5a-candidate` 推任何提交（含文档）**，否则 `acceptance_sha` 与 ref tip 对不上，第一道护栏就拒。
- **数据获取有两条独立链路，谈「采集渠道」时必须说清是哪一条**（详见 2130 第 3.0 节）：
  - **链路 A 采集器**（Phase 4A，cron 定时，落事实表）：五家交易所官方 akshare 接口；DCE 官方全线 412，实际走新浪 fallback（`DEC-041`）；**东方财富已按 `DEC-043` 接入为席位专用兜底源**（`datacenter-web.eastmoney.com` 报表 `RPT_FUTU_DAILYPOSITION`，不经 akshare，排在全部官方源之后，五家通用，不承担行情与目录，INE 排除）。`1111` 里记的 `qhhqzl.eastmoney.com/...` 地址是错的。
  - **链路 B 服务端只读代理**（Phase 5A，`DEC-042`，用户查一次打一次）：**三禾 `www.sanheshuju.com`**，连接器 `sanhe_spread_v1`，三个端点 `all_varieties`/`variety_contracts`/`arbitrage_varieties`，只允许服务端调用，前端不得直连。**其数据一条都不进事实表**，只落 `spread_provider_*` 与 `spread_window_segments`。同端点+同参数按 `Asia/Shanghai` 业务日只取一次并持久缓存，请求间隔 2000ms。
- 业务口径：三禾序列会混入反向组合（查 09-01 时混进 `jm2609-jm2601`，那属于 01-09），规则=前腿必须先到期，不满足整段排除。**换段边界有三个不同的数**（服务端发 16 / 图上画 15 / 真实换月 13），别混用，见 2130 第 4 节；**服务端 `segment_boundaries` 语义仍错，只在前端绕过了**。
- 待办：部署本批 → 服务端段边界语义 → Phase 5A 独立 Evaluator → 4B-2 → 前端肉眼验收 → collector 凭据轮换。Phase 5 尚未合 main（main 停在 `b5db24e`），待 Evaluator PASS 后合。

- **两处「文档说的和机器上的不一样」，见 2130 第 6.5 节**：①`run-backfill.sh` 从来不在发布包里，生产那份停在 2026-08-05 手工安装的快照（已修，并加了断言：仓库拥有的每个运维脚本必须同时出现在打包与安装两处）；②`futures-backfill-phase4b1.service` **这个 systemd 单元根本不存在**——`systemctl is-active` 对不存在的单元也答 `inactive`（退出码 4），据此误判过。**判断东西在不在就 `ls` 它，别问一个对缺失和停用给同样答案的命令。**

接手须以 Git、Actions、VPS 实态复核，不盲信摘要；不得输出密钥、恢复回填、清理数据或未经授权合 main/打标签。
