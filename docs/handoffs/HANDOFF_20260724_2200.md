# Agent Handoff

## 当前目标

Phase 1 工程基础建设已完成并通过本地与 `futures` VPS 验证。下一阶段开始前，应先确认 Phase 2 范围，不得直接进入业务功能实现。

## 当前阶段

Phase 1：工程基础建设，状态 PASS。

## 已完成

- 接管时按规则阅读了 `AGENTS.md`、`PLANS.md`、`docs/DECISIONS.md`、`docs/handoffs/LATEST.md`、上一份 handoff、Phase 1 计划和 Phase 0 审查报告。
- 实际执行了 `git status`、`git branch --show-current`、`git log -5 --oneline`。
- 通过 MCP SSH 重新核对 `futures` VPS。
- 在 `futures` VPS 安装 Docker 与 Docker Compose。
- 将本地 Git 源码派生的部署包上传到 `/opt/futures-platform`。
- 创建远端秘密目录、主密钥文件、`.env`、项目数据目录和备份目录。
- 创建并启用项目 swapfile，以缓解小内存 VPS 上 Rust 构建风险。
- 启动 PostgreSQL、API、Worker、Frontend、Nginx 容器。
- 执行数据库迁移 `202607240001_foundation.sql`。
- 验证 API live/ready/version、前端页面、Nginx 代理、Worker、容器状态、迁移记录、重启/停止启动/redeploy。
- 扫描远端日志，未发现明文秘密。
- 修复 Phase 1 部署过程中发现的问题：
  - 前端 dev 容器只读挂载导致 named volume 挂载失败。
  - 远端 base64 数据库密码导致 `DATABASE_URL` 解析失败。
  - `checked_at` 序列化为数组而非 RFC3339 字符串。
- 创建 `docs/reviews/PHASE_01_EVALUATION.md`，最终 PASS。
- 更新 `PLANS.md` 与 `docs/deployments/FUTURES_VPS_PHASE_01.md`。
- 提交：`146a614 fix: resolve phase one evaluation findings`。

## 正在进行

- 无正在进行的开发任务。
- 本 handoff 创建后将单独提交 checkpoint。

## 尚未完成

- 本机 Docker 仍不可用，因此本机 `docker compose config` 无法运行；已在 VPS 上通过 Compose 配置校验。
- 未推送 GitHub；当前无明确远程仓库。
- 未创建生产静态前端镜像；当前 Phase 1 VPS 使用 Vite dev server 作为基础验证。
- 未给 API 镜像注入真实 Git SHA，`/api/v1/version` 当前返回 `git_sha=local`。
- 未进入 Phase 2 业务功能。

## 关键决策

- 数据归属于 Workspace，MVP 每个用户只有个人 Workspace，不实现共享和邀请。
- 所有业务表必须通过 `workspace_id` 做租户隔离。
- MVP 排除规则驱动历史回测，只做统计、季节性、百分位、极值、回撤和情景计算。
- 系统时间使用 UTC 与 `TIMESTAMPTZ`；业务时区为 `Asia/Shanghai`。
- 市场数据独立保存 `trade_date` 与 `session_type`。
- 收盘价和结算价分别保存，分析结果必须标记 `price_basis`。
- 原始成交不可覆盖，开平配对默认 FIFO。
- 金融数据最终存储不得使用浮点数。
- 敏感数据采用信封加密；主密钥不得进入数据库、代码仓库或镜像。
- 远程登录采用隔离 Chromium 容器、Playwright 和 noVNC。
- MVP 不开放任意 URL，只支持域名白名单和已实现连接器。
- Phase 1 不实现 AI、OCR、采集或正式业务功能。

完整决策以 `docs/DECISIONS.md` 为准。

## 修改过的文件

- `PLANS.md`
- `docker-compose.yml`
- `docs/deployments/FUTURES_VPS_PHASE_01.md`
- `docs/reviews/PHASE_01_EVALUATION.md`
- `rust/Cargo.toml`
- `rust/crates/application/src/lib.rs`
- `docs/handoffs/HANDOFF_20260724_2200.md`
- `docs/handoffs/LATEST.md`

## 未提交修改

本 handoff 提交前，Phase 1 修复提交后工作区干净。创建 handoff 后将产生 handoff 文档与 `LATEST.md` 修改，并应以独立提交 `docs: add agent handoff checkpoint` 收口。

## Git 分支与提交

- 当前分支：`phase/01-foundation`
- 交接前源码 HEAD：`146a614 fix: resolve phase one evaluation findings`
- 交接 checkpoint 提交后 HEAD 会前移；接管者必须以 `git log -5 --oneline` 的实际输出为准。
- 最近提交：
  - `146a614 fix: resolve phase one evaluation findings`
  - `81308ed docs: add agent handoff checkpoint`
  - `0ee8671 feat: add phase 1 foundation scaffold`

## 本地测试结果

最近验证时间：2026-07-24 21:50 +08:00。

| 命令 | 结果 |
| --- | --- |
| `cargo +stable fmt --check` | 通过 |
| `cargo +stable clippy --workspace --all-targets -- -D warnings` | 通过 |
| `cargo +stable test --workspace` | 通过 |
| `pnpm install --frozen-lockfile` | 通过 |
| `pnpm lint` | 通过 |
| `pnpm test` | 通过；需真实文件系统权限，沙箱内受 esbuild 限制 |
| `pnpm build` | 通过；需真实文件系统权限，存在大 chunk 提示 |
| `docker compose config` | 本机未通过；本机无 Docker |

## futures VPS 状态

- VPS：`futures`
- 系统：Ubuntu 26.04 LTS，Linux `7.0.0-22-generic`
- Docker：29.1.3
- Docker Compose：2.40.3
- 部署目录：`/opt/futures-platform`
- 主密钥：`/etc/futures-platform/secrets/master-key-v1`，`root:root`，权限 `0400`
- `.env`：`/opt/futures-platform/.env`，权限 `0600`
- 访问地址：`http://172.238.11.174:8088`
- 运行容器：
  - `futures-analysis-platform-postgres-1`
  - `futures-analysis-platform-api-1`
  - `futures-analysis-platform-worker-1`
  - `futures-analysis-platform-frontend-1`
  - `futures-analysis-platform-nginx-1`
- 对外端口：8088
- 数据库端口未映射到公网。
- 日志秘密扫描：通过。

详见 `docs/deployments/FUTURES_VPS_PHASE_01.md`。

## 数据库迁移状态

已执行：

- `202607240001`：`phase 1 foundation`

已验证：

- `schema_versions` 表存在并包含上述记录。
- `app.current_workspace_id()` 函数存在。
- 尚无正式业务表。

## 已知问题和风险

- VPS 内存约 956MiB，虽然已启用 2GiB 项目 swapfile，但后续 Playwright/noVNC/OCR 可能需要更高规格。
- 当前前端部署为 Phase 1 开发型服务，不是生产静态资源部署。
- API `git_sha` 当前为 `local`。
- 本机无 Docker，Compose 校验依赖 VPS 或未来 CI。
- 曾读取本地 Codex SSH 配置文件，其中包含凭据；不得把该内容复制到文档、Git、日志或回复。

## 下一步第一条操作

不要继续写业务代码。下一步应由 Planner 明确 Phase 2 范围、准入条件和验收标准，并确认是否先处理生产静态前端镜像、Git SHA 注入、本机/CI Docker 校验这些 LOW/SUGGESTION 项。

## 新 AI 必读文件

1. `AGENTS.md`
2. `PLANS.md`
3. `docs/DECISIONS.md`
4. `docs/handoffs/LATEST.md`
5. `docs/handoffs/HANDOFF_20260724_2200.md`
6. `docs/phases/PHASE_01_FOUNDATION.md`
7. `docs/reviews/DOC_REVIEW_PHASE_0.md`
8. `docs/reviews/PHASE_01_EVALUATION.md`
9. `docs/deployments/FUTURES_VPS_PHASE_01.md`

接管后必须实际执行：

```powershell
git status
git branch --show-current
git log -5 --oneline
```

如涉及 VPS，必须通过 MCP SSH 重新核对容器、服务、健康检查和迁移状态。

## 禁止重复或禁止修改事项

- 不要修改原始 Word 设计方案。
- 不要重复 Phase 0 文档拆分和 `ce-doc-review` 审查，除非用户明确要求。
- 不要重复已通过的 Phase 1 基础工程实现。
- 不要提前实现行情、导入、套利、成交、席位、采集、OCR 或 AI 业务功能。
- 不要把密码、API Key、Cookie、Token、主密钥、数据库明文凭据写入文档、日志、Git 或最终回复。
- 不要手工编辑 VPS 上的业务源码；VPS 必须由本地 Git 源码派生部署。
- 不要伪造测试、迁移、部署或健康检查结果。
