# Agent Handoff

## 当前目标

完成 Phase 4A 复核残留 HIGH-03 候选的受控发布链：将 CI 与 container-images
迁移到 4 GiB `futures` VPS 的 self-hosted runner，在总编译峰值不超过 2.5 GiB 的
护栏下完成 CI→四镜像→Deploy→E2E，并把证据交给独立 Evaluator 做单项终验。

## 当前阶段

Generator 发布链已完成，Phase 4A 尚未由独立 Evaluator 宣告最终 PASS。不得合并
main、打标签、启动 Phase 4B-2 或恢复 Phase 4B-1 连续回填。

## 已完成

- HIGH-03 业务修复 `23e679d` 已补齐 exchange 被 `trading_calendar_versions`
  引用时的回滚预检依赖和 `for update` 锁定，并含 PostgreSQL 集成测试及 E2E 场景。
- `4e39c69` 将 CI 与 container-images 迁至 `[self-hosted, futures-vps]`，设置
  Cargo 2 jobs、Node 768 MiB、CI PostgreSQL 384 MiB、BuildKit 2 GiB 且矩阵串行。
- `d4265c4`、`e627ab8` 将 collector 检查固定在 Python 3.13.11 容器内只读运行，
  ruff cache 与临时文件仅写 tmpfs。
- runner systemd `MemoryMax` 已设为 2500M；用户明确授权 `github-runner` 加入
  Docker 组。完整构建期间 `oom=0`、`oom_kill=0`，生产容器保持健康。
- CI Run `30969365344` success；Container images Run `30970280360` success；
  Deploy Run `30971024520` success。
- VPS E2E 输出 `PHASE4A_E2E_PASS`：授权矩阵、目录 upsert、正式投影回滚、
  exchange/calendar 依赖冲突拒绝与零变更、DCE 来源恢复、同日重放幂等均通过。

## 正在进行

无运行中的实现或发布操作。Phase 4B-1 连续五年回填未启动；已有驱动提交，10 日
试跑进程已结束但未在本单形成终验交接。

## 尚未完成

- 由全新的独立 Evaluator 只读复核 HIGH-03 单项并追加最终结论。
- 若独立 Evaluator PASS，后续版本收口、Phase 4B-1 连续运行和 Phase 4B-2 均需
  用户另单授权；本交接不授权其中任何一项。

## 关键决策

- 用户因 VPS 升配至 4 GiB，明确解除“VPS 不承担编译”的旧运行约束；只允许
  Actions self-hosted runner 在 2.5 GiB 总峰值护栏内编译，仍禁止 VPS 手工改源码
  或绕过工作流构建生产镜像。
- runner 加入 Docker 组具有 root 等价安全边界，此风险已向用户说明并获明确授权；
  不得把 Docker 组权限扩散到其他账号。
- deploy-futures 的 SSH、secrets、GHCR 与严格 host key 逻辑未改变。
- CI 启动阶段的失败 Run `30968166197`、`30969042094` 分别来自 Docker 权限、宿主
  编译工具、Python toolcache 与只读 ruff cache 适配问题，修复后权威成功 Run 为
  `30969365344`；不得用早期失败替代最终候选结论。

## 修改过的文件

- `.github/workflows/ci.yml`：self-hosted CI 与资源护栏。
- `.github/workflows/container-images.yml`：self-hosted 串行镜像构建与 BuildKit 限额。
- `rust/Dockerfile.api`、`rust/Dockerfile.worker`：release build 固定 `--jobs 2`。
- `AGENTS.md`、`PLANS.md`、`docs/ENVIRONMENT.md`、`docs/DEPLOYMENT.md`：同步
  4 GiB runner 运行边界、发布链和 HIGH-03 实证。
- 本交接及 `docs/handoffs/LATEST.md`。

## 未提交修改

创建本交接时，长期文档与交接文件待以单独 docs 提交提交；业务代码、collector、
deploy 脚本和 workflows 均无未提交修改。接管时以 `git status` 为准。

## Git 分支与提交

- 分支：`phase/04-akshare-collection`。
- 写交接时 HEAD：`e627ab8c3b797cc77f872a9c02439c1dfca0d4eb`。
- 最近提交：`e627ab8` collector 只读检查；`d4265c4` 固定 Python 容器；
  `4e39c69` self-hosted 构建；`9900971`、`222fd3e` Phase 4B-1 驱动。
- docs 提交前 `git status` 仅含本节列出的长期文档与交接变更；提交后应为干净并
  推送到 `origin/phase/04-akshare-collection`。

## 本地测试结果

- 已运行并通过：`git diff --check`。
- 权威门禁由 CI Run `30969365344` 实际通过：Rust fmt/clippy/workspace tests、
  HIGH-03 PostgreSQL 集成测试、前端 lint/test/build、Python ruff/35 tests、
  Compose 检查与四个非发布镜像构建。
- 本轮 docs 收口未重跑本地完整编译，原因是同一候选的权威 self-hosted CI 已通过，
  且文档提交不改业务或工作流行为。

## futures VPS 状态

- 已实际只读核对；API `/api/v1/version` 返回
  `e627ab8c3b797cc77f872a9c02439c1dfca0d4eb`，API/PostgreSQL 与生产容器保持健康。
- 当前四镜像 digest：API `cb9145ec…f3de`、Worker `597fe69d…b0af`、Frontend
  `54c3c0e7…0f36`、Collector `e07b34a0…1e60`；完整值见 `docs/DEPLOYMENT.md`。
- runner active，`MemoryMax=2621440000` bytes，`oom=0`、`oom_kill=0`。
- 根分区使用率 19%；bootstrap token absent；collector 凭据文件仍为
  `root:root`/`0400`，未读取或输出内容。
- 最新证据目录：
  `/var/lib/futures-platform/deployment-evidence/e627ab8c3b797cc77f872a9c02439c1dfca0d4eb-20260805T030140Z`。

## 数据库迁移状态

- 已执行 `202607240001` 至 `202608030003` 的全部现有迁移，包含 Phase 4A
  `202608020001`、`202608020002`、`202608030001`、`202608030002`、`202608030003`。
- 只读快照：手动批次 144、自动批次 470、用户 32、`market_prices=9020`、
  `seat_positions=186083`；两类业务唯一键重复组均为 0。
- E2E 断言手动批次数和整行指纹不变；127 历史手动批次及其后既有数据未清理。

## 已知问题和风险

- Phase 4A 只有 Generator 链路完成，独立 Evaluator 单项结论尚缺，不得宣告 PASS。
- runner 的 Docker 组权限为 root 等价边界；继续保持仓库级、专用账号和资源限额。
- 构建 cgroup 曾触及 2.5 GiB 上限但未 OOM；后续 workflow 扩容并行度或 job 数前
  必须重新评估，禁止提高总编译峰值。
- Phase 4B-1 试跑进程已结束但未在本单终验；其自动批次如实保留。连续任务未
  启动，老年份适配仍属 4B-2。

## 下一步第一条操作

由全新的独立 Evaluator 按 `.agents/Evaluator.md`、最新 Phase 4A 评审报告、Git、
Actions 三个成功 Run 和 VPS release 证据，只读终验 HIGH-03 单项。

## 新 AI 必读文件

- `AGENTS.md`
- `PLANS.md`
- `docs/DECISIONS.md`
- `docs/reviews/PHASE_04A_EVALUATION.md`
- `docs/phases/PHASE_04_AKSHARE_COLLECTION.md`
- `docs/DEPLOYMENT.md`
- 本交接与 `docs/handoffs/LATEST.md`

## 禁止重复或禁止修改事项

- 原始 Word 设计方案不得修改。
- 不得写入、读取后输出或提交任何密码、Token、Cookie、数据库凭据或主密钥。
- 不得重复已通过的 Generator 完整发布链；Evaluator 只做独立只读取证与报告追加。
- 不得合并 main、打标签、启动 Phase 4B-2、恢复连续回填或清理既有数据。
- 不得在 VPS 手工修改业务源码或绕过 Actions 编译生产镜像。
