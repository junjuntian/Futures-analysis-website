# Agent Handoff

## 当前目标

完成 Phase 3 版本收口：将已获独立 Evaluator PASS 的 `phase/03-import-foundation` 以普通 merge commit 合入 `main`，等待 main CI、打 Phase 3 PASS 标签，并修正状态文档。本单不修改业务代码、不重新部署 VPS。

## 当前阶段

Phase 1、Phase 2、Phase 3 均已完成并经 Evaluator PASS。Phase 3 已合入 `main` 并以 `phase-3-pass-20260801` 标签收口；phase 分支使命结束，不再回灌状态文档。

## 已完成

- 独立 Evaluator 复核提交 `6ff4c2b` 已确认 Phase 3D 最终 PASS，HIGH-01、HIGH-02、MEDIUM-01 至 MEDIUM-04、LOW-01 全部 CLOSED；MEDIUM-05 按用户裁定延后。
- 将 `origin/phase/03-import-foundation` HEAD `6ff4c2b5bee74b8bb6854734f504ee2275a8d381` 以普通 merge commit 合入 `main`，没有 squash、没有 rebase。
- merge commit 为 `33aa838c9ef3f39c4e32bb5749982d82d358bf7a`，提交信息为 `merge: phase 3 import foundation (evaluator PASS)`，两个父提交为 `78f0673` 与 `6ff4c2b`。
- 冲突处理清单：仅 `.github/workflows/deploy-futures.yml` 出现 add/add 冲突，位于 DEC-026 bootstrap-token 修正处，显式采用 phase 版本；`.github/workflows/ci.yml` 没有冲突，自动合并结果与 phase 版本逐字节一致；除 workflows 外没有任何冲突。最终 merge tree 与 phase HEAD 完全一致。
- `main` push 自动触发 CI Run `30703979390`，事件为 `push`、分支为 `main`、head SHA 为完整 merge SHA；`validate`、`Build api image`、`Build worker image`、`Build frontend image` 四个 job 全部 success。
- 在 merge commit 上创建并推送附注标签 `phase-3-pass-20260801`；远端 peeled target 为 `33aa838c9ef3f39c4e32bb5749982d82d358bf7a`，没有创建任何 `v*` 标签。
- 标签自动触发 Container images Run `30704223198`，API、Worker、Frontend 三个 publish job 全部 success。
- 只读核验 Deploy futures 最新 run 仍为既有 `30689392268`；本单没有手动或自动触发新部署。
- 已修正 `PLANS.md` 与 `README.md` 的 Phase 1/Phase 3D 过时状态，并记录 Phase 3D 时间线、标准发布链实态和延期运维事项。

## 正在进行

Phase 3 版本收口已完成；当前没有进行中的实现、部署或数据操作。

## 尚未完成

- MEDIUM-05：VPS 的 127 个历史测试批次尚未清理，按用户裁定在项目完工时执行生产库归零重置。
- 生产 HTTPS 入口与 `AUTH_COOKIE_SECURE=true` 验证尚未执行，按用户裁定延后到项目完工时处理。
- 总方案重审尚未启动，采集域裁剪与 akshare 方案仍是待用户确认的提案，不是已确认决策。

## 关键决策

- 本次只允许普通 merge，禁止 squash/rebase；已遵守。
- workflow 冲突统一采用 phase 版本；唯一冲突已按此处理，完整清单见“已完成”。
- 标签必须指向 merge commit，而不是后续 docs 提交；已遵守。
- Phase 3 状态文档只在 `main` 更新，不再同步回已结束使命的 phase 分支。
- 不修改 `docs/DECISIONS.md`；采集域裁剪与 akshare 尚未获确认，不得写成 DEC。
- VPS 保持运行 `45ee802`，本单禁止重新部署和数据清理。

## 修改过的文件

- `PLANS.md`：更新 Phase 3 全部完成、评审修复时间线、merge/CI/tag、GHCR digest 部署实态与延期事项。
- `README.md`：把当前状态与本地启动标题更新为 Phase 3 收口实态。
- `docs/handoffs/HANDOFF_20260801_2248.md`：本收口交接。
- `docs/handoffs/LATEST.md`：指向并概括本交接。

## 未提交修改

本交接随上述四个文档文件组成独立 docs 提交推送到 `main`；不包含 `rust/`、`frontend/`、`deploy/`、workflow 或 `docs/DECISIONS.md` 修改。提交完成后工作树应保持干净。

## Git 分支与提交

- 当前分支：`main`。
- 本交接编写前 HEAD：`33aa838c9ef3f39c4e32bb5749982d82d358bf7a merge: phase 3 import foundation (evaluator PASS)`；`origin/main` 已指向同一提交。
- 标签：`phase-3-pass-20260801`，指向上述 merge commit。
- phase 分支：`origin/phase/03-import-foundation=6ff4c2b5bee74b8bb6854734f504ee2275a8d381`，使命结束，不再回灌。
- 本交接与状态文档的单独 docs 提交会在 merge commit 之后推进 `main` HEAD，但不会移动 Phase 3 PASS 标签。
- 编写本交接前 `git status` 仅有 `PLANS.md`、`README.md` 的预期文档修改；加入本交接和 `LATEST.md` 后仍应只有四个预期文档文件。

## 本地测试结果

已运行并通过：

- Phase 3D Evaluator 复核：Rust fmt/clippy/test 通过，119 tests；前端 lint/test/build 通过，6 files / 18 tests；`git diff --check 6dfea78..45ee802` exit 0。
- merge 冲突解决后：`git diff --cached --check` exit 0；merge tree 与 phase HEAD 完全一致；两个 workflow 与 phase 版本一致。
- main CI Run `30703979390`：四个 job 全部 success，包含 Rust、前端、Compose config 和三镜像 CI build。
- 标签镜像 Run `30704223198`：三个 publish job 全部 success。

已运行但失败：无未解释失败；merge 命令仅因预期的 deploy workflow 冲突暂停，已按授权解决。

未运行及原因：本收口单未重新运行本地业务测试，因为 merge tree 与已通过 Evaluator 的 phase HEAD 完全一致，merge 后权威 main CI 已完整通过；后续仅修改 Markdown 状态文档。

## futures VPS 状态

2026-08-01 22:46（Asia/Shanghai）通过 MCP SSH 只读核对：

- `/api/v1/version` 返回 `git_sha=45ee8028647a1b8e4b8cda043e8012b4e281d739`。
- PostgreSQL、API、Worker、Frontend、Nginx 五个服务均 running；API/PostgreSQL healthy。
- `users=31`、`import_batches=127`，bootstrap-token absent。
- 本单没有重新部署、没有清理数据、没有修改 VPS 文件；标签新镜像没有部署到 VPS。

## 数据库迁移状态

- Phase 3D 迁移 `202607260001`、`202607260002` 均已执行，现场聚合为 2/2。
- 本单没有创建、执行或改写任何迁移。

## 已知问题和风险

- 127 个历史测试批次仍占用生产候选数据库；这是用户明确延期的 MEDIUM-05，不影响 Phase 3 PASS，但项目完工归零前必须继续显式登记。
- 当前仍是 HTTP 验收入口；生产 TLS 与安全 Cookie 验证未完成，不得把当前 HTTP 验收描述成生产 HTTPS 已就绪。
- 标签已发布新镜像，但 VPS 有意继续运行已验收的 `45ee802` digest；不得误把标签镜像发布等同于重新部署。
- 总方案重审中的采集域裁剪与 akshare 只是待确认提案，不得提前实现或写入 `DECISIONS.md`。

## 下一步第一条操作

总方案重审（采集域裁剪与 akshare 方案）待用户确认后启动。

## 新 AI 必读文件

- `AGENTS.md`
- `PLANS.md`
- `docs/DECISIONS.md`
- `docs/handoffs/LATEST.md`
- `docs/handoffs/HANDOFF_20260801_2248.md`
- `docs/reviews/PHASE_03D_EVALUATION.md`
- `docs/phases/PHASE_03D_IMPORT_FINALIZATION.md`

## 禁止重复或禁止修改事项

- 原始 Word 设计方案不得修改。
- 不得写入、输出、复制或重新生成任何密码、Token、Cookie、私钥、主密钥或数据库明文凭据。
- 不得重复 Phase 3 已通过的实现、Evaluator 或 VPS 部署验收工作。
- 未经新的明确授权，不得重新部署 VPS、清理 127 个历史测试批次或执行生产库归零。
- 不得把状态文档回灌到已结束使命的 phase 分支。
- 不得在用户确认总方案重审提案前修改 `docs/DECISIONS.md` 或实现采集域裁剪/akshare 方案。
