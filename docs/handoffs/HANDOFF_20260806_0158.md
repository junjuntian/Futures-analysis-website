# Agent Handoff

## 当前目标

以 Phase 5A 候选 `08f4637` 为起点完成镜像发布、生产部署、VPS 全门禁、真实三禾查询
与页面截图；随后才由全新会话的独立 Evaluator 只读审查。用户在第二次部署验收运行中
明确要求“先收尾，下次再做”，本交接记录安全中止与回滚后的事实。

## 当前阶段

Phase 5A 发布链 **尚未验收完成**。发布所需 CI、不可变镜像和 5A VPS 验收脚本已准备
完成；第一次部署因验收夹具假设错误失败并自动回滚，修复后第二次部署按用户要求取消，
并已人工按发布脚本同一备份恢复流程回滚。生产现已恢复到上一稳定版本。独立 Evaluator
尚未启动，必须等发布段完成后在全新会话执行。

## 已完成

- 发布前只读核验回填状态：`waterline_date=2026-07-11`、
  `newest_processed_date=2026-08-03`、`processed_date_count=24`、
  `failed_pair_count=6`、`driver_running=no`；
  `futures-backfill-phase4b1.service` 为 inactive。全程未恢复、重启或修改回填。
- 候选 `08f4637` 的业务代码与本地全门禁通过；补齐部署链：
  - `.github/workflows/deploy-futures.yml` 打包并应用 5A 迁移、运行 Phase 5A VPS E2E、
    将 5A PASS 写入部署证据；
  - `rust/tests/phase_5a_e2e.sh` 覆盖 schema/RLS/OpenAPI、认证/CSRF、三禾品种与月份、
    JM 09-01 真实查询、并发 cache miss 限频、持久缓存、来源诚实、窗口/段/月统计、
    收藏与跨 Workspace 越权，以及回填状态不变。
- 提交 `6e65728`（`ci(phase-5a): add production acceptance gate`）已推送；CI Run
  `31019205792` success。
- 镜像发布 Run `31020852354` success，不可变 digest：
  - API：`sha256:f47d8c3fbe54945db9fe96e3568a8ec2006427f893cb5c2cbfc7effe3697801d`
  - Worker：`sha256:b20cb9e6d02b003a9ab93ee857cbd2572821b68bcaed2319b495f74b23e0c00e`
  - Frontend：`sha256:de30a8928dd9d3b85fb0aef65801dbdd522f845e0f8bcb53b15f57fc72b74f0d`
  - Collector：`sha256:6691e0012c9595813abacc4857216546b3f977d26b5435693310683ea9a339a5`
- 第一次 Deploy Run `31022410133`：Phase 4A VPS E2E 完整 PASS；5A 在创建真实查询前
  因生产仅有一个管理员 Workspace、脚本要求恰好两个而失败；workflow 输出
  `ROLLBACK_STARTED` / `ROLLBACK_PASS`，恢复上一稳定版本。
- 修复授权夹具为临时隔离 analyst 用户/Workspace，并补 acceptance-only allowlist；提交
  `5d30e44`（`fix(phase-5a): isolate VPS authorization fixture`）已推送；CI Run
  `31029671239` success。
- 第二次 Deploy Run `31031247994` 使用运行 SHA `6e65728`、验收 SHA `5d30e44`；
  在 Phase 4A E2E 期间按用户要求取消，GitHub 结论为 cancelled，临时 registry 认证与
  runner SSH 材料清理步骤均 success。
- GitHub 取消后发现 VPS 脱离 SSH 的远端验收进程仍在运行；已终止其独立进程组，并用
  本轮切换前备份
  `database-before-6e65728d2f498cd487805a5f2b5fe2d25dfd3e02-20260805T174229Z.dump`
  按部署脚本定义的步骤恢复数据库和上一稳定镜像，结果 `ROLLBACK_PASS`。

## 正在进行

无。部署、验收和回填均没有后台任务在继续运行。

## 尚未完成

- 尚无成功的 Phase 5A VPS 全门禁结果，也没有 `PHASE5A_E2E_PASS`。
- 尚未完成真实三禾 JM 09-01 查询记录，未取得返回点数、裁剪前后点数、段数、来源标注
  与取数时间。
- 尚未截取自由价差页三区块。
- 尚未写最终 Phase 5A 发布交接。
- 独立 Evaluator 尚未启动；`docs/reviews/PHASE_05A_EVALUATION.md` 不存在且本会话不得
  代做。第二段必须由完成发布后的全新会话执行。

## 关键决策

- 用户要求暂停后不再续跑第二次 Deploy；不能把已取消或部分通过的运行记为 PASS。
- 取消后必须恢复到上一稳定生产状态，避免候选镜像和 5A 数据库迁移构成半发布。
- 下一次发布继续使用已通过 CI 的运行候选 `6e65728` 与验收修复 `5d30e44`，除非出现
  新修复提交；若有新修复，最终候选及 Evaluator 基线必须顺延。
- 仍不得合并 main、打标签、启动 5B、恢复或重启回填。

## 修改过的文件

- `.github/workflows/deploy-futures.yml`：加入 5A 迁移、VPS E2E、证据输出和
  acceptance-only 验收脚本规则。
- `rust/tests/phase_5a_e2e.sh`：新增生产 5A 验收门禁，并修复隔离授权夹具。
- 本交接与 `docs/handoffs/LATEST.md`。

## 未提交修改

写交接前工作树干净；仅本交接与 `LATEST.md` 待创建 docs 提交。业务代码、workflow
和测试脚本均已提交并推送。

## Git 分支与提交

- 分支：`phase/05-spread-analytics`。
- 写交接前 HEAD：`5d30e4487e133cf0ec47565ed25885aa6a03cbf9`，与
  `origin/phase/05-spread-analytics` 一致。
- 起始候选：`08f46375522dcfb9a81c268a2bcf477b2378e90e`。
- 发布运行候选：`6e65728d2f498cd487805a5f2b5fe2d25dfd3e02`。
- 最新验收脚本提交：`5d30e4487e133cf0ec47565ed25885aa6a03cbf9`。
- 未合并 main，未打标签，未启动 5B。

## 本地测试结果

- Rust：`cargo fmt --check`、`cargo clippy`、`cargo test` 均 PASS；所有非 ignored 测试
  通过，PostgreSQL ignored 测试由 CI 执行。
- Frontend：lint PASS；测试 9 files / 25 tests PASS；build PASS（2051 modules，仅 chunk
  大小提示）。
- `bash -n rust/tests/phase_5a_e2e.sh`：PASS。
- `git diff --check`：PASS。
- CI Run `31019205792` 与 `31029671239`：validate、PostgreSQL 集成测试和四个
  non-publish image job 全部 success。

## futures VPS 状态

- 2026-08-06 01:58 CST 已实际核对。
- `/api/v1/version`：`git_sha=e627ab8c3b797cc77f872a9c02439c1dfca0d4eb`。
- `/api/v1/health/ready`：ready。
- 本次候选的 deploy、Phase 4A E2E、Phase 5A E2E 进程均不存在。
- `/tmp/futures-ghcr.*` 目录计数为 0。
- 回填 service inactive；状态仍为水位 `2026-07-11`、最新处理日 `2026-08-03`、
  累计 24 日、失败对 6、`driver_running=no`。

## 数据库迁移状态

- 本次中止后已从切换前备份恢复；`schema_versions` 中
  `202608050001` 计数为 0。
- 生产数据库回到上一稳定版本对应状态，Phase 5A 迁移未保留。
- 没有清理既有业务或回填数据；恢复对象仅为本次候选切换前的原子数据库快照。

## 已知问题和风险

- 第一次部署失败是验收夹具与生产 Workspace 数量耦合，已在 `5d30e44` 修复，但尚未
  通过完整 VPS 5A 验收证明。
- GitHub Actions 取消不会自动终止已脱离 SSH 的远端进程；下一次若取消部署，必须同时
  只读检查 VPS 远端进程与生产版本，不能仅信任 Actions conclusion。
- 第二次运行只进行到 Phase 4A，未进入 Phase 5A；任何 5A 生产查询、限频、缓存、
  错误稳定性、SSRF/DNS 重绑定或 UI 结论均不得引用该运行。

## 下一步第一条操作

在北京时间 22:30 之后、且不在 16:30-22:30 禁止窗口内，先只读复核生产版本、ready、
回填 inactive/`driver_running=no` 和磁盘水位；随后以运行 SHA `6e65728`、验收 SHA
`5d30e44` 及上述四个 digest 重新触发 Deploy，完整等待 Phase 4A 与 Phase 5A E2E。

## 新 AI 必读文件

- `AGENTS.md`
- `PLANS.md`
- `docs/DECISIONS.md`
- `docs/phases/PHASE_05_SPREAD_ANALYTICS.md`，尤其第 5.6、11、13、14 节
- `.github/workflows/deploy-futures.yml`
- `rust/tests/phase_5a_e2e.sh`
- `docs/reviews/PHASE_04A_EVALUATION.md`
- 本交接与 `docs/handoffs/LATEST.md`

## 禁止重复或禁止修改事项

- 原始 Word 设计方案不得修改。
- 不得输出、提交或记录密钥、Token、Cookie、数据库凭据或 collector 凭据。
- 不得把 Run `31022410133` 或 `31031247994` 记为 Phase 5A PASS。
- 不得重复发布镜像；已存在的四个不可变 digest 可直接复用，除非运行代码另有修复。
- 不得合并 main、打标签、启动 5B、恢复/重启回填或清理既有数据。
- 独立 Evaluator 必须在发布段完成后的全新会话执行，不得由发布会话兼任。
