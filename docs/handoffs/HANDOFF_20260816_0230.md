# 交接 2026-08-16 02:30(覆盖 08-15 白天至 08-16 凌晨的全部工作)

上一份:`HANDOFF_20260815_0150.md`(其"生产运行 ac152a2"等版本信息由本份取代,
技术结论仍有效)。本份收口时**生产=GitHub=本地=`5535478`**(部署 08-16 成功,
引擎已在生产手动重跑,signals.json 为最新规则产物)。

## 0. 先读这三条

1. **引擎买点规则变了两处**(`DEC-055`/`DEC-056`):v2 区间条件已撤销;新增
   **重挫共振买点**(仅黄金,伦敦金口径,详见下文第 3 节)。改引擎前先读
   `engine/smart_money.py` 里 `RULES["crash_dd"]` 的注释块与 `DEC-056`。
2. **引擎改动部署后不会自动生效**:deploy 只装文件,必须手动
   `/usr/local/sbin/run-smart-money` 一次(或等工作日盘后 cron)。本批已跑。
   **判断"跑没跑过"看 `signals.json` 的 `generated_at` 与 `rules.buy` 串
   (含不含"重挫共振…伦敦金口径"),不看文件 mtime。** 生产上引擎文件与
   `london_xau.csv` 的 mtime 是 17:00Z,晚于 signals.json 的 16:46Z——那只是
   17:00 又 deploy 了一次重装同内容文件,不是没重跑,照 mtime 判会误判。
   2026-08-16 01:23 复核重跑一次:新旧产物**除 `generated_at` 外逐字节相等**
   (160220 字节,顶层键逐个 diff 无差异),幂等与二次部署零行为变化双双坐实;
   关键数同交接第 5 节(AU 已了结 91 笔 +146.2%、重挫单 12 笔 91.7% 零止损、
   AG 重挫 0 笔、`crash_zone=false`),线上 `/smart-money/signals.json` 取到的
   就是新产物。备份 `/root/signals.json.bak.20260815T172247Z`。
3. **待办仅一件**:2026-08-17(周一)收盘后
   `sudo /usr/local/sbin/futures-cron-selfcheck`,验消退 7 日 + 重挫共振规则的
   首次真实 cron 实跑;顺带补验上批遗留的席位页视觉四项(要登录,代理不做)。

## 1. 平台功能批次(全部已上生产)

按提交序,均走完整发布链(CI→镜像→preflight→deploy→生产验证):

- **`6e00c89` 总览页黄金白银报告表**:上半压力位/支撑位运营者手填(表
  `overview_report_levels`,按交易日存,jsonb 网格;8.14 那份已代填入库),
  下半席位净持仓与筹码全自动现算不落表(理由在迁移文件注释里)。席位分组
  可配置(`overview_report_seat_groups`,institution/watch/foreign/retail 四组
  语义各异)。写端点走 `PUT /api/v1/overview/report/*`。
- **`aced5fc` 品种汇总合成 K 线 + 小窗四价逐行**:金银按持仓量加权、其余品种
  主连不复权(`VarietyCandleMode`);**合成价只画图,测试钉死不得进成本链路**;
  图边标注口径。库里只有 8 品种(AU/AG/FG/JM/JD/SA/AP/LH)。
- **`df8e727` 席位页选择记忆 + 缓存修复**:品种/合约记 localStorage(到期合约
  自动退回汇总);`frontend/nginx.conf` 补 Cache-Control——index.html no-cache、
  /assets/ 一年 immutable。**此前无缓存头,发版后浏览器拿旧清单,界面停在上
  一版且不报错,实战坑过一次**;nginx add_header 不继承,每个 location 要重写
  安全头。
- **`3b0de94` 净持仓页**(`/seats/net-position`):≤10 席位合计逐日净持仓,
  掉榜不计入且图上标注、小窗点名;席位组合收藏进库(表
  `seat_member_favorites`,迁移 `202608150002`)。会员去重挡三处(后端
  parse_member_list/前端读 localStorage/表唯一约束)——同一家算两遍合计翻倍
  且图上看不出。
- **`8e3c495` 收藏 403 修复**:CSRF 令牌是懒加载的,写请求前必须
  `loadCsrf()`;直接 `?? ''` 会 403 "request is not allowed"。测试改为不预设
  令牌(上线时的测试预设了,把最常见的刷新态排除在外才漏掉)。
- **`738d1de` 两图联动 + 拼音搜索**:SpreadChart 加 group(echarts connect);
  品种/席位搜索大小写不敏感、支持拼音首字母(`frontend/src/pinyin.ts`,内置
  227 字表,生产全字覆盖有测试钉;曾混入 'golden' 手误与漏「君圳源」三字,
  两条测试防复发)。
- **`09034f6`/`3273124` 历史信号全量 + 分页**:双截断——输出端 `[:20]` 只是
  表象,真根因 `trades[key][-12:]` 每品种取 12 笔。**修截断必须拿真实产物验
  条数**。历史(全部信号)与成熟期统计(2019 起已了结)是两个口径,已写进
  界面(`da7970f`)。
- **`5a7485b` 信号位置标注**、**`84db3b9` v2+高位提醒**、**`8ec4744` 历史表
  一屏化**(进出场一格两行)。
- **`829911e`→`cdde29a`→`5535478` 重挫共振三连**:见第 3 节。

## 2. 研究结论批(记录在 DEC-055/056/057,回测设施见第 4 节)

- **高位否决系列全被否**:区间上 1/4 的 52 笔贡献全样本 +314% 里的 +272%,
  贴高点中继是利润引擎;硬禁高位买 +314→+138;"高位否决+大规模增仓例外"
  是重定时噪音(删 35 笔+190% / 新出现 22 笔+194%,同一批行情晚几天进)。
- **席位海选收官**(`DEC-057`):七家为尽头;国贸/中泰永久排除(逆势小空单
  照妖镜);中金哑火观察;永安/浙商=β户(永安金 100% 天数净多,躺赚 β 20 亿,
  盈亏拆解实证"跟着它赚钱≠抄它的动作能赚钱");华泰=恒温器(三时代 t≈2.2),
  国投=烟花(全部光芒在 2025-26,剔除决定 DEC-051 不翻案但账算明白)。
- **消退核对案例**(2026-03-24 笔):国泰 03-31/04-03 增仓**不计分**(条件计分
  规则),7 个安静日+清明假期→04-07 出场正确;老 10 日制会被 04-08 华泰事件
  重置拖到 05-25 只赚 +1.47%,新规则 +5.94%。

## 3. 重挫共振买点(本批核心,`DEC-056`)

- **规则**:伦敦金美元价 ±10% 之字形分轮(运营者表
  `黄金跌幅回撤2026_8_6.xlsx` 原口径);轮内自轮高回撤(按日低)触及 15%
  置位、**粘滞**到低点回升 10% 开新轮;重挫态里席位共振→免"贴低点/低仓"、
  **次日开盘市价买入**(不挂区间);仅黄金;不受跑路压制窗。
- **运营者两次纠正口径,均被数据证实**:250 日滚动高点→本轮高点(日级 t
  2.95→7.79);人民币→伦敦金(人民币把 2021 标 69 个汇率假重挫日)。
- **终版成绩**:AU +138.6→+146.2,重挫单 12 笔胜率 91.7% 零止损,新增仅
  2026-02-02(+7.65%,当时被区间挂单杀掉的旗舰案例)。
- **数据链**:基线 `engine/data/london_xau.csv`(已入库随发布包装到
  `/opt/futures-platform/smart-money/data/`);增量 GC=F 自动更新缓存
  `gold_usd.csv`;**重叠区基线为准**;源全挂时判定自动关闭。
- **三个工程坑**(改这块前必读):日线 OHLC 分不出当天高低先后,反弹确认
  不许当天新低自证;沪金信号用美盘前一日(北京晚间美盘未收);MarketEngine
  的 crash_ref 参数在 `__init__` 而掩码在 `replay()`,要过 `self.crash_ref`。

## 4. 回测设施与验证纪律

- `/tmp/sm-research/`(生产机,**重启即失**):CSV 导出、十余个变体
  (`c15L_au.py`=终版)、probe_*(消退核对/席位对比/海选/盈亏拆解/命题验证)、
  `out/*.json`。重建:照 make_*.py 重生成,CSV 用 run-smart-money.sh 里的
  psql \copy 命令导。
- **上线纪律(本批三次执行)**:改引擎规则必须先在研究环境跑出变体,新引擎
  与变体**逐笔全等**(182 笔含标签)才放行部署;部署后生产重跑并核对关键数。
- 引擎回放里 `basis` 换源会翻转贴线轮次的历史标签(2018 轮 15.07%),合并
  优先级是行为的一部分,不是实现细节。

## 5. 生产状态(08-16 02:30 实核)

- 生产 sha `5535478`(`/api/v1/version` 已核);前端 chunk 含全部新功能;
  引擎 `usd_crash_state` 在,`london_xau.csv`/`gold_usd.csv` 在位。
- signals.json:AU 已了结 91 笔 +146.2%,重挫单 12 笔;今日 crash_zone=False
  (金价已反弹开新轮,轮高 $4445 实时延伸正常),AU 持仓中 +5.94%。
- 8.14 压力位已在 `overview_report_levels`(直写库绕过 API 校验,已逐条对
  约束;运营者页面正常显示确认过)。
- 无热备机器(`DEC-050`),异地 dump 每日 15:40Z 推 ssp 正常。
