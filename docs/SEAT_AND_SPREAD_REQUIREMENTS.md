# 席位与套利:需求与口径（运营者 2026-08-09/10 确认）

本文是这两个功能的**需求定稿**，写下来是因为口径决定散落在对话里容易丢，而错一个口径整块数字就是错的。
实现顺序是运营者定的：**先把数据全部采齐整理好 → 再写规则 → 最后做界面**。

## 0. 为什么自己做

运营者原话，两条都是针对三禾的：

1. **席位持仓成本**：三禾的计算规则不准。实证支持这个判断——三禾的 `cost` 无法用任何
   单一价格基准（结算价 / 收盘价 / 区间中值）配合净持仓加权平均复现，中位误差约 3.8%，
   而且三种基准误差几乎相同，说明差异在累积模型本身，其模型未公开。
2. **套利价差**：三禾**不排除散户不可交易的日期**。本平台的 `retail_window_v1` 排除。

## 1. 数据范围

| 内容 | 范围 | 主源 | 兜底 |
| --- | --- | --- | --- |
| 逐合约日行情 | **全历史**（黄金 2008-01 起最深） | 交易所官方 | 见下 |
| 席位持仓 | **2025-01-01 起** | **交易所官方** | 东方财富（`DEC-043`） |
| 之后 | 每日增量 | 现有 cron | — |

八个品种：苹果 AP、鸡蛋 JD、焦煤 JM、玻璃 FG、纯碱 SA、黄金 AU、白银 AG、生猪 LH。
落在三家交易所：DCE（JM/JD/LH）、CZCE（AP/FG/SA）、SHFE（AU/AG）。广期所、中金所不涉及。

**官方优先**是贯穿的原则，价格和席位一致。东方财富只在官方拿不到时兜底，且其席位历史
只到约 2025-11，覆盖不了 2025 年上半年——所以它不能当主源。

### 行情每行必须有

日期、**开盘价、最高价、最低价、收盘价、结算价**、成交量、成交额。

三家表头不同，必须归一到一套结构。运营者对"整理"的原话要求：

> 把数据全部按我需要的目的进行整理，比如开盘价、收盘价、最高价、最低价、结算价、日期。
> 后续我要算什么、用什么规则，你就很容易写个规则进行计算。**数据库要很容易看，不要弄得
> 很乱，然后你自己也看不懂**——因为每个交易所的表头数据可能有不同。

验收标准：进库之后一个查询一视同仁地拉出来，不需要知道它来自哪家、原表头长什么样。

| | 合约 | 日期 | 开 | 高 | 低 | 收 | 结算 | 成交量 | 成交额 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DCE（年度文件） | 合约名称 | 交易日期 | 开盘价 | 最高价 | 最低价 | 收盘价 | 结算价 | 成交量 | 成交额 |
| CZCE / SHFE（akshare） | symbol | date | open | high | low | close | settle | volume | turnover |

`normalize_market` 已同时认中英文两套列名，所以三家落进同一张表。

**上期所继续走 akshare `get_shfe_daily`，不自研 `kx.dat` 解析器。** 我一度以为 akshare 那个
不带开高低、打算改用上期所官网的 `kx.dat`，实测后发现 `get_shfe_daily` 与郑商所同为
`open/high/low/close/settle/turnover` 一整套，换掉是不必要的——少一个自研解析器，少一处
将来会坏的地方。`kx.dat` 仅在 akshare 失效时作为已知备选（VPS 可直取，字段更全，含
`PRESETTLEMENTPRICE`）。

## 2. 计算口径

### 2.1 套利价差 —— 用**收盘价**

价差 = 两腿**收盘价**之差。不用结算价。

价差看的是两个合约的价格关系，收盘价是市场公认的那个时点；结算价是成本口径。
**两个功能刻意用不同基准，这不是不一致。**

排除散户不可交易日期（`retail_window_v1`），以及腿序规则（前腿必须先到期）。

### 2.2 席位净持仓成本 —— 用**结算价**

- **净持仓**计价，多空不分开记账（与三禾一致，运营者拍板）
- 成本基准 = **结算价**
- 加仓按加权平均累积，减仓不改均价
- **净头寸翻向时（净多↔净空）成本重置**——跨符号做加权平均没有意义
- 字段名用「**净持仓成本（推算）**」，不叫成交均价

**为什么是结算价**：国内期货的结算价定义就是当日成交价按成交量的加权平均（VWAP），
它本身就是那天所有真实成交的加权均价。实测焦煤 2015 全年 845 个有成交合约日，结算价
844 次落在当日最高最低之间；以结算价为基准，收盘价的中位偏离达当日区间的 **25%**，
区间中值 7.1%，(O+H+L+C)/4 为 9.7%。收盘价最差且偏离有方向性，趋势日里不会自我抵消。

运营者原本考虑用价格区间中位数，据此改为结算价。

### 2.3 品种汇总成本 —— **金额累计 ÷ 总手数**

品种汇总把某席位在该品种各合约的持仓加总。各合约价格不同，成本按**金额累计除以总手数**
（不是按手数加权平均各合约成本）。这与单合约口径自洽——单合约本来就是金额累计÷手数，
汇总只是分子分母同时相加。

### 2.4 必须排除或标记不可靠的情形

| 情形 | 处理 |
| --- | --- |
| **当日零成交** | 结算价是推导值不是真实成交，该日持仓变化标记成本不可知 |
| **席位掉出前 20 名** | 该段标记不可靠，不硬算（见 3.2） |
| **数据起点前的存量仓位** | 明确标注不可知，不静默当 0 |

### 2.5 盈亏必须用 `price_multiplier`，不是 `contract_multiplier`

一手的盈亏 = 价格变动 × **price_multiplier**。这不是交易单位。

八个品种里七个两者相等，唯独**鸡蛋**不等：合约 5 吨但报价按 500 千克，一元变动值 **10 元**
而非 5 元。用交易单位算，鸡蛋席位盈亏会**错一倍**，且数字看着完全正常。

| 品种 | 交易单位 | 报价单位 | 最小变动 | 一手最小波动 | **price_multiplier** |
| --- | --- | --- | --- | --- | --- |
| JM 焦煤 | 60 吨 | 元/吨 | 0.5 | 30 | 60 |
| **JD 鸡蛋** | **5 吨** | **元/500千克** | 1 | 10 | **10** |
| LH 生猪 | 16 吨 | 元/吨 | 5 | 80 | 16 |
| FG 玻璃 | 20 吨 | 元/吨 | 1 | 20 | 20 |
| SA 纯碱 | 20 吨 | 元/吨 | 1 | 20 | 20 |
| AP 苹果 | 10 吨 | 元/吨 | 1 | 10 | 10 |
| AU 黄金 | 1000 克 | 元/克 | 0.02 | 20 | 1000 |
| AG 白银 | 15 千克 | 元/千克 | 1 | 15 | 15 |

来源为交易所合约说明（运营者 2026-08-09 提供），并由两条独立算术印证：交易所自己给的
`一手最小波动 ÷ 最小变动价位`，以及 `成交额 ÷ (成交量 × 结算价)`——后者在有数据的品种上
四位小数一致，鸡蛋的错误正是它先抓出来的。行情行携带成交量与成交额，使该校验常驻。

## 3. 界面

### 3.1 席位每日持仓

选**品种** + 选**日期**，按品种 → 合约列出多头持仓 / 空头持仓 / 总净持仓。

运营者说的"放到表格里面"指的是**落进我们自己的数据库表**（历史行情表 + 席位持仓表），
不是导出 Excel。界面上的表格是读这些表的结果。

### 3.2 建仓过程

三联图（参照三禾同名页面，但不照抄其口径）：上为 **K 线**（所以开高低必须存）、
中为**净持仓**、下为**估计当日盈亏柱 + 成本线**。

**两个视图都要**：

| 视图 | 会否中断 | 回答的问题 |
| --- | --- | --- |
| **单合约**（如 au2610） | **会**——掉出前 20 那几天空白 | 该席位在这个合约上怎么建仓 |
| **品种汇总**（沪金全部合约） | 几乎不会 | 该席位对这个品种的总体态度 |

交易所每个榜只公布**前 20 名**，这是上限，任何数据源都变不出更多（实测东财单合约
`AU2610` 亦为三榜各前 20，其"44 名"是品种汇总下各合约前 20 的并集）。**品种汇总视图
正是为了补上单合约会断的洞**——运营者要求增加它的原因。

单合约序列中断时的显示方式（断开显示 vs 前后连线）**尚未决定**，见第 4 节。

## 4. 尚未决定 / 尚未查清

1. **单合约建仓过程遇到榜单中断时怎么显示**：断开（如实）还是连线（好看但中间是猜的）。
   建议断开。**待运营者定。**
2. **上期所、郑商所的官方席位接口能回溯到哪一年**——决定 2025 全年能否用官方源覆盖。
   大商所已实证可回溯至 2013（`memberDealPosi/batchDownload`）。**待查。**
3. 套利页切换到自建价差引擎的时机（当前仍读三禾算好的价差）。

## 5. 实现顺序（运营者指定）

1. **采数据**：八品种全历史行情 + 2025 起席位，归一入库 ← 当前阶段
2. **写规则**：价差引擎、席位成本引擎
3. **做界面**：席位两个页面；套利页切换到自建数据

不要跳步。运营者明确要求：需求不清楚就问，不要乱做。
