# 已确认决策

> **现行口径速查(2026-08-18)**——本文 append-only,旧条目是历史记录不改写;
> 被修订的口径以最新条目为准。
>
> **⚠️ 老编号在本文里查不全,这是已知状态,不是文档丢了**(2026-08-20 核实):
> `DEC-001`~`DEC-012` 只有下面「技术基线」表里的一行结论;`DEC-013`~`DEC-086`
> 之间**有 73 个编号在本文里没有任何条目**,只在文首这几行速查里被提过。
> 而全仓有 260 多处代码注释与文档写着"见 DEC-0xx"——翻到这里会扑空。
> 本文自 `DEC-087` 起才是逐条全文。
>
> **查老编号的正确姿势**:`git log -S "DEC-0xx" --oneline` 找到落地那笔提交,
> 提交信息与 diff 就是它的正文(本项目的提交信息写得比条目还细)。
> 不要为了"补全"去追写老条目——那等于事后重构记忆,比缺着更危险。
>
> 套利监控现行规则的最短路径:
> 分层徽标体系 `DEC-063`(+同条两次修订) → 交割红线/MAE `DEC-067` →
> 30 天门槛 `DEC-068` → 手工备注 `DEC-069` → 分品种回撤档+⚡只认首次 `DEC-070` →
> 历史信号跨年 `DEC-071` → 焦煤 either 组对 `DEC-072` →
> **取价改结算价兜底** `DEC-073`(收盘价 0 是无成交,连带撤回一个误判) →
> 现货基差接入 `DEC-074` → **AP 退回默认档** `DEC-075`(脏数据修掉后重测,
> 原结论被推翻)。
> 回归率口径 `DEC-062`(注意后被 067/070 补充)。机构资金引擎 `DEC-043~061`。
> 页面结构:净持仓取代建仓过程 `DEC-077`;报告表缓存与加减方向 `DEC-078`;
> 净持仓摘要补多空手数与成本、反推时序修复 `DEC-079`;
> **净持仓并回席位页、席位改多选** `DEC-080`(推翻 077 的页面形态)。
> 品种汇总 K 线缺三天修复、滑钮联动、当日盈亏上标题 `DEC-081`。
> **格林三名归一(大华期货不并)、净持仓摘要拆到逐家、席位多选框两处失效** `DEC-082`。
> **生猪机构资金(合计流向跟随,与金银两套独立引擎)+ 品种选择器** `DEC-083`。
> **生猪改一年选人、关闭做多支路、界面摆基准对比** `DEC-084`(修订 083 三处口径)。
> **散户反向跨品种固定名单;生猪先加维度不改规则** `DEC-085`。
> **套利监控抢早轮写残缺快照(修)、生猪改方案 C、玻璃纯碱上线** `DEC-086`。
> 采集时刻与会话时长 `DEC-076`。
> 教训类不用翻本文,已收敛到 `docs/PITFALLS.md`。

## 1. 文档用途

本文记录已经确认并对后续设计、实现和验收具有约束力的架构与业务口径。状态为“已确认”的事项不得在实现阶段自行更改；如需调整，必须新增决策记录并同步更新受影响文档。

确认日期：2026-07-24。

## 2. 技术基线

| 编号 | 状态 | 决策 |
| --- | --- | --- |
| `DEC-001` | 已确认 | 前端包管理器使用 pnpm |
| `DEC-002` | 已确认 | Rust 使用进入实现阶段当日的稳定版，并由 `rust-toolchain.toml` 固定 |
| `DEC-003` | 已确认 | 数据库使用 PostgreSQL，数据库访问使用 SQLx |
| `DEC-004` | 已确认 | 登录使用 Cookie Session |
| `DEC-005` | 已确认 | 第一版文件存储使用本地适配器，预留 S3 兼容适配器 |
| `DEC-006` | 已确认 | 第一版任务队列使用 PostgreSQL 任务表 |
| `DEC-007` | 已确认 | API 文档使用 OpenAPI/Utoipa |
| `DEC-008` | 已确认 | 前端测试使用 Vitest |
| `DEC-009` | 已确认 | 后端测试使用 Cargo Test 和 SQLx 测试数据库 |
| `DEC-010` | 已确认 | 部署使用 Docker Compose |
| `DEC-011` | 已确认 | 数据库命名使用 `snake_case` |
| `DEC-012` | 已确认 | API 版本前缀使用 `/api/v1` |

## 3. Workspace 与租户隔离

### `DEC-013` Workspace 数据归属

状态：已确认。

- 业务数据归属于 `Workspace`，不直接归属于 `User`。
- MVP 中每个用户自动对应一个个人 Workspace。
- MVP 不实现 Workspace 共享、成员邀请和跨用户协作。
- 所有业务表必须包含 `workspace_id`，查询、唯一约束、审计、任务、对象存储和 AI 工具均以 `workspace_id` 作为强制隔离条件。
- `users`、认证 session 和系统级角色属于身份基础设施，不以 `workspace_id` 作为业务归属。
- 不允许由客户端自由指定可访问的 `workspace_id`；服务端从已认证 session 解析当前 Workspace。
- 系统管理员身份不自动授予跨 Workspace 业务数据读取权限。

影响：产品权限、数据库模型、API 查询、后台任务、文件对象、浏览器会话、AI 工具和验收标准。

## 4. 分析范围和时间口径

### `DEC-014` MVP 排除历史回测

状态：已确认。

- MVP 不实现规则驱动的历史回测、自动生成交易信号或模拟成交。
- MVP 只提供历史统计、季节性、百分位、极值、回撤和情景计算。
- 真实成交的胜率和盈亏统计不属于回测，但只能基于完整 `trade_group`。

### `DEC-015` 时间、交易日和交易日历

状态：已确认。

- 系统时间点统一以 UTC 和 PostgreSQL `timestamptz` 保存。
- 业务展示和默认业务时区为 `Asia/Shanghai`。
- 市场数据独立保存 `trade_date` 和 `session_type`。
- 夜盘按照对应交易所的交易日历归入下一交易日。
- 交易日历按交易所分别维护，具有版本号，并支持带审计的人工修正。
- 任何按日统计都必须记录所使用的交易日历版本。

## 5. 价格、合约和交易口径

### `DEC-016` 价格口径

状态：已确认。

- 收盘价 `close_price` 与结算价 `settlement_price` 分别保存。
- 行情图默认使用收盘价。
- 日终持仓、未实现盈亏和权益默认使用结算价。
- 已实现盈亏使用实际成交价。
- 价差、统计、图表、持仓和报告结果必须记录 `price_basis`。

### `DEC-017` 连续合约与换月

状态：已确认。

- MVP 不自动生成连续合约。
- 允许导入外部连续合约数据，但必须记录数据来源和换月规则。
- 后续内部换月规则必须版本化，并单独记录换月事件。
- 外部连续合约不得与实际交割合约数据混用或伪装成原始合约行情。

### `DEC-018` 成交、配对和盈亏

状态：已确认。

- 原始 `trade_fill` 不允许覆盖修改；纠错使用冲销/补偿记录并保留关联。
- 开平配对默认采用 FIFO。
- 加权平均仅用于展示当前持仓成本，不用于改写成交配对或已实现盈亏。
- 已实现净盈亏包含手续费。
- 胜率按已完成的完整 `trade_group` 计算，不按单笔成交计算。

## 6. 标识和数值类型

### `DEC-019` 主键策略

状态：已确认。

- 普通业务实体使用应用层生成的 UUIDv7。
- 高频时间序列和明细表使用 PostgreSQL `BIGINT GENERATED BY DEFAULT AS IDENTITY`。
- 使用 BIGINT 的表必须通过业务字段建立包含 `workspace_id` 的唯一约束。
- 对外 API 不得依赖 BIGINT 的连续性表达业务顺序或数量。

### `DEC-020` 金融数值精度和币种

状态：已确认。

| 语义 | PostgreSQL 类型 |
| --- | --- |
| `price`、`spread` | `numeric(20,8)` |
| `money`、`fee`、`pnl` | `numeric(24,4)` |
| `ratio` | `numeric(20,10)` |
| `lots` | `bigint` |

- 金融数据不得使用浮点数作为数据库最终存储类型。
- 数据库模型支持 `currency_code`，默认 `CNY`。
- MVP 界面仅开放人民币，不实现汇率换算。
- Rust 领域和数据库边界使用十进制定点类型；浮点数只允许用于明确误差范围的临时统计计算。

## 7. 数据装载

### `DEC-021` 批次回滚

状态：**已废止**（2026-08-13，随 `DEC-049` 摘除导入通道）。

原文规定了导入批次的整批原子回滚、冲突清单与补偿批次语义。批次这个概念已经
不存在了——不是规则改了，是规则没有对象了。数据层面的退路现在由部署前的整库
备份承担，见 `docs/RELEASE_PROCESS.md`。

## 8. 凭据和浏览器安全

### `DEC-022` 信封加密和主密钥

状态：已确认。

- 敏感数据使用信封加密：数据由 DEK 加密，DEK 由版本化主密钥/KEK 包裹。
- 主密钥不得存入数据库、代码仓库或 Docker 镜像。
- 密文记录保存加密算法、`wrapped_dek` 和 `kek_version`，不保存主密钥本身。
- 主密钥轮换通过重新包裹 DEK 完成，不要求解密并重写全部业务明文。
- 数据库备份与主密钥恢复副本必须分开保存。

### `DEC-023` 远程人工登录

状态：已废止（由 `DEC-031` 替代，2026-08-02）。

以下为原决策内容，仅为变更审计保留，不再生效：

- 远程登录使用隔离 Chromium 容器、Playwright 和 noVNC。
- 每个 Workspace 与数据源组合使用独立 Browser Context。
- noVNC 会话必须短期授权、不可直接公开暴露，并与应用 session 绑定。
- 登录状态使用信封加密保存，不得写入 Git、普通日志或未加密对象。
- 采集浏览器与图表渲染浏览器必须保持独立信任边界。

## 9. 数据源和提取

### `DEC-024` 数据源访问边界

状态：已确认。

- MVP 不开放任意 URL 访问。
- 只允许域名白名单和已经实现的数据源连接器。
- Phase 4 采集范围由 `DEC-031` 和 `DEC-039` 约束；第一批仅包含五家交易所公开数据。二手数据源原则上不进入第一批，现有例外只有两条且都写在决策里：`DEC-041` 的新浪（仅 DCE，官方失败后兜底）与 `DEC-043` 的东方财富（仅席位，全部官方兜底之后）。
- 原三禾登录连接器及其登录基础设施已由 `DEC-031` 替代并移除；`DEC-042` 仅为 Phase 5 自由价差有限恢复三个无需登录的只读 POST 端点，不恢复登录能力。
- 重定向、子资源、XHR、WebSocket 和下载仍受相同白名单与出口策略约束。

### `DEC-025` 数据提取优先级

状态：已确认（2026-08-02 修订）。

2026-08-02 起有效内容（Phase 4 自动采集）：

- 删除 OCR 级。
- 数据获取路径固定为 akshare 封装的交易所公开接口与公开文件。
- Phase 5 自由价差的三禾只读 provider 是 `DEC-042` 的独立有限例外，不进入 Phase 4 自动采集管线。

以下为修订前决策内容，仅为变更审计保留，不再生效：

提取顺序固定为：

`官方 API > 网络请求 > HTML 表格 > 下载文件 > OCR`

- 只有前一级无法满足需求时才进入后一级。
- OCR 结果必须经过人工确认，确认操作形成审计记录后才能进入正式数据表。

## 10. Phase 1 补充决策

### `DEC-026` 首次用户初始化

状态：已确认。

- 系统中不存在用户时，允许访问一次性初始化接口或初始化页面。
- 初始化必须校验一次性 bootstrap token。生产与 VPS 环境使用 `BOOTSTRAP_TOKEN_FILE` 指向只读挂载文件，当前 futures VPS 约定宿主路径为 `/etc/futures-platform/secrets/bootstrap-token`，`root:root` 且权限 `0400`；不得写入 Git、数据库、镜像、日志或普通 `.env`。开发环境可使用等价临时文件；环境变量 `BOOTSTRAP_TOKEN` 仅允许非生产本地调试。
- 初始化在单个数据库事务中创建首个管理员用户、个人 Workspace 和 Workspace Owner 成员关系。
- 初始化不自动创建虚构交易账户。
- 首个用户创建成功后，初始化入口永久关闭，并立即使 token 失效；文件来源的 token 必须删除。
- 并发初始化只能有一个请求成功，必须通过集成测试验证。

### `DEC-027` 成交纠错和计算版本

状态：已确认。

- `trade_fills` 是不可变成交流水。
- 错误成交通过冲销记录和修正记录处理，不覆盖原始成交。
- 默认采用 FIFO 开平配对。
- 加权平均只用于展示当前持仓成本。
- 胜率按照完整 `trade_group` 计算。
- 多腿套利按照完整套利交易组计算。
- 所有计算必须保留来源记录和计算版本。

### `DEC-028` 反向代理

状态：已确认。

- Docker Compose 和 VPS 部署采用 Nginx。
- `/` 提供 Vue 前端静态资源。
- `/api/` 转发到 Axum API。
- `/events/` 支持 SSE。
- 原 noVNC 独立路径和 WebSocket 代理要求已随 `DEC-023`、`DEC-034` 废止，不再实施。
- TLS 配置预留；未配置域名时允许先通过指定端口验证。
- Nginx 不承载业务逻辑。

### `DEC-029` futures VPS 主密钥托管

状态：已确认。

- 当前 futures VPS 采用主机文件托管主密钥：`/etc/futures-platform/secrets/master-key-v1`。
- 主密钥文件必须由 `root` 所有，权限为 `0400`。
- 主密钥不得进入 Git、Docker 镜像、PostgreSQL、普通日志或最终回复。
- 主密钥只读挂载给需要解密的容器。
- 数据库只保存 `wrapped_dek`、`kek_version`、`nonce` 和算法信息。
- 支持主密钥版本轮换；离线恢复副本与数据库备份分开保存。
- 部署文档必须写明恢复和轮换步骤，且不得泄漏明文密钥。

### `DEC-030` OCR 运行规格

状态：已废止（由 `DEC-031` 替代，2026-08-02）。

PaddleOCR 容器不建。以下为原决策内容，仅为变更审计保留，不再生效：

- OCR 使用独立 PaddleOCR 容器。
- MVP 使用 CPU 模式。
- 固定镜像和模型版本，禁止使用 floating `latest`。
- Rust 通过内部 HTTP API 调用 OCR 服务。
- OCR 容器不直接暴露公网端口。
- 必须设置文件大小、图片尺寸、超时、并发和内存限制。
- OCR 结果只能进入待确认区，人工确认后才能写入正式业务表。

### `DEC-031` 第一批交易所连接器

状态：已确认（2026-08-02 修订）。

2026-08-02 起有效内容：

- 五交易所清单不变：大连商品交易所 `DCE`、上海期货交易所 `SHFE`、郑州商品交易所 `CZCE`、广州期货交易所 `GFEX`、中国金融期货交易所 `CFFEX`。
- 实现方式改为 akshare 采集器：独立 Python 容器，按需运行；每交易日盘后由调度拉起，跑完退出，不常驻。
- 采集器经 akshare 拉取交易所公开数据，产出标准化 CSV 后调用平台导入 API 入库，保留来源、批次、回滚全套机制。
- 原 Phase A/B 分批约束取消，akshare 一次覆盖五家。
- 东方财富仅按 `DEC-043` 作为席位专用兜底源接入（排在全部官方源之后，不承担行情与目录），其余二手数据源不进入第一批。
- 隔离 Chromium、Playwright、noVNC、信封加密浏览器会话整套移除。登录类数据源如未来需要，另行立项；`DEC-042` 的三禾自由价差特例无需登录且不恢复上述基础设施。

以下为修订前决策内容，仅为变更审计保留，不再生效：

- Phase A 先实现大连商品交易所 `DCE` 和上海期货交易所 `SHFE`。
- Phase B 再实现郑州商品交易所 `CZCE`、广州期货交易所 `GFEX` 和中国金融期货交易所 `CFFEX`。
- 连接器只处理交易所公开页面、公开文件和公开接口。
- 三禾数据连接器保留设计，但授权范围确认前不得启用。
- MVP 不实现任意网站通用抓取器。

### `DEC-032` SSE 与 WebSocket

状态：已确认。

- 普通后台任务进度使用 SSE。
- AI 流式回复优先使用 SSE。
- 原 noVNC WebSocket 要求已随 `DEC-023`、`DEC-034` 废止，不再实施。
- 普通业务通知不使用 WebSocket。
- 除非存在明确双向实时通信需求，否则不新增 WebSocket。

### `DEC-033` PostgreSQL RLS

状态：已确认。

- 启用 Workspace 级 Row-Level Security 作为纵深防御。
- 系统表和公共字典表不启用 RLS。
- 所有 Workspace 业务表从首次创建时启用 RLS。
- 每个业务事务使用 `SET LOCAL` 设置当前 `workspace_id`。
- 不允许依赖连接池中的持久 session 状态。
- Repository 查询仍必须显式包含 `workspace_id`。
- RLS 是第二层防护，不能代替应用层授权。
- 必须编写跨 Workspace 越权集成测试。
- 在创建第一张 Workspace 业务表之前完成 RLS 基础设施。

### `DEC-034` noVNC 浏览器会话

状态：已废止（由 `DEC-031` 替代，2026-08-02）。

以下为原决策内容，仅为变更审计保留，不再生效：

- 一次性访问令牌有效期为 5 分钟。
- 无操作 30 分钟关闭。
- 单次会话最长 2 小时。
- 每个 Workspace、数据源和外部账户最多一个活跃登录会话。
- 每个会话使用隔离 Browser Context。
- 登录状态使用信封加密保存。
- noVNC 不直接暴露固定公网端口。
- 会话关闭后销毁临时桌面环境。
- Cookie、LocalStorage 和认证头不得进入普通日志。

### `DEC-035` AI 模块部署边界

状态：已确认。

- AI 模块暂不在 Phase 1 实现。
- OpenAI、Grok、Gemini 使用统一 Provider 接口。
- API Key 使用信封加密。
- AI 默认只读，不直接执行任意 SQL。
- AI 只能调用系统定义的数据查询和统计工具。
- AI 不允许修改、删除业务数据或执行交易。
- AI 流式输出使用 SSE。
- 保存模型、工具调用、数据来源、Token 使用和数据截止时间。
- 结构化金融数据通过 SQL 工具查询；文档和交易笔记才使用 `pgvector`。
- 不把整库数据直接发送给模型。

## 11. Agent 工作流与上下文交接

### `DEC-036` Planner/Generator/Evaluator 工作流

状态：已确认。

- 主 Agent 负责编排，固定使用 Planner、Generator、Evaluator 三个角色。
- 工作流为：Planner 制定计划 → 文档审查 → Planner 修正文档 → Generator 实现 → Evaluator 审查 → Generator 修复 → Evaluator 复核 → 全部验证通过 → Git 提交。
- Planner 不写业务实现代码；Generator 不改变已确认业务口径；Evaluator 原则上只读并输出证据化审查。

### `DEC-037` 上下文轮换与交接

状态：已确认。

- 上下文接近上限、阶段完成、日志/代码积累过多、用户要求换新 AI 或 AI 无法可靠记住前序决策时，必须执行交接。
- 交接文档使用不可覆盖路径 `docs/handoffs/HANDOFF_YYYYMMDD_HHMM.md`。
- `docs/handoffs/LATEST.md` 只指向并概括最新交接状态，不删除历史文件。
- 交接必须记录 Git、未提交修改、任务状态、决策、测试、VPS、迁移、服务和下一步第一条操作。
- 交接文档不得包含密码、API Key、Cookie、Token、主密钥、数据库明文凭据或其他秘密。
- 新 AI 必须通过 Git、测试和运行环境核验交接内容，不得盲目信任摘要。

## 12. 总方案重审决策

### `DEC-038` 自动采集免确认

状态：已确认。

- 白名单数据源的结构化自动采集数据免人工确认、免提取预览，直接进入正式表。
- 兜底机制：解析或校验失败的批次自动隔离为 `failed`，不写正式表；批次可整批回滚；数据质量警告仅记录、不拦截。
- 人工确认仅保留于手动文件导入的既有流程，该流程不变。

### `DEC-039` 采集范围与历史回填

状态：已确认。

- 范围：全市场日行情（收盘价与结算价）、全市场席位龙虎榜（各合约成交量、持买、持卖排名）、交易日历、合约参数。
- 历史回填全部可得历史。
- 执行策略：行情全量一次拉取；席位倒序分层，先近 5 年，再由后台任务向更早年份补拉；不可得年份留记录、不阻塞。
- 磁盘达到 80% 水位时扩容 VPS 磁盘，不更换机器。

### `DEC-040` 图表导出

状态：已确认。

- 不建服务端图表渲染沙箱。
- PNG/SVG 由 ECharts 前端直接导出。
- SVG 安全清洗验收条款保留。

### `DEC-041` DCE 采集来源例外

状态：**已被 `DEC-045` 取代（2026-08-10）**。原文保留供变更审计。
被推翻的两条：新浪作为 DCE 兜底（覆盖率只有 44%，永远补不齐一天），
以及「东方财富缺少结算价」（只对 K 线接口成立，行情快照带结算价）。
仍然成立的一条：DCE 全局 WAF 是真实的，且至今未解。

- 实证依据：2026-08-02 从国内网络直连 `www.dce.com.cn` 首页、行情接口并使用浏览器 User-Agent 均返回 HTTP 412；VPS 同样返回 412；AKShare 上游已记录“大商所所有接口返回 412 待解决”。该现象属于 DCE 全局 WAF 挑战，不是单一出口 IP 问题，因此不建设独立采集出口。
- DCE 固定采用“官方直连优先、经审核聚合源自动 fallback”：每次仍先调用 AKShare 封装的大商所官方接口；发生 HTTP 412 或等价的网络、响应格式、解析失败时，自动切换到经审核的聚合源。AKShare 上游修复后，优先级逻辑无需改动即可自动恢复一手来源。
- Phase 4A 经审核的 fallback 固定为新浪期货接口：其 AKShare `futures_zh_daily_sina` 同时提供指定合约的历史收盘价与结算价，`futures_hold_pos_sina` 提供指定日期、逐合约的成交量、持买和持卖排名；东方财富 `futures_hist_em` 缺少结算价，因此不选用。
- fallback 数据必须如实使用独立 `data_sources` 记录和域名白名单；批次、`market_prices.source_id`、`seat_positions.source_id` 必须指向新浪聚合源，不得标注为 DCE 官方。官方失败尝试与 fallback 成功批次均须可审计、可追溯。
- 该例外仅限 DCE；SHFE、CZCE、GFEX、CFFEX 继续只允许官方直连，不得引入聚合源。

### `DEC-044` 大商所官方历史文件接入

状态：已确认（2026-08-09，运营者授权）。

- 背景：焦煤（2013-03 上市）与鸡蛋（2013-11 上市）在 2013–2018 的逐合约行情，
  此前判定为「拿不到」。该判定是**错的**，错在只试了 AKShare 封装的接口与聚合站，
  没有查交易所官网自己的历史下载页。实证（2026-08-09）：
  - 大商所官方 API 仍全线 412（`DEC-041` 的 WAF 问题未解，当日复测首页与行情页均 412）
  - 新浪逐合约只保留到约 **2018-09**（`JM1909` 有、`JM1801`/`JM1901` 全空）
  - 东方财富**一条退市合约都不保留**（`114.jm1909` 亦为空，仅在市 14 个合约）
  - 三禾逐合约仅 **2026-01 起 7 个月**
  - **大商所官网历史下载页有 2006 年起的年度逐合约文件**，且含结算价
- 接口：`GET /dcereport/quote/history/download?type=1&year=YYYY&variety=<id>&lang=zh`，
  返回 `<variety>_ftr.xlsx`。工作表「日历史行情」，列含商品名称、合约名称、交易日期、
  开高低收、前结算价、**结算价**、涨跌、成交量、持仓量、持仓量变化、成交额。
- **取得方式（2026-08-10 更正）**：这些是 CMS 静态资源，**并不在 WAF 后面**——
  VPS 直接 curl 返回 **HTTP 200**，实测可得。此前记录的两个说法都不准确：
  「运营者本人下载」不对（是 Claude 下的），「操作运营者的 Chrome 下载」也不对
  （用的是应用内浏览器，页面脚本抓取后分片落盘）。**真实情况是这条路径根本不需要浏览器。**
  WAF 挡的是该站的 HTML 页与 JSON 接口（`DEC-041`），静态文件不挡。
- 仍然只做**一次性取得**、读本地文件，理由是**历史不可变**：取得一次永远正确。
  **不得编写任何绕过机器人检测的代码**——这条对被挡的那些路径始终有效。
- 采集器以本地文件源读取：来源代码 `dce_official_history`，类型 `exchange_public`、
  授权 `whitelisted`、连接器 `dce_history_files_v1`（迁移 `202608100001` 放行）。
  **域名白名单为空**——该源永不打开套接字，将来若有人试图从这里发请求，出站围栏会直接拒绝。
- 落库仍走既有导入通道，按交易日一批（catalog → calendar → market），审计批次、
  来源标注、RLS 全部复用。`market_prices` 业务唯一键含 `source_id`，故与新浪/官方源的
  行共存不冲突：同一交易日可由多个来源各自作证。
- 该源**不承担席位**。这些年份的席位在交易所的日排名归档里
  （`/dcereport/publicweb/dailystat/memberDealPosi/batchDownload`，实测 2013-06-14 可得），
  属另一次一次性取得，未在本决策范围内。

### `DEC-048` 总方案修订：取消成交绩效与 AI 两阶段，机构资金进入总方案

状态：已确认（2026-08-11，运营者指示「成交持仓绩效直接取消掉，AI 分析也取消掉……
加入了机构资金这个模块，总方案进行修正」）。

- **取消 Phase 6（成交、持仓与绩效）与 Phase 8（AI 分析）**。原范围整体移入
  `DEVELOPMENT_PLAN.md` 暂缓项；未来要做须单独立项，不因其他阶段完成自动复活。
  阶段编号退役不复用，避免历史标签与文档引用错位。
- **机构资金（席位 Alpha）以 Phase 10 进入总方案**。设计定稿 `docs/SMART_MONEY_DESIGN.md`，
  离线研究在 `research/`。进入总方案时的实际状态：黄金 v1 回测完成
  （`research/REPORT_AU_v1.md`）、金银八家双强组规则定稿、`engine/smart_money.py`
  每日信号与主力跑路警报上线、机构资金页平台内渲染（均由并行会话交付）。
- 当前并行推进三条线：套利（5A 收口）、席位分析、机构资金。

### `DEC-047` 大商所行情日更改由新浪供给，带到货检查

状态：已确认（2026-08-10，运营者指示「按你的建议做」）。取代 `DEC-045` 里
「大商所全量改走东方财富」中关于**行情**的那一半；席位仍走东财，那条是通的。

- **背景**：大商所四条路当天全断，实证见上一条。新浪是唯一还应答的源。
- **这不是引入一个新源**：`price_history` 里大商所 **2025-01-02 之后的行情本来就是
  新浪的**（回填时补缺口写入，11,262 行）。运营者当前看到的焦煤/鸡蛋/生猪价差，
  最近一年半的点全部来自新浪。所以真正的选择不是「要不要用新浪」，而是
  「要不要让已经在用的那条路每天继续走」。
- **与交易所口径对照（2026-08-10 实测，843 个重叠日，六个合约）**：

  | 字段 | 结果 |
  | --- | --- |
  | 收盘价 | 843 天中 **837 天完全相同** |
  | 结算价 | 同上 |
  | 开盘、最高 | 同上 |
  | 持仓量 | **恰好 2.0000 倍**（交易所双边、新浪单边），843 天无一例外 |
  | 成交量 | 约 2 倍，同一原因 |

  字段齐：`open/high/low/close/volume/hold/settle`，**有结算价**（席位成本引擎要的
  就是它），缺的只有成交额。

- **那 6 天的差异，原因都查明，不是随机噪声**：
  - **2024-09-25 一天**，新浪把六个合约的结算价全写成 `0.0`，而交易所有正常结算价。
  - 另三天是**当日只成交 1–2 手**的合约日，收盘价与交易所差最多 60 点；这三天新浪的
    结算价与交易所**完全相等**，只有收盘价偏。这类合约不会是套利页的腿。

- **到货检查**（`deploy/collector/sina-dce-daily.py` 的 `usable`）：
  1. 收盘价或结算价 `<= 0` → 丢弃。**0 不是价格，是缺失**；灌进去席位持仓成本会按零
     结算价算出一个合法但错的数，整条链路无一处报错。
  2. 收盘价不在当日 `[low, high]` 区间内 → 丢弃。
  3. 成交量 `<= 0` → 丢弃。
  4. 收盘价与结算价都没有 → 丢弃。

  被拒的行**连原始值一起打进日志**，不是安静跳过。用例在
  `collector/tests/test_sina_dce_daily.py`，取的是上面两处真实故障的数值。

- **取数偏好显式化**：`load_own_spread_points` 的去重原本按 `source` 字典序挑行，
  那只是「确定」而不是「对」——`'sina'` 排在 `'dce_official_history'` 之后，官方数据
  反而会被聚合源盖掉。改为按来源可信度排序：交易所文件 > akshare > 东财 > 新浪。
  以后大商所官方通了，同一天两个来源都有时官方那行自动胜出，不用改代码。

- **对套利页的影响面**（运营者关心的核心）：历史部分**一点不会变**——2024 年底之前
  全是交易所年度文件，新浪只往后加新的一天。当前的腿都很厚：2026-08-10 单边成交
  JM2701 123,476 手、LH2701 10,009、JD2701 9,429，薄腿问题在这个量级上不存在。
  薄腿风险是季节性且可预期的：每年 1 月新的 01 腿刚挂牌那几周量才小。

- **已知且不掩盖的缺陷**：极低成交日新浪的收盘价可能与交易所差几十点。官方源不可达，
  这是唯一可得来源的代价。**没有加成交量阈值去修饰**——阈值是编的，如实呈现比用一个
  拍脑袋的规则遮住它诚实。

- **合约清单取自东财** `futsse-static` 那个接口：行情端点全断之后它仍正常应答且当日
  更新。取不到时退回按月份推，宁可多试几十次也不漏掉新挂的合约。

### `DEC-045` 的前提在 2026-08-10 失效：东财行情与 K 线两条路都不再应答

状态：**已实证的故障，尚未决定怎么办**（等运营者定，源的选择历来是运营者的决定）。

- **现象**：每日采集对大商所的 `trading_calendar_v1` 与 `daily_market_prices_v1`
  两个数据集均以 `ConnectionError` 失败，四次重试全废。
- **实证（2026-08-10，生产机直连，每个地址各一次请求）**：

  | 请求 | 结果 |
  | --- | --- |
  | `futsse-static.eastmoney.com/redis`（目录，采集器在用） | **200**，232 行 |
  | `push2his.eastmoney.com/` 根路径 | 404（主机通，TLS 正常） |
  | `push2his.eastmoney.com/api/qt/stock/kline/get` | **连接被直接断开**，无状态行 |
  | `push2.eastmoney.com/api/qt/stock/get` | **502** |

  **第一个请求就失败**，不是量大了被限频——我一开始按限频判断，是错的。
  同一批主机在别的路径上正常应答，所以不是网络、DNS 或 IPv6 的问题；
  容器内 TCP 连 443 也是通的。
- **于是大商所四条路全断**：官方站 412（`DEC-045` 记录，今日复验仍是 412，含首页）；
  新浪只覆盖 44% 的在市合约；东财行情/K 线如上；年度文件是当初手工下载的，
  而它们的下载地址在大商所站内，同样 412。
- **影响面**：只影响**日更**。焦煤、鸡蛋、生猪的历史价格已回填到 2026-08-10，
  两个页面现在能看；不解决的话它们会停在这一天，而郑商所与上期所五个品种照常前进——
  **一半品种在动、一半不动**，比整体停更更容易让人误判。
- **候选源实测（同日，只读探测，未改任何配置）**：
  - **akshare 的 `get_dce_daily` 也不行**：`JSONDecodeError`——它打的同样是
    `dce.com.cn`，拿回来的是 412 挑战页而不是 JSON。这条排除，不用再试。
  - **新浪对我们这三个品种是全覆盖的**：JM/JD/LH 当前在市的 **29 个合约，
    新浪 29 个全有，且全部更新到 2026-08-10**（当日最新交易日）。
    这与 `DEC-045` 里「新浪只覆盖 44%」并不矛盾——那 44% 是**全大商所 186 个合约**
    的口径，缺的是我们根本不看的品种。两张历史表现在已由 `product_instrument_scope`
    收敛到八个品种，`DEC-045` 当时那条「少一个合约即判整天不完整」的守卫
    要守的范围也随之变了。
- **未做的事**：没有另选源，也没有改任何请求去试探怎样才能被放行。
  运营者的规矩是不得为被挡的路径写绕过机器人检测的代码；恢复新浪等于推翻 `DEC-045`，
  那是运营者的决定，不是我能顺手做掉的。

### `DEC-046` 自由价差改用自建引擎，三禾降为可回退的备选

状态：已确认（2026-08-10，运营者指示「按你的建议做」，先切自研价差再做日更）。

- **背景**：建这个网站的两个理由之一，就是三禾的价差不排除散户不可交易的日期。
  自建引擎（`domain/spread_analytics.rs` + `load_own_spread_points`）在 8 月初已经写完
  并有测试，但页面一直还在请求 `provider: "sanhe"`——引擎在，没接上。八个品种的
  历史价格（238,291 行，最早到 2008-01）灌完之后，接上的前提已经具备。
- **口径**（运营者定）：价差 = 同一交易日两腿**收盘价**相减，**时间早的减时间晚的**。
  用收盘价而不是结算价：价差看的是两个合约的价格关系，收盘价是市场公认的那个时点；
  结算价（当日 VWAP）留给席位持仓成本。
- **年份配对**：后腿月份大于前腿的是同一年（01-05），否则是次年（09-01 的 01 腿是次年）。
  生产数据实证：焦煤 09-01 组合切出 14 个年段，每段起点落在 1 月 16–19 日，
  即后腿上市首日——与运营者说的「从 1.18 开始，两腿之间有价差的时间开始」一致。
- **三禾不删，降为备选**：切换只是请求里的一个字符串，出问题能立刻切回去，
  也便于把两边的数字摆在一起比。`DEC-042` 授权的三个只读端点维持不变。
- **落账不许冒充**（`202608100009`）：`save_series` 原本把任何算完的序列都写成
  `provider_code='sanhe'` 并挂上三禾的 `data_sources` 行。自研接上来以后这就是假账。
  改为：
  - `spread_provider_series.source_id` 改可空，并加双向约束
    `(provider_code = 'self') = (source_id is null)`。**不给 self 造一条 data_sources**——
    那张表建模的是**外部**来源（授权状态、连接器代码、白名单），我们自己算的没有外部
    来源，硬造一条只会让那张表说谎。
  - 口径跟着来源走：sanhe→`upstream_spread`，self→`own_close_difference`，错配插不进来。
  - 留存清理原本写死 `provider_code='sanhe'`。两路的 `query_hash` 对同一组腿是相同的，
    不按 provider 分开会把另一路的历史一并删掉。已按 provider 限定，并有测试盯着。
- **品种与月份也脱离三禾**：新增 `/providers/self/varieties` 与
  `/providers/self/varieties/{variety}/months`，以 `price_history` 为准而不是 `instruments`
  ——能不能算价差取决于有没有价格，登记过但没有 K 线的品种列出来只会让人点了报空。
  月份只看最近 400 天，且窗口基准是该品种自己的最后一个交易日而不是今天：
  苹果、生猪不是月月挂牌，早已摘牌的月份摆进下拉框选了也算不出东西；用今天做基准
  则一旦数据停更下拉框就空了。
- **已知缺口（未解，见下一项工作）**：`price_history` 与 `seat_history` 全仓库
  **没有任何写入方**，只有一次性手工装载。日更不接上，这两个页面会停在 2026-08-10 不动。

### `DEC-045` 移除大商所官方源与新浪源，大商所全量改走东方财富

状态：已确认（2026-08-10，运营者指示「去除大商所和新浪，找替换源」）。

- **背景**：`DEC-041` 定的「官方优先 + 新浪 fallback」在 2026-08-09 的生产验收里整体失效，
  导致部署失败并回滚。运营者指出这不是新问题——大商所采集用不了才改新浪，新浪也不行才
  加东财，之后又加三禾；本决策把这条路径一次走完。
- **实证（2026-08-09 生产 + 2026-08-10 本机复验）**：
  - 大商所官方：`www.dce.com.cn` 全站 HTTP 412 挑战页（首页亦然），`portal.dce.com.cn` 502。
    两个不同 IP、相隔 14 小时结果一致。`DEC-041` 记的 WAF 问题至今未解。
  - **新浪对大商所在市合约的覆盖只有 44%**：2026-08-07 共 186 个合约，
    **105 个新浪没有任何历史**（LH/LG/BZ 等较新品种，含我们要的生猪）。
    这是新的一条——旧文档只记了新浪的历史深度到 2018-09，没记它连当日合约都缺一半。
    因此 `baced5f` 那道「少一个合约即判整天不完整」的守卫是对的，它不该被放宽，
    该被移除的是这个补不齐的源。
  - akshare `futures_settle` 的文档字符串声称支持 DCE，**代码里没有 DCE 分支**，
    走 else 打印 Unsupported 返回空表。akshare 全库无大商所结算价来源。
  - 三禾三个授权接口（`DEC-042`）不含逐合约价格，`variety_contracts` 只返回主力月份。
- **`DEC-041` 的一条前提被推翻**：「东方财富缺少结算价」只对 `futures_hist_em`
  （K 线接口，K 线本就不带结算价）成立。东财**行情快照接口带结算价**：
  字段 **`f130` = 昨结算价**，以上期所官方 2026-08-07 结算价对照，
  cu2609/cu2610/cu2612/cu2703 **四条全等**；同接口 `f60` 是昨收、不等。
  当初选新浪不选东财的唯一理由不成立。
- **新的大商所来源矩阵（全部东方财富，不再有官方直连与新浪）**：

  | 数据集 | 接口 | 字段 |
  | --- | --- | --- |
  | 合约目录 | `futures_hist_table_em` 合约总表 | 市场=大商所，按品种过滤 |
  | 日行情 | `push2.eastmoney.com/api/qt/stock/get?secid=114.<合约>` | `f46/f44/f45/f43` 开高低收、`f47/f48` 量额 |
  | 结算价 | 同上 | **`f130`（昨结算）** |
  | 席位 | `RPT_FUTU_DAILYPOSITION`（`DEC-043`） | 已在生产实证 |
  | 交易日历 | 上述任一接口对该日有应答 | — |

- **结算价按 D+1 回填**：`f130` 是昨结算，故 D 日的结算价在 D+1 日读取写入。
  取得的是交易所最终数，不是盘中推算值。
- **成本**：我们要的 JM/JD/LH 共 30 个在市合约，每日约 30 个请求（新浪那套是 186 个且缺一半）。
- **批量列表接口不可用**：`api/qt/clist/get?fs=m:114` 对期货返回全零（开高低收皆 0），
  只能逐合约取。30 个请求的量级下这不构成问题。
- **本决策不扩大到其它交易所**：SHFE、CZCE、GFEX、CFFEX 继续只走官方直连，
  `DEC-041` 最后一条「例外仅限 DCE」维持不变。历史数据仍按 `DEC-044` 用交易所年度文件。
- **不得编写任何绕过大商所机器人检测的代码**，此前与此后均适用。

#### 待收口：批量接口（2026-08-10 发现，尚未落实现）

逐合约取数在生产机上实测**会被节流**：约 1 秒间隔、连续 60 个请求时，K 线接口
`push2his` 大量以「连接被关闭且无状态行」中断（表现像主机不可达，不是 HTTP 错误码）。
已加 1 秒节流 + 单请求重试，但更根本的解法是换接口：

- **`https://futsseapi.eastmoney.com/list/<mktid>` 一次请求返回整个交易所**
  （大商所 302 个合约），字段含 `dm` 合约、`o/h/l/p` 开高低最新、`vol/cje/ccl`
  成交量/额/持仓，**价格已是真实单位，无需按 `f59` 缩放，也不需要 `ut` token**。
- **`zjsj` = 结算价**：与上期所官方 2026-08-07 结算价对照，cu2608/2609/2610/2612/2703
  **五个全部相等**（`rzjsj`、`fzjsj` 为同值别名）。
- 代价从每天约 60 个请求降到 **1 个**，节流问题随之消失。

**未验证、因而未采用的一条**：`zjsj` 在当日收盘结算公布后，是否会前进为当日结算价。
2026-08-10 上午 10:04（盘中）观测时它仍是 08-07 的值，这与「最近一次已公布结算价」
和「昨结算」两种解释都相容，**分辨不开**。必须在收盘结算公布后（约 16:00–20:00）
再观测一次：
- 若前进为当日 → 收盘后跑一次即可拿到当日完整行（开高低收 + 结算），最简；
- 若不前进 → 维持现有「按证据归属到前一交易日」的做法，当日开高低收另取。

在这条观测完成前，不得按任一假设发版。

#### 部署两次回滚的根因（2026-08-10 查实）

`5bc0e6b` 给价差历史加了保留量限制（删旧数据），**授权没有跟上**。API 实际以
`futures_runtime` 连库，该角色在这几张表上只有 select/insert（缓存表多一个 update），
没有 delete：

- 清缓存 → 每次上游取数都记 `permission denied for table spread_provider_cache`
- 限制已存序列的保留量 → 在**刚写完该序列的同一个事务里**失败，就是
  `free-spread/query`（焦煤 09-01）那个 500，连续两次部署因它回滚

`spread_provider_observations` 与 `spread_window_segments` 由 `on delete cascade`
牵连——级联删除同样是对被引用表的删除，同样需要该表的权限，只授权父表会把同一个
错误往下推一张表。

修复：迁移 `202608100005` 补齐四张表的 delete 授权，并在迁移内断言授权确已生效
（静默无操作的迁移会原样留下它本该修掉的故障）。

**为什么拖了这么久**：`futures_app` 是超级用户，任何权限检查都通过，所以"没写的授权"
和"写了的授权"在它身上看起来完全一样。之前我确实猜过 DELETE 权限，但拿它去验，
于是排除掉了正确答案。证据其实一直在部署失败时抓取的 `failure-service-logs.txt` 里，
写着 `permission denied`——**我之前没去看那个文件**。

已加静态测试 `every_table_this_module_deletes_from_is_granted_delete_to_the_runtime_role`：
代码里 `delete from spread_*` 的每一张表，都必须在迁移里对 `futures_runtime` 有 delete
授权，否则测试失败。

#### `price_multiplier` 种子没落库的根因（2026-08-10 查实）

`202608100003` 加了列、断言"种子已落库"、迁移记录在册、部署成功——**但八个品种全是 NULL**。

根因是我在那条迁移的注释里写了一句**没有核实的话**：「migration role has BYPASSRLS」。
实际 `futures_migrator` 的 `rolsuper=f, rolbypassrls=f`，而 `instruments` 开着
FORCE RLS，策略是 `workspace_id = app.current_workspace_id()`。迁移没设工作区上下文，
于是 UPDATE 匹配 0 行。

**更要命的是那条断言本身也瞎了**：它 `select count(*)` 读的是同一张被 RLS 挡住的表，
数出 present=0、seeded=0，两者相等，于是通过。**读不到自己要断言的行的断言，不是断言。**

修复 `202608100006`：照 `202608030002` 已有的写法按工作区循环 `set_config` 设上下文再写。

**断言改成校验机制而不是数据量**：设完上下文立刻断言
`app.current_workspace_id() = 目标工作区`——当初坏的正是这一环。
（第一版我写成"跨所有工作区一个都没种到就报错"，被 CI 打回：全新空库本来就没有
instruments，那不是故障。数据量做不了判据，机制可以。）
另加一条：鸡蛋必须是 10（不是 5 吨的合约单位），在循环内逐工作区检查。

**教训与 DELETE 权限那次完全同类**：对角色权限的假设不核实就写进注释，然后照着假设
设计校验。两次都是这样。

#### 开高低量额一直没落库（2026-08-10 单日验收时发现）

`19b5d97` 加了迁移（`market_prices` 的 open/high/low 三列）、也让采集器开始输出
开高低量额，**但写库的那一步没改**。列建好了、数据也送到了，投影只写 close 和
settlement，其余静默丢弃。导入全程报成功。

实测：全库 14,429 行，**0 行有开盘价**；`volume` / `turnover` 连列都没有。
这直接砍掉两件事——建仓过程的 K 线图就是这个区间，`price_multiplier` 的交叉校验
（成交额 ÷（成交量 × 结算价））也依赖它。

**这条链路一共断了四处**（第一次修完再验，才发现前面还断着）：

| 环节 | 状态 |
| --- | --- |
| 迁移建列（开/高/低） | 做了 |
| 采集器输出五个字段 | 做了 |
| **领域层 `MARKET_DATASET_FIELDS` 声明** | **没做——真正的闸门，CSV 送到了在入口被按模板丢弃** |
| **投影写库** | 没做 |
| 量/额的列 | 根本没有 |

修复：
- `rust/crates/domain/src/import.rs` 的 `MARKET_DATASET_FIELDS` 补上
  开盘价/最高价/最低价/成交量/成交额
- `202608100007` 给 `market_prices` 加 `volume` / `turnover` 两列（numeric，
  十三年成交额求和会溢出 bigint）
- 投影的 insert 与 update 两条路径都补上开、高、低、量、额
- 测试 `the_market_projection_writes_every_value_column_the_table_carries`：
  投影的 insert 必须写、update 必须刷新每一个价格类列，少一个就红
- 测试 `test_the_collector_and_the_platform_agree_on_every_column`：**跨语言契约**，
  采集器 `DATASET_FIELDS` 与领域层 `*_DATASET_FIELDS` 必须逐字段双向相等。
  两边分处两种语言两个 crate，此前没有任何东西把它们连起来，这就是字段被静默
  丢弃的根本原因

**这是今天第三次同类问题**：改动只做了一半（迁移建了列 / 注释假设了权限 /
断言读的是被挡住的表），而"部署成功"和"导入成功"都不会告诉你少了什么。
真正发现它的是拿一天的数据去和交易所原文件逐字段对。

#### 一条被推翻的中间结论（留档）

一度判定「`requests` 把逗号编码成 `%2C` 导致 K 线接口断连」。**该结论错误**：
以 2 秒间隔交替 A/B 三轮，`%2C` 成功 2/3、字面逗号成功 3/3——端点两种都接受，
失败源自节流。当初的三次 `%2C` 失败是背靠背发出的，与限速混淆了。
代码里仍以字面逗号发送，理由只是「与上游自己的请求形状一致」，不是修复。


### `DEC-043` 东方财富席位聚合源接入

状态：已确认（2026-08-09，运营者授权）。

- 背景与实证：`DEC-041` 当初选新浪不选东方财富，理由是东方财富 `futures_hist_em`
  缺少结算价，无法承担行情数据。该理由**只对行情成立，对席位不成立**。2026-08-09
  实证东方财富数据中心报表 `RPT_FUTU_DAILYPOSITION` 同时覆盖 SHFE、DCE、CZCE、CFFEX、
  GFEX、INE 六个市场的成员级成交持仓龙虎榜，单日约 9939 行（`TYPE="0"`），且当日即可
  取到当期交易日数据。
- **接口更正**：早期交接文档记录的
  `qhhqzl.eastmoney.com/marketFutuWeb/dragonAndTigerInfo/getLongAndShortPosition`
  是错的，该地址对任何参数组合都返回 `code:40000`。实际在用的是
  `datacenter-web.eastmoney.com/api/data/v1/get`，报表名 `RPT_FUTU_DAILYPOSITION`，
  过滤条件 `(TRADE_DATE='YYYY-MM-DD')(TYPE="0")`，`SECURITY_CODE` 大写，`pageSize`
  被服务端硬性截到 500。
- 定位：**只服务席位（`seat_positions_v1`），永远排在官方源之后**。它不提供结算价，
  也不提供合约目录，因此不得承担 `daily_market_prices_v1`、`futures_catalog_v1`、
  `trading_calendar_v1` 任何一项——采集器对此有显式拒绝。
- 顺序：官方 → 东方财富。官方在应答时永远不会被抢先；本决策只是把原先“官方失败即
  整块席位失败”的情形，变成还有一次尝试。适用于全部五家交易所。
  （原文的中间一档「（仅 DCE）新浪」已按 `DEC-045` 移除。大商所的席位现在直接以东财
  为主源——不是兜底——因为它已经没有会应答的官方源，走兜底路径会先记一次并未发生的
  失败尝试。）
- 连接方式：AKShare 1.18.81 **没有**任何东方财富期货席位函数，因此该源不经 AKShare，
  由采集器自有 HTTP 客户端直连，并受与 AKShare 路径完全相同的域名白名单与 DNS 围栏
  约束（`official_requests_only`）。为不谎报取数方式，其 `connector_code` 独立为
  `eastmoney_seats_v1`，不复用 `akshare_v1`；迁移 `202608090002` 放行该值。
- 如实标源：`data_sources.code = eastmoney_seats_fallback`，类型 `aggregator_public`，
  授权状态 `whitelisted_exception`，域名白名单仅 `datacenter-web.eastmoney.com`，
  优先级 40（低于新浪 50，低于官方 100）。落库批次与 `seat_positions.source_id`
  必须指向该源，不得标注为交易所官方。
- INE（上期能源，`069001016`）在该报表中存在，但**不属于本平台编目的五家交易所**，
  一律排除；否则会凭空造出任何目录数据集都未声明过的合约。
- 完整性：报表分页若超出页预算，整日判为失败而不是提交被截断的席位表。

### `DEC-042` 三禾只读 API 接入

状态：已确认（2026-08-05，用户授权；具体实施仍须先确认 Phase 5 Planner 契约）。

- `sanheshuju.com` 加入数据源与出口白名单，数据源类型固定为 `aggregator`，UI
  如实显示“数据来源：三禾数据”。本决策只恢复无需登录的只读 API，不恢复已废止的
  Chromium、Playwright、noVNC、登录会话或凭据基础设施。
- Phase 5 首期只允许服务端 `POST` 调用
  `/ajax/arbitrage_varieties.php`、`/ajax/all_varieties.php` 和
  `/ajax/variety_contracts.php`。前端不得直连。
- **席位持仓接口：2026-08-12 用户授权采集，同日采完即停。** 原文写“不在授权范围，
  留待 Phase 7 另议”，运营者当天直接指示采八家信号席位在八个品种上的全部历史持仓，
  以支撑趋势跟随策略；采集在同日按下述结论停止，本条据实修订，不再与实现相矛盾。
  停止的依据是数据本身，不是限制：
  - 三禾在库里的 50.7 万行**全部属于 JD/JM/LH**。AU/AG/AP/FG/SA 一行都没有——那五个
    有交易所官方全史（黄金回溯至 2008-01-10，白银与玻璃 2012，苹果 2017，纯碱 2019）。
  - 大商所那一段已经采满：现有 725 个交易日，2023-08-11 至 2026-08-10，而 2023-08-11
    就是三禾自身数据的起点。继续采不会往前扩历史。
  - “覆盖全部会员”的诉求已经满足：三禾在每个（合约, 日, 榜）上给出 21–27 个会员，
    而交易所官方严格封顶 20，多出来的正是它对掉榜日的反推。
  - 它的增减不如官方干净，且缺口可以自己补——见 `DEC-048`。
  今后如需重开，按本决策的限频、缓存与稳定失败要求执行，并先更新本条。
- Rust API 新增 `sanhe` 适配器并通过 `SpreadSeriesProvider` 应用端口使用。该端口
  同时提供 `sanhe` 首发实现和 `self_hosted` 自有行情实现，保证上游变化或自有回填
  恢复后可替换 provider，而不改变页面主查询契约。
- 所有实际三禾请求之间至少间隔 1.5 秒；同端点、同规范化参数在
  `Asia/Shanghai` 同一业务日期只取一次并持久缓存，缓存记录取数时间。并发 cache miss
  必须跨 API 实例串行化，不能形成惊群。
- HTTP 200、业务码 `code:0` 且 data/序列为空是合法空结果，必须与网络、HTTP、
  业务码和字段契约错误分开。适配器失败映射为稳定平台错误，不转发上游正文；cache
  miss 只尝试一次，不自动重试轰炸，也不以过期缓存伪装当日结果。
- 三禾历史接口只返回已计算差值和两腿实际合约代码，没有原始腿价格。Phase 5A 必须
  按每点 `from_code`/`to_code` 对照本库合约交割月，使用版本化“散户可交易窗口”规则
  截取并拼接；季节图和月度矩阵由截取后序列在平台服务端重算。三禾
  `year_data`、`stat_ret`、`stat_rate` 仅可用于受控调试对照，不作为用户统计真值。
- 默认窗口起点为两腿均有有效数据的首日；止点为较早交割腿“交割月前一月最后一个
  交易日”，并允许按品种发布版本化覆盖规则。算法与交易日历版本、段界、排除数量和
  取数时间必须可追溯，并以跨年、晚上市和规则覆盖单测固定。
- 三禾接口没有公开版本或稳定性承诺。字段、鉴权或访问限制变化时必须稳定失败并停止
  扩大请求，不得临时加凭证、提高频率、扩大端点或绕过限制。

影响与优先级：本决策是 `DEC-024`、`DEC-025`、`DEC-031` 中“三禾/聚合源不进入
第一批”对 Phase 5 自由价差的用户授权特例。上述决策对 Phase 4 五交易所 akshare
采集仍完全有效；`DEC-041` 的 DCE fallback 也不受影响。详细数据、API、页面和验收
契约见 `docs/phases/PHASE_05_SPREAD_ANALYTICS.md`。

### `DEC-048` 掉榜持仓自行反推，大商所以三禾在榜日的增减为基

状态：已确认（2026-08-12，实证后由用户拍板）。

- 交易所只公布每个榜的前 20 名。会员掉出榜单的那天官方文件里没有他这一行，
  **那是“不知道”，不是“零”**。数据层、成本引擎、页面三处一律以 NULL 表达未知，
  口径见 `docs/SEAT_AND_SPREAD_REQUIREMENTS.md` 第 4A 节。
- 掉榜前一日可以反推且只能反推一天：`昨日真实持仓 = 今日持仓 − 今日增减`。
  该恒等式在官方数据 267,373 个相邻交易日样本上 100.000% 成立、零例外。
  推不出来的日子**不写行**。
- **大商所以三禾在榜日的增减为基。** 大商所没有官方那条路（`DEC-041`，WAF 全局拦截），
  三禾是唯一有历史的源。原先一律排除三禾，理由是它的增减是“清零差分”；实测表明
  那只出现在它自己回写过的行上，指纹为 `增减 = −前一行持仓`，可精确识别：
  33.3 万个相邻交易日样本中，恒等式成立 327,675、带指纹且失败 5,317、无指纹且失败 9。
  排除指纹行后复现率 99.997%，与官方源同一水准；而真正要用的“回榜行”那一侧
  10,722 个样本中零指纹。
- 带指纹的行由 `fix-sanhe-fabricated-changes.sql` 修成真实增减
  （真实增减 = 今日持仓 − 昨日持仓，两值均在库且已印证，是算术不是造数）；
  持仓仍为 0 的由 `fix-sanhe-fabricated-zeros.sql` 处理。两者都在日更管线里，
  因为三禾每天仍按同样手法写入新行。
- 反推行标 `source='reboard_inferred'`，名次与增减留空，且不进品种汇总——一份
  “有时含反推、有时不含”的合计会凭空造出 ΔNet 跳变，对趋势跟随比稳定地少算更糟。

影响与优先级：本决策取代 `DEC-042` 中“席位接口不在授权范围”所隐含的“大商所席位
只能靠三禾直采”的做法。缺口由自建反推补齐，覆盖全部会员而非八家，且推不出来的
日子如实留空。

### `DEC-049` 摘除导入通道，日更改走直灌

状态：已确认（2026-08-13，由用户拍板）。

- **背景。** 导入中心是给「上传文件让 AI 分析」建的。AI 分析功能后来取消，
  用户从未手工导入过数据；而每天的自动采集一直被迫走这条为「人工上传需要预览
  和回滚」设计的七层流水线：上传 → 暂存 → 逐行校验 → 冲突检测 → 人工确认 →
  血缘 → canonical → 投影 → 宽表。
- **代价是实测的。** 生产上暂存行 1448 MB、变更记录 832 MB、校验错误 66 MB、
  血缘 292 MB——中间产物和最终业务数据一样大，而且每天都在长；27 张表里有 9 张
  从建库至今一行数据都没有过。席位一次采集要往暂存表写 665,940 行，其中
  CFFEX/GFEX 占 99,044 行而在宽表里产出 **0 行**（本平台不跟踪其品种）。
- **决定。** 通道整体删除：27 张表、后端 16,000 余行实现、`worker` 服务与镜像、
  前端导入中心。采集全部改走直灌（`deploy/collector/load-*-direct.sql`）。
- **直灌本来就在生产跑。** `run-official-seats.sh` 走的就是这条路，且它的数据
  **比通道的更全**——带增减量与持仓量两列，通道那条路没有。这次是删冗余，
  不是造新路。
- **保留 `data_sources`。** 自由价差的原始序列（`spread_provider_series`）引用着它；
  它现在的角色只是「来源目录」，与通道无关。
- **退路。** 不是回滚脚本，是数据本身：执行前新机器做过完整备份（214 MB /
  706 条目，已验可读），老机器仍在运行且保有全部历史。
- **验收改写。** `rust/tests/phase_4a_e2e.sh` 从 763 行重写为 179 行，旧断言
  验的是通道本身，随对象一起失效。新版每一步都比对**前后行数**而非退出码：
  首次试跑时五个 CSV 全报「装载成功」而库里一行没多——`\copy` 是 psql 元命令，
  不做变量插值，出错也不中断执行，最后 commit 了一个空事务。

### `DEC-050` 老机退役，退路由异地备份承担

状态：已确认（2026-08-13 迁移并退役；2026-08-15 用户已在 Linode 面板删除实例）。

- **决定。** 生产从 `futures`（172.238.11.174）迁到 `qh`（172.238.18.206，
  `shejimao.trade`）。老机停容器、停 runner、cron 移入 `/root/retired-cron`、关机，
  实例已删除。
- **退路改变了形态，必须说清。** 迁移当日的退路是「老机还在、数据还在」；
  实例删除之后，退路只剩两条：①每日整库 dump 推到 `ssp`
  （`/root/futures-db-backup/latest.dump`，只留最新一份，先 `pg_restore --list`
  验可读再原子覆盖，脚本 `deploy/collector/offsite-db-backup.sh`，
  cron 每日 15:40 UTC）；②部署链自身的回滚（每次部署前在 qh 本机备份）。
  **不再有一台随时可切的热备机器。**
- **首次自动执行已验证**：2026-08-14T15:40Z，234,407,468 字节，落在 ssp，目录内
  只有 `latest.dump` 一个文件。
- **构建改用 GitHub 托管 runner**（仓库 2026-08-13 转公开），不再依赖自建 runner。

### `DEC-051` 机构资金信号组移除国投期货，改为七家

状态：已确认（2026-08-14，由用户拍板，理由是交易理念而非统计）。

- **理由是用户的交易判断，不是回测结论。** 用户原话：*"国投是浮盈加仓，
  这种方式非常不好"*。浮盈加仓的席位，其增仓动作与「看对方向」不是同一件事，
  混进聪明钱组会污染全局判断。
- **不是只删一个名字。** 组内席位数从 8 变 7，θ 阈值与权重都依赖组的规模，
  必须重算——用户明确指出过这一点（*"你不要偷懒，去掉国投之后，要重新跑权重
  还有对应的策略"*）。重调参见 `research/REPORT_RETUNE7_v1.md`。
- **只做过一个方向的试探并已否决**：曾试「进场保留国投、出场剔除」，
  两种都跑过，无改进，不采用。
- **基准也随之改写**：后续所有对照实验的基准是「七家 + 原参数」，
  不再与八家比——用户指出与八家对比没有意义。

### `DEC-052` 消退天数由 10 改为 7

状态：已确认（2026-08-14，由用户拍板）。

- **理由。** 用户判断*"市场的流动性强"*，信号衰减应更快。
- **数据是分歧的，如实记录。** 全样本上 7 日与 8 日均优于 10 日；但逐年看，
  10 日在若干年份更好。用户看过逐年明细后仍选 7。这是**判断**，不是最优化结果，
  不要在后续实验里把它当成已证明的结论。
- **一个容易读错的连带效应**：改出场规则会改变**成交笔数**。引擎对同一品种
  同时只持一仓（busy 闸），出场早了才腾得出仓位接下一个信号——AU 2024 年有
  63 个信号日，10 日配置只成交 1 笔。看回测差异时先看笔数。
- 六轮系统性寻优（645 格出场搜索、42 格参数重调、315 格权重规格等）**全部
  返回负结果**，记在 `research/PITFALLS.md`。现行规则已达该信息集上限。

### `DEC-053` 席位成本统一为逐合约口径，并以对拍守卫防双实现分叉

状态：已确认（2026-08-14）。

- **背景。** 「机构成本」这一个概念有两份独立实现：Rust
  `domain/src/seat_cost.rs`（面板建仓过程）与 Python `engine/smart_money.py`
  （信号引擎的买入区间）。2026-08-14 发现两者长期不一致：东证 2026-07-15 引擎
  算出 960.40，而它两个实际合约都在 900 附近。
- **根因。** 引擎那版按**品种汇总**推成本、用主力合约结算价；移仓时（平旧约 +
  开新约）只看到净仓大幅波动，而算法是「加仓按结算价加权、减仓不改均价」，
  加仓腿把成本拉高、减仓腿不修正，反复移仓后系统性偏离。
- **决定。** 引擎改为逐合约推算再加权合并，与 Rust 对齐。另有三处按 Rust 语义
  修正：掉榜日冻结全部状态（不补零）、加仓量取净持仓绝对值增量（不用交易所
  公布的 change）、建仓当日无结算价则成本永久不可知（不硬算）。
- **加守卫。** `ops/cost-parity-check.py` 直接调引擎的 `seat_cost` 与 Rust 端点
  对拍，容差 0.01 元，样本不足 50 组即判失败；已接入 `phase_5a_e2e.sh`，
  每次部署自动跑。**守卫自己不实现算法**——首版就是自己抄了第三份，结果引擎
  修好后守卫仍报同样的分歧。
- **两处有意保留的差异**（守卫已避开，不是 bug）：引擎只记多头成本（买点不该
  由净空席位参与）；`reboard_inferred` 反推行 Rust 保留而引擎的 `clean_seat`
  一律剔除（`DEC-048` 的既定口径）。
- 本项目已在「同一概念多处实现」上栽过两次，第一次是 v51 权重快照与生产引擎
  漂移。见 `research/PITFALLS.md` 第 9 条。

### `DEC-054` 合约汇总按各合约净方向分两腿，取数用逐合约榜

状态：已确认（2026-08-15，由用户细化）。口径全文见
`docs/SEAT_AND_SPREAD_REQUIREMENTS.md` 2.3 节。

- **决定。** 建仓过程的合约汇总档，把该席位在各合约的持仓**逐一算完成本再合并**，
  合并时按当日**该合约的净方向**分组：净多的合约合成「多单」、净空的合成
  「空单」，两者相减才是净持仓，均价各按手数加权。不跨方向做加权。
- **不用交易所的品种汇总榜。** 那一行只有一个总手数，推不出成本，也分不出两腿。
  代价是覆盖差：逐合约榜只覆盖他进了某个合约前二十的部分。永安黄金 3316 个
  交易日实测，两者完全相等 2722 天（82%），平均差 55.6 手、最大 2120 手。
  **这一段写在页面上，不藏着。**
- **顺带修掉两个取数缺陷**：①汇总档此前取不到任何结算价（行情被 `contract`
  限死），成本/当日盈亏/累计盈亏三张图全是空的；②取数没过滤 `rank_type`，
  某席位某天只上成交量榜时被算成「多空双零」即平仓，成本清零并算出一次不存在
  的巨额了结——生产 AU/AG 共 166,247 个席位-合约-日属于这种情形。第二条同时
  影响单合约档。


### `DEC-055` 首进场「区间下 70%」条件:当日采纳、次日撤销

状态:已撤销(2026-08-15 采纳,2026-08-16 由用户撤销)。全程留档以防重提。

- **来历。** 用户担心引擎在高位追买,要求首进场压在 60 日高低区间的下 70%。
  回测六个变体后采纳(变体 v2),同批已明确记录:全样本 +314.0%→+307.5%,
  2025-26 +167.5%→+170.5%,差异均为一两笔单的重定时,**噪音级**,采纳依据是
  交易哲学不是数据。
- **撤销。** 用户次日复核后判"确实没什么用",撤销,首进场退回三条件。
- **保留的副产品**:信号位置标注(range_pos/pct_250d,只显示不判定)与
  持仓高位独立提醒(high_zone,rangepos≥0.9 时提醒,非卖出指令)继续存在。
- **回测里真正的结论**(别丢):区间上 1/4 的 52 笔贡献了全样本 +314% 里的
  +272%——**贴高点的中继单是这套策略的利润引擎**,一切"高位否决"类规则
  要么砍半收益(硬禁买 +138.5%),要么是重定时噪音。

### `DEC-056` 重挫共振买点:伦敦金口径,免起点条件,市价执行

状态:已确认(2026-08-16,用户立项并两次纠正口径,终稿=回测变体 c15L_au)。

- **规则。** 基准=**伦敦金美元价**(用户统计表 `黄金跌幅回撤2026_8_6.xlsx`
  的原口径),日内高/低 ±10% 之字形分轮;轮内自轮高回撤(按日低)触及 15%
  置位并**粘滞**到低点回升 10% 开启新轮。重挫态里出现席位共振(分数过门槛)
  即视为新一轮启动:免"贴低点/低仓"起点条件,**次日开盘市价买入,不挂
  机构成本区间**。仅黄金;不受跑路压制窗拦截;止损/消退照旧。
- **用户两次纠正口径,均被数据证实,按序记录**:
  1. 250 日滚动高点 → 本轮高点:固定窗口会把 1246 之后一整年标成重挫态;
     分轮口径日级 t 从 2.95 跳到 7.79。
  2. 人民币沪金 → 伦敦金美元:汇率扭曲回撤深度——人民币口径把 2021 标出
     69 个假重挫日(美元那轮只跌 14.96%,差一根头发不到 15%),回放里 2021
     那串 -4% 止损全是汇率假信号;又漏掉 2016-2018 与 2022 的真实轮次。
- **为什么改执行方式是核心**:2026-02-02 那笔三条件信号全过,却因区间挂单
  (限价 1033.25)十日未回踩而"未回踩放弃",错过 1050→1246 的 V 型反转。
  重挫后的 V 型底不给回踩机会,区间挂单在这种日子结构性失效。回测证明
  "仅加新进场路"零增量——现有信号路已覆盖重挫共振日,缺的只是执行方式。
- **证据**(全样本 2015-2026):AU +138.6%→+146.2%,重挫单 12 笔胜率 91.7%
  零止损,逐轮对上用户表中 ≥15% 的轮次(2015/2016/2018/2022/2026),新增交易
  仅 2026-02-02(+7.65%);日级:重挫态且共振的日子后 20 日胜率 73.3%
  (t=6.45,N=120)。**仅黄金**:白银同类日级 t=-3.49,显著为负。
- **工程纪律**(实抓的三个坑):日线 OHLC 分不出当天高低先后,之字形反弹
  确认只用昨日为止的低点,当天不许自证(伪序 bug 曾让 02-03/03-24 漏判);
  沪金信号在北京晚间算而当天美盘未收,一律用美盘前一日;数据合并**重叠区以
  伦敦基线为准**,COMEX 只往后延(两源日内高低略有出入,2018 轮 15.07% 贴线,
  换源会翻转历史标签)。
- **数据链**:基线 `engine/data/london_xau.csv`(gitignore 例外强制入库,
  随发布包装到生产);增量 GC=F 自动更新缓存 `gold_usd.csv`;全部来源不可用
  时重挫判定自动关闭,引擎不死。

### `DEC-057` 席位海选收官:七家为尽头,国贸/中泰永久排除

状态:已确认(2026-08-15/16,用户拍板)。

- **背景。** 2013-2026 全席位海选(200+ 家、金银双市场、事件后 20 日收益
  统一口径,含前后半段稳定性拆分)。
- **决定。** 现有数据与标准(金银双栖+长历史穿越周期+前后半段稳定+当前
  仍活跃)下,信号组七家就是尽头;**国贸、中泰永久排除,不再讨论**。
- **排除依据=「当前持仓照妖镜」,并入册为今后筛席位的必查维度**:两家 2026-08-14
  最新持仓为逆势小空单(国贸金净空 700 手、中泰金净空 1013 手,对比中信两万手),
  小账本+与大趋势反向;事件 t 值只测加仓时机,账本可以又小又反向。
- **落选归档**:中金(双栖合格但 2023 后零事件=哑火,观察);申万(后半段
  翻身但已被四维筛砍,复议权在用户);物产中大/恒力/中泰系年轻面孔(单时代
  t=5 级,选择偏差标准形状,观察不进);永安/浙商(无预测力,盈亏大=重仓吃
  β——永安金 100% 天数净多躺赚 β 20 亿,拆解已实证)。
- **提升方向不是再挖席位**:单品种纯多头体系的席位+价格信息集上限已测尽
  (`research/PITFALLS.md`),空间在品种分散与新信息维度。

### `DEC-058` 中继趋势态闸:收盘在 MA200 下方不做中继再进场

状态:已确认(2026-08-16,用户发起回测并拍板采纳)。

- **来历。** 用户问"中继规则在 2006-2011 是否有用"。先澄清窗口:沪金
  2008-01-09 上市,2006-2007 无标的;库内 2008 年起行情与席位数据完整
  (AU 席位 120 万行,change 自首日即有)。把引擎 `replay_start` 提前到
  2008 做中继开/关全量对照:
  - **2008-2010 零信号**(七席位名单是 2013+ 时代海选的,当年榜上只有
    海通/国泰活跃——此段回测存在名单存活偏差,方向可信,精度勿当真);
  - **2011 年中继为净拖累**:中继 8 笔合计 -4.1%(9-12 月金价 1920 见顶
    崩盘段四笔连续 -4% 止损接飞刀),不开中继反而多赚 2.4 个点;
  - **逐年拆分的分水岭在 2019**:2011-2018 八年中继合计 -11.5%(年年拖累
    或打平),2019-2026 中继合计 +108.4%。中继的本质是"卖出后在趋势里
    接回",只在结构性牛市里有肉;同时对照证实牛市不能没有中继
    (2019-26 关中继 +55.2% 对开中继 +138.2%,差 83 个点;白银更极端,
    2015 起关中继为 **-27.2%** 对开 +175.4%)。
- **决定。** 中继再进场加趋势态闸:**收盘价在 200 日均线下方时不做中继**。
  仅闸中继;首进场三条件、重挫共振买点均不受影响。引擎参数
  `relay_trend_ma: 200`(`engine/smart_money.py`)。
- **四方案对照(预先声明一次跑完,未扫参数)**:
  | 方案 | AU 11-18 段中继 | AU 19-26 段合计 | AU 全期 | AG 2015 起 |
  | --- | --- | --- | --- | --- |
  | 中继开(原状) | -11.5% | +138.2% | +150.7% | +175.4% |
  | 中继关 | 0 | +55.2% | +68.4% | -27.2% |
  | **T1 =MA200(采纳)** | **-3.0%** | **+140.4%** | **+153.3%** | +168.6% |
  | T2 =MA200 且向上 | -8.9% | +140.4% | +147.4% | +165.4% |
  | T3 =12 月动量 | -6.5% | +135.6% | +143.8% | +168.5% |
  | T4 =席位净仓≥60 分位 | -11.5% | +118.4% | +130.8% | +189.6% |
  T1 是唯一在金银两市场同向改善的方案(跨市场一致=稳健性佐证);T2 又一次
  验证"多余条件是重定时噪音,简单的赢";T4(最贴纯席位初衷)熊市不止血、
  牛市倒扣,且仅 AG 单边好看=不稳健,不采。
- **均线基准用沪金不用伦敦金(用户追问后补测,T1L 变体)**:伦敦金 Close 的
  MA200(严格因果取美盘前一日)全维度更差——11-18 段中继 -3.0%→-9.7%、
  全期 +153.3%→+144.8%。分歧全在汇率:人民币贬值期沪金先上自己的均线
  (2016/2017/2022 的好中继被伦敦金口径拦掉),而汇率分量本就是沪金持仓
  收益的一部分。口径分工:**重挫态量全球恐慌深度用伦敦金(汇率会伪造
  恐慌),趋势态量交易标的自身牛熊用沪金(汇率是收益的一部分)**。
- **与既往决策的关系**:用户曾裁定"纯席位初衷,禁用价格规则"——那是对
  **买点时机**的约束;本闸是中继的**环境态**,与跑路警报压制窗同类。此点
  已向用户点破,拍板采纳。
- **生产口径影响(2015 起,上线时实测)**:AU 93→91 笔、累计 152.2→151.8
  (含持仓浮盈口径),成熟期统计反而改善(55 笔、胜率 63.6%、+134.4);
  AG 切 6 笔中继、累计 -6.8、成熟期 -10.4——白银代价已披露,采纳时知情。
  当前持仓与今日状态零变化。
- **上线纪律**:生产引擎与研究变体 t1_ma200 在 2008 口径下 214 笔
  (含标签)逐字节全等后放行;回测产物 `/tmp/sm-relay/`(生产机,重启即失,
  重建照 run-smart-money.sh 的 \copy 导出 + 变体生成脚本)。
- **回滚**:删 `relay_trend_ma` 参数与 `trend_ok` 两行,`relay_mask` 退回
  三条件式即可,其余互不影响。

### `DEC-059` 中继共振质量闸:触发席位不足两家不做中继;「吃派发段」两案否决

状态:已确认(2026-08-16,用户复盘 2025-09/10 踏空段后拍板)。

- **来历。** 用户复盘 2025 年 9-10 月:09-12 消退出场后金价 832→1014,系统
  空窗一个月,10-15 才以弱信号追高(-2.28%),11-13 再亏 -4%。逐日诊断结论:
  **那五周七席位在清仓式派发**(净持仓 09-23 峰值 126,936 手 → 10-20 仅
  73,496 手,五周 -42%,空头端全程平稳=纯多单兑现;期间价格 +17%)——
  系统没进场是**看对了机构**,错过的是散户接力段;但 10-15/11-13 两笔坏中继
  暴露"单家擦线过门槛"的空子(海通单家 718 手,score 5.28 vs θ4.72)。
- **决定。** 中继再进场加共振质量闸:**5 日窗内有事件的触发席位 < 2 家时
  不做中继**(参数 `relay_min_seats: 2`)。单家不叫共振;首进场不受影响
  (其门槛分数天然需多家叠加)。
- **证据(四变体预先声明一次跑完,基于 DEC-058 后的现行规则,2008 起)**:
  | 方案 | AU 全期 | AU 胜率 | AG 2015 起 | 2025-08~11 案例段 |
  | --- | --- | --- | --- | --- |
  | 现行 | +153.3% | 60.2% | +168.6% | +1.7% |
  | **A1 家数≥2(采纳)** | **+159.4%** | **62.6%** | -2.3 | **+8.0%** |
  | A2 十日流向为负禁中继 | +161.7% | 61.1% | **-29.8** | +7.0% |
  | B1 消退+破 MA20 才出 | +128.2% | 51.0% | -44.3 | +15.9% |
  | B2 消退后转追踪 6% | +153.8% | **43.4%** | -24.7 | +21.2% |
  A2 单市场好看跨市场重伤(与 DEC-058 的 T4 同病),否。
- **「吃派发段」判死,全程留档以防重提**:B1/B2 在 2025 案例段确实吃到
  (+13.91%/+25.21% 的单笔),但全历史被打回——消退出场的真正价值是及时
  了结大量将变小亏的小赢单,"消退后续涨 15%"是少数,"消退后回落"是多数,
  为幸存案例改出场等于用几十笔退化换一笔延长。**派发段收益的领先信息不在
  "机构买什么"而在"机构卖多快",不在纯多头席位信息集内**;要攻它需引入
  新信息维度另行立项(候选:七席净持仓斜率、全市场总持仓变化、基差/仓单),
  不是调出场参数。
- **回滚**:删 `relay_min_seats` 参数与 replay 内两行 `continue` 判定即可。
- **发布记录**:生产=`b67bbf9`(deploy run 31942385458,引擎 md5 `cef33189`
  已手动重跑);新口径 AU 89 笔/中继 32,成熟期 49 笔/胜率 67.3%/+138.9%
  (较 DEC-058 后三项全升),持仓零变化。备份 `/root/signals.json.bak.dec059`。

### `DEC-060` 派发段攻坚总结:卖出端无信号是结构性的;引擎零改动、警示不做

状态:已裁定(2026-08-16 深夜,用户全程逐项复核并两次纠正方向)。本条是
**留档决议**,不改任何规则;价值在于把"这条路走不通"钉死,防止日后重提。

- **来历。** 用户复盘 2025-09-12 消退出场后 832→1014 的踏空段,连环追问:
  聪明钱在干什么→能否吃派发段→警示能否做准。当日完成状态层、规则层、
  出场层、警示层四层共 12 个预声明变体的全量回测(2008 起,金银双市场)。
- **机制事实(用户判读正确,我方"散户接力"说法有误已纠正)**:那五周是
  **空头回补驱动的缩量上涨**——全市场 OI -18.1%、多头榜 -26.7%、空头榜
  -19.1%,无新资金进场;七席位从净仓 93 分位清仓式派发到 4 分位
  (126,936→73,496 手),回补的空头全在七席之外(七席空头端稳定)。
- **状态层(有效但效应量小)**:D1(七席 10 日净减+价格 20 日新高,240 日)
  后 20 日 -0.35% vs 基线 +0.62%,t=-3.02;镜像 ANTI(净增+新高)+1.26%,
  t=+2.54——"新高的成色由机构仓位方向决定"成立。F2(全市场 OI)无增量
  信息(IC -0.016);AG 全部不适用(D1 反而 +1.79%)。**全部效应量都只是
  "把期望压到零",不是"转负"**。
- **规则层(否)**:D1 禁中继回测 +159.4→+163.6 好看,但逐笔 diff 只有
  4 组"同一轮共振推迟一两天进"的重定时(+7.8 来自 2016 一组,2020 组
  反而 -3.9),被切 4 笔里还有一笔 +4.79% 是赚的——按 DEC-055 立的标准
  属噪音级,**不上线**。真正的坏中继(2025 两笔单家擦线)DEC-059 家数闸
  已拦,D1 只剩边角料可打。
- **出场层(六路全灭,消退 7 日即出场就是上限)**:①run_exits 破 MA20
  (2024,负优化);②run_tops 七种席位判顶(2024,全无预测力);③消退+破
  MA20 双确认(全期 -25,胜率 -9);④消退转追踪 6%(胜率崩至 43%);
  ⑤F1 条件追踪 E1(-11,胜率 -10)——**关键败因:2025-09 的消退从第一天
  就是派发型,F1 全程为负,"机构未撤则持有"的前提在最想吃的行情里恰恰
  不成立**;⑥深阈值 E1b(-17)。消退出场的价值=及时了结大量将转跌的小赢
  单,任何"再等等"都是负期望。
- **警示层(否,含用户三要素定义)**:P1(价涨+OI 降+七席降,460 日)
  t=-2.61 显著但只是期望归零,成片检验 11 段中 3 段明显误报(2008-10 崩盘
  反弹/2020-04/2024-07 之后均大涨)、2025 那次成片确认在 10-17 顶部当天
  ——用户"命中率像掷硬币"的批评成立。P2(高位+撤退强度历史最深 20% 分位
  +OI 降)**方向反转**:后 40 日 +3.09%,t=+4.79——机构在高位撤得最猛的
  日子平均后市更强(2024-11 亮灯后 +9%、2025-10-21 亮灯在回调底后 +12.4%)。
- **结构性结论(第七次独立验证)**:这套席位数据的信息量在**买入端**
  (建仓=有信息的主动行为,t=6.2);**卖出端是锁利润的被动行为,不含方向
  观点**——高盛 1-29 砍半仓 2-02 崩完买回、永安躺赚 β、机构派发五周价格
  +17%,同一件事的 N 个面。判顶/派发警示的理论天花板=把期望收益压到零,
  做不出"要跌"。
- **裁定**:①引擎零改动;②派发警示**不做**(用户拍板采纳我方建议:
  "亮灯后平均不涨不跌"的警示只会诱导错误操作);③吃派发段的钱两条路:
  另立纯价格趋势跟随策略与本系统并行(新立项),或接受为系统边界;
  ④P2 反向(高位机构猛减=统计看涨态)备查但不规则化——与现有共振进场
  高度重叠,再挖即过拟合。
- **设施**:全部变体与产物在生产机 `/tmp/sm-relay/`(重启即失;CSV 按
  run-smart-money.sh 的 \copy 重导,变体由 engine_prod_dec059.py 文本替换
  生成,分析脚本 research_*.py 同目录)。
- **修正(同日稍晚)**:本条"出场层六路全灭、消退即上限"的结论被 `DEC-061`
  部分突破——第七路(高盛持有闸)成立。修正后的表述:**价格系与合计流量系
  的出场增强全部无效;单席位存量状态(高盛在场)是唯一有效的出场增强**,
  且仅在高盛持仓数据可用的年代(2023-08 起)可检验。其余结论不变。

### `DEC-061` 高盛持有闸:消退触发时高盛净多 >2000 手不卖出(仅黄金)

状态:已确认(2026-08-16 深夜,用户提出规则与阈值并两次纠正数据口径)。

- **来历。** 用户查建仓过程页发现高盛 2025-10-16 满仓 2,770 手(成本
  818.31)、10-17 顶部当天翻净空 947 手,完美逃顶;提出"高盛持仓大于两千手
  就没必要卖出"。此前 DEC-060 六路出场增强全灭,但此规则与它们本质不同:
  不是拿价格或合计流量赌"再等等",而是**换持有信号源**——消退跟的是流量
  (七家不再加),此闸跟的是存量(海选 t=6.2 的最强单席位还押着 2000+ 手)。
  全程纯席位框架。
- **规则。** 仅黄金:消退满 7 日触发时,若高盛净多(昨日盘后,多头榜合计
  −空头榜合计)> 2,000 手,不卖出继续持有;此后高盛 ≤2,000、掉榜或翻空的
  次日开盘恢复消退卖出;硬止损照旧盘中有效;重挫共振、首进场、中继判定
  均不受影响。参数 `hold_seat / hold_seat_min / hold_markets`。
- **掉榜口径(用户裁定)**:掉榜=持仓低于当日该合约第 20 名门槛,按
  「低于阈值」保守判定为不在场(高盛掉榜实测门槛 2,334/2,055,恰在阈值
  附近;1-29/1-30 案例:掉榜判不在场恰好正确)。对照口径"沿用前值"回测
  差 24 个点(会拿着 3,457 的旧数字在顶部多扛数日)。数据用 clean_seat 后
  的官方行,反推行(reboard_inferred)一律不参与判定——**查席位数据必须按
  source 过滤,反推行只能用于展示,不能进规则**(本轮实抓的教训:把高盛
  自己的反推行当榜单门槛,曾得出"压线回榜"的循环论证,用户指正)。
- **证据(2008 起全量,DEC-059 基线,预声明口径不扫参数)**:
  | | AU 全期 | 激活窗 2023-08 起 | 胜率 | 最差笔 | AG |
  | --- | --- | --- | --- | --- | --- |
  | 基线 | +159.4% | +103.8%(29 笔) | 63% | -4.0 | +166.3% |
  | DEC-061 | **+179.7%** | **+126.2%**(27 笔) | 60% | -4.0 | +166.3%(零变化) |
  四笔主升段大单:2025-11-24 +2.02→**+32.84**(拿到 2026-01-30,吃满 1246
  主升浪)、2025-01-02 +2.24→+25.66、2025-08-28 +5.95→+21.91(出场
  10-16=高盛翻空前夜)、2023-11-23 组 +21.4。代价:胜率 -3 个点、2025-04/05
  高位两笔 -4% 止损(高盛在场陪跌,硬止损兜底)。
- **边界(采纳时知情)**:①激活窗仅三年——高盛 2023-08 前只上成交量榜,
  规则此前自动不激活(退回消退),熊市表现不可检验;②阈值 2,000 来自用户
  案例读数(2,770 满仓→947 翻空),未扫敏感性,恰落在高盛满仓(2,000-3,500)
  与撤退(<1,500)的自然分界;③大改善集中于 2025 牛市年;④高盛若哑火
  (长期不上持仓榜)规则自动休眠,不带病运行;⑤AG 天然不适用(高盛金多
  银空比价角色,不限市场时 AG -46.8)。
- **上线纪律**:与研究变体 g1c_auonly 在 2008 口径 198 笔逐字节全等后放行
  (md5 9768689e)。
- **掉榜日展示口径(用户拍板,已上线取代原「<门槛」显示方案)**:掉榜且
  反推不出的日子按 **0** 计入展示曲线(能反推的用反推值),折线不留缺口;
  掉榜底色与小窗「按 0 计入(实际低于当日榜单门槛)」说明保留;**成本与
  盈亏链仍走三态口径,0 不进成本引擎**。两处配套修复:①前端 netSeries
  null→0(`3830e9e`);②汇总档日期轴补轴(`157a8d4`)——席位行驱动的轴在
  「全部合约同日掉榜」时整天蒸发,K 线与折线一起断(高盛 2026-01-29 实例,
  用户发现),已改为以合成行情日历为外轴补回缺失日(补行持仓/盈亏全 None、
  累计沿用前值、cost_unknown_reason=seat_off_the_board)。
- **回滚**:删三个 hold_* 参数、__init__ 的 hold_seat_on 块、出场判定退回
  单条件式即可。

### `DEC-062` 套利监控:加「新触发」标记与「历史回归率」,并改掉暗示回归的读法

状态:已确认(2026-08-17,用户看完极值事件研究后指示落地这两项)。

- **来历。** 用户提出「今年鸡蛋和焦煤都是历史级别的价差,3% 触发时有多少套利
  席位进场」。按监控脚本的口径重算了全部历史(监控表本身只回算到 2026-04-14),
  做事件研究,得到三条与页面现有读法直接冲突的结论:
  1. **极值处并没有套利资金涌入。** 触发日席位的同步反向建仓活跃度只有平常日的
     1.14~1.16 倍(2026 年口径),已确认的套利席位只占进场量的 21~24%。
  2. **价差在极值处不回归。** 段首日起 20 个交易日,按「该回归的方向」计:鸡蛋
     445 段里 200 段(45%)朝回归走、均值 −22 点;焦煤 68 段里只有 20 段
     (**29%**)、均值 −19 点。这条只用价格数据,不依赖任何席位假设。
  3. **阈值在单边突破的行情里失去筛选力。** 焦煤 2026 年有 **64.2%** 的交易日都在
     3% 触发——价差持续创新低,滚动区间天天被刷新;而连续触发段的中位长度只有
     3 日(鸡蛋 2 日),说明绝大多数段很短,长段拖着不放才是噪音来源。

- **决策一:段首日标记(`is_new_alert`)。** 表里存**前一交易日的两轨位置**
  (`prev_pair_position` / `prev_years_position`),不存「是不是段首日」。读时对
  前一日套同一个阈值,前一日不触发而今天触发即为新触发。这样任何阈值都能在任何
  一天重判,历史跟着一起变——与 `DEC-047`(监控只存位置不存触发结论)同一条原则。
  界面加「仅新触发」筛选与「新」徽标。
  - 前一日位置整个缺失(该组合第一天、或前一日无快照)时**不打标记**:判不了就
    不标,宁可漏标也不假报。这一条单独有测试钉住——把「不知道」当成「刚触发」,
    页面上会天天冒出假的新触发,而且看不出是假的。

- **决策二:历史回归率(`revert`)。** 每行冗余存该**月份组合模板**的命中数与样本
  数,3% / 5% / 10% 三档 × 低/高两侧共 12 列。口径与取舍:
  - **主体是模板不是具体合约对**(同品种 + 同月份对 + 同年差,如鸡蛋 09-01)。
    JD2609/JD2701 一辈子只有一个生命周期,极值段两三段,算不出有意义的比率。
  - **极值段按当年轨划分**,不用页面报警的合成轨。合成轨要先有历年百分位,而历年
    轨是逐 (组合,日期) 跑一次 `percentile_cont` 算的,推到全历史会从百来次变成
    几万次。**这是工程折中,不是口径上更优**——因此页面必须标注口径,并说明与报警
    口径可能有出入。
  - **段首日起 20 个交易日**看方向。不足 20 日的段自然不计入,统计因此永远落后
    20 个交易日,这是对的:那些段的结果还没发生。
  - **存整数计数而不是比率**:界面上「52 段里 28 段」比孤零零一个 54% 更能让人看
    出样本有多薄;样本为 0 存 NULL 而不是 0,`0/0` 显示成「0% 回归率」是最坏的
    一种错——看着像结论,其实是没有数据。
  - **三档而不是一档**:请求阈值是连续的,只存一档会出现「页面按 3% 报警、底下挂
    着 10% 的回归率」这种对不上的展示。读时取与当次阈值最接近的一档,并把档位原样
    回给界面标明。
  - **配色避开红绿**:回归率是个概率,不是价格方向。全站红涨绿跌是价格语义,借过来
    会被读成「红=看涨」,只用主色与灰色的深浅分强/中/弱。

- **决策三:文案改中性。** 页头加一段「贴到极值不等于会回归」,并把鸡蛋 45% /
  焦煤 29% 的对比直接写在上面。**这不是措辞问题**:按原读法用这个页面会亏钱。

- **上线后的实测**(2026-08-14 快照,43 组):同为低位触发,鸡蛋 JD2611/JD2612 的
  历史回归率是 45/54 = **83%**,JD2609/JD2702 只有 15/37 = **41%**;焦煤
  JM2701/JM2705 高位 21/75 = **28%**。组合之间差一倍以上,而页面此前把它们画成
  同一个红条——这就是加这个字段的全部理由。

- **⚠️ 开发期事故留档。** 做「事务里跑一遍再 rollback」的只读验证时,拼接脚本的
  grep 转义没生效,compute 脚本自带的 `commit;` 没被剥掉,这套 DDL 与一次窗口内
  重算**当场落进了生产库**。后果可控(列可空无默认、当时的生产代码不读新列、日更
  不会失败),但它让迁移在正式部署时必然撞「列已存在」。已把 14 个加列改成
  `add column if not exists`、约束改成 `drop constraint if exists` + `add`,并加
  测试钉住。**规则:迁移必须能在已处于目标状态的库上再跑一遍而不报错**——同类
  教训仓库里已有一次(concurrently 预建索引的迁移),这是第二次。

- **⚠️ 修订(同日,决策二被推翻重做 → 迁移 202608170002)。** 运营者指出回归率算错了:
  「回归率应该按月份计算,比如 JD09 和 01,交易时间是 1.29-8.31…比如 6.1 日 741 报警了,
  后续回归概率要从历年的 6.1-8.31 进行计算…后续的任何一天收盘低于 741 都算回归」。
  原口径两处硬伤:
  1. **固定 20 个交易日与合约剩余寿命无关。** 鸡蛋 09 合约 8 月末就退出散户可交易
     区间,8 月中旬触发时只剩十来个交易日,拿 20 日后的价格去比,比的是已经不可
     交易的东西。
  2. **只看第 20 天那一个点。** 中途回落过又涨回去的按终点算是「没回归」,可套利仓
     在那期间随时能平掉。
  同一个模板(鸡蛋 09-01)从 6-01 起算,原口径 45%,新口径是 **12 年 12 次全都曾跌破
  起点**。差距不是调参能补的,是口径错了。新口径:
  - **可交易窗口照 5A 窗口引擎**(`rust/crates/domain/src/spread_analytics.rs`),不另
    立一套:起点 = 两腿都有数据的第一天;止点 = **先到期**那条腿的散户最后交易日,
    即 `last_weekday_before_delivery`(交割月前月最后一个非周末日)。运营者明确要求
    「改成 5A 窗口那套规则」。
  - **历年按月-日对齐**,一直看到各自窗口止点;**曾经触及**即算回归,不比终点;只用
    **已走完**的年份实例;**不要求**历年该时点也处于极值(运营者定:绑上报警会把样本
    砍到三四年)。
  - **幅度用点数**(运营者定):价差跨零时百分比会爆——2019-08-14 起点 −8 点、回落
    407 点,百分比是 5000%。
  - **不设样本年数门槛**(运营者定):生猪 5 年、纯碱 6 年,设门槛整个品种就没统计了;
    年数原样显示,由人自己判断。
  - **存四个数而不是一个比率**,因为单看回归率会骗人:剩余期一长它就趋近 100%(苹果
    剩 128 个交易日的组合全是 100%,鸡蛋剩 12 天的才拉开到 75~92%);更要命的是
    **JD2612/JD2701 回归率 100% 而「持到期净变化」中位 −166 点,方向是反的**。所以
    hit/n 之外必须并列给 move(最有利幅度)、drift(持到止点净变化,已标准化成正数=
    朝回归走)、days(剩余交易日中位)。
  - 三档 3/5/10 全删:新口径**与阈值无关**,只跟日历位置和方向有关。
  - **上线前与 Python 参考实现逐行对拍**(381 行交集、10 个字段全等)才放行;参考实现
    的口径注释与 SQL 一一对应,日后改任何一处都该两边一起改。
  - 途中另修一处旧瑕疵:`legs` 取数的来源优先级写的是 `like '%\_official'`(要求以
    `_official` 结尾),而大商所的源叫 `dce_official_history`,**匹配不上**,官方数据反
    掉到最后一档。实测影响为零(两种写法跑出的统计逐字节相同),但郑商所能匹配、
    大商所匹配不上这种不对称迟早会在别处咬人,已改成 `like '%official%'`。

- **回滚**:前端去掉徽标/筛选/回归率三块;API 去掉 `is_new_alert`、`revert`
  两个字段与 `revert_pick`;采集侧 SQL 退回上一版(`snap`、`template`、`hist_*`、
  `revert_*` 全删,`pair_stat` 退回两层、delete 窗口去掉 +7);列可留着不删,
  留着不影响任何东西。

### `DEC-063` 套利监控:分层通用规则上线(✓ 合格 + 已拐头)

状态:已确认(2026-08-17,运营者指示"开始做吧,直接部署上线")。

- **来历。** 运营者的核心痛点:「历史性价差大概率回归,但进早了扛不住亏损,回归
  之前被打爆」(2026 鸡蛋 08-02 实例:5 月 1200 进场,6-01 冲 1600,浮亏 400 点);
  随后又指出单品种回测出的「回撤 150 点再进」是定制参数、类似过拟合。由此立的
  验证课题:找一个全品种、全年份通用的规则。

- **关键实证(全部在 qh:/tmp/arb,重启即失,结论落此处)。**
  1. **裸规则不成立**:6 品种 22 模板 265 个报警段(高低两侧),报警即进持到底
     中位 −3.7% 区间;确认式(回撤 5/10/15% 区间)也 −9.8%,三档同烂——
     「逢 3% 极值做回归」本身在全品种上是亏的,与事件研究(鸡蛋 45%/焦煤 29%
     回归率)互证。
  2. **分层后成立(留一法,零未来信息)**:第一层按「其他年份触及率 ≥80% 且持到
     期中位 >0」筛资格 —— 合格 120 段持到底中位 **+28.8%**,被筛掉 145 段
     **−25.5%**;第二层确认式进场在合格段上 +28.8%→**+39.1%**,触回率 92→96%。
     筛后段落横跨全部六品种(玻璃30/焦煤29/鸡蛋24/苹果17/纯碱13/生猪7)。
  3. **通用化的关键**:回撤量无量纲化成「区间宽度的 10%」——鸡蛋 08-02 折 75 点、
     焦煤自动更小;5/10/15% 同向,不靠挑参数。此前的「150 点」实为该组合区间的
     20%,确系定制数,运营者的过拟合质疑成立。

- **实现(存事实,判定读时)。**
  - 迁移 `202608170004`:`pair_pos_hi20/lo20` = 当年轨位置近 20 个交易日的
    max/min。**只存这两个事实**;报警带(0.97/0.03)与回撤量(10 个百分点)是
    API 常量。「自极值回撤区间 10%」≡「位置退 10 个百分点」(报警时价差贴着
    滚动极值,极值即区间端点),所以不必另存极值。
  - API `turn` 字段:hi20 进过 3% 带且当前位置 ≤0.90 → "high"(低位对称);
    20 日窗自带过期。**只看当年轨**(资格统计与回放全在当年轨,口径闭环)。
    未报警但已拐头的行按拐头侧给 revert 统计——拐头行多半已退出报警带,不给
    统计的话恰恰在该看数字的时候一片空白。
  - 前端:「✓ 合格」徽标(`isQualified`:rate≥80% 且 drift>0,主色蓝——不是方向
    也不是警告);「已拐头」徽标(警示橙);拐头行并入「触发中」列表(否则该进场
    时它掉进未触发堆);计数条加 已拐头/✓合格;页头写明三步用法。
  - **门槛全部写死不做旋钮**(80%、drift>0、3% 带、10% 回撤):都没扫参数,故意
    的——给旋钮就是诱导挑参数。

- **诚实的局限**:筛后 120 段高位 82/低位 38 偏科;MAE 中位仍 ~70% 区间——
  资格决定受苦值不值,仓位决定受不受得起(第三层仓位规则不进系统,页面给
  「最有利/持到期」点数供手动预算);20 日窗与 3% 带是事实窗口/基准的选择,
  未扫描;报警带固定 3% 最严档,与页面可调阈值(5% 缺省)不联动,已在 tooltip
  写明。

- **回滚**:前端删两徽标与 fired 并组逻辑;API 删 `turn` 与 `monitor_turn`;
  采集 SQL 删 pair_pos_hi20/lo20 两个窗口列;列留库无害。

- **修订(同日,运营者追加):⚡ 进场标 + 置顶排序。** 「已拐头」最多挂 20 个交易日,
  「处于可进场状态」≠「今天就是进场日」——FG2701/SA2701 实例:08-05 首次拐头
  (收盘 −45),08-14 还挂着标但位置已走到 52%,晚 7 个交易日进价位差 50 点。
  新增 `is_new_turn`(拐头成立且前一日位置还在回撤线另一侧 = 位置今天才穿线),
  素材就是段首日用的 `prev_pair_position`,零迁移零采集改动。**不必看前一日的
  hi20**:band 触碰当天位置必然 ≥0.97 > 0.90,「昨天不成立今天成立」只可能因为
  穿线。界面:⚡ 进场 = is_new_turn × 合格,全页唯一实心蓝,置顶排序,加「仅进场日」
  筛选(每天盘后勾上,列表要么空、要么就是当天该动手的单子);线附近抖动会再亮
  一次(FG-SA 的 08-07),是特性不是 bug——没上车的人得到第二次提示。统计盘后算,
  执行等于次日,与回放口径(信号日收盘)有一日之差,页头已写明。

- **修订二(同日,运营者拍板):⚠ 信号差降级标。** 同一组合近 20 个交易日内第 2 次
  及以后的拐头穿线,读成「拐头质量差」——JM2609/JM2701 八天三次穿线(08-04/06/13),
  期间 08-11 打回区间顶,前两次进场按「创报警后新高离场」都得止损;对照 FG2701/SA2701
  干脆的拐头一次穿线一路走。**次数本身就是筛子**。实现:`turn_crosses_high/low_20`
  两列(穿线旗标的 20 行滚动和,迁移 202608170005)。**例外声明:这两列烙进了
  0.90/0.10/0.97 三个常量**,与「存位置不存结论」相悖——次数是路径的函数,读时从
  单行推不出;常量与 API 的 TURN_* 同值且有意写死,改一处必须同批改另一处。界面:
  ⚠ 信号差 ×N(红描边,不能比 ⚡ 抢眼);排序降级为「干净进场 > 信号差进场 > 其余」。
  **勘误(上线当晚数据核出)**:本条初版把 FG2701/SA2701 写成「一次穿线」是错的——
  它实际也有两次(08-05、08-07,第二次来自贴线抖动 0.906→0.855),按本规则同样挂
  ×2。它与 JM 的真实区别不在次数而在**回抽深度**:JM 两度回到区间顶(1.000)=完整
  重测失败,FG-SA 只是贴线抖动。若嫌此门槛过严,可改成「回到报警带(≥0.97)之后的
  再穿线才计第二次」——该口径下 JM=2(08-04/08-13)、FG-SA=1(不被标),更贴直觉
  但实现要 gaps-and-islands;是否收紧由运营者定,当前版按字面规则(每次 ⚡ 都计)。

### `DEC-064` 总览报告表:回榜日的「昨持仓」用当日增减反推回填

状态:已确认(2026-08-17 晚,运营者看到高盛白银 08-17 回榜后拍板)。

- **来历。** 高盛白银 08-14 掉榜(空仓 2,350 手,差 79 手没进 AG2610 空单榜前 20),
  08-17 以 2,364(+14)压线回榜。报告表按三态口径把「银/昨持仓」画成横杠,运营者
  指出:今天有持仓有增减,昨仓可反推(2364−14=2350),**要写进报告表**;同时确认
  「金/今」那类掉榜横杠维持不变(前向推不出)。
- **规则。** 报告表的「昨持仓」:实际昨行优先;席位整日掉榜、且**当日各腿增减齐全**
  时,用 Σ 方向×(持仓−增减) 反推回填(交易所增减相对会员**全量仓**算,回榜反推的
  既有口径,DEC-048 龙虎榜铁律);任何一腿增减缺失 → 整个反推作废(吸收律),照旧
  横杠。合计行按成员逐一取值(真数或反推)再求和。**含反推成分的格子打「推」字上标**,
  与真数区分。
- **与 DEC-061「反推行不进规则」的边界**:那条禁的是 reboard_inferred 行进引擎规则
  与榜单统计;本条是**展示层**按运营者指令回填,且用的是 API 读时计算(不落库、
  不产生新行),互不冲突。
- **实现**:`load_report_nets` 带出 change 并算 inferred_prev/inferable;
  `report_prev_net`(可测纯函数,4 条测试:回榜反推/缺增减污染/真数压反推/合计混合);
  响应加 `previous_net_inferred`;前端「推」上标。零迁移零采集改动。

### `DEC-065` 推算持仓全面打标 + 建仓过程品种下拉改历史口径

状态:已确认(2026-08-17 深夜,运营者两条指令)。

- **推算持仓打标。** 运营者:「把所有高盛或者其他席位推算的持仓,都打上标记,显示
  那天是推算的持仓,实际未上榜」。回榜反推行(reboard_inferred)此前在各展示面与
  真数无区分。现在四个面全标:
  1. **建仓过程**(合约档+汇总档):`BuildingDayItem.inferred`,小窗加「推算持仓 ·
     实际未上榜(由回榜日增减倒推)」行,图上加**更淡的底色带**(掉榜带 16% 透明度,
     推算带 7%——有数但数是倒推的);
  2. **席位持仓表**:行级「推算·未上榜」标(source=reboard_inferred);
  3. **净持仓页**:`inferred_members` 日级点名,小窗「按反推计入:XX(实际未上榜)」;
  4. **总览报告表**:DEC-064 的「推」上标,已有。
  实现全在读路径(SQL 聚合层 `bool_or(source='reboard_inferred')`),零迁移;
  报告表成本链的 VarietyBuildingRow 复用处 inferred 恒 false(成本链不吃展示标)。
- **建仓过程品种下拉改历史口径**(运营者当日报的 bug:高盛掉出金榜后建仓过程选不了
  黄金)。原口径=「所选日期当天在榜的品种+已选」,席位当天掉榜品种就消失,而建仓
  过程是历史序列。新增 `GET /seats/member-instruments`(该席位历史持有过的全部品种,
  按归一名含全部历史写法,排除反推行),下拉=历史品种 ∪ 当天在榜 ∪ 已选;接口瞬断
  退回旧口径,宁可少列不可空白。
- **附带修正**:同日曾把 inferred 塞错进净持仓 mapper(替换锚点撞到文件里第一处
  同形代码)——编译器拦下;教训:python 批量替换的锚点必须含足够上下文。

### `DEC-066` 席位契约扩列 change:东财增减入库,DCE 掉榜反推续供

状态:已确认(2026-08-17 深夜,运营者拍板「把东财源纳入」)。

- **病根两层。** 表层:infer-offboard 的源范围没含东财;深层:**东财行的增减在库里
  全空(7,020 行全部 NULL)**——`seat_positions_v1` 契约(SEAT_FIELDS)里根本没有
  change 列,东财 API 明明给了(实测 LP_CHANGE/SP_CHANGE/VOLUME_CHANGE 齐全,
  国泰君安 SN2609 +194/−420)在归一化就被丢掉。这是 akshare 席位「契约无 change」
  的同款老坑第二次发作。只改反推脚本是空转,必须先修契约。
- **契约扩列(三处同批,生产方消费方同一提交)**:normalize.py SEAT_FIELDS 加
  `change`(新 `_signed_integer`:增减可负可零,与持仓的拒负校验分开);Rust 域
  `SEAT_DATASET_FIELDS` 同步(契约一致性测试钉着两边);load-seats-direct.sql
  stage 表加列(\copy 按列序对位)、insert 取真值、**upsert 补 change**(晚间第二轮
  采集把早间缺的增减补上;source 在唯一键里,不跨源覆盖)。pivot 透传三个 *_CHANGE。
- **反推纳源**:basis 加 `eastmoney_seats_v1 & DCE`,并加**同日去重层**(三禾停采前
  与东财窗口内并存,不去重 lag 会把同日两行配成假相邻日;官方>东财>三禾,同
  SEAT_SOURCE_RANK)。指纹闸照旧兜底。**东财增减的恒等式检验已完成:635/635 =
  100.000%**(2026-08-17 部署后手动重采当日与 08-14,相邻交易日全样本),与三禾
  (99.997%)/官方同一水准,纳源放行。
- **生效路径**:部署后手动重采当日→东财行 change 补上→infer 重跑→DCE 反推续供;
  历史(08-06~08-14)东财行的 change 可按日重采回填,待运营者要时再做。

### `DEC-067` 交割红线 + 历年 MAE 风险预算(两份交易文档的因子化,第一批)

状态:已确认(2026-08-17 深夜,运营者交来《期货交易体系》《天道金友群盖楼要点》
两份文档并指示"①②直接开工")。

- **因子① 交割红线。**《体系》硬纪律:所有合约交割前 15 个交易日全部清仓(其 IPS
  教训:近月临近交割逻辑变了没止损,错误放大)。留一法验证(120 合格段):剩余
  <15 日持到底中位 **−21.7%**(为正仅 20%)、15~40 日 −32.5%、>40 日 **+54.8%**
  ——合格段全部利润来自剩余 40 天以上,红线方向完全正确且衰减比文档更早开始。
  实现:API `days_left`(距先到期腿散户最后交易日的剩余交易日,周内日近似,口径
  照 5A);前端 ≤15 日挂灰标「临近交割」并**压制 ⚡**(isEntry 第四条件),16~39 日
  虚线「衰减区」只提示不压制。门槛 15/40 一个来自文档一个来自数据分桶,均不做旋钮。
- **因子② 历年 MAE 两档。**《盖楼》猪 11-05 分批法("-1300 首笔/-1600~-1700 补/
  风险 300-400")的数据化:同锚点历年先朝不利方向走的最大幅度,中位=**补仓参考**
  (浮亏到这里是历年常态),最大=**风险预留**(仓位=可承受亏损÷(此数×点值))。
  管线即 revert_* 那条,加 4 列(迁移 202608170006)。
- **⚠️ 盈亏比分级明确否决**:《体系》模块四按 move÷MAE 分档回测,">2.5 重点档"
  实际持到底中位 −0.2% 全场最差(疑与临近交割混杂,恰好反证因子①)——比值不用,
  分母有用。此结论防止日后有人把盈亏比做成筛选器。
  - **2026-08-20 用新构造重测一次,结论不变(见 DEC-098 附录)。**
    新构造是「到第一目标 ÷ 到止损」,两个距离都量在**今天这张图的今天这个价位**上,
    与被否决的那个(两个跨起点的历年中位数)完全不同,**所以值得重测**——
    但结果依然是否决,而且这次弄清了**为什么会看起来像有用的**。
- **文档其余提炼(未立项,记录备选)**:移仓换月标记(近月 OI 迁移)/每组合手工
  产业备注(夏天煤不空、纯碱逼空窗口、12 月焦煤交割量>1 月)/CZCE 仓单库存分位旗
  (逼空风险 regime,非方向信号)/基差-现货数据源(真缺口,需另立项)。

### `DEC-068` 监控历史门槛 60→30 个交易日

状态:已确认(2026-08-18,运营者拍板)。

- **来历**:运营者问生猪 2611/2705 与 2701/2707 为何不在监控。逐层核实:持仓量前 6
  候选与 15 对组合**都在**,卡在「共同历史 ≥60 交易日」——LH2705 上市 58 天差 2 天,
  LH2707 才 14 天。(排查时的干扰项:多源重复行会把共同天数灌水到 67,数天必须
  `distinct trade_date`。)
- **决策**:门槛降到 30。理由:等待成本(新次主力合约两三个月进不了监控)高于年轻
  区间的噪音成本;30 天仍挡得住上市头几周的假极值(不加门时 57% 组合天天报警的
  教训仍在注释里)。位置有效性(pair_pos)与保留门同步改,三处同源。
- **代价照实说**:组合出场头一个月的滚动区间只有 30~60 天样本,位置比成熟组合抖;
  回归率统计不受影响(那套按日历锚点,与此门无关)。
- 生效:LH2705 系组合(58 天)重算后立即出现;LH2701/LH2707 再等 16 个交易日。

### `DEC-069` 监控加手工产业备注(月份模板级);拐头标永远带方向

状态:已确认(2026-08-18,运营者指令「先做4吧」,即 DEC-067 备选清单里的 ④)。

- **动机**:统计层(回归率/MAE/红线)回答「历年数字怎么说」,回答不了「为什么」。
  运营者从直播/盖楼学到的品种级知识——夏天的煤不能做空(安监停产)、纯碱低库存是
  逼空窗口(17 万吨→1500 涨到 3700)、玻纯反套大仓正套小仓、12 月焦煤交割量大于
  1 月——此前只存在于聊天记录。这类知识**无法算法化**(样本一次、来源是人),
  但可以在统计旁边一起呈现:统计说「合格」,备注提醒「但现在是夏天,这是煤」。
- **键是月份模板不是合约对**:`(品种1, 月1, 品种2, 月2)`。产业规律跟月份走,
  JD2609−JD2701 与 JD2709−JD2801 共享同一条「09-01」的知识,写一次年年有效。
  跨品种(FG−SA 同月)天然被四元键覆盖。
- **实现**:新表 `spread_template_notes`(RLS + workspace 政策 + futures_runtime
  四权限,长度 1~2000,空即删行),迁移 `202608180001`;读时在监控 API 按合约尾
  两位月份挂到每行;写走 `PUT /monitor/template-note`(权限复用
  ManageOverviewReport——单人面板,与压力位表同一把钥匙)。前端触发/未触发两组
  都显示,整块可点进编辑,弹窗里写明「所有年份的同月组合共用一条」。
- **纯手填,零算法**:表里不进任何引擎或统计计算,只是展示。与 DEC-061「反推行
  不进规则」同一哲学:人的知识标给人看,不混进数字。
- **顺手修的显示缺陷**:已拐头标此前在有报警时省略「·高位/·低位」后缀,而
  LH2611−LH2705 实证出现过「高位报警 + 低位拐头」并存(年轻区间 20 天内从区间底
  穿到顶,低位带触发过、现已回撤 ≥10%),省略后缀会让人把低位拐头读成高位的。
  改为永远带方向。DEC-063 的拐头定义本就双侧独立判,这里只是显示如实。

### `DEC-070` 回撤档分品种 + ⚡只认首次 + 历史信号视图

状态:已确认(2026-08-18,运营者拍板「按你的建议进行改动」「进场信号只首次触发」
「加历史按钮」)。

- **回撤档分品种(修订 DEC-063 的单一 10%)**:运营者质疑"不同品种波动率不同,
  统一 10% 不对",并两问 JM 为何 20%、JD 波动大为何反而 5%。全量回测(47 模板全部
  已完年份,每档 ~350-380 笔,逐年留一样本外检验)+ 复检后定档:
  **JM/AP 20%、JD 5%、FG-SA 跨品种 8%、其余(LH、FG 跨期)10%**。
- **两条机制,一条分水岭**:回撤线画在「位置」刻度上,起作用的是**位置日抖动**
  (价格波动率已被区间宽度除掉):JM 抖动全场最高(中位 6.4pp/日,P90 21pp),
  10% 线仅 1.5 倍日抖动,噪音一天就穿,20%≈3 倍才滤得住(逐笔核查:躲开 −211/−88/
  −228 三笔大亏,多抓 +905 一笔);JD 抖动也高(5.4pp)但它是唯一**早进不受罚**的
  品种(早进组中位 +7.4%,季节备货趋势盖过波动),深回撤对它纯粹让利——分水岭
  不是波动率,是早进受不受罚。AP 全曲线单调偏深但机制非抖动(AP 抖动最低),置信
  中档。LH/FG 相邻档正负翻转、逐年选择不稳,挑了就是过拟合,不动。
  样本外总检验:逐年留一挑参 OOS 中位 +14.7% vs 固定 10% 的 +12.9%,胜率同 53%
  ——改善温和,别期待质变。
- **⚡ 只认首次(推翻 DEC-063 修订里"抖动再亮是特性")**:运营者拍板不要重复触发。
  实现零迁移:前端 isEntry 加 `turn_crosses === 1`——20 日窗内第二次及以后的穿线
  只挂 ⚠ 信号差不亮 ⚡;窗滑过后计数归 1,新一轮照常亮。
- **历史信号视图**:监控 API 加 `history=true`(一次取回全部快照日的行,读时判定
  与单日同一套),前端「历史信号」勾选框——列出历来全部 ⚡ 进场行,新日期在前,
  每行带日期,不必逐日期点选。
- **实现联动**:采集 SQL turn_crosses 的回撤线随档位改(lateral 分档表,与 API
  `turn_retreat()` 一一对应,注释互指;改一处必须同批改另一处并**手动重跑重算**)。
  部署后已重算 window_days=45,覆盖表内全部历史,存量 turn_crosses 已按新档重写。
- **回测方法与脚本**:qh:/tmp/arb2(legs.csv 去重导出 + scan_retreat.py 全档扫描 +
  verify2.py 抖动/早进率/逐笔复检),重启即失,结论落本条。回放口径:生产同款分层
  规则,每实例每侧只取首笔进场,红线压制,留一法资格,幅度=占区间宽度。
- **顺带的大发现(未立项)**:全品种 ~80% 的进场事后位置会回触报警带,X 调深也只能
  把早进率 83%→78%;早进组(除 JD)中位全深亏、非早进组 +108%~+308%。分批纪律
  (首笔轻、回触补仓)对结果的影响比回撤参数大一个量级——与《盖楼》猪 11-05
  分批法互证,已有「补仓参考/风险预留」两个数,执行层面靠人。

### `DEC-071` 历史信号跨年全量:过期实例入库、打灰标、可翻页

状态:已确认(2026-08-18,运营者指令「放所有历史进场信号…做成可以翻页的…
过期了的组合直接标记过期」)。

- **组合圈定扩展(只向过去)**:combo 表在现有「当前挂牌且有量」圈定之后,把每个
  现役模板(品种+月份对+年差)按同口径往回展开成历年实例,**只收已到期的**
  (先到期腿的交割月已开始)。现役侧的持仓量前 6 圈定门原样生效——未到期的年轻
  实例不进来,免得绕过流动性门污染当前页;当前页另有 7 天新鲜度兜底,过期组合的
  老行不会冒出来。过期实例只在大窗口全量重算时产出行,日更 3 天窗内它们无数据,
  零成本。
- **expired 字段**:API 按「最新快照日 vs 组合窗口止点」判(不是墙钟,可复现),
  历史行到期的打「已过期」灰标。
- **翻页**:历史模式一页 20 笔(仿机构资金历史信号列表),切筛选回第一页;
  当前模式不翻页——当天的列表本来就该一眼看全。
- **回填**:window_days=5000 全量重算一次,覆盖全部历年实例的完整生命周期
  (JD/JM 等回到 2013 前后)。历史接口仍是服务端粗筛(is_new_turn+本轮首穿),
  返回量数百笔级。
- **口径提示**:历年实例的资格统计(留一法)与现役同一套读时逻辑;历史 ⚡ 是
  「当时按今天这套规则会怎么标」的回放,不是当时真实报过的信号(当时系统还不存在)。

### `DEC-072` 焦煤组对放宽:一主力 + 一非主力也进监控;会员括号后缀归一

状态:已确认(2026-08-18,运营者指令「焦煤改成一个不是主力合约、一个是主力合约
也组合成套利对」)。

- **焦煤组对**:`main_month` 加 `pair_mode` 列。`both`(玻璃、纯碱)保持原规则
  两腿都必须 1/5/9;**`either`(焦煤)只要有一条腿是主力月即成对**。实现要点:
  either 品种在 `liquid` 层**不**筛月份(在那筛掉就配不出「主力 × 非主力」),
  月份门移到 `combo` 层判;非主力腿仍受 `oi_rank <= 6` 流动性门约束,冷门远月
  进不来。生产试跑实测:**JM 3 对 → 12 对**(新增全部是主力配非主力,如
  JM2609−JM2610、JM2611−JM2701;无双非主力对),FG/SA/AP/JD/LH 一组不变。
- **会员括号后缀归一(引擎侧补齐)**:大商所东财源写「一德期货(代客)」、三禾写
  「一德期货」,同一家两个写法。**Rust 侧本来就免疫**(展示查询一律
  `distinct on (…member_key)`,而 `MEMBER_KEY` 正则剥括号);实测反推行零重复
  (12,883 组)、AU/AG 零括号写法。唯一没剥括号的是 `engine/smart_money.py` 的
  `clean_seat` 去重键,已补(先剥括号再套 alias)。**影响面勘误**:此前一度判断
  「凡跨源合并处都会灌水」,查证后为过头——实际重复只有 DCE 三品种的
  2026-08-06/07 两天,且页面显示全程正确;引擎不碰大商所,这次是防御性修复。
  教训:**临时分析 SQL 必须走与生产同一套归一化**,否则会把自己的写法问题
  误判成生产缺陷。

### `DEC-073` 收盘价 0 = 无成交,取价改用结算价兜底;撤回对 −8020 的错误判定

状态:已确认(2026-08-18,做基差探针时挖出)。

- **缺陷**:郑商所对当天无成交的合约**如实把收盘价写 0**,结算价照常有效。
  采集 SQL 的 `where p.close_price is not null` 只挡 NULL 不挡 0,于是那天的
  价差被算成 `0 − 对手价`。实测 `price_history` 有 9,795 行零收盘价
  (2012-2026,郑商所三品种),污染 `spread_monitor_daily` **79 个组合 200 行**。
- **危害**:假极值会**永久钉死组合区间的下界**——`AP2603−AP2605` 的区间下界被
  拉到 −10,753(真实应为 −1,313),由此产生假的低位报警、假拐头,并污染历年
  百分位区间。
- **修法**:取价改 `coalesce(nullif(close_price, 0), settlement_price)`
  (无成交日交易所本就以结算价为当日代表价),两者都无效才丢弃。取价只在
  `legs` 一处,改这里全链路生效。
- **⚠️ 撤回 DEC-071 里的一个错误判定**:2026-08-18 回填撞 sane 约束时,我查到
  「AP2111−AP2112 历史低点 −8020」并**判定它是真实观测**,据此放宽护栏
  (迁移 202608180002)。查证:2021-11-03 AP2111 收盘价就是 0,前后两天真实价差
  是 −363 与 −102——**−8020 是脏数据,护栏本来是对的,是我把守卫放宽了**。
  **收尾(同日)**:根因修掉后全量重算,`years_position` 实际值域是
  **−2.85 ~ 5.01**,原 ±10 有充足余量——迁移 `202608180004` 已把护栏收回 ±10。
  顺序不能反:先重算再收紧,表里还留旧值时收紧会被 check 当场拒绝。
- **教训入档**:见 `docs/PITFALLS.md` 四(数据口径)与七(判断与流程)。

### `DEC-074` 接入现货价与基差(生意社,经 akshare)

状态:已确认(2026-08-18,运营者「基差数据源绝对价值最高,现在做一下」;
原 DEC-067 备选清单⑥,当时判定为「真缺口」)。

- **先验后纳的探针结论**(与 DEC-066 同一套流程):接口
  `akshare.futures_spot_price(YYYYMMDD)` 稳定,1-2 秒/日,非交易日显式警告;
  字段给全了现货价、近月/主力合约与价、基差、基差率。**期货价与我方库内
  逐笔对拍 26/27 完全一致**——第 27 笔的差异恰好挖出我方的收盘价 0 缺陷
  (`DEC-073`,连带推翻我对 −8020 的错误判定)。历史:FG/JD/JM 到 2018、
  SA 2020、LH 2022(上市即有)。**苹果 AP 全程没有现货报价**,如实缺着。
- **它的增量信息在哪(别用错)**:跨期套利两条腿相对同一个现货,所以
  **近月基差 − 主力基差 ≡ 跨期价差本身,信息重复**。真正的增量是
  **现货价序列**与**基差的绝对水平/历史分位**——「现在期货整体比现货贵或
  便宜到什么程度」,这是期货价格里没有的独立信息,也是《体系》模块一
  「期现基差」的要求。因此先落成事实层与背景信息,**不进规则**,除非日后
  回测证明有预测力。
- **实现**:新表 `spot_basis_history`(一天一品种一行,近月与主力两套照存源的
  口径——源自己挑近月与主力,我们不重挑,重挑就得复制它的规则,两套迟早分叉;
  RLS + runtime 四权限;现货价必须 >0,收盘价 0 的教训同样适用)。采集
  `deploy/collector/fetch-spot-basis.py` + 装载 `load-spot-basis.sql`,接在
  日更管线里(回看 8 天,**失败不阻断后面的步骤**——基差是背景信息,拿不到
  不该让监控快照跟着不跑)。
- **为什么不进 collector 的 ExchangeSource 契约**:生意社不是交易所,一次返回
  全品种、不按交易所分,硬塞要开一堆例外。照 `run-official-seats.sh` 的先例做
  独立脚本,但**脚本与装载 SQL 都进仓库、随发布包下发**——服务器上手工装的脚本
  换机器会丢,这是 2026-08-13 迁机时的教训。
- 郑商所在源里是三位年月(FG609),入库前归一成四位;**补世纪按交易日锚定**,
  不按品种上市年(后者是郑商所世纪判错事故的根因,PITFALLS 四)。

### `DEC-075` 用干净数据重测回撤档:苹果退回默认档,分档整体降级为「只保留有机制的两个」

状态:已确认(2026-08-18,运营者问「SA 的价差被修复了,前面 FG-SA 8% 需要重新
测吗」——**问得对,而且不止 FG-SA**)。

- **为什么必须重测**:DEC-070 定档时,我导出回测数据用的是
  `close_price is not null`——**与当时的生产 SQL 同一个缺陷,只挡 NULL 不挡 0**
  (`DEC-073`)。零收盘价只出现在郑商所,所以受污染的是 **FG(8,844 行)、
  SA(830)、AP(119)**;大商所的 JD/JM/LH 一个数没变。生产错、回测跟着错,
  **自己验证不了自己**。
- **重测结果(干净数据,同一套脚本与口径)**:
  - **AP 变化最大**:各档从「+0.2% → +14.9%,陡峭单调」变成
    「+22.4% → +34.0%,**8/10/12 三档完全相同(+28.9%)**」;深档优势从 14.7pt
    缩到 5.1pt,逐年留一一致性从 **8/8 掉到 5/8**。
  - **FG-SA**:8%(−3.9%)仍是局部最优、逐年 4/6 选 8,但**全部档位仍为负**
    ——这个组合本身就该谨慎,档位选择意义有限。
  - **样本外挑参的收益归零**:留一挑参 OOS +16.5% vs 固定 10% **+16.5%**
    (旧数据是 +14.7% vs +12.9%)。当初那 +1.8pt 是脏数据造的伪信号。
  - JD/JM/LH 逐字未变。
- **决策**:**AP 退回默认档 10%**(证据消失,且 AP 的位置抖动是全场最低的,
  深档优势从来没有机制解释——我当初只敢标「中置信」);**JM 20% 与 JD 5% 保留**
  (数据未受污染,机制+数据两条腿都在);FG-SA 8% 与 LH/FG 10% 维持。
  三处同步:API `turn_retreat()`、采集 SQL lateral 分档表、前端 tooltip 文案。
- **方法论教训**:回测数据必须与生产同源同口径——**同源的好处是一致,坏处是
  同一个缺陷会同时污染两边**。修了生产口径,必须回头重跑所有基于旧口径的结论
  (已入 `docs/PITFALLS.md` 六)。

### `DEC-076` 采集加一轮 16:00 抢早;会话时长放到 30 天

状态:已确认(2026-08-18,运营者「采集时间太晚,能不能北京 16 点就开始」
「会话总是过期,让我 30 天登录一次」)。

- **采集抢早轮**:新增 08:00/08:25/08:40 UTC(北京 16:00/16:25/16:40)三条,
  分别是采集 → 官方席位 → 引擎,顺序与原有链一致(引擎读席位表,必须排在后面)。
  **是加一轮,不是把 17:30 挪早**:日盘 15:00 收盘,但各交易所的结算与持仓排名
  通常 15:30~16:30 才陆续发布,16:00 能否采全**没人验证过**。采集与席位都是幂等
  upsert,多跑一轮无害——采到就早看到一个半小时,采不全也有原有轮次补齐。
  **先观察几天日志,确认 16:00 稳定采全,再决定要不要撤掉 17:30 那轮**;先撤后验
  等于拿每天的数据完整性赌一个没验证的假设。
- **会话 30 天**:`absolute_session_ttl` 7 天 → 30 天,`idle_session_ttl`
  **4 小时 → 30 天**。**每天重登的元凶是闲置那条**(盘后看一眼、隔天再开就超时),
  绝对期限反而不是主因。这是单人自用面板、唯一使用者是运营者本人,频繁重登的
  安全收益接近零而摩擦是每天一次。保留的防线:会话可在设置页逐个吊销、每用户
  最多 5 个会话、改密码使旧会话失效。两个常量在 `auth.rs` 一处定义,要收紧改那里。

### `DEC-077` 净持仓页补盈亏与成本,取代建仓过程;当日盈亏小窗口径修正

状态:已确认(2026-08-18,运营者「净持仓应该取代建仓过程…净持仓下面加上盈亏
界面,完全可以取代建仓过程这个按钮」)。
**页面形态已被 `DEC-080` 推翻(同日)**:功能(多席位合计 + 收藏 + 盈亏 + 成本)
全部保留,但承载它的不再是独立页面,而是席位页的第二个子页——本条下文提到的
「净持仓页」「侧栏入口」「代码暂留待清理」均已不是现状,以 DEC-080 为准。

- **先修的一个显示缺陷**:建仓过程的三张图共用同一份 tooltip 内容,而那份内容
  按早先取舍「只留累计」,于是**悬停在「当日盈亏」的柱子上读到的是「估计累计
  盈利 +1.67 亿」**,图名与数字对不上(运营者发现)。数值本身经独立 SQL 复算
  无误(08-13 4,501 万、08-18 2,689 万,与图逐根吻合),**问题只在显示层**。
  已给盈亏图配专用小窗,第一行是当日那个数。
- **净持仓补盈亏与成本**:新增「当日盈亏」「累计盈亏」两张图,tooltip 补净多/
  净空的加权成本(覆盖不全时说明覆盖手数)。
  **算法一行不改地复用 `build_variety_series`**——它本来就是「多条序列各自走
  成本引擎再按日归并」,唯一的差别是分组维度从「合约」换成「**席位 × 合约**」。
  掉榜那天不贡献手数、成本不可知的合约照样计手数但不进均价、累计线不因不可知
  而断开,这些口径都已在那套逻辑里验证过。**另起一套只会让两个页面的盈亏迟早
  对不上。**
- **取价用结算价**:净持仓的查询原先不取价,这次补的是 `settlement_price`
  而非收盘价——无成交日收盘价是 0(DEC-073),用它算盈亏会造出假的巨额了结。
- **入口已撤,代码暂留**:侧栏「建仓过程」与页内 tab 按钮都撤掉了;
  `?tab=building` 的旧链接仍能打开。**等运营者验收净持仓页确实够用,下一轮再
  清代码**——万一还缺什么,加回入口只要一行。单席位的用法没丢:多选框里只选
  一家就等价于原来的建仓过程,而且多了收藏。

### `DEC-078` 报告表后台预热;加减按绝对值判;席位页并排净持仓入口

状态:已确认(2026-08-18,运营者三条:「报告表过一会打开就要转半天」
「空单减少这是减不是加,按绝对值计算」「边上加上净持仓的按钮」)。
**第 ③ 条(并排的净持仓入口)已被 `DEC-080` 取代(同日)**:那个按钮当时是跳去
独立页面的,现在净持仓就是本页的第二个子页,两者并列在同一组标签里。前两条不变。

- **加减方向(真 bug)**:合计行的「加/减」原先用净持仓的**代数差**判,而空头的
  净持仓是负数——机构持仓银从 −10,621 变到 −7,143,代数差 +3,478 被显示成
  「加」,实际是空单**减了** 3,478 手。改按**持仓量绝对值**比:加减说的是
  「仓位变大还是变小」,与他站哪一边无关。翻向那天(净持仓变号)按绝对值仍成立:
  多 2,000 翻成空 5,000 报「加」,他确实把仓位做大了。
- **报告表慢的根因,与预期正好错开**:①它的筹码列要对七家席位 × 金银跑**全历史
  成本推算,十几秒**;②缓存 TTL 只有 15 分钟,隔一会儿再开必然重算;
  ③**运营者以为的「每天采集后更新一次」是价差预热**(`--warm-spread-cache`),
  那条只写三禾价差缓存,**从没碰过报告表**。
- **修法:缓存改由「数据变了」驱动,不再按时间过期**。
  我第一版做的是「TTL 延长到 6 小时 + 每 25 分钟重算」,**被运营者当场纠正**:
  报告表的数据只在采集后变,那就该「存起来一直用,直到下次采集数据有变化才
  重算」。按时间过期错在两头——空闲时段反复重算一模一样的东西,而到点失效
  又必然让人再撞一次那十几秒。
  终版:缓存里存下算这份时的 `report_data_version`(该交易日 seat_history 与
  price_history 的 `max(updated_at)`),命中只比版本,**数据没变就永远命中**;
  后台任务每 3 分钟查一次版本,**变了才算**,所以采集落库后最多 3 分钟页面上
  就是算好的新数,没数据变动的时段一次重算都不做。
  取 `updated_at` 而非 `loaded_at`:后者自 2026-08-16 起固定为「首次到达时刻」、
  upsert 不刷新,拿它当版本号会漏掉晚间补采。
  **为什么不做成 cron**:报告表缓存在**进程内存**里,外部进程(compose run 的
  临时容器)算完写进的是它自己的内存,对正在服务的进程毫无影响——要么把缓存
  落库,要么由服务自己算,单人面板选后者,省一张表和一次失效设计。
  handler 与预热**共用同一条计算路径**(`compute_overview_report`),预热要是走
  另一套代码,两边迟早算出不一样的东西。手工改的压力位/席位组不体现在数据版本
  里,仍由写端点保存后整体 `.clear()` 兜住。
- **席位页入口**:建仓过程撤掉后只剩一个孤零零的按钮,不像可切换的东西;
  旁边并排加「净持仓」跳转。

## 13. 变更控制

- `DEC-013` 至 `DEC-025` 已关闭原 `OPEN-PRD-001`、`OPEN-PRD-002`、`OPEN-DATA-001`、`OPEN-DATA-002`、`OPEN-PORT-001`、`OPEN-DB-001`、`OPEN-DB-002`、`OPEN-IMP-001`、`OPEN-SEC-001`、`OPEN-COL-001` 和 `OPEN-COL-002`。
- `DEC-026` 至 `DEC-035` 已关闭原 `OPEN-PRD-003`、`OPEN-ARC-001`、`OPEN-SEC-002`、`OPEN-COL-003`、`OPEN-COL-004`、`OPEN-ARC-002`、`OPEN-DB-003`、`OPEN-BROWSER-001`、`OPEN-AI-001` 以及 AI 模块部署边界相关开放项。
- 2026-08-02 总方案重审废止 `DEC-023`、`DEC-030`、`DEC-034`，修订 `DEC-025`、`DEC-031`，新增 `DEC-038`、`DEC-039`、`DEC-040`；同日因 DCE 全局 WAF 实证新增 `DEC-041`。2026-08-05 用户授权新增 `DEC-042`，仅为 Phase 5 自由价差恢复三禾无需登录的三个只读 API，不改变 Phase 4 采集来源边界。旧正文保留用于变更审计，但不再构成活性要求。
- 尚未决定的实现细节直接立 `DEC-xxx` 草案或写进最新交接文档
  (原 `OPEN_QUESTIONS.md` 的开放项已全部被 DEC 系列关闭,文件于
  2026-08-16 随过时文档清理删除,git 历史可查)。
- 任何偏离本文的实现必须先更新决策、设计和验收文档。

## 14. 与既有文档的冲突与处理

| 决策范围 | 既有文档状态 | 冲突结论 | 已完成修正 |
| --- | --- | --- | --- |
| Workspace 归属 | 原设计把共享范围列为开放事项，部分表使用 `owner_user_id` | 有冲突 | 新增 `workspaces`；业务表、任务、对象和 AI 全部使用 `workspace_id` |
| 回测边界 | 原方案同时出现“按规则统计胜率”和“后续再做回测” | 有冲突 | MVP 只保留历史统计、情景计算和真实 `trade_group` 统计 |
| 时间与夜盘 | 原设计标记为待确认 | 无既定实现冲突，但存在缺失 | 固定 UTC、Asia/Shanghai、`trade_date`、`session_type` 和版本化日历 |
| 价格口径 | 原数据模型以单一 `price_type + price` 表达 | 有模型冲突 | 改为分别保存 `close_price`、`settlement_price`，派生结果使用 `price_basis` |
| 连续合约 | 原设计将自动连续与换月口径留空 | 无既定实现冲突，但范围不清 | MVP 禁止自动生成，只允许带来源和换月规则的外部数据 |
| 成交与盈亏 | 原设计把 FIFO/LIFO、费用和归组口径列为开放 | 无既定实现冲突，但缺少确定口径 | 固定原始成交不可覆盖、FIFO、净盈亏含手续费、胜率按完整组 |
| 主键与精度 | 原数据库文档明确为待确认 | 无既定实现冲突 | 固定 UUIDv7/BIGINT identity 和四类金融精度 |
| 批次回滚 | 原设计已倾向冲突时中止，但仍保留选择空间 | 轻微歧义 | 固定整批原子回滚、冲突零变更、无部分回滚、补偿批次纠错 |
| 敏感数据 | 原设计只有“加密 + key_version” | 有设计深度冲突 | 升级为 DEK/KEK 信封加密、重新包裹轮换和备份分离 |
| 远程登录 | 原设计明确未确定交互通道，后曾确认浏览器登录基础设施 | 2026-08-02 已裁剪 | 废止 `DEC-023`、`DEC-034`；整套登录浏览器基础设施移除，未来需要时另行立项 |
| URL 与连接器 | 原设计已反对通用任意 URL，但 API 边界不够强 | 2026-08-02 已修订 | `DEC-031` 固定五交易所 akshare 采集器；`DEC-041` 仅为 DCE 增加官方优先、如实标源的新浪 fallback，其余四家仍禁止二手源 |
| 提取优先级 | 原设计采用多级网页提取与 OCR | 2026-08-02 已修订 | `DEC-025` 固定 akshare 封装的交易所公开接口与公开文件，删除 OCR 级 |
| 首次初始化 | 原设计未定义首个管理员创建方式 | 缺失 | 固定 `BOOTSTRAP_TOKEN` 一次性初始化、单事务创建用户/Workspace/Owner |
| 反向代理 | 原文在 Nginx/Caddy 间未选 | 有待确认项 | 固定 Nginx，职责限定为静态资源、API 代理和 SSE 支持；noVNC 代理已废止 |
| RLS | 原文作为可选纵深防御 | 有待确认项 | 固定启用 Workspace 级 RLS，并要求 `SET LOCAL` 与显式 `workspace_id` 双层防护 |
| noVNC 参数 | 原文缺少 token、超时和并发口径，后由 `DEC-034` 补充 | 2026-08-02 已废止 | 不再建设 noVNC 会话；登录类数据源未来另行立项 |
| 自动采集确认 | 原设计统一要求提取预览与人工确认 | 有冲突 | `DEC-038` 固定白名单结构化自动采集免预览、免人工确认，并以失败隔离、回滚和质量警告兜底 |
| 采集范围与历史 | 原设计未固定全市场范围和回填顺序 | 缺失 | `DEC-039` 固定四类数据、全部可得历史、分层回填与 80% 磁盘扩容水位 |
| 图表导出 | 原设计使用服务端渲染沙箱 | 有冲突 | `DEC-040` 改为 ECharts 前端导出 PNG/SVG，并保留 SVG 安全清洗验收 |
| 三禾自由价差 | `DEC-024/031` 曾随登录基础设施一并移除三禾 | 2026-08-05 有限恢复 | `DEC-042` 仅授权三个无需登录的只读 POST 端点，由 Rust 代理、限频和缓存 |
| 三禾席位接口 | `DEC-042` 原文写“不在授权范围，留待 Phase 7 另议”，而 2026-08-12 实际已按用户指示采集 | 文档与实现矛盾，已修订 | `DEC-042` 据实记录当日授权与同日停采及其数据依据；缺口改由自建反推补齐，见 `DEC-048` |
| 掉榜日持仓 | 原文只定了“断开显示，不连线”（画法），没定数据怎么表示 | 实现按缺省补零，一路错到成本引擎 | `DEC-048` 固定“缺席 = 未知 ≠ 零”，三层一律 NULL；反推只推一天，推不出来不写行 |
| AI 部署边界 | 原文存在 AI 能力设想但 Phase 1 边界不清 | 范围风险 | 固定 Phase 1 不实现 AI，后续统一 Provider、只读工具、SSE 和来源审计 |
| 导入通道 | 原设计为「人工上传 + 预览确认 + 批次回滚」，自动采集也走同一条路 | 服务的功能已取消，成本与收益倒挂 | `DEC-049` 整体摘除，日更改走直灌；`DEC-021` 随之废止 |
| 灾备形态 | 迁新机时以「老机仍在、随时可切」为退路 | 老机已删除，该退路不复存在 | `DEC-050` 固定退路为每日异地 dump + 部署链回滚，**无热备机器** |
| 机构成本 | 面板与引擎各有一份实现，无人保证一致 | 已实证长期分叉（东证 960.40 vs 900） | `DEC-053` 统一逐合约口径，并以对拍守卫接入部署验收 |
| 合约汇总 | 2.3 节只说「各合约加总、金额累计÷总手数」，未定方向 | 分不出多空两腿，且取数取不到结算价 | `DEC-054` 固定按各合约净方向分两腿，取数改走逐合约榜 |
| 首进场区间条件 | 曾于 08-15 加入「60 日区间下 70%」四条件 | 回测全程为噪音（±3pt，一两笔重定时） | `DEC-055` 当日采纳、次日撤销，全程留档 |
| 重挫买点基准 | 首版用 250 日滚动高点、次版用人民币本轮高点 | 固定窗口把 1246 后一整年标成重挫；人民币被汇率扭曲（2021 标出 69 个假重挫日） | `DEC-056` 终稿=伦敦金美元价 ±10% 分轮，运营者两次纠正均被数据证实 |
| 席位组扩充 | 海选曾提议中金进组、年轻席位 t=5 级频现 | 中金 2023 后零事件=哑火；年轻高 t=选择偏差标准形状；国贸/中泰现持仓为逆势小空单 | `DEC-057` 七家为现有数据与标准下的尽头，国贸/中泰永久排除，新增「当前持仓照妖镜」维度 |
| 极值的含义 | 监控页把「贴到历史极值」直接呈现为机会，页面读法暗示回归 | 事件研究实证：鸡蛋极值段仅 45%、焦煤仅 29% 朝回归方向走，按原读法用会亏钱 | `DEC-062` 每组标注自己的历史回归率并写明口径，页头改中性文案 |
| 触发的粒度 | 只有「触发/不触发」两态，长段与新进入极值不分 | 焦煤 2026 年 64.2% 的交易日都在 3% 触发，一片红等于没报 | `DEC-062` 存前一交易日位置、读时判段首日，界面加「新」徽标与「仅新触发」筛选 |

### `DEC-079` 净持仓摘要补多空手数与成本;反推日单独标色;反推时序修复

状态:已确认(2026-08-18,运营者:「净持仓要显示成本,多单空单多少手,成本多少,
都要显示。17 号高盛持仓推算出来了,要显示进去」)。

- **常驻摘要补齐**:原来只有「净 2,415 手 · 计入 1 家」。净持仓是个**差**,
  它说不出持仓结构——净 2,415 可能是「多 2,436 空 21」,也可能是「多 5,000
  空 2,585」,两者的成本与风险完全不是一回事。摘要补多空手数与两腿加权成本,
  口径与建仓过程一致(`legCost`:覆盖不全时注明覆盖了多少手)。数字本来就在
  小窗里,搬到常驻区只是让鼠标移开也看得见。
- **反推日单独标色**:净持仓页此前只标「掉榜少算了几家」,不标「这天算进去了,
  但那家其实没上榜,数字是回榜日倒推的」——两件事,反推日和实测日在图上长得
  一模一样。建仓过程页早就是两条带并存(掉榜带 0x29、推算带更淡),这里补齐,
  并在正文写明各有多少天。
- **反推时序(真 bug,根因)**:`infer-offboard-seats.sql` 只挂在 collector 里
  (16:00 / 17:30),而上期所与郑商所的席位 16:25 / 17:55 才由
  `run-official-seats.sh` 入库。反推的唯一依据是**回榜日的增减量**,拿不到回榜
  行就整段推不出来,官方源品种因此永远滞后一整轮。2026-08-18 实测:大商所
  (席位由 collector 自采)反推已到 08-17,上期所与郑商所停在 08-14。高盛 08-17
  掉榜、08-18 回榜,报告表走**读时**反推算得出 2,334 手,净持仓页读**库里**的
  反推行,那天就是一片空白——同一个数字两个页面对不上,差别只在这条时序。
  修法是在官方席位脚本末尾补跑一次反推,SQL 自身幂等(先删本次窗口内自己写过
  的行再重算),与 collector 那趟重复执行无副作用。
  生产已手动补跑验证:917 行新反推,八品种全部推进到 08-17,高盛 AU 反推
  2,347 多 + 13 空 = 净 **2,334**,与报告表逐字一致。

### `DEC-080` 净持仓并回席位页:顶部席位多选,两个子页共用这组选择

状态:已确认(2026-08-18,运营者:「名字还是叫净持仓按钮,实际还是应该引用建仓
过程的界面,点到建仓过程之后,就让这个席位能多选,再加上收藏功能,再加上多席位
加起来运算」)。**推翻 `DEC-077` 的页面形态**——功能保留,承载它的地方换了。

- **错在哪**:DEC-077 把净持仓做成了独立页面,还特意让两边的选择各记各的
  (「那边看一家,这边看一组」)。那正是运营者说的**没有联动**:在席位持仓选好
  一家,点净持仓过去还得再选一次。席位与日期本来就该是一组选择,两个子页共用。
- **现在的形态**:席位页顶部一组选择(席位多选 + 交易日 + 收藏),下面两个子页:
  - **席位持仓**:按家分段罗列,勾几家列几段。榜单的「名次」与「增减量」是
    逐家公布的,加起来没有意义;筛选器只出一次,它对所有分段生效。
  - **净持仓**:原「建仓过程」那四张图(行情/净持仓/当日盈亏/合约累计盈亏),
    按所选那几家合计。
- **数据源**:净持仓子页由单席位 `seats/building` 换成合计 `seats/net-position`。
  后者的三态语义(`counted/missing/inferred_members`)本来就是为多席位设计的,
  比 `BuildingDay` 的布尔 `inferred` 更全。掉榜判据随之从「净持仓是 null」换成
  「有几家没算进来」,只勾一家时两者等价。汇总档与单合约档的两套分支就此合并
  ——单合约只是「只有一条腿有手数」,不必为它留第二套写法。
- **后端只加一个字段**:`net-position` 响应补 `price_multiplier`(顶部那句
  「合约汇总 · 点值 1000」要它),且 `members` 为空时也回——不该因为一时没勾
  席位就消失。席位持仓仍走单席位接口,前端并发取 N 份(上限十家),契约一行不动。
- **旧设置不丢**:单选时代的 `seats.member` 搬一次家到 `seats.members`。watch 只在
  值变化时写回,初始值原样读进来不算变化,不主动搬就会「升级即丢设置」。
- 独立的净持仓页面已删,`/seats/net-position` 重定向到 `/seats?tab=building`。

### `DEC-081` 品种汇总 K 线缺三天(残缺行顶掉完整行);滑钮联动;当日盈亏上标题

状态:已确认(2026-08-19,运营者从生猪品种汇总图上看出「缺不少蜡烛」,又指出
「滑钮联动被改没了」「当日盈亏也跟累计盈亏一样,数字放到上面」)。

- **K 线缺口(真 bug)**:`variety_candles_sql` 的取数是「先按源优先级挑一行,
  再检查它四价齐不齐」——顺序反了。同一天同一合约若既有残缺的高优先级行、又有
  完整的低优先级行,`distinct on` 挑中前者、随后被完整性过滤筛掉,那天整根蜡烛
  就没了,而好数据明明躺在旁边。咬到的是大商所:通道退役前后留下一批
  `akshare_v1` 行(2026-07-31~08-12),只有收盘价与结算价,开高低和持仓量全空;
  郑商所与上期所有官方源(优先级 0)压着不受影响,大商所只有新浪(优先级 3),
  被 akshare(优先级 1)压住,于是 JM/LH/JD 三个品种在 07-31、08-03、08-05 三天
  的品种汇总 K 线整根消失。修法:把完整性过滤提到 `distinct on` 之前,与
  `load_contract_candles`(单合约 K 线,本就先过滤再挑,没这毛病)对齐。生产实测
  三天全补回、序列连续。**成本与盈亏不受影响也不变**:那批 akshare 行的结算价与
  收盘价都在,且与新浪逐行相等(DCE 90/90 实测),成本链走 `settlement_price`,
  一直取得到值。新增测试钉住顺序:完整性过滤必须与 `distinct on` 同处一个 select。
- **滑钮联动**:净持仓那四张图(行情/净持仓/当日盈亏/累计盈亏)搬进席位页时漏了
  `group`,各缩放各的。补回 `group="seats-net-position"`,四张图重新联动。
- **当日盈亏上标题**:与累计盈亏同一待遇。取序列**最后一天**而非「最后一个有值
  的天」——累计线可跳过不可知的天接着累加(它本就是已知部分的累加),但「今天
  赚了多少」跳过去就成了拿前几天的数冒充今天;最后一天不可知就如实标「当日不可知」。

### `DEC-082` 格林三名归一;净持仓摘要拆到逐家;席位多选框两处失效修复

状态:已确认(2026-08-19,运营者一次提四件:「显示各个席位的多单均价和空单均价」
「格林有3个名字,保留一个格林大华,其余2个删掉,数据应该是一样的,你核查一下」
「一个席位不选的时候,不能输入文字选择席位」「输入 gs 出高盛,目前完全出不来」)。

- **格林三名归一——核查后发现不是「重复」,是「互补」。** 运营者的判断前提
  (数据一样、删掉两个)与库里实态不符,照那么做会丢数据,所以先核查后落笔:
  - 「格林大华」= AU/AG(上期所)+ JD/JM/LH(大商所),2014-03-27 至今;
    「格林大华期货」= **AP/FG/SA,全是郑商所**,2014-01-06~2026-06-25。两者
    **品种零重叠**,是郑商所写全称、上期所大商所写简称的源命名差异,与已在表内的
    「申银万国/申万期货」同一模式。
  - 「格林期货」是 2014-03-27 之前的旧名。交接当天两名字并存,但持仓**逐笔相同**
    (AG1406 的 11603、AU1406 的 1486/906、AU1412 的 34,FG 在 2014-01-06 的 505),
    且重复的那份是 `reboard_inferred`——去重时官方行优先,并进来既不重算也不丢。
  - 所以处置是**归一而不是删除**:删两个名字会丢掉郑商所 AP/FG/SA 的全部持仓与
    2014 年前的历史;归一之后是一家,AU 序列从 2008 年连起来(与乾坤→高盛同理)。
  - **「大华期货」不并,已立断言钉死**:它 2008-04-29~2013-09-17 与「格林期货」
    在同日同合约同时上榜 **266 次**——同一家不可能在一张榜上占两个名次,那是 2013 年
    被吸收合并的另一家公司。它在下拉里搜「格林」搜不到,所以运营者没看见它。
  - 别名两处同步:`spread_analytics.rs` 的 `MEMBER_ALIASES` 与 `engine/smart_money.py`。
    引擎那份是数据卫生,**不影响信号**——格林大华不在 `group` 信号组内(同国投两条)。
  - **不需要数据重算**:别名是**读时**归一(只进 SELECT / distinct on / order by),
    `infer-offboard-seats.sql` 与 `compute-seat-totals.sql` 全走原始 `member` 列,
    落库数据一个字节不动。
- **净持仓摘要拆到逐家**:合计那排说不出持仓结构里「谁在多、谁在空」——五家合起来
  净空一万四千手,可能五家都在空,也可能一家重仓空、四家轻仓多,两种情形该有的判断
  完全不同。响应补 `latest_members` + `latest_trade_date`,按 member 分组各跑一遍
  **同一个** `build_variety_series`。同源是硬要求:合计的均价本就是各家按手数加权
  还原,分项与总数必然对得上,已由 `splitting_by_seat_reproduces_the_combined_total`
  钉住。掉榜那家报「当日掉榜,持仓未知」而不是 0 手(三态口径照旧)。
  「哪天算最新」由后端定一次并连日期回出,不让两边各判一次。
- **一家不选时不能输入(真 bug)**:`:disabled="loadingPositions"` 与 `loadPositions()`
  里「一家不剩就自动补名录第一个」两个设计凑出一个死角——清空即进 loading(框变灰
  不能输入),加载完又被塞回一家,「空着且能搜」这个状态根本不存在。修法:去掉自动
  补选(多选场景下清空是「我要重挑」,不该替他做决定),禁用改为只在名录尚未到手时
  生效,并补一个「先在上面选几个席位」的空态。
- **拼音搜索在中文输入法下完全失效(真 bug)**:根因不在拼音表,在 el-select——
  `handleQueryChange` 头一行 `if (… || isComposing.value) return`,IME 预编辑期间
  **根本不调 filter-method**。那条短路是为「打中文」设计的(敲拼音时先别搜,等选完字
  再搜),而这个搜索框的主力用法恰恰是**永远不选字**:敲 gs 要的就是这两个字母,
  composition 永不结束,查询就永远是空串。挂真实组件对拍实测:英文输入法下查询是
  `gs`、下拉只剩高盛;中文输入法下查询恒为空、列表原封不动。修法:监听冒泡到
  el-select 根节点的 `compositionupdate`,预编辑期间直接读输入框当前值。

### `DEC-083` 生猪机构资金:合计流向跟随(与金银两套独立引擎);品种选择器

状态:已确认(2026-08-19,运营者立项「做生猪的聪明钱跟随策略,先筛出真正赚钱、
能预测方向的席位」,并要求机构资金模块加品种选择、黄金白银算一个品种)。
四轮研究报告在 `research/REPORT_LH_SKILL_v1.md`、`REPORT_LH_PHASE1_v1.md`、
`REPORT_LH_PHASE2_v1.md`,脚本 `run_lh_skill / phase1 / phase2 / momentum / parity`。

**先验证再动手,过程中推翻了三个前提,两个是运营者的、一个是我的默认做法:**

- **「筛出赚钱的席位」不能按总盈亏筛。** 生猪 2023-08 至今是单边熊市,一直挂空单
  就赚钱。把「恒定仓位能赚到的钱」(beta)扣掉之后:国泰君安 13.88 亿里 4.99 亿是
  择时;**永安期货 9.99 亿里只有 0.92 亿**(91% 是方向暴露);东证期货 6.42 亿里
  5.40 亿(84%)。同一段样本外实测,按 alpha 选 t=5.22 > 按总盈亏选 t=3.57 >
  不挑人 t=2.81 > **按仓位规模选 t=0.11**——挑人的价值全部来自 alpha。
- **金银那套「逐家权重 × 多席位共振」在生猪上不成立。** 生猪单家席位的加减仓事件
  整体胜率只有 50%(2026 年 44.7%),只有东证一家 t≈2.8 显著;而**八家合计的流向**
  控制动量后偏相关 t=5.4~7.5,与金银核心因子同量级。**合计比逐家强,与金银相反。**
- **信号不能逐合约算(推翻运营者当时的拍板,数据为据)。** 逐合约 IC_t 仅 0.58,
  品种合计 t=5.22。根因查实:**84.6% 的交易日里同日不同合约的信号方向相反**,
  `|合计净变化|/Σ|各合约变化|` 中位数只有 0.62——那是移仓换月,机构的方向判断
  是品种级的,拆到合约就被撕成两个相反的信号。
  **但运营者的价差担忧对计价完全成立**:各合约相对主力偏离最大 49%,所以收益、
  成本、盈亏一律逐合约算,换月日用新合约自己的前一日结算价。两件事分开。

**落地形态**:

- **两套独立引擎、独立产物**(`engine/hog_money.py` → `hog_signals.json`)。不合并进
  smart_money.py:信号形态不同,且失败要隔离——生猪挂了不该让金银信号停更。
  run-smart-money.sh 里生猪那段用 `if docker run …; then` 包住,失败只告警。
- **席位组滚动重选**(每 3 个月按截至当时的历史 alpha 取前 5),不硬编码名单。
  金银敢硬编码七家是有 17 年样本兜底,生猪只有三年、且只有一种市况。
- 信号 = 组内合计净持仓 5 日变化 / 滚动标准差;进场 |z|≥1;出场 = 反向 / 止损 6% /
  持满 40 日。参数不敏感(止损 4/6/8/10% 与最长持有 20/30/40/60 相邻档同向)。
- 回测(2023-08~2026-08):恒定满仓做空基准 +99.2%/夏普 1.65/回撤 −14.8%;
  本策略离散版 +104.5%/夏普 2.26/回撤 −10.2%。**优势在风险调整后收益,不在绝对
  收益**——夏普 +37%、回撤少三分之一。注意基准要写对:价格跌 52.9% 对应做空复利
  +99.2%,不是 +52.9%(逐日复利不对称)。
- **做多支路上线但强制标注「未验证」**:多头 15 笔累计仅 +4.5%,而样本期内机构
  合计净持仓**一天都没转成净多**(穿越零轴 0 次)。它符合运营者「等机构转多就
  转向」的意图,但没有数据背书,界面上用独立配色 + 首屏告示牌与空头信号区分开。
- 前端:`SmartMoneyView` 加品种选择器(黄金白银 / 生猪,选择记 localStorage),
  生猪内容在 `components/HogMoney.vue` 独立渲染,四个 tab 与金银页对齐。
- **两处实现必须对拍**:引擎与研究回测各写了一遍同一套规则,
  `research/run_lh_parity.py` 逐笔比日期/方向/毛收益(36 笔全等),改任一边都要跑。

### `DEC-084` 生猪:改一年选人、关闭做多支路、界面摆出基准对比

状态:已确认(2026-08-19,DEC-083 上线当天运营者四点反馈)。修订 DEC-083 的三处口径。

- **选人 3 个月 → 一年**(运营者:「3 个月太短,会有很多噪音」)。数据支持这个判断:
  换组从 10 组降到 3 组,而且后两组只差排序——三年实际只换过一次人;3 个月那 10 次
  换组大多只是核心四家(东证/东吴/国泰君安/兴证)内部的排序抖动。胜率 58.3%→63.9%,
  回撤 −10.2%→−8.6%。**代价如实记录**:累计从 +104.5% 掉到 +86.5%。
  另注:3/6/12 月三档是 94.2/67.7/77.1,**不单调**,所以这三个数之间的差异多半是
  噪音,不能反过来论证 3 个月更好。
- **关闭做多支路**(`long_enabled=False`)。运营者问「为什么做多显示未验证」,查下来
  有两层:
  - 现在的「做多」跟的不是「机构转多」而是**「机构减空」**:15 笔做多进场当日,
    机构合计净持仓**全部仍为负**(−28,284 ~ −974 手)。
  - **「机构真转多」也不是买入信号**:按 alpha 选的这组,样本里净持仓转正过 14 天
    (集中在 2025-07,最高 +4,046 手)——这修正了 DEC-083 里「一天都没转过」的说法
    (那是按总盈亏选的八家算的)。但那 14 天之后 20 日主力仍平均跌 1.18%,
    最好一次只有 +0.61%;对照净空最深的那些天之后跌 4.73%。转多只意味着跌得慢。
  - 关掉后:21 笔全空头、胜率 71.4%、最差 −3.8%,夏普 1.96→2.39。
  - **保留开关不删代码**:那 14 天全挤在一个窗口里,实质是 1 个事件,只能说
    「没有证据支持」,不能说「证明必亏」。页面**仍然显示**机构转多状态并醒目提示,
    只是明说不会进场。
- **界面必须摆出与「恒定满仓做空」的对比**(payload 的 `compare`)。运营者问
  「明显熊市为什么胜率低、单笔不高」,根子在这:三年单边熊市里躺着做空本身就有
  **+99.2%** 复利(注意不是价格跌幅 52.9% 取反,逐日复利不对称),而策略是 +79.7%。
  **策略在绝对收益上没跑赢基准**,赢的是回撤(−9.4% vs −14.8%)与夏普(2.39 vs 1.65),
  以及趋势反转时会退出而不是硬扛——最后这条样本内无法验证。不摆基准,熊市 beta
  会被当成策略的本事。
- 翻页统一用 `el-pagination`(与金银历史信号同款,运营者要求后续品种都用它)。
- **试过但否决的改进**:0-5 日的短命笔胜率只有 22.2%,试图用「最短持有期」挡掉,
  但 3/5/7/10 日 = 94.2/44.0/69.5/47.7 **不单调**,按 PITFALLS 六「相邻档同向」
  的标准不予采纳。记在这里,免得下一个会话看到那 22% 又去重做一遍。

### `DEC-085` 散户反向:跨品种固定名单;生猪先加维度不改规则

状态:已确认(2026-08-19)。缘起是运营者的一句判断——**「散户确实是反向指标」**。
四轮研究:`REPORT_RETAIL_CROSS_v1` → `REPORT_RETAIL_V2_v1` → `REPORT_RESONANCE_PHASE2_v1`,
外加玻璃/纯碱两份(`REPORT_FG_SKILL_v1`、`REPORT_SA_FGSA_v1`)。

**名单**:东方财富、平安期货、徽商期货。三条性质决定了它的价值:

- **跨品种固定,不逐品种重选**——这正是它相对「找聪明钱」的优势:**没有挑人的
  过拟合**,新品种可直接套用。生猪那套要每年重选前 5,玻璃那套怎么选都是 7 正 7 负。
- **判据是「散户天然站多头」**(运营者给的):一致净空的是套保席位,为交割锁价、
  不在乎盈亏。据此运营者点名剔除**格林大华**(生猪净空 2,499 手);按同一判据自动
  筛选独立地剔出了格林大华与长江期货,与运营者判断一致。**剔干净后信号翻倍**:
  生猪去均值 t 从 +2.84 升到 +5.96。
- **加人反而变差**:四家不如三家。中信建投虽然确是散户(早期就亏、全净多、无前视
  也选得出),但它在生猪上只有 171 手——与运营者嫌弃中衍期货(FG 407 手、SA 58 手)
  是同一个毛病;方正中期则是 2021 后才变成散户,且偏好品种不重合。
  **「是散户」不等于「该进名单」**:名单要的是一组在同一批品种上同步犯错的人。

**共振**:聪明钱流向与散户反向流向同号。样本外实测——共振时信号明显更强、
**背离时基本消失**(FG 共振 +3.42 / 背离 −0.06;SA +4.69 / +0.38)。
且「共振」优于「两者相加」,说明聪明钱应当当**开关**用,不是当权重。

**生猪落地:只加维度,不改进出场规则**(`RULES["resonance_trades"]=False`)。
**理由是「两者表现相当」,不是「新信号不够好」**——这两个说法差很多。

正确对比(同一时间轴 2023-08 起、**各用各的信号进出场**,并与生产页面对拍过):

| | 笔数 | 净 | 胜率 | 回撤 | 夏普 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 现有主信号(生产在跑) | 21 | +79.7% | 71.4% | −9.4% | 2.39 |
| 散户反向(只做空) | 21 | +91.4% | 76.2% | −6.8% | 2.42 |
| 共振进场 | 18 | +88.4% | 72.2% | **−4.1%** | **2.80** |

**单笔均值差的 t 只有 0.22 / 0.49——21 笔样本上分不出高下**;逐年也互有胜负
(现有在 2026 更好,另两个在 2024 更好)。所以不换规则。

它的定位是**独立的第二意见**(与主信号相关 0.59),不是"更好的信号":
**看两者一致还是背离,比看它自己的方向更有用**。共振时回撤小一半
(−4.1% vs −9.4%),方向一致但尚不显著,继续观察。

> **本条曾经写错并已更正(2026-08-19 当天)**:初版写"现有主信号 23 笔 +39.8%/
> 夏普 1.73、共振 +74.2%/2.57,共振全面更优"。那组数字出自一个有两处错误的对比
> 脚本——①时间轴用 2024-01 而生产引擎从 2023-08 起;②**出场判断一律用散户信号**,
> 连"只用现有引擎"那一行也是,测出来的根本不是现有引擎。我还据此反过来主张切换、
> 并自我批评"双标",**那次自我批评本身也建立在错数上**。
> 是运营者对照生产页面的数字发现对不上才揪出来的。教训见 PITFALLS 六。

**其他品种的结论**(未上线,留档):
- **玻璃 FG**:跟机构不行(逐年 7 正 7 负),**反着跟散户行**(去均值 t=+4.07)。
  之前 `REPORT_FG_SKILL_v1` 判「不能做」的结论**因此被修正**:能做,方向反了。
- **纯碱 SA**:两条路都行(聪明钱 6 正 1 负;散户反向 t=+4.70)。
- **白银 AG 始终无效**:那三家在 AG 上没有一致方向(东方财富近中性),
  **用之前必须先确认「这批席位在该品种上一致站多头」这个前提成立**。
- FG-SA 价差另有一条信号(两品种流向之差,t=+5.43,滚动样本外全正),未落地。

### `DEC-086` 套利监控抢早轮写残缺快照(修);生猪改方案 C;玻璃纯碱上线

状态:已确认(2026-08-19)。三件事,前两件由运营者当场发现。

**一、套利监控 BUG:品种下拉只剩鸡蛋**(运营者报)

`compute-spread-monitor.sql` 里那句 delete 是**无条件的**——数据没到齐时照样先把
窗口内旧快照删光,再用残缺数据重写。DEC-076 新增的 16:00 抢早轮**首次运行**就是
这么坏的:那时只有大商所到货(DCE 16:03,郑商所与上期所要等 16:26),于是当天只算出
鸡蛋一个品种,**并把 08-17、08-18 两天完整的六品种快照一并重写成了五品种**
(缺焦煤)。快照 `computed_at` 全是 16:04:27,坐实是那一轮。
——**这同时回答了 DEC-076 留下的那个待办:16:00 采不齐,原轮次必须保留。**

修法:delete 之前加数据完整性门,判据是**「大商所与郑商所是否都到了」**。
不用「品种数够不够」当判据:品种会因真实停牌而正常减少,会误伤。
生产库只读预演验证过:郑商所已到时放行、反事实回到 16:00 只有大商所时拦截。
部署后实测三天全部恢复 56 组六品种。

**二、生猪改用方案 C:共振进场 / 散户出场**(运营者拍板)

进场 = 聪明钱与散户反向同号且散户反向 z ≤ −1;出场 = 散户反向翻向 / 止损 / 持满。
18 笔 净 +88.4%/胜率 72.2%/回撤 −4.1%/夏普 2.80。
**必须如实记住:三个候选(原信号 +79.7%/2.39、散户反向 +91.4%/2.42、方案 C)
单笔均值差的 t 只有 0.22~0.49,统计上分不出高下。选 C 是运营者的判断
(回撤最小、夏普最高),不是数据证明最优——不许在后续文档里写成「实测最优」。**

**三、玻璃、纯碱上线;引擎泛化为多品种**

`VARIETIES` 表按品种配点值/回放起点/做多开关/dip 条件,主流程逐品种跑、
各写各的 JSON,一个品种挂了不影响其余两个。前端组件参数化,**不按品种写 if**。

**规则按品种实测,没有照抄生猪**——这是本条最该记住的:

| | 做多 | long_needs_dip | 回测(样本外) |
| --- | --- | --- | --- |
| 生猪 LH | **关**(多头逐笔 −1.5%) | 留(用不上) | 18 笔 +88.4%/夏普 2.80 |
| 玻璃 FG | **开** | **关** | 206 笔 +4264%/夏普 1.63 |
| 纯碱 SA | **开** | **关** | 100 笔 +1522%/夏普 2.02 |

生猪关做多是因为它样本里只有单边熊市;玻璃跨了 13 年完整周期,做多支路有真实机会
(双向夏普 1.63 vs 只做空 1.04)。`long_needs_dip` 是生猪时代的遗留,在玻璃上带着它
收益差 3.5 倍(206 笔 +4264% → 157 笔 +1225%)。

对照基准:FG 买入持有 −33.1%/恒定做空 −17.7%;SA −23.7%/−17.5%。
FG 夏普只有 1.63、回撤 −19.4%,且逐年有 3 年为负(2015/2016/2019),
比生猪那条曲线毛糙得多,页面上不要只报累计收益。

---

## DEC-087 FG−SA 相对资金流向接进套利监控(2026-08-19)

研究阶段发现两品种聪明钱流向之**差**比各自的绝对流向更有信息量(全样本偏相关
+0.140,t=+5.43,N=1480;FG 单品种 t=+2.96、SA +4.41),而套利监控本来就盯着
FG-SA 这个组合——它此前只看价差位置与历史分位,缺「资金在往哪边调」这一维。

口径:两品种各取 alpha 前 5 席位合计净持仓的 5 日变化,**各自**标准化后相减。
必须各自标准化再减:两品种持仓量级差一倍以上,直接相减等于让量级大的那个说了算。
正 = 玻璃这边资金相对更强 → 价差(FG−SA)倾向走扩。

引擎 `pair_fgsa()` 每日写 `pair_fgsa.json`,页面只读不算,取不到就不显示。
**明确标注「背景参考,非进场信号」**:它预测的是价差方向,不含进出场与仓位,
而且 2026 年这个信号是负的(t=−1.16,不显著),当下正处在它的哑火年份。

页面文案不写死腿序:建组 SQL 现在恒定 a=FG、b=SA,但把这个前提写进文案,
哪天腿序变了会**反着显示而不报错**,所以按行自己的 `instrument_1` 翻译方向。

---

## DEC-088 套利监控:进场标与合格标此前指的不是同一笔交易(2026-08-19)

运营者提问「JM2612−JM2705 这行显示进场,该做多还是做空」时挖出来的。**两个独立
缺陷叠在一起,后果是页面会放行一笔没有任何统计支持的交易,而且不写方向。**

**缺陷一:统计取侧用了 `alert.or(turn)`(报警侧优先)。**
⚡ 进场是**拐头**触发的,资格却拿报警侧的统计去判。两侧相反时,合格标说的是 A
方向、进场标指的是 B 方向,乘在一起就放行了。
实例 JM2612−JM2705 @2026-08-06:历年轨 3.6% 报低位、当年轨自 100% 退到 70.8% 拐
高位。页面显示低位侧(13/13、持到期 +45,合格),⚡ 指的却是高位侧做空,而高位侧
持到期 −45 本该判不合格。此后两周价差 −155 → −71.5,**涨 83.5 点,做空方向是反的**。
改为 `turn.or(alert)`(抽成具名函数 `trade_side`)。没拐头只报警的行仍按报警侧
给统计——那是「机会出现、还没到上车点」,本来就没有 ⚡。

**缺陷二:`monitor_turn` 两侧并存时的平局判据方向反了。**
原来取「离自家门槛更远」的一侧,想的是「退得更多 = 拐得更实」;但退得多恰恰说明
那一侧的回归**已经走完了**。改为取**离门槛更近**(= 刚穿线)的一侧。
实例 LH2611−LH2705 @2026-08-19:位置 0.865,离高位线 0.90 只有 0.035(08-18 还在
1.0,今天刚穿下来),离低位线 0.10 有 0.765(20 日前创的新低,那波做多已走 715 点)。
原实现报低位,把走完的机会当信号,还顺带把统计切到低位侧(持到期 −635)判成不合格
——高位侧是 5/5、持到期 +635,**本该亮 ⚡ 做空**。这正是运营者报的「昨天有合格·高位、
今天全没了」。

**缺陷三(界面):进场标与合格标不写方向。** 运营者要求补上,已改成
`⚡ 进场 · 做空价差` / `✓ 合格 · 做多价差`(`high` = 做空价差 = 卖腿1买腿2,
`low` = 做多价差)。另加 `revert_alt`:两侧方向相反时把另一侧统计单列出来 + 挂
`⚠ 两侧方向相反`——**藏起来正是这个 BUG 的成因**,只显示一侧会让人以为只有一笔
交易可做。

**全量回放(88,473 行快照,不含交割红线压制)**:旧 ⚡ 692 → 新 ⚡ 799,消失 10、
新增 117;旧 ⚡ 里有 8 笔资格判在反方向。20 日前向验证(**只是理智检查,不是回测**:
无出场规则、无成本,且价差点数跨品种不可比,只看胜率与中位):
两者都有 659 笔 胜率 52.2%/中位 +4.5;新增 110 笔 55.5%/+4.8;消失 9 笔 55.6%/+2.0。
**新增那批不比现有的差,消失那批样本只有 9 笔、统计上什么都证明不了——去掉它们的
理由是逻辑(资格判在反方向),不是数据。**

修的是读时逻辑,历史快照不用重算。

---

## DEC-089 合计流向品种:主力进入交割窗口前 10 个交易日强制离场(2026-08-19)

运营者要求,原话与算例:「**我是散户**,玻璃 2609 合约 8.31 之前需要离场,要提前
10 个交易日,8.18 之前要离场」。

**口径**(与套利监控 `days_to_window_end` 同源,两个模块对「散户还能拿多久」必须
给同一个答案):窗口止点 = **交割月前月最后一个非周末日**;`days_left` 从**次日**
起算到止点(含)的工作日数——信号是盘后出的,今天判「剩 10 天」,平仓动作在明天。
验算 FG2609:止点 2026-08-31,08-17 剩 10 → 触发 → T+1 于 **08-18 平仓**,与运营者
给的日子一致。

**两件事一起做,不能只做一件**:`days_left ≤ 10` 时既**强制平仓**,也**禁止进场**
——只挡出场不挡进场的话,平了立刻又开,天天空转。

**适用范围**(运营者 2026-08-19 拍板):
- **生猪 / 玻璃 / 纯碱:加。** 规则写在 `hog_money.py` 的**共用 RULES** 里,
  后续要做的**鸡蛋、焦煤自动继承**,加品种时不用再补。
- **贵金属 AU/AG:不加。** 金银是另一个引擎(连续复权序列、持仓常跨月),不动它。

**回测数字一字未变** —— LH 18 笔 +91.8%、FG 206 笔 +5247.0%、SA 100 笔 +1690.8%,
出场原因分布里**一笔「临近交割」都没有**。原因是主力总在撞线前换月:全历史里主力
剩余 ≤10 个交易日的天数,LH 1 天、FG **0 天**、SA 4 天,而那几天恰好没有持仓。
把门槛临时调到 25 天能跑出 6/25/15 笔「临近交割」平仓,证明这条路径不是死代码。

**所以这是前瞻性的安全网,不是历史改进——别把它写成「优化了收益」。**
但它离生效只差一天:2026-08-14 玻璃主力还是 FG2609、**只剩 11 个交易日**,
08-17 才换到 FG2701,而页面当时对此只字不提。界面因此加了「可持有 还剩 N 个交易日」
的倒计时(撞线显示「必须平仓」),数据来自 payload 的 `delivery` 块。

**`delivery` 与 `rules.exit_before_delivery` 在前端都是可选字段**:前端先于引擎上线,
当晚引擎跑过之前线上 JSON 还是上一版,写成必填会让整页白掉——已有测试钉住这条。

---

## DEC-090 合计流向引擎改为「次日开盘成交」(2026-08-19)

运营者要求审查 LH/FG/SA 的进出场规则有没有过拟合或未来函数,查出来的。

**问题**:引擎的进场与出场**都按信号日的结算价成交**,而**席位持仓排名是收盘之后
才公布的**——大商所约 15:30-16:00、郑商所约 16:26,我们自己的采集就是 16:00 与
17:30 两轮。**拿 16:26 才拿到的数据去按 15:00 的结算价成交,做不到。**

原代码的注释写着「T+1:今日收盘算出的信号,吃的是明日收益」,这句本身没错(它确实
不算信号日当天的收益),但它只解决了一半:成交价用的还是信号日的结算价。

**改法**:信号日收盘出信号,**次日开盘成交**。生猪没有夜盘,次日开盘是它唯一能成交
的时点;玻璃纯碱有夜盘,现实比这条口径还好一点,这里取保守的。
三条计价线全部逐合约、都不跨合约相除:`ret_open`(开→开,持仓净值走它)、
`o2c`(同日开→结算,止损判的是**今天收盘时已知的**浮亏)、`ret`(结→结,留给基准)。

**代价——这是本条最该记住的部分**:

| | 旧口径(信号日结算价) | **新口径(次日开盘)** |
| --- | --- | --- |
| 生猪 LH | 18 笔 净 +88.4%/胜率 72.2%/回撤 −4.1%/夏普 2.80 | **18 笔 净 +79.8%/61.1%/−6.8%/2.23** |
| 玻璃 FG | 206 笔 净 +4264%/胜率 56.8%/回撤 −19.4%/夏普 1.63 | **207 笔 净 +385%/51.7%/−43.1%/0.61** |
| 纯碱 SA | 100 笔 净 +1522%/胜率 64.0%/回撤 −23.6%/夏普 2.02 | **101 笔 净 +244%/46.5%/−53.3%/0.80** |

**收益几乎全部集中在信号后的第一天,而那一天恰恰是拿不到的。** 玻璃缩水 91%、
纯碱 84%,胜率双双掉到抛硬币附近,夏普从 1.6~2.0 掉到 0.6~0.8,回撤翻倍。
**生猪几乎没受影响**(单笔均值 3.81%→3.48%),因为它的信号更慢。

逐年也跟着变:玻璃为负的年份从 1/13 变成 4/13(2015/2017/2018/2024),
纯碱从 0/6 变成 1/6(2025)。

**方案 C 的三方案对比在新口径下重算**(同一时间轴 2023-08 起):
A 现有单信号 21 笔 +66.7%/61.9%/−12.0%/1.79;B 散户反向 21 笔 +73.8%/57.1%/−8.0%/1.74;
C 共振进场 18 笔 +79.8%/61.1%/−6.8%/2.23。**单笔均值差的 t 仍然只有 0.13/0.49,
统计上分不出高下** —— 选 C 依旧是运营者的判断,不是数据证明。

**顺带修掉一个从没人发现的错**:逐日净值原本在 `build_payload` 里用
`pos.shift(1) × 结算价收益` **另算**一条,与逐笔记账不是同一条曲线,对不上也不报错
——夏普和回撤描述的是另一个策略。现在 `replay` 直接产出逐日净值,并在函数末尾
**断言「逐日连乘 == 逐笔连乘」**,对不上直接抛错(该品种保留上一版 JSON)。
断言时要**排除持有中那笔**:它的浮盈盯最新结算价,逐日走到最新开盘,差半天。

研究侧 `run_lh_compare_v2` / `run_fgsa_planc` / `run_lh_phase2.main_returns` 同步改口径,
`run_flow_parity` 三品种逐笔一致。

**审查的其余结论(没有发现别的未来函数)**:
- 席位选人 `alpha_upto` 严格 `trade_date < cut`,选完从 cut 起生效;alpha 用昨日持仓
  乘今日价格变动,因果正确。
- 主力合约用**前一日** OI 决定、次日生效、只向远月切换。
- 全文无 `shift(-N)` / `center=True` / `bfill` / `expanding`,三处 `rolling` 全是后视窗。
- 共振过滤器方向对称(聪明钱 z 为负 50.5%~54.8%,进场触发看空/看多 90:62、332:344、
  162:165),不存在「一侧恒真」的结构性偏袒。

**过拟合方面留下的三个风险点(没改,记录在案)**:
1. `long_enabled`(生猪关)与 `long_needs_dip`(玻纯关)是**逐品种、在同一份样本上**
   定的,属于样本内选择。其中**纯碱这条值得复议**:新口径下带 dip 的夏普几乎一样
   (0.78 vs 0.75)但**回撤好很多**(−40.1% vs −53.3%)。
2. 参数曲面很毛(纯碱 `sig_win` 3→5 累计从 +676% 掉到 +264%),**累计百分比这个
   指标本身不稳**,看单笔均值与 t 值更靠谱。好消息是选中值大多**不在峰值上**,
   而且 `enter/stop/max_hold/group_k` 是在生猪上定的、玻纯直接套用(样本外迁移)。
3. 新口径下的显著性:LH t=+2.52、FG t=+2.31、SA t=+1.94。**纯碱已经不显著了。**

---

## DEC-091 为什么「散户反向很准」与「策略回撤 −43%」同时成立(2026-08-19)

运营者问:「之前测出散户席位是很好的反向指标,这次为什么回撤这么大?」
两件事都对,不矛盾。查清楚了,顺带否掉两个我先猜错的方向。

**先排除掉的**:
- **不是 DEC-090 那个日净值 bug 造成的。**跑旧引擎(48ae390)拿旧口径的逐笔,
  用**正确的**日净值构造法重算:回撤仍是 −19.4%(玻璃)/−23.6%(纯碱),
  与上线过的数字一模一样。**回撤变大 100% 来自成交口径的改变。**
- **不是「事件型信号被当连续持仓用」。**平均只持 11~12 天;把 `max_hold` 压到
  5/10/15/20 天,玻璃回撤是 −51.1/−41.0/−44.6/−44.8%,**没有变好**。
- **不是可以靠延迟进场躲开的。**把进出场再推后 1/2/3/5 天,三个品种全线更差
  (玻璃夏普 0.58 → 0.40/0.55/0.33/0.22)。

**真正的原因:信号准的那一段,结构性地拿不到。**
按信号方向统计触发后第一天的超额,再把它拆成「隔夜跳空」与「日内」:

| 品种 | 触发数 | D→D+1 全天 | 其中隔夜跳空 | 其中日内 | **跳空占比** |
| --- | --- | --- | --- | --- | --- |
| 生猪 LH | 151 | +0.38% | +0.26% | +0.12% | **69%** |
| 玻璃 FG | 676 | +0.51% | +0.44% | +0.07% | **86%** |
| 纯碱 SA | 327 | +0.78% | +0.65% | +0.14% | **83%** |

席位持仓排名 16:26 才公布,**所有人同一时刻看到同样的数据**,价格在夜盘/次日开盘
一步跳过去。指标预测的确实是次日方向——但 83~86% 的兑现发生在你还没法下单的
那个缺口里。剩下能吃到的日内 +0.07%~+0.14%,对玻纯 1.5% 量级的日波动就是噪音,
所以净值曲线又毛又深。

**这也解释了为什么生猪几乎没受影响**:①它跳空占比最低(69%);②它的信号有中期
余温——D+1→D+5 +0.84%、D+5→D+20 +1.05%,而玻璃对应只有 +0.27%/+0.43%。
生猪不靠那一跳,玻璃基本上只有那一跳。

**处理**:不改规则(没有可改的——三个方向都试过了)。改成**在页面上讲清楚**:
`edge_split` 由引擎每天实算,写进 caveats,数字不写死。

**运营者追问「跳空看着不大,为什么收益差这么多」——三个放大器**(`run_gap_math.py`):

1. **跳空绝对值确实不大,但它不该跟价格比,要跟单笔利润比。**
   |跳空| 中位数:生猪 0.34%、玻璃 0.47%、纯碱 0.67%。可是**跳空标准差 ÷ 日波动
   = 0.70**(三个品种一模一样)——**一天的波动有七成发生在隔夜**。而玻璃的单笔
   利润只有 +2.18%,跳空一来一回就吃掉一半。
2. **两头各丢一次,不是一次。**进场时错过顺向的跳空;出场信号是「反向极值」,
   那个反向本身也有一部分发生在跳空里,于是出场还要**吃下**一段逆向跳空:

   | 品种 | 旧单笔均值 | 进场跳空(丢) | 出场跳空(吃) | 合计 | 新单笔均值 |
   | --- | --- | --- | --- | --- | --- |
   | 生猪 | +3.42% | −0.25% | **+0.08%** | −0.17% | +3.57% |
   | 玻璃 | +2.18% | −0.52% | −0.54% | **−1.06%** | +1.00% |
   | 纯碱 | +3.21% | −0.76% | −0.61% | **−1.37%** | +1.61% |

   **生猪的出场跳空是顺的(+0.08%)**,所以它两头相抵几乎不受影响;玻纯两头同向扣。
3. **笔数是最后那个放大器。**生猪 18 笔 × 单笔差 0.24 点 = 总差 9 个点;
   玻璃 206 笔 × 1.14 点,连乘之后**差 10 倍**(+5247% → +436%),纯碱差 4.9 倍。

**给以后的教训**:验证一个信号「准不准」和它「赚不赚得到」是两件事。
事件研究里的超额收益必须**再拆一次**——落在跳空里的部分,对所有同时看到数据的
人来说都是拿不到的。**任何以「盘后公布的数据」为输入的信号,都要先做这个拆分。**
而且别只看进场那一头:**出场同样要付跳空的钱**,反向出场尤其贵。

---

## DEC-092 玻璃/纯碱保留,页面顶部加数据驱动的风险条(2026-08-19)

DEC-090/091 之后运营者的裁定:**两个页面都留,加醒目风险标识**(生猪维持不动)。

**关键是这条风险条不是按品种硬编码的**——门槛写死在引擎 `risk_flags()` 里、数字
每天实算,够不上门槛就是空数组。哪天玻璃真的变好了条目自己消失,哪天生猪变差了
它自己会挂上来。当前:**生猪 0 条、玻璃 3 条、纯碱 5 条**。

五个门槛与它们各自的意思:

| 门槛 | 意思 |
| --- | --- |
| 夏普 < 1.0 | 一年赚到的抵不上一年的波动 |
| 最大回撤 ≥ 25% | 单次回撤超过四分之一,多数人拿不住 |
| 胜率 < 50% | 不到一半的交易赚钱,全靠少数几笔大赢撑 |
| 单笔均值 t < 2.0 | 统计上分不出与 0 的差别,**等于还没验证** |
| 亏损年 ≥ 1/4 | 四年里有一年亏,不是偶发 |

**位置在收益数字之前**(副标题下、状态条上)——放在下面等于没放,有测试钉住顺序。

顺带修掉一个一直存在的显示问题:引擎的文案(caveats)一直用 `**加粗**` 记法,
而页面用 `v-text` 渲染,星号被原样印在生产页面上。改用 `mdBold()`(先转义、
只认 `**`)。

**判定各品种成色的依据(留档,别再重新推一遍)**:
- **生猪 LH**:18 笔/3.0 年,年化 21.4%,夏普 2.23,回撤 −6.8%,胜率 61.1%,t=+2.52。
  可以跟,**但只有 18 笔、3 年、而且全程只有熊市一种市况**——牛市里如何,样本没答案。
- **玻璃 FG**:207 笔/13.6 年,年化 12.3%,夏普 0.62,回撤 −43.1%,t=+2.31,13 年 4 年亏。
  边际是真的(样本量够,统计站得住),但**不能按 +436% 那个印象去做**。
  另外:聪明钱信号**单独在玻璃上是失效的**(只做空 126 笔 −20.9%/夏普 −0.01),
  散户反向单独也很弱(0.16),只有共振 + 双向才有 0.58 ——玻璃对「共振」这个组合
  方式依赖很重,而共振当初是在生猪上定的,没在玻璃上独立验证过。
- **纯碱 SA**:101 笔/6.2 年,夏普 0.80,回撤 −53.3%,胜率 46.5%,**t=+1.94 已不显著**。
  我建议过降级为观察指标,运营者裁定保留并加风险标识。

**别再重复的一个误解**:玻璃纯碱的**原始信号比生猪还强**(信号后首日超额
生猪 +0.38%、玻璃 +0.51%、纯碱 +0.78%)。问题不是「指标不准」,是准的那部分
落在拿不到的跳空里(DEC-091)。

**另一件已确认的事实**:玻璃纯碱行情来自 `czce_official`,郑商所日行情按**交易日**
口径,`open` 就是**夜盘 21:00 的开盘价**。席位数据 16:26 公布、21:00 开盘——
**那已经是数据公布后最早能成交的时点**,不存在「还有更快的窗口没用上」。
生猪无夜盘,是次日 09:00。

---

## DEC-093 机构资金加鸡蛋 JD 与焦煤 JM(2026-08-19)

运营者要求「类似生猪聪明钱跟随」。**先验再接**——PITFALLS 那条铁律:同一套规则换
品种必须重新验,玻璃当初就是带着生猪的 `long_needs_dip` 跑,少了几十笔。

**数据前提**:JD/JM 的席位数据都从 **2023-08-11** 起(大商所,与生猪同一个起点),
样本同样只有三年、一种市况。行情本身能回到 2013,但**信号的样本长度由席位定**。

**实测(方案 C、次日开盘成交、与线上同口径)**:

| 品种 | 做多 | 笔数 | 净累计 | 回撤 | 夏普 | 胜率 | t 值 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 生猪 LH | 关 | 18 | +79.8% | −6.8% | 2.23 | 61.1% | +2.52 |
| | 开 | 36 | +81.4% | −14.6% | 1.58 | 50.0% | +2.05 |
| **鸡蛋 JD** | **关** | 13 | +33.8% | −13.6% | 1.04 | 38.5% | **+1.08** |
| | 开 | 26 | +21.0% | −23.8% | 0.52 | 46.2% | +0.55 |
| **焦煤 JM** | **关** | 22 | +89.7% | −14.7% | 1.11 | 68.2% | **+2.61** |
| | 开 | 45 | +90.5% | −38.6% | 0.85 | 53.3% | +1.51 |

**两个品种都关做多**——开着一律夏普更低、回撤更大、t 更小。三个大商所品种一致。
**但别把这读成三个独立证据**:它们共用 2023-08~2026-08 这同一段行情,只能算一次
观察;而且这是**在同一份要报告成绩的样本上做的选择**,属于样本内选择(DEC-090)。

**结论分开说,两个品种成色差很远**:
- **焦煤 JM 站得住**:t=+2.61、夏普 1.11、回撤 −14.7%、胜率 68.2%,而且**大幅跑赢
  基准**(恒定满仓做空 +18.2%/夏普 0.34/回撤 −45.2%)。风险条 **0 条**。
  限制与生猪相同:22 笔、3 年、单一市况。
- **鸡蛋 JD 没验出来**:t=+1.08、胜率 38.5%、只有 13 笔,而且**风险调整后还不如
  躺着满仓做空**(夏普 1.04 vs 基准 1.14)。风险条 **3 条**,页面自己会说清楚。
  运营者要这个品种,那就上,但它现在与「未验证」没有区别。

跳空占比(DEC-091 那把尺):鸡蛋 74%、焦煤 76%,与生猪 69% 同量级——
没有比生猪更糟,但同样的结构性损耗都在。

**顺带加的一条风险门槛**:`风险调整后还不如躺着满仓做空`(策略夏普 ≤ 基准夏普)。
鸡蛋就是被这条抓出来的。其余四个品种都不触发。

**工程上的收口**:
- 前端品种按钮由 `FLOW` 列表 `v-for` 渲染,**加品种只改那一行**,不再逐个写死;
  `FlowCode` 类型收归 `api.ts` 一处。有测试钉住按钮清单。
- `run-smart-money.sh` 的导出品种、`FLOW_CODES`、拷贝清单三处一起改(旧教训:
  只改一边不报错,只是那个品种没数据)。
- `run_flow_parity.py` 扩到五品种并显式给 `FLOW_CODES`(不给的话引擎按默认只跑三个,
  新品种产物根本不存在);`SHORT_ONLY` 表必须与引擎 `VARIETIES` 一致。
- 交割纪律(DEC-089)、风险条(DEC-092)、跳空说明(DEC-091)对新品种**自动生效**,
  一行没改——这正是当初写在共用 RULES 里的用意。

---

## DEC-094 跨月价差引入机构资金:**验过了,不成立,不接**(2026-08-19)

运营者问:套利监控里玻纯引入了机构资金判断,生猪/焦煤/鸡蛋能不能也这样?
(苹果运营者明确不做,后续不再提。)

**第一层:FG-SA 那条信号在结构上就搬不过来。**
FG-SA 是**跨品种**——两个品种各有自己的合计流向,相减才有方向。而这三个品种在
套利监控里**全是同品种跨月**(生猪 10 组、焦煤 12 组、鸡蛋 15 组;FG-SA 是全平台
唯一的跨品种组合)。机构资金引擎产出的是**品种合计流向**,一个品种一天只有一个数,
对跨月价差两条腿是同一个数,**相减恒等于 0——数学上零信息**,不是效果差。

**第二层:换成逐合约流向差,这条路是通的,而且有理由试。**
席位数据本身逐合约,所以可以算
`signal = Δ净持仓(聪明钱, 近月) − Δ净持仓(聪明钱, 远月)`。
DEC-083 当初嫌「逐合约信号 84.6% 的日子符号互相打架」——**对跨月价差,打架恰恰
就是信号**。所以验了。

**第三层:验的结果是不成立。两个独立的理由。**

*(a) 数据不够。* 两条腿同一天**都有**聪明钱持仓的组合-日只占:
生猪 37%、鸡蛋 39%、**焦煤 19%**。根因不是掉榜(在榜的合约里聪明钱 100% 至少一家
在场、焦煤 79% 五家全在),而是**大商所只对达到门槛的合约公布持仓排名**,远月与
清淡合约常年整天不在榜:覆盖率 <60% 的合约,生猪 8/23、焦煤 14/24、鸡蛋 14/24。
再要求算 z(120 日窗、40 个样本)之后只剩 4~16%,**焦煤连一个能做横截面检验的
交易日都凑不出来**。

*(b) 去掉 z 之后信号也不成立。* 横截面排序本来就不需要时序标准化(改用当日组内
排序,覆盖率回到 19~39%),主检验用**逐日多空组合**(每天买 z 最高的三分之一、
卖最低的三分之一),这样横截面相关被抵掉,t 值才算得准:

| 品种 | 可用天数 | 日均点数 | t 值 | 胜率 |
| --- | --- | --- | --- | --- |
| 生猪 | 148 | +13.38 | **+1.99** | 56.1% |
| 焦煤 | **52** | +0.56 | +0.33 | 51.9% |
| 鸡蛋 | 238 | −2.00 | **−1.07**(反向) | 50.0% |

生猪那个 +1.99 看着擦边,**但一动就塌**:

| 流向窗 \ 持有期 | 1 日 | 5 日 | 10 日 |
| --- | --- | --- | --- |
| 3 日 | **+0.21** | +1.25 | +1.47 |
| 5 日 | +1.99 | +2.00 | +1.73 |
| 10 日 | +1.20 | +1.91 | +0.57 |

相邻档不同向、峰值孤立(窗 3/持有 1 那格几乎是零),逐年也都不显著
(2025 t=+1.88、2026 t=+0.79)。**典型的噪音峰。**
而且到这一步我已经试过两种构造(带 z / 不带 z),任何 t 值都该再打个折。

**结论:不接。** 不改生产,不上页面。探针留在 `research/run_calspread_flow.py`。

**什么情况下值得重验**:席位历史变长(大商所现在只有 2023-08 起的三年),
或者出现一个远月常年在榜的品种。**但不要因为「换个参数好像行」就回头接**——
上面那张表就是为防这件事留的。

---

## DEC-095 套利监控加平台位、到达概率、卖点与止损(2026-08-20)

运营者指出页面缺了他实际下单看的那一维:**价差自己图上的横盘位**。原话——
「上一轮 −1700 到 −1355 就上不去了,−1355 就是向上的平台位;收盘突破平台位,
才能继续往下看」。此前套利监控有区间位置、历年分位、回归统计、基差,
**唯独没有平台位,也没有卖点和止损**。

### 一、平台位的口径(参数是试出来的,不是拍的)

转折位 = 收盘价差是**前后各 3 个交易日**里的极值。窗口大小拿运营者点名的 −1355 试:

| 窗口 | 台阶数 | −1355 在不在 |
| --- | --- | --- |
| ±2 | 19 | 在,但 −1150/−1155/−1160/−1170 被拆成四档 |
| **±3** | **11** | **在** |
| ±5 | 8 | **丢了** |
| ±7 | 7 | **丢了** |

三条工程纪律,每条都对应一个会犯的错:
- **整窗 7 行**(`cnt = 7`):序列两端窗口不全会造出假转折。
- **因果**:t 处的判定要到 t+3 才成立,`as_of` 只能看到 `rn+3 ≤ rn_asof` 的转折位。
  不加这一条,历史行就带着未来信息(与 `pair_series` 那段注释同一个道理)。
  **代价:最近三个交易日的转折当天不算数**,界面要写「待确认」。
- **存区间不存单点**:50 点内并档会链式变宽(−1080/−1115/−1155 并出跨 75 点的一档),
  只报均值 −1117 是假精度,所以 `lo`/`hi` 一起存,跨度 ≥30 点时界面显示区间。

序列用**完整价差历史**,不是监控窗口那一段——LH2611−LH2705 在监控表里只有两个月,
只能识别 3 档,完整历史有 6 档。

### 二、到达概率:验过了,但只能给一半

**能给**:这一档在噪音尺度上有多远、历史上够得着的比例(14.4 万个观测)。
**不能给**:往哪边走。方向由运营者那条规矩定(日线收盘突破平台位)。

验收过程(`research/run_shelf_prob.py`):
- ✓ 单调:79.5 → 64.0 → 51.7 → 42.0 → 34.2 → 28.4 → 19.5 → 13.3。
- ✓ 剩余期分桶:5~20 日那档明显低于中间档,不分桶会在快到期时给虚高的数。
- ✗ **逐品种、逐方向样本外崩了**:焦煤 25.3%→47.7%、生猪 69.3%→51.9%。
  我原以为「焦煤粘、生猪活」是品种特性,**样本外证明那只是那段行情的漂移**。
  方向同理(生猪向下 78.6%/向上 49.6% 就是那三年的熊市)。**两维都合并掉。**
- ✓ 合并后按剩余期分桶,样本外差 ≤2.6 个点(剩余 ≤80 日的三档)。

**逐年离散必须写在界面上**:z=1.0 长期中位 42%,最低 17%(2014)、最高 53%(2019)。

曲线**固化成 Rust 常量,不每晚重算**——这是研究结论,应当随代码评审一起变,
不该因为多了一天数据就让页面上的数字无声漂移。重估跑 `run_shelf_prob.py`。

### 三、卖点与止损

角色按 DEC-088 的同一个 `trade_side` 派:做空价差(high)时下方全是卖点、
**上方最近的一档是止损**;做多价差(low)整个翻过来。与运营者的规矩对称——
「收盘突破平台位就前往下一个平台」,反过来突破就是止损。没有交易侧就不派角色。

### 四、界面(运营者在 A/B 两版设计稿里选了 A:直接写百分比)

摘要一行常驻(止损 + 前两个目标 + 概率),完整阶梯用 `<details>` 折叠——
每行挂 8 档会把整页压垮。选 A 的理由是他要拿概率排序挑目标档位,数字比标签好用;
代价是百分比天生显得精确,所以「长期频率·逐年 17~53%·不含方向」与它写在同一块。

### 五、工程

- 迁移 `202608200001_spread_shelves.sql`:`shelves jsonb` + `spread_sigma numeric`,
  已登记进 `deploy-futures.yml` 的显式清单。
- 计算在 `compute-spread-monitor.sql`(存事实),距离/z/概率/角色在 Rust **读时算**
  (不存结论),与报警/拐头/合格同一条纪律。
- 上线前把整份 `compute-spread-monitor.sql` 在**生产库里跑了一遍再 rollback**,
  确认解析通过、448 行写入、平台位与手算一致。**注意**:试跑时用
  `add column if not exists` 在生产上提前加了这两列,迁移仍会跑(幂等),
  它负责补 check 约束与 `schema_versions` 那一行。

---

## DEC-096 持仓留在自己的合约里,交割纪律查持仓合约而不是当日主力(2026-08-20)

运营者指着玻璃那一行:「fg2609 触发了进场信号,最后时间不够了需要平仓,
如果 2701 的位置好,直接平完仓到 2701 开仓」。页面上是
`进场 966 · FG2609` 配 `现价 912 · FG2701`,+13.01% 一路算下来。

**根因**:DEC-089 的交割纪律查的是「**今天主力合约**还剩几天」,不是「**持仓所在
合约**还剩几天」。主力一换,检查对象就变成远月,老仓免费滚过去。
FG2609 窗口止点 08-31,08-14 时只剩 11 个交易日(**差一天撞线**),08-17 主力换到
FG2701——那笔仓从此再没被检查过,而它本该在 08-17 被强制平掉。

**免费滚过换月的那些笔,收益普遍高好几倍**:鸡蛋 +7.38% vs −1.83%、
纯碱 +6.59% vs +0.87%、玻璃 +3.21% vs +0.58%。它们拿到了一次**不用付钱、也不用
重新判断**的续命。

**改法(运营者原话那条规矩)**:持仓留在**进场时那个合约**里,持到**它自己的**窗口
止点前 10 个交易日强制平。主力换月不影响它。进场仍按当日主力开——换月之后要不要
在新合约上开,是新的一次判断(「2701 位置好就到 2701 开仓」)。

考虑过更严的「换月即平仓」,**没采纳**:它不是运营者说的那条,而且实测在生猪上
代价最大(+85.0% → +69.8%)。而对「跟合约」唯一的顾虑——持有非主力合约期间没有
成交——**实测不成立**:换月后还要拿 1~6 天(最长玻璃 21 天),那些天
**无成交价的比例是 0%**,五个品种都是。

**结果(修掉反而更好)**:

| 品种 | 改前 | **改后** |
| --- | --- | --- |
| 生猪 | 18 笔 +79.8%/−6.8%/2.23/61.1% | **18 笔 +85.0%/−5.0%/2.25/66.7%** |
| 鸡蛋 | 13 笔 +33.8%/−13.6%/1.04/38.5% | 15 笔 +29.8%/−13.6%/0.96/46.7% |
| 焦煤 | 22 笔 +89.7%/−14.7%/1.11/68.2% | 22 笔 +90.4%/−14.7%/1.11/68.2% |
| 玻璃 | 207 笔 +389%/−43.1%/0.62/51.7% | **214 笔 +442%/−45.9%/0.65/50.9%** |
| 纯碱 | 101 笔 +242%/−53.3%/0.80/46.5% | **104 笔 +256%/−55.8%/0.84/46.2%** |

**`临近交割` 这个出场原因第一次真的触发了**(生猪 1 笔、玻璃 18 笔),以前一直是 0
——因为它从来没检查过持仓所在的合约。该在交割前平掉的仓不再硬扛到远月,
所以修完反而更好。

**修的过程中犯了两次同类错误,都被断言当场抓住(值得记)**:
1. 出场成交被延后时,新仓在老仓真正平掉之前就开进去,两笔持仓日重叠。
   改成:次日没成交价就继续持有,理由挂着等到能成交那天再平。
2. **记账用简单收益 `−(p_out/p_in−1)`、日净值用逐日连乘**——做空时两者不相等
   (生猪 +79.6% vs +88.3%)。引擎其余部分(基准、夏普、回撤)全是连乘口径,
   所以记账与止损判据都统一成连乘。

**做多开关在新口径下重验**(它是活参数,口径变了必须重来):生猪/焦煤/玻璃/纯碱
四个结论不变;**鸡蛋翻了**——开做多且要 dip 每项都略好(夏普 1.02 vs 0.96、
回撤 −11.5% vs −13.6%、t +1.78 vs +1.49)。**2026-08-20 运营者知情后拍板打开**
(开做多必须配 dip:不要 dip 掉到 0.60/t+1.08)。开完 25 笔(空15/多10)、
净 +42.4%/回撤 −11.5%/夏普 1.02/胜率 52.0%/t=1.78,风险条从 3 条降到 2 条
(胜率过半那条清了,「不如躺着做空」与「t<2」仍挂着)。
**我提示过这属于样本内选择、差距也在噪音量级内**——不许在后续文档里写成「实测最优」。

**对拍口径同步换掉**:DEC-096 之前对的是 `run_lh_compare_v2`/`run_fgsa_planc`,
它们跟着主力走。持仓改成跟合约之后再拿它们对拍,就是**拿旧口径给新口径背书**——
门禁会一直绿但什么都没守住。现在对的是 `run_roll_fix` 的 hold_contract 口径,
五品种逐笔一致(18/15/22/214/104)。

---

## DEC-097 有了平台位之后,四个历年点数怎么摆(2026-08-20)

运营者问:「最有利 +1360 · 持到期 +635」和「补仓参考 −200 · 风险预留 −4965」
这两组数是不是没用了?逐个看,结论不一样。

**页面上当时有一处硬矛盾**:同一行里并排摆着「风险预留 −4965 点」与
「止损 −885」(距现价 −935 只有 **50 点**),而三步用法写着「仓位按风险预留那个点数算」。
按 4965 算是 79,440 元/手的预留,按止损算是 800 元/手 —— **差 100 倍**。
有了止损之后真实风险就是到止损的距离,继续按历年最坏 MAE 定仓位等于把仓位
压到实际风险的百分之一。

| 数字 | 处理 | 理由 |
| --- | --- | --- |
| 最有利 +1360 | **收进悬停** | 就是运营者前一天挑出问题的那个:五个历史锚点 −815/+90/+685/+2645/+3305,今天 −935 在分布之外,点数搬不动。平台位阶梯已经取代它。 |
| 持到期 +635 | **符号留用,点数收进悬停** | 它不是目标,是**资格判据**:`✓合格` 徽标要求它 >0,防的是「回归率 100% 却一路朝反方向走」(JD2612/JD2701 12/12 回归、持到期 −166)。但点数同样跨起点不可比。 |
| 补仓参考 −200 | **留在行内,改叫「历年常态浮亏」** | 它答的是「浮亏到多少算常态」,平台位答的是「图上哪里说明我错了」,两件事不互相取代。 |
| 风险预留 −4965 | **不再当仓位分母,改叫「跳空最坏参考」** | 仓位改按**到止损的距离**算。它剩下的唯一用处是「止损被跳空穿掉能穿多远」—— 止损不是保证成交价。 |

行内因此只剩三件事:**去哪**(目标+概率)、**错在哪**(止损,并列出距离)、
**路上会有多难受**(历年常态浮亏)。不再有两套互相打架的仓位算法。

止损那一档现在**把距离一起印出来**——它是仓位分母,不是装饰。

引擎与数据一行未动,只改前端与文案。

---

## DEC-098 并档阈值按品种波动定;行内加双向盈亏比(2026-08-20)

### 一、「5/5 年曾回归」这个数接近没有信息量(运营者指出)

运营者:「同一个价位,往下做统计的是小于它的,往上做统计的是大于它的,这样不会
冲突吗?」——会,而且几乎总是同时成立。判据是对称的:

```
高位侧(做空)  hit = fmin < p0     低位侧(做多)  hit = fmax > p0
```

只要价差在窗口内上下动过一次(哪怕 1 个点、1 天),两边同时算「回归」。全表实测:

| | 行数 | 占比 |
| --- | --- | --- |
| 高位侧 100% | 51,431 | 63% |
| 低位侧 100% | 47,853 | 59% |
| **两边同时 100%** | **33,583** | **41%** |
| **两边都过 80% 门槛** | **68,423** | **84%** |

它问的不是「会不会回归」,是「**这条价差动没动过**」。SQL 注释里其实早就写了
「单看 hit/n 会骗人:剩余期一长它就趋近 100%」,但**还是把它当成资格门槛的第一条,
并且把它当成行内字号最大的那个数字印出来**。

`✓合格` 没有因此误判——真正在筛的一直是第二条 `持到期 > 0`(它有方向、不对称,
两侧不可能同时成立)。**但页面把单条件写成了两条。**
运营者确认:撤掉行内那个大号百分比、合格判据只留持到期,两条都没问题。

**实现与影响**:`isQualified` 去掉 `rate >= 0.8`,只留 `drift > 0`;行内那个
`.rate` 大号数字撤掉,`hit/n` 收进「历年点数 ⓘ」的悬停,行内只留「剩 N 天」。
全表实测:旧口径 43,140 行合格 → 新口径 45,301 行,**新增 2,161 行(占 2.7%)**。
幅度这么小正好印证了那条门槛几乎不筛东西。

**记一笔**:DEC-098 那次提交的信息里已经写了「撤掉行内大号百分比、合格只留
持到期」,但**代码当时并没有改**——是下一条提交才补上的。提交信息不是意图书,
写了就得是已经做完的。

### 二、并档阈值写死 50 点,与品种波动脱钩(运营者的例子暴露的)

运营者举焦煤 2609−2701 说明方向风险不对称,我去算,发现**这个组合的阶梯是废的**:

```
level −205,  区间 −276 ~ −126,  触碰 6 回合     ← 一档跨 150 点
```

该组合日波动只有 **15.8 点**,50 点阈值 = 3.2σ,把 31 个转折位链式并成了一档,
宽度是十个日波动。而 50 这个数是在**生猪**(σ≈94,50≈0.5σ)上试出来的——
**又一次把一个品种上试出来的常数套到所有品种**(PITFALLS 那条我当天早上刚写过)。

改成 **0.5σ**(σ = 该组合近 20 日价差日变动标准差),触碰带同步改 0.25σ:

| 组合 | σ | 并档带 | 档数 | 最宽一档 |
| --- | --- | --- | --- | --- |
| JM2609−JM2701 | 15.8 | 7.9 | **1 → 7** | 150 → 38 点 |
| JD2609−JD2701 | 50.1 | 25.0 | 11 | 55 点 |
| LH2611−LH2705 | 94.4 | 47.2 | **6(不变)** | 75 点 |

**生猪一档没变**——0.5σ=47 点与原来验证过 −1355 的那个 50 点几乎一样,那次验证
依然成立。全表平均 7.9 档。

### 三、行内加双向盈亏比(运营者要的那件事)

运营者:「二月往下做价差没问题,但到了 −200 附近只能往上做,虽然往下也能赚钱但
风险更大;往上至少能回到 −150 的平台。**我担心不同方向的风险性问题**。」

页面此前**只显示拐头那一侧**的目标与止损,另一个方向的盈亏比根本看不到,
这种不对称只能靠脑子记。而 `持到期` 判不了它:**它只说历年最终朝哪边走,
不说去那边的路有多难**——一个方向持到期为正、却要先扛五百点浮亏换一百点目标,
照样是笔烂交易,而合格判据照样盖章。

行内因此并排给两个方向的**第一目标(距离+到达概率)、止损(距离)、盈亏比**,
信号指的那一侧加底色。盈亏比 = 到第一目标 ÷ 到止损,**<1 用警示色**。
数据全部来自已有的阶梯,没有新采集、没有新统计。

**明确不做的一件事**:把盈亏比当成合格判据的一部分。DEC-067 记着盈亏比分级
(move/MAE)被回测否决过——「>2.5 重点档实际持到底中位 −0.2% 全场最差」。
要重来必须先弄清那次为什么失败,不能因为这次看着有道理就直接加。

`edgeOf` / `shelfLabel` / `offsetText` 抽到 `frontend/src/shelf.ts` 并配测试:
埋在 `<script setup>` 里导不出来就测不到(revert.ts、seatChange.ts 都是这么栽的),
而方向反了、盈亏比分子分母倒了,页面照样好好显示。

---

## DEC-098 附录 盈亏比重测:又一次否决,这次弄清了它为什么看着像有用(2026-08-20)

运营者问「拿盈亏比进合格判据做了吗」——**没做**,而且补测之后**不该做**。

### 一开始的结果好看得离谱

以 σ 为单位、按盈亏比分档看期望值,**完美单调、恰好在比值=1 处穿零**:

| 盈亏比 | <0.5 | 0.5~0.8 | 0.8~1.2 | 1.2~2 | 2~3 | 3~5 | >5 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 期望(σ) | −0.247 | −0.084 | −0.006 | +0.083 | +0.119 | +0.250 | +0.322 |
| t | −20.6 | −3.9 | −0.3 | +4.0 | +5.0 | +9.8 | +19.3 |

按**组合**聚类去掉重叠之后仍然成立(>5 档 t=+16.8,跨 400 个组合)。
与赌徒破产理论值 `P = 1/(1+盈亏比)` 相比,>5 档理论 8.9%、实测 26.6%,**三倍**。

### 但它是假的:收盘价判触碰造出来的

理论公式假设**连续障碍**,而我用**收盘价**判触碰。止损只有 0.2σ 远时,价格可以整天
贴着它而没有一根收盘价越过去——**这个漏计恰好在「止损很近、盈亏比大」时最强**,
正好造出那条单调曲线。

正确的对照不是理论公式,是**同样离散、同样波动、但没有平台结构的随机路径**:
把每个组合自己的日变动打乱重采样,barrier 与起点完全不变,每个观测配 3 条:

| 盈亏比 | 真实期望σ | **随机路径期望σ** | 差 | t |
| --- | --- | --- | --- | --- |
| <0.5 | −0.226 | −0.279 | +0.052 | +1.39 |
| 0.8~1.2 | −0.041 | +0.022 | −0.064 | −0.80 |
| 2~3 | +0.169 | +0.210 | −0.042 | −0.54 |
| **>5** | +0.253 | **+0.399** | **−0.147** | **−3.21** |

**打乱之后那条漂亮的单调曲线一点没少,而且比真实的还陡。**
真实减随机六个档五个为负,唯一显著的那个是 −3.21。
**扣掉机械偏差之后,平台位的盈亏比没有正的预测力,还略微反向。**

### 结论与处置

- 盈亏比**只作显示**,回答「这笔交易的形状」;**不进合格判据**。
  线上 `isQualified` 只有 `drift > 0` 一条(DEC-098),盈亏比一个字都没进去。
- 运营者决定**不在界面上加免责说明**(B 方案),结论留在文档里。
- 探针:`research/run_rr_test.py`。

**给以后的教训(这条比结论本身值钱)**:用离散数据(收盘价)去检验连续模型
(赌徒破产、首次通过时间)的预测,**离散化本身就会造出单调的伪效应**,而且偏在
「障碍很近」的那一侧。**对照组必须与被检验的数据同样离散**——把序列打乱重采样、
barrier 保持不变,是最省事的做法。这次差一步就报了一个假发现。

---

## DEC-099 部署自动重算信号 + 页面在指纹对不上时露馅(2026-08-20)

运营者在 DEC-096 上线的第二天问:「昨天不是改了进场出场吗?为什么还显示
进场 2026-07-06 @ 966 · FG2609?」

**根因不是改动没生效,是我忘了重跑引擎。**

```
hog_money.py     Aug 20 04:24   ← DEC-096 的代码在里面
fg_signals.json  Aug 19 14:42   ← 比代码早 14 小时
```

机构资金那几个页面读的是**每日定时任务产出的静态 JSON**,不是接口实时算的。
**部署只换代码、不重算 JSON**,于是「代码已上线、页面还是旧引擎算的数」,
**而且看不出来**——页面照常显示,数字也不是明显的错,只是旧的。
前几次部署我都手工跑了 `run-smart-money`,这次忘了。**靠人记显然不行。**

### 两道保险,一道自动、一道露馅

**① 部署自己重算**(`deploy-futures.yml`)。装引擎文件那一步本来就在做变更检测
(`ENGINE_SUPERSEDED`),顺手立个 `ENGINE_CHANGED` 旗;在 `stable.env` 落定之后
(那之后已经没有会失败的步骤)跑一次 `run-smart-money`。
**失败一律不阻断部署**(`|| true`):信号晚几小时由定时任务补上,把一次已经验收
通过的发布判失败,是拿真问题换假问题。日志里留 `ENGINE_REFRESH ok/failed/skipped`。

**② 页面露馅**。引擎把**自己源码的 sha256 前 12 位**写进 payload
(`engine_fingerprint`);部署把装到位那份的指纹写进 `web/engine.json`;
前端两边一比,不一致就在最上面挂「这份信号是旧引擎算的」。

**为什么用文件指纹而不是 git sha**:`/api/v1/version` 给的是**镜像构建时**的 sha。
镜像复用是常态——本次发布 `4f47a50`,而 api 镜像复用自 `cb4b396`,
`/api/v1/version` 就返回 `cb4b396`。拿它比会**每次复用都误报**。
文件指纹回答的正是要问的那个问题:**这份 JSON 是不是当前这个引擎算的**。

两边任一为空都不判 —— 旧 JSON 没有这个字段,不能因为缺字段就报警。

**上线当天这道保险自己先坏了一次,值得记**:第一版把 printf 的换行在 YAML 里写成了
`\n`,落到文件里是**字面的反斜杠 n**,`engine.json` 因此不是合法 JSON。
后果是:前端 `res.json()` 抛异常 → 被 catch 吞掉 → 指纹永远是 null →
**这道兜底静静地失效,而页面看起来一切正常**。这正是它本来要防的那种失败模式。
修法:除了改对转义,部署写完立刻 `python3 -m json.tool` 验一次,不合法就删掉文件并
`ENGINE_FINGERPRINT_INVALID` 吼出来 —— **宁可没有这道保险,也不要有一道假的**。

**③ 流程侧**:`RELEASE_PROCESS.md` 第 5 步补上「改了 engine/ 要确认 JSON 已重算」
与手工核查/补跑的命令,并写明 `/version` 的 sha 在镜像复用时**本来就对不上**
(我当天正是拿它误判过一次)。preflight 在 `engine/` 有改动时提示这次部署会多花
几分钟重算——**是提示不是门禁**。

### 教训

**「部署完成」不等于「用户看到的东西已经更新」。** 凡是「代码算一次、结果存下来、
页面读存下来的那份」的链路,部署都必须把「重算」也做掉,否则就会出现
**代码是新的、展示是旧的、而且不报错**的状态。这类不一致最危险的地方在于
它**长得和正常一模一样**。

---

## DEC-100 LATEST 与生产 SHA 的一致性做成门禁(2026-08-20)

三文件规则里 LATEST 是**覆盖式状态快照**,它就该描述当前生产。这一天部署七次、
**漏更新一次**,而漏掉的表现和 DEC-099 那个引擎 bug 一模一样:
**看起来一切正常,只是记录是旧的**。靠人记不行,做成门禁。

**判据是「LATEST 里的 SHA == 上一次成功部署的 SHA」,不是「== HEAD」。**
一个文档提交没法在自己被部署之前就写出自己的 SHA(自指)——我先按「写即将部署
的那个」做了两轮,每轮都留下一个新的过期,才意识到规则本身有问题:

```
提交 a3846b9「生产 = bcf92c0」→ 部署的却是 842e6e6 → LATEST 又过期了
```

所以流程定成:**部署完再用一个纯文档提交把 LATEST 更新成刚部署的那个**,
那个提交不单独部署,跟着下一次发布一起走。这样门禁就是一个干净的等式。

门禁加在 `ops/preflight-deploy.sh`,与 `run_live_collection` 共用已经取到的
`last_deployed_sha`。三个分支:一致→绿;不一致→红并直接说出两个 SHA;
**读不出「生产 = \`<sha>\`」这个格式也判红**——格式一变这道门禁就等于没有,
而它会安静地一直绿(`docs/DEPLOY_PREFLIGHT.md` 二·守卫自己不能静默死掉)。

写完当场跑,它抓住了现存的那处过期(LATEST 停在 a3846b9、生产 842e6e6)。

**但第一版这道门禁自己是坏的,值得记**:我把它放在了失败汇总 `exit 1` **之后**,
而那之后**没有第二次失败检查**——它失败时会打印 ✗、却照样把部署命令吐出来。
**一道只会喊、不会拦的门禁,比没有门禁更糟:它占着「已经检查过了」的位置。**
这正是 `docs/DEPLOY_PREFLIGHT.md` 二·「守卫自己不能静默死掉」那一条,
写它的时候我还引用了那条。修法:把 `last_deployed_sha` 的取值与这道门禁一起
提到失败汇总之前(run_live 与引擎提示复用同一个变量)。
**验收要正反两面跑**:一致时绿;把 LATEST 故意改成 `deadbee`,确认退出码 1
且不再打印部署命令。只验绿的那一面,等于没验。

## DEC-101 信号文件权限写死 644,并在引擎收尾自检(修 DEC-099 引入的 403)

**背景**:DEC-099 让部署自动重算信号。部署脚本 `umask 077`,子进程 `cp` 出来的
JSON 是 600,容器内非 root 的 nginx 读不到,机构资金全部品种(含黄金白银)报 403。

**决定**:
- `run-smart-money.sh` 两处落盘一律 `install -m 644`,不再依赖继承的 umask;
- 脚本收尾扫描 `$WEB/*.json`,权限不在 644/664/666 之列的**告警并自动纠正**;
- 线上已先行 `chmod 644` 止血,不等发版。

**为什么不是「在部署里 chmod」**:那只覆盖部署这一条调用路径。cron、手动执行、
以后任何新调用方都会各带各的 umask。**责任归产出方**,谁写文件谁保证可读。

**验证**:在生产以 `umask 077` 跑完整引擎一遍(复现故障环境),8 个 JSON 全部
644、HTTPS 全部 200、自检未触发(说明 `install` 一次写对,自检只是保险)。

**代价**:信号 JSON 明确是公开可读的。本来就是 nginx 对外发的静态文件,不含隐私;
把它写成 600 从来不是一种保护,只是一次事故。

## DEC-102 全仓审计的九条整改(2026-08-20)

运营者要求「检查一下目前这个项目的代码和文档,看看有没有需要修复的地方」,
列出九条后要求全部修掉。逐条的决定与理由:

**① 引擎接入 CI + 22 个回归测试。** `hog_money.py`+`smart_money.py` 共 2594 行,
决定进场/出场,此前安全网只有 `replay` 末尾一句断言,CI 一行不跑(只覆盖
`collector/`)。新增 `engine/test_hog_money.py`,不追覆盖率,只钉那些**错了不报错、
只会安静给个看着合理的数**的地方:交割纪律查持仓合约(DEC-096)、次日开盘成交
(DEC-090)、做空逐日连乘、持仓不重叠、逐笔与逐日恒等。
**测试写完做了变异验证**:注入三个历史 bug,第一个(交割纪律查当日主力)与第三个
(闭合记账用简单收益)当场被抓;第二个(持有中估值用简单收益)**21 个用例全过**——
那条分支根本没人覆盖,于是补了第 22 条。**没做过变异验证的测试不算数。**

**② 到达概率曲线的重估路径修好了。** 代码注释写着「重估要跑
`research/run_shelf_prob.py`」,而那个脚本只有三段探索性打印,**根本不产出**
那 4×31 个数——烤死的常量,来路在仓库里无法复现。给脚本补了第四节:产出可直接
粘贴的常量,并与仓库现值逐桶对拍。跑通验证:124 个数最大差 0.00,完全可复现。
(顺带确认了样本数是行数的两倍——曲线把 zdown/zup 两个方向合并计数。)

**③ 剩余不足 5 天不再给到达概率。** 拟合脚本第一步就是 `a[a["T"] >= 5]`,
把不足 5 天的观测排除在样本外;而 `reach_bucket` 的 `..=20 => 0` 会把 1~4 天
也喂给那条「5~20 日」的曲线。**脚本自己打印的警告就是**「否则快到期时会给出
虚高的概率」。每一对合约都会一路走到 0 天,这不是理论边界。现在返回 `None`,
页面显示「—」并写明原因——**不能只留一列横杠**,那看着像数据没采到。

**④⑤ 文档可发现性。** DECISIONS 文首现在写明:`DEC-013`~`DEC-086` 里有 73 个
编号在本文没有任何条目(全仓却有 260 多处引用),并给出正确查法
`git log -S "DEC-0xx"`。**不追写老条目**——那等于事后重构记忆,比缺着更危险。
`research/PITFALLS.md`(245 行研究脚本的坑)此前只被 DECISIONS 深处引用过,
接手必读路径里一次没提;现在写进 AGENTS.md 的三文件规则作为明确例外。

**⑥⑦⑧ 清理。** 我 2026-08-20 那条 PITFALLS 游离在七个分类之外,已拆成两条归位;
一个空文件 `0`(重定向手滑)从仓库删除;四处失效引用标注清楚
(`docs/reviews/*` 随 `c9a9b9e` 删除、`sanheapi/*` 从未进仓、`project-history.sql`
随 DEC-049 删除)——**标注而不是恢复**:运营者早已拍板「git 历史即归档」。

**⑨ 套利监控拆进子模块。** `spread_analytics.rs` 4984 行,监控又是改得最勤的一块。
拆成 `spread_analytics/monitor.rs`(1417 行),逐行原样搬、用 `use super::*` 不改
依赖,对外路径靠 re-export 保持不变。**验证不靠读代码**:在生产全量数据上跑新旧
两版引擎对拍 7 个产出文件,剔除指纹与 `computed_at` 后完全相同。
拆的过程中编译器报可见性错,顺带发现原文里两个不相干函数的文档注释黏成了一块
(`monitor_turn_is_new` 的文档错贴在 `report_prev_net` 头上,前者一句文档没有),已归位。

**没做的两件事,以及为什么**:
- **没给引擎套 collector 那套完整 ruff 规则集**(E,F,I,B,UP,S)。全开是 101 条,
  多数是测试里的 `assert`、分析代码里的 SQL 字面量这类噪音;而 B905 要求 `zip()`
  写明 `strict=`,`zip(days, days[1:])` 的两个长度天生差一,加 `strict=True` 会
  **直接把引擎打挂**。默认规则集(E4/E7/E9/F)已经覆盖真正的错误类——事实上
  它当场抓出了我自己改名时漏掉的 12 处未定义引用。
- **没有恢复 `docs/reviews/`**。见上 ⑥⑦⑧。

**代价**:CI 多一步(引擎装运行期锁 + ruff + pytest),约一分钟。

## DEC-103 净持仓页的日期轴改由行情日历驱动(掉榜段整段蒸发,K 线一起没)

状态:已确认(2026-08-20,运营者第二次发现同一个症状:「高盛黄金 3.25-4.27 减仓
过程的界面,又没有蜡烛图,之前这个 bug 不是修了吗」)。

**根因。** `build_net_position_series` 的 `by_date` 只由席位行喂养,某席位连续掉榜
的交易日根本不进序列。前端横轴是 `type: 'category'`、`data` 直接取自返回的 `days`,
**缺的那天不是留空,而是整列消失**,K 线跟着一起没——而 `price_history` 里那些天
四价与持仓量一天不缺(已逐日核过,8 个合约天天齐全)。

**「不是修过吗」——修过,修的是另一条路。** 完全相同的缺陷 2026-08-16 在建仓过程
汇总档修过(`157a8d4`,DEC-061 附,以合成行情日历为外轴)。随后 `DEC-080` 把页面
形态换成净持仓,`build_net_position_series` 是新写的,照着席位行又造了一遍轴,
那个修复**没有被带过来**。所以不是旧修复失效,是同一个缺陷在替代它的新路径上
重新长了一遍;当初的测试只钉住了旧函数,拦不住新路径。

**规模(实测)。** 高盛(库里旧名**乾坤期货**,2026-05-26 更名)在黄金上
2023-08-04~2026-08-20 区间内共 738 个交易日,只有 667 天有席位行——
**71 个交易日此前被静默吞掉**;最长一段正是运营者点名的 2026-03-25→2026-04-27,
连续 **21 个交易日**。

**修法。** `build_net_position_series` 增加 `calendar` 参数(行情完整交易日历),
把**席位序列首尾之间**缺的交易日补成空行:手数为零、`counted_members` 为空、
`missing_members` 等于全部选中席位。首尾之外一天不补——那家本来就不在场,
补出来是无中生有。前端早已按 DEC-061 的口径实现(掉榜日按 0 画、掉榜底色、
小窗说明),它只是**从来没收到过这些天**。

**连带必须一起改的一处。** `cumulative_pnl` 原先在拿不到 pnl 时兜底成 `"0"`。
补轴之后掉榜段每天都没有 pnl 行(盈亏引擎照三态口径把掉榜当未知,那是对的),
于是累计曲线会**直接砸到零**,画出一根不存在的巨亏——比原来的缺口更糟。
按 DEC-061 既有口径改为**累计沿用前值**,当日盈亏留空。
**补轴与这一处必须同一笔提交**,只做前者是把一个可见的缺口换成一个可信的假象。

**顺带纠正的一处文档。** `seat_net_position.rs` 的注释写着「与其画一个零,不如让
曲线断在那里」,与运营者 2026-08-16 的拍板冲突;既存测试
`a_day_where_everyone_is_off_the_board_simply_does_not_appear` 的名字也在教人
理解成「掉榜日就该消失」。已改名为「没有行情日历时全员掉榜那天不会凭空出现」
并写明它只覆盖「日历为空」那半边。

**测试。** 域层新增 4 条:补回掉榜日、只补首尾之间、补轴不改动在榜日的任何数字、
日历为空退回原行为。

## DEC-104 「进场条件」显示的是机构信号,而引擎比的是共振后的散户信号(修)

状态:已确认(2026-08-20,运营者问「玻璃为什么没有触发多单进场条件」)。

**症状。** 玻璃页面写着「进场条件 信号强度需达 1(现 2.09)」,2.09 > 1,却又显示
**无持仓**——页面自相矛盾。

**根因。** 现行策略是**方案 C**(`signal_source = "resonance"`,DEC-086):
`entry_exit_signals` 返回的进场信号是**共振后的散户信号** `retail.z`,
而页面那一行取的是**机构合计流向** `signal.z`。当天机构 2.09、散户 0.92 ——
真正被拿去比门槛的那个数**差 0.08 没到线**,不进场是对的,**显示的数字是错的**。

**为什么会写错。** `HogPayload.retail` 的类型注释一直写着「当前只作展示,
**不参与进出场**」。那句话在切到方案 C 之后就过期了,而引擎 payload 里的
`retail.note` 明写「现行策略(方案 C)就是用它进出场」。**同一件事两处说法相反,
页面跟了错的那一处**,而且一跟就是从方案 C 上线至今。

**这不是只多报,也会漏报——而且当天就有一例。** 五个品种当天的实况:

| 品种 | 页面旧显示 | 引擎实际比的 | 旧显示让人以为 | 真相 |
|---|---|---|---|---|
| 玻璃 FG | 2.09 | 散户 **0.92** | 已达标 | 没到线(差 0.08) |
| **纯碱 SA** | **0.52** | 散户 **1.19** | 没信号 | **已达标** |
| 鸡蛋 JD | 0.59 | 背离 | 没到线 | 背离,与强度无关 |
| 焦煤 JM | −0.26 | 背离 | 没到线 | 背离 |
| 生猪 LH | −0.81 | 散户 −0.15 | 没到线 | 也没到线 |

纯碱是**反方向的漏报**:散户 1.19、共振成立、做多开着且不要求回撤、交割还剩
95 天不挡,进场条件当天就是满足的;而页面显示 0.52 会让人判定「没信号」。
(当天仍显示无持仓是对的——DEC-090 次日开盘成交,最后一个数据日没有次日开盘价。)

**修法。** 判据抽进 `frontend/src/entry-gate.ts`,由 payload 里本来就有的
`retail.trades`(= 是否走方案 C)与 `retail.resonate` 决定该比哪个数:
- 走方案 C 且共振 → 比 `retail.z`;
- 走方案 C 但**背离** → 直接判死,**再大也不进场**,文案只说背离不报数字;
- 不走方案 C(`signal_source = "flow"`)→ 退回比 `signal.z`,原显示本来就对。

文案**必须点名比的是哪一路**,并把机构那个数一起说清("机构 2.09 不是这里比的
那个数")——它就摆在旁边的卡片里,不说清楚读者一定会拿它去对门槛。达标时写明
「次日开盘进场」,免得当天看到无持仓又生一次疑。

**顺带。** 「机构合计流向」那条强度条按进场门槛缩放,玻璃当天被画成满格红条,
视觉上也在说「已达标」。**这条没改缩放**——它长在「机构合计流向」卡里、旁边写着
「机构在减空/加多」,本就是机构口径;只把注释改对,免得下一个人照着它再写错。
要不要改成固定量程,留给运营者定。

**测试。** `entry-gate.test.ts` 10 条,含玻璃当天的真实取值、背离判死、做空方向
对称(比绝对值)、`flow` 口径不被一起改坏、数缺失时不谎报达标。

## DEC-105 净持仓子页接上交易日(此前从未接过,不是联动坏了)

状态:已确认(2026-08-20,运营者选了 8.19,净持仓摘要仍显示 2026-08-20)。

**根因。** `SeatNetPositionQuery` 里**根本没有交易日字段**,handler 永远取
`series.last()` 当「最新一天」。席位页顶上写着「选几个会员和一个交易日,
**两个子页共用这组选择**」——会员确实共用了,交易日没有。前端的 watch 一直在
日期变化时重新调 `loadBuilding()`,只是请求里没带日期,于是每次取回同一份。
**所以这不是联动坏了,是从来没接过。**

**修法。** 域层加 `cut_at(series, as_of)`,接口加 `trade_date`(可选,不传＝看最新)。

三个口径上的取舍(**第一条已于 2026-08-21 被运营者推翻,见下方勘误**):
- ~~**截整条序列,不是只改摘要那一行。**~~ 见勘误。
- **用 `<=` 不用相等。** 选到周末或休市日时给「截至那时」,而不是给一片空白。
- **选的日期早于全部数据时给空,不退回最新。** 给「最新」等于默默无视用户的
  选择,而那正是这次要修的毛病;日期解析失败同理——报 `invalid_trade_date`,
  不静默当成没选。

### DEC-105 勘误(2026-08-21):范围选错了,只改摘要,图不动

运营者当天验收后否掉第一条:「应该**只改净持仓的多单空单显示,就是改上面的文字**,
方便我看各家情况,**其他全部不用变**」。

我当时的理由是「摘要说 8.19 而图画到 8.20,页面上同时存在两个今天」,听着自洽,
**但把他的用法想反了**:他要的正是**一边看某天的各家明细、一边保留完整的图**——
K 线、净持仓曲线、累计盈亏都是判断那一天所需的上下文,截掉等于把上下文一起拿走。
「两个今天」在这里不是矛盾:摘要那行本来就带着日期,图是历史,两者角色不同。

改法:`cut_at` 换成 `as_of_day(series, as_of)` —— **只挑出「摘要与各家分腿看哪一天」,
序列一天不动**。哪天算 as-of 由后端一处决定,前端摘要与下面那排分腿都读它,
两半不会各判各的(此前摘要取 `days.last()`、分腿取 API 的 `latest_members`,
是两个来源)。前端**不给兜底**:后端没给 as-of 日就整行不渲染,
不退回显示最新。域层测试里专门留了一条 `不动序列本身` 钉住范围。

**教训**:改动范围拿不准时,「更一致」不等于「更有用」。我用一个自洽的理由
(不要两个今天)替运营者做了取舍,而他真正的用法是拿图当上下文查某一天。
这条已进 PITFALLS。

**测试。** 域层 4 条:截到当天(含当天)、不选原样返回、非交易日退到之前最近一天、
早于全部数据给空表。

## DEC-106 全仓复核第二轮:同一事实两处维护,逐个收口(2026-08-20)

运营者要求「重新全部核验一遍,包括文档,看看有没有冲突的地方」。三栈机械检查全绿
(Rust 152 / 前端 123 / 引擎 22 + ruff),编号无缺号、代码注释里的文件引用零失效、
LATEST 与实际部署一致。**找到的五处全部属于同一类:同一件事在两处描述或两处实现,
其中一处过期或缺失。** 这正是 DEC-103/104 两个线上问题的共同根因。

**① 引擎注释自己跟自己打架(DEC-104 的上游)。** `hog_money.py` 里 `signal_source`
那段:第 121 行写「**不参与进出场**」,13 行后写「运营者拍板改用**方案 C:共振进场
/散户出场**」,末尾 `signal_source = "resonance"`。第一句是切换前的遗留,而**前端的
类型注释照抄了它**,「进场条件」那行就长期显示机构的数。已改写,并把当年那句话里
仍然成立的**理由**(三方案表现相当、不是新信号不够好)原样保留 —— 过期的是状态,
不是理由。
同段还写着方案 C「(只做空)」且只列了空头的进场条件 —— 那是在生猪上写的
(生猪 `long_enabled=False`),**对 FG/SA/JD 不成立**,会让人以为玻璃根本不会做多,
而玻璃恰好是双向的。已删「只做空」并补上做多那一侧。

**② `run-smart-money` 是整条链上唯一不加锁的作业。** 它有两个互不知情的调用方:
cron(08:40/10:10/14:10 UTC)与部署的 `ENGINE_REFRESH`(DEC-099,引擎一变就跑)。
一轮跑六到八分钟,部署赶在 10:08 触发就会和 10:10 那轮叠上。两轮共用同一个
`$TMP`:后来者的 psql 导出会盖掉前者正在读的 CSV,先跑完的那轮结尾
`rm -f "$TMP"/*.csv` 会删掉另一轮的中间文件。**结果是信号算错或没产出,而
ENGINE_REFRESH 失败是有意不阻断部署的,没人会当场发现** —— 偏偏它产出的是运营者
据以下单的信号。
已加**自己的锁** `/run/lock/futures-smart-money.lock`,用 `flock -w 900`:
- 不共用 `futures-collector.lock`:部署跑 ENGINE_REFRESH 时**仍握着**那把锁
  (fd 8 从迁移一直开到脚本结束),共用会让部署自己的重算被自己挡住;
- 用「等」不用「跳过」:两个调用方产出同一份东西,等前一轮跑完再跑一遍是幂等的,
  而跳过会让 DEC-099「引擎换了要立刻重算」悄悄落空。

**③ preflight 的窗口提醒不全,而且措辞会给错信息。** 昨天那版只覆盖采集器三轮、
时刻表手抄一份,并且不管窗口里是哪个作业都统一说「会整轮跳过」。实际三个作业对
抢不到锁的反应完全不同(collector `-n` 整轮跳过 / official-seats 与 spread-warm
`-w 900` 会等 / offsite-backup 不碰这把锁)。已改为**从 `deploy/collector/*.cron`
现读时刻表**、按作业分别措辞;**新增 cron 文件而没在 `lock_behaviour` 表里登记会
当场 `fail`**,逼一次判断,不被静默漏掉也不一律乱报。

**④ `docs/DEPLOYMENT.md` 的 cron 描述停在两轮。** 实为三轮(08:00 抢早轮后加的),
且完全没提另外三个 cron 作业。已补全四个作业的时刻与各自的抢锁行为,并注明
**真值以 `deploy/collector/*.cron` 为准,别照抄该条**。

**⑤ `REACH_MIN_DAYS` 在 Rust 与 TS 各写一份,没有守卫**(2026-08-20 我自己造的)。
分叉后页面会安静地自相矛盾:后端调低下界就返回概率而前端仍挂着「不给概率」的说明,
调高则出现一列「—」却没有任何解释。跨语言没法共享常量,已加一条 Rust 测试读
`frontend/src/shelf.ts` 断言两边相等(同
`rust_normalisation_matches_the_sql_member_key` 的做法),正反两面验过。

**查过、确认不是问题的:** `VARIETIES` 里五串写死的回测成绩与线上 `stats`/`compare`
**逐项一致**(笔数/胜率/净值/回撤/夏普全对),虽仍属「两处维护」但当前是准的;
`docs/reviews`、`sanheapi`、`project-history.sql` 三处失效引用已在 DEC-102 标注;
DEC 编号 87~105 无缺号且递增(98 那个是「附录」不是撞号);LATEST 与上次成功部署
一致;`range_pos`「只标注不参与买卖判定」与代码相符。

## DEC-107 机构资金页加「已卸掉 X%」——**只摆不用**(2026-08-21)

运营者提出「和机构反向,要等机构出货出得差不多」。这个数此前页面上根本没有,
他只能盯着净持仓曲线目测。本条只把它如实摆出来。

**为什么只摆不用。** 作为进场判据,它只在**纯碱、5 日窗口**上通过了全部检验
(`research/REPORT_SA_UNLOAD_DEEP_v1.md`:掉榜控制、逐年 5/6、非重叠、
置换第 0.2 百分位);玻璃(样本量最大)**样本外符号翻转**、焦煤明确否、
生猪鸡蛋数据不够验 —— **横截面上没有支持**。而且现行引擎持仓中位数在 20 日以上,
那个 5 日效应对它用不上。`replay` 一个字都不读它,测试
`test_它不进任何进出场判据` 扫 replay 的函数体钉住这条边界。

**口径:三处必须重置,错了都不会报错,只会显示一个看着合理的百分比。**
- **换组当天重来一轮** —— 新旧两组持仓水平不同,不重置会把「换了一批人」读成
  「机构大幅出货」。`signal_series` 算 `chg` 时为同一个理由分组算过一遍。
- 方向翻转 / 归零重开。
- **掉榜那天冻结、不给值** —— 掉榜是「不知道」不是「卸完了」
  (`research/PITFALLS.md` 第 4 条)。研究脚本第一版在这里把整轮重置掉,
  于是掉榜一天峰值就重新起算,出货程度掉回 0。

**掉榜混淆必须写在界面上。** 五家掉两家会让合计净持仓下降,而他可能一手没动;
实测这个混淆**专门吃掉长窗口**(纯碱 20 日的表观效应几乎全由它贡献:
仅同家数 −0.06% vs 仅掉过家 −4.70%)。所以 payload 带出 `legs_now`/`legs_at_peak`,
两者不等时出提示,**并分两种情形措辞**:今日少于峰值日 = 分不清出货还是掉榜;
今日多于峰值日 = 峰值那天有人没上榜、比例是偏低的估计。
第一版只写了前一种,而生猪当时正好是后一种(今日 5 家、峰值日 4 家),那句话对它是错的。

**口径与研究不完全相同,如实记下**:页面用的是 `sig["net"]`(**品种合计**,
与页面上「合计净持仓」同源),而研究用的是**主力合约**。展示维度必须与页面上
已有的数同源,否则两个数打架;但因此页面这个百分比不能直接拿去对照研究报告里的
结论。

上线实测:生猪 45%、玻璃 93%、纯碱 41%(4/5 家,出提示)、鸡蛋 46%、焦煤 14%。

## DEC-108 修掉机构资金引擎的回榜前视(2026-08-21)

**这是修 bug,不是调策略。** 运营者拍板「先修引擎」。

### 问题

`clean_seat` 把 `source='reboard_inferred'` 的行与官方行一视同仁。那些行是
**用回榜日的增减倒推**出来的(`fix-sanhe-fabricated-changes.sql` 有精确实例:
04-17 的持仓 215 = 04-18 的 824 − 609)。实测**可见滞后恒为 1 个交易日,100% 如此**
(玻璃 5.0 万条、纯碱 3.1 万条、鸡蛋 7.5 千条,最长都是 1)。时点因此很清楚:

| | 第 D 日收盘(算信号) | 第 D+1 日开盘(成交) | 第 D+1 日收盘后 |
|---|---|---|---|
| D 日的反推值 | **不存在** | **不存在** | 才推得出 |
| D−1 日的反推值 | 已存在 | 已存在 | — |

而信号是 `net.diff(sig_win)`,**一头正好踩在 D 日那一格上**。
`research/PITFALLS.md` 第 4 条早写着该用 `reboard_visibility` 的
`reboard_date` 门控,引擎没做。

**要分清哪里受影响**:实盘运行时,库里本来就还没有当天的反推行
(`infer-offboard-seats.sql` 要等次日数据才写),所以**当天发出的信号是干净的**。
被污染的是**历史回放** —— 而历史回放既决定页面上展示的成绩,也决定
「当前是否持仓、进场价多少」。两头都要修。

### 修法

`clean_seat` 多出一列 `net_off`(**只用官方行**的净持仓),与原有的 `net`
(事后完整)并排存着。`_pit_pair` 据此分出「当日可见」与「事后完整」两条线,
`signal_series` 与 `retail_series` 都改成:
**当天用可见口径,`sig_win` 天前的被减数用全量**(滞后恒 1 天,那一格必已可见)。

两个坑,第一个我自己踩了:

- **逐腿判,不是整行判。** 一家可能多头榜在(官方)、空头榜掉了(反推)。
  第一版按「任一腿来自反推即整行作废」,把那条**实盘看得见的**多头腿也扔了 ——
  实测玻璃 3,131 天里 1,352 天因此算错。改成逐腿之后与研究脚本**逐日零差异**。
- **两腿都没有官方行 → NaN,不是 0。** 掉榜≠清仓,给 0 会凭空造出一次清仓。

### 代价(全部重算,已写进 `VARIETIES.backtest`)

| 品种 | 修前(扣费) | **修后(扣费)** | 夏普 | 回撤 | 基准 |
|---|---|---|---|---|---|
| 生猪 LH | +85.0% | **+87.4%** | 2.25 → **2.34** | −5.0 → −4.2% | +99.2%(**仍输**) |
| 玻璃 FG | +442% | **+34.9%** | 0.65 → **0.21** | −45.9 → **−67.9%** | −17.7% |
| 纯碱 SA | +256% | **+47.0%** | 0.84 → **0.36** | −55.8 → **−61.8%** | −17.5% |
| 鸡蛋 JD | +42.4% | **+30.9%** | 1.02 → 0.79 | −11.5 → −13.3% | +61.6%(**输**) |
| 焦煤 JM | +90.4% | **+64.2%** | 1.11 → 0.91 | −14.7%(不变) | +18.2% |

**生猪反而略好**(它只有 2,520 条反推行);玻璃纯碱掉一个数量级,
**回撤还更深、胜率还更低** —— 三项全面恶化。

**策略本身没有变差,变的是对它的认识。** 实盘的成交与盈亏一直就是这个水平,
之前那些数字是回测让引擎看到了实盘拿不到的数据。

### 需要接着定的事(**本条未决**)

- **玻璃夏普 0.21、回撤 −67.9%,纯碱 0.36 / −61.8%** —— 赢基准但赢得很薄。
  这两个品种还上不上,是运营者的判断,本条不替他定。
- **鸡蛋输基准(+30.9% vs +61.6%)**,生猪也输(+87.4% vs +99.2%,这一条 DEC-084
  时就知道)。
- **做多开关要重议**:`VARIETIES` 里「FG 双向夏普 0.65 vs 只做空 0.36」那组比较
  是**修前视之前**算的,已作废(FG 修后双向只有 0.21)。三个品种的做多开关都得
  在新口径上重扫。

### 守卫

`engine/test_hog_money.py` 新增 4 条(共 36):官方腿在另一腿是反推时要留下、
两腿都反推给 NaN 不给 0、当天反推不进信号而 `sig_win` 天前的照用、
全是官方行时新旧口径一模一样。**改回去不会报错,只会让回测重新变好看** ——
这几条是唯一的屏障。

## DEC-110 纯碱做多改成要回撤;部署流水线自动补 LATEST(2026-08-21)

> **⚠️ 本条的「纯碱改要 dip」部分已被 DEC-111 推翻并回退(同日)。**
> 下面那组「两档夏普完全相同(0.36 vs 0.36)」是在 `past` 混了换月跳空的口径上
> 算出来的,修掉之后是 **0.36 vs 0.30**,平局不存在,依据不成立。
> 本条列出的鸡蛋/生猪三档数字同样作废。**当前实数以 `engine/hog_money.py`
> VARIETIES 上方那张表为准。**
> 「部署流水线自动补 LATEST」那部分不受影响,仍然有效。

运营者同一轮拍了四件事,两件改代码、两件维持现状。

### 纯碱 `long_needs_dip` 由 False 改 True

修掉回榜前视后重扫,两档**夏普完全相同**(开−dip 0.36/t+0.98、开+dip 0.36/t+0.95),
而带 dip 的**回撤小 9.6 个百分点**(−52.2% vs −61.8%)、笔数少 18 笔(换手更低)。
**夏普分不出高下时按回撤取。** 代码注释里从 DEC-090 起就标着「这一条值得复议」。

如实记两点:①这是在同一份样本上比出来的,不是样本外验证过的最优;
②但它**不是「翻开关找更高收益」,而是「同等收益下挑更浅的坑」** —— 风险性质不同,
不适用 DEC-096 鸡蛋那次「样本内选择」的同一条警告。

新数字:**93 笔 净 +44.4%/胜率 47.3%/回撤 −52.2%/夏普 0.35**(基准 −17.5%)。

### 部署流水线自动补 LATEST

DEC-100 那道门禁 2026-08-21 一天之内拦了我**两次**,两次都是同一个疏漏 ——
部署完忘了补 LATEST。门禁按设计工作了,但它拦的是**下一次部署**,
中间那段时间 LATEST 一直指着旧 SHA,新会话接手就照着错的现状干活。
这一步本来就是机械的,不该靠人记。

做成部署成功后的自动步骤。**权限从 contents: read 上调到 write** ——
理由要写清:这个工作流只能手动 dispatch 触发,而它跑在自建 runner 上、
本来就握着生产的 SSH 私钥,再给仓库写权限是边际增量,不是新开一个口子。

三道自保,任何一条不满足就**跳过而不是硬来**:

- 分支 HEAD 必须仍等于本次部署的 SHA —— 不等说明期间有人推了提交,让他自己收口,
  别在这里制造冲突;
- 改动只允许碰 `docs/handoffs/LATEST.md`,碰到别的文件就回滚并中止;
- `continue-on-error` —— 部署已经成功了,不能因为一个文档提交把它判失败。

本地把三种情形都验过:SHA 不同时改 SHA 并刷新日期、重跑 noop、
LATEST 格式被破坏时读不出 SHA 直接退出且**不动文件**。

### 维持现状的两件(运营者拍板)

- **「机构合计流向」强度条不改量程**(仍按进场门槛缩放)。
- **鸡蛋与生猪的做多开关不动** —— 重扫结果它们现行配置就是三档里最优的
  (生猪 关 2.37 vs 开+dip 1.90 / 开−dip 1.60;鸡蛋 开+dip 0.80 vs 关 0.70 /
  开−dip 0.40),换成别的只会更差。
  两者绝对收益仍低于「恒定满仓做空」基准(生猪 +87.4% vs +103.5%、
  鸡蛋 +30.9% vs +62.7%),这一点未变,也未因此下架。


## DEC-111 回撤判据跨了合约,纯碱因此回退;展示价改收盘价(2026-08-21)

运营者报了两件事:「昨天触发了做多信号,现在信号怎么没了」,以及「价格显示改成收盘价」。
第一件查下去是**同一天早些时候我自己改出来的线上 bug 的引信**,不是策略判断。

### 病灶:`past` 在混合主连上直接 `pct_change`

`main_series` 的第一句话就是「换月日用新主力自己的前一日结算价 ——
一旦跨合约相除,换月那天会凭空多出几个百分点,而且不报错」。
`ret` 和 `ret_open` 都照做了,**唯独 `past` 漏掉**:

    out["past"] = out["settle"].pct_change(RULES["dip_win"])

`settle` 是混合主连。纯碱 2026-08-13 主力由 SA2609 换 SA2701,结算价
990 → 1031,**+4.1% 全是合约价差**。于是:

| 日期 | 旧 past | 真实 past |
|---|---|---|
| 08-14 | **+1.37%**(假涨) | −4.33% |
| 08-18 | **+3.08%** | −2.72% |
| 08-20 | **+2.38%** | **−3.29%** |

`past` 只喂一个地方 —— `entry_side` 里的「做多要求先有回撤」。
所以后果是:**近 20 日其实跌了 3.3%,判据说「在涨」,把 8/20 那个做多信号挡在门外。**

全量量化(判据符号被改写的天数占比):鸡蛋 12.2%、生猪 10.4%、纯碱 8.6%、
玻璃 7.5%、焦煤 5.6%。最大单日偏差 55.3 个百分点。

修法是复用**已经处理过换月**的逐日 `ret` 连乘。新增两条测试:一条钉住
「两个合约都在跌时 past 不许为正」,另一条是**反证** —— 用同一夹具跑旧写法必须
出现假涨,否则前一条形同虚设。

### 引信:是我把这个潜伏 bug 变成了线上事故

`past` 写错很久了,但纯碱一直 `long_needs_dip: False`,根本不读它。
DEC-110 当天把它开成 `True`,**等于把一个坏判据接进了纯碱的进场必要条件**,
当天就吃掉一个真信号。

**教训不是「改配置有风险」,是「开启一个此前没人走过的分支时,要先看那个分支
读的量对不对」。** 我当时只比了两档的回测数字,没有回头看 past 本身。

### 处置一:纯碱回退 `long_needs_dip → False`

DEC-110 选 True 的**唯一**理由是「两档夏普完全相同(0.36 vs 0.36),按回撤取更浅的」。
在修好的 past 上重扫,平局不存在了:

| 档位 | 笔数 | 胜率 | 累计 | 回撤 | 夏普 |
|---|---|---|---|---|---|
| 关 | 56 | 50.0% | +20.6% | −32.2% | 0.25 |
| **开−dip** | 111 | 45.9% | **+47.0%** | −61.8% | **0.36** |
| 开+dip | 92 | 46.7% | +31.5% | −52.4% | 0.30 |

夏普与收益都是不要 dip 更好,只有回撤更浅是它。
**改动的理由被证伪就退回原状,而不是换个理由把改动留着** —— 后者是结论找依据。
回撤 −52% 与 −62% 之间的取舍如果要重议,连同 `dip_win` 一起重扫,单独翻这个
开关等于在噪音上做选择。

顺带说明:**修完 past 之后,8/20 那笔做多在两档配置下都会回来**(真实 past 是
−3.29%,回撤条件本来就满足)。回退不是为了把信号弄回来,是为了让配置站在成立的
理由上;信号是 bug 修好后自己回来的。

### 处置二:鸡蛋排序翻了,但不跟

修完之后鸡蛋三档变成 关 0.69 / **开+dip 0.59(现行)** / 开−dip 0.39 ——
原来注释里写的「开着且要 dip 每项都略好」不成立了。**没有跟着翻是有意的**:
26 笔 / 三年样本,0.69 与 0.59 在噪音量级内,跟着每次重扫翻开关就是拟合噪音
(DEC-090/093 记过)。注释已改成实数并注明等运营者拿主意,不许再写「实测最优」。

**本条同时作废三处旧的三档数字**:DEC-096(鸡蛋「每项都略好」)、
DEC-109(「五个品种现行配置全部仍是最优」「纯碱两档夏普相同」)、DEC-110(纯碱/鸡蛋/生猪三档)。
那三处按本仓惯例保留为历史,**但都不是现行结论**。
**唯一现行来源是 `engine/hog_money.py` 里 VARIETIES 上方那张表**,
它已按本条重扫的实数改写,并注明 t 值未重算、旧 t 不许再引用。

玻璃、焦煤、生猪不受影响(玻璃 `long_needs_dip: False`,另两个做多本就关着)——
`backtest` 串逐项复核对得上,可以当交叉验证。

### 处置三:页面价格改成收盘价(运营者指定)

`main_series` 多带一列 `close`,payload 的 `price` 从结算价改成它,
前端标签由「现价」改为「收盘价」。

`close` 取 `clean_price` 里已按 DEC-073 处理过的 `px` 列 ——
郑商所对无成交合约会把收盘价写 0,那是「当天没成交」不是价格,用结算价兜底。

**只有这一处展示改口径,盈亏与成本一律仍走结算价。** 混用两种价会让页面上的数
与回测里的数对不上,那比标签写错更难查。


## DEC-112 鸡蛋进场信号换成机构成本;纯碱组合实验立项(2026-08-22)

运营者对策略的根本批评:现行进场是流量信号(5 日净持仓变化 z ≥ 1),
那是机构**大规模**进场的确认时刻,趋势往往已启动 ——「信号慢了」。
两个实验支撑了这个诊断:限价回踩(REPORT_LIMIT_ENTRY_v1)证明信号响了之后
愿意回踩的行情都是不走的行情;成本进场(REPORT_COST_ENTRY_v1)证明
按「价不劣于机构成本 + 机构没大规模减仓」进场,回撤全线砍三分之一到一半。

### 鸡蛋切成本进场(运营者拍板)

五个品种全测过,只有鸡蛋把五道闸门全过(REPORT_COST_GATES_v1):
逐年 3/4、选臂 walk-forward +42.8% vs +21.3%、收盘价源 1.34、
排除翻向日/换组余波仍赢、单笔 t +1.91。

实现(全部走已有管线,不另起炉灶):

- `RULES` 加 `cost_unload_max: 0.30`(**预注册值不是旋钮**,0.3→0.5
  五品种单调变差);`use()` 按品种注入 `signal_source`;
- 新函数三个:`inst_cost_series`(成本/方向/轮龄逐日重建:加仓按当日
  结算价加权、减仓不动、换组/翻向重置、掉榜冻结)、`cost_entry_frame`
  (纯函数,三条件 → cost_z ±1.5 与带数字的挡单原因)、`attach_cost_signal`;
- `entry_exit_signals` 加 cost 分支:**进场换,出场一字不动**(仍散户反向
  四件套)—— 出场换了闸门作废;
- 页面:挡单原因直接转述引擎(「价 3702 高于机构成本 3660,等回到成本再进」),
  策略方案页对 cost 品种换整套文案;前端 entry-gate 加 'cost' 路径并有测试
  钉住「不许再出现 z 门槛话术」(DEC-104 的病根);
- 配置:`long_needs_dip` 关(价不劣于成本本身就是不追高,再叠 dip 双重计数)。

**对拍**:引擎真实路径与闸门用的研究实现**逐笔一致**(34 笔),
其余四品种逐笔零变化(228/111/21/18 与修改前完全相同)。
测试 44 → 50(成本会计规则/挡单原因/出场未被换掉/配置接线各有钉子)。

如实记两点:①鸡蛋新口径 +48.9%/夏普 1.23/回撤 −8.9%,**首次**在夏普与回撤上
同时胜过「恒定满仓做空」(1.14/−20.1%),但绝对收益仍略低(+61.6%);
②34 笔、三年样本,t=1.91 未过 2 —— 页面 risk_flags 会继续挂「t<2 未验证」。

### 纯碱组合实验立项(运营者拍板)

纯碱 4/5 挂在选臂 walk-forward,挂法即发现:成本与流量两信号**逐年强互补**
(2021/22/25 成本大胜含躲掉 −34.8% 大坑,2023/24 流量大胜)。
预注册规格与判据已写死在 `research/PLAN_SA_COMBO_v1.md`,
执行前不许改判据。玻璃(2/5)不立项;「轮龄≥2 后玻璃 +218%」是跑闸门时
顺带看见的,已钉死为未预注册的观察,要用必须当新假设重走全套闸门。

### 验证范围备忘(运营者问「只验证了鸡蛋吗」)

五个品种都做了公平对比(REPORT_COST_ENTRY_v1);焦煤生猪实测不如现行,
未进候选;玻璃/纯碱/鸡蛋进了闸门,只有鸡蛋全过。散户单路
(2026-08-21 三路对比,纯碱/鸡蛋/焦煤强)**尚未走闸门**,按运营者排序
在成本信号之后。


## DEC-113 纯碱进场信号换成机构成本 —— 闸门 4/5,运营者知情破例(2026-08-22)

**这条与 DEC-112 的性质不同,必须分开记**:鸡蛋是五关全过;纯碱是 4/5。

成本信号在纯碱:72 笔 +93.1%/夏普 0.59/回撤 −36.7%(流量信号 111 笔
+47.0%/0.36/−61.8%),逐年 4/7、收盘价源 0.56、排除翻向日/换组余波仍赢、
t +1.49。**挂掉的是选臂 walk-forward**(−23.3% vs +47.0%):只用之前年份选
「流量 or 成本」,2023/2024 会选中成本臂,而那两年它让出 +90.0%/+20.8%;
另一面是 2025 它 +8.8% 而流量 −34.8%。组合实验(REPORT_SA_COMBO_v1)
按预注册跑完 1/4,不比成本单臂好。

我把这个结构如实摆给运营者:挂掉的那关不是技术性的,说的是
「事前没法判断今年该信哪条臂」,2023/2024 的让利是真实代价。
**运营者知情后拍板上线**:用回撤砍近半换「可能再遇 2023/24 那种年份」。

实现零新代码:`VARIETIES["SA"]["signal_source"] = "cost"`,其余全部复用
DEC-112 的管线(出场四件套不动)。对拍:引擎路径与闸门研究实现逐笔一致;
FG/JM/LH 逐笔不变,JD 维持 DEC-112。

**记账口径**:这是知情破例,不是「验证通过」。backtest 串与本条都这么写,
页面 risk_flags 照挂。如果某年纯碱成本臂明显跑输,回看本条时不许说「当时验过」。


## DEC-114 玻璃进场信号换成机构成本 + 还在加仓 + 轮龄≥2(2026-08-22)

六关全过(REPORT_FG_AGE_v1),**但来历是事后发现**:跑成本信号闸门时玻璃候选
只过 2/5,闸门 4 的「排除轮龄 <2」变体顺带跑出 +218%。按纪律先立项预注册
(PLAN_FG_AGE_v1),同一套五关 + 为事后假设加的第六关(参数面),再跑。

结果:120 笔 +218.1%/夏普 0.65/回撤 −31.6%/t +1.92(流量 228 笔
+34.9%/0.21/−67.9%);逐年赢 10/14、选臂 walk-forward +109.8% vs +34.9%
(只用往年数据事前就会选中,连选 8 年)、收盘价源 0.52、轮龄≥4/避开换组仍赢、
参数面轮龄 2/3/5 = 0.65/0.56/0.38 单调不翻脸。机制:进场时机构建仓中位 10 日
(流量 26 日),成本优势中位 +0.77%。运营者拍板「玻璃换」。

实现:引擎加两个按品种的成本选项 `cost_need_adding`(机构近 sig_win 日仍同向
加仓)与 `cost_min_age`(本轮已持仓 ≥N 日),默认关 —— 鸡蛋纯碱逐笔不变。
对拍:引擎路径与研究实现逐笔一致(120 笔);JD/SA/JM/LH 逐笔零变化。测试 51。

如实记:①记账标「事后假设复验通过」,证据等级低鸡蛋一档;②2014/2016/2022/2026
四年输基线;③轮龄 2 是边界最优(3、5 单调变差),**不是旋钮**;④玻璃流量信号
本是五品种最弱的(0.21),赢它门槛不高 —— 但回撤砍掉一半与事前选中是实打实的。

至此三个郑商所/鸡蛋品种全部切成本进场(JD 5/5、SA 4/5 破例、FG 6/6 事后复验),
焦煤生猪保持流量信号(实测成本信号在它们上更差)。


## DEC-115 席位持仓表每个合约后面带净持仓成本(2026-08-22)

运营者看席位持仓表(一家席位在一天的逐合约多头/空头持仓)时要求:
「把不同合约算出来的成本放后面」。

**放哪算、怎么算**:后端 `seats/positions` 响应新增 `costs[]`,对表里出现的每个
(品种, 合约)各跑一遍**净持仓页同一个成本引擎**(`build_cost_series`),
从该合约有记录起逐日推到所选交易日,取那一天的点。不在 SQL 里另拼一套成本逻辑,
也不在前端用当天的持仓和价格现算 —— 成本是路径量,只能由引擎从头推。
建仓接口里那段「建仓日 → 引擎输入」的映射顺手抽成 `daily_positions_from`,
两处共用(「两边没行 = 不在榜」「只缺一边按 0」只写一次)。

**口径必须写在表头上**:净持仓计价、多空不分开 —— 所以**一个合约一个成本**,
不是多头一个空头一个;表头写「成本·净持仓·推算」,单元格下面标「净多/净空 N 手」,
否则看的人会拿它当多头成交均价。成本为空时露出原因(不在榜 / 建仓日无结算价 /
那天没记录 / 仓位为零),不能只画横杠 —— 空是「不知道」,不是 0。

代价:一家会员一天几个到几十个合约,逐个查。自用面板可接受,换来的是零新口径。
Rust 测试 +2(取所选日那一点与引擎对拍;三态保持),前端 +1(成本列渲染与原因文案)。


## DEC-116 焦煤开做多(知情破例);「焦煤机构出场」立项(2026-08-23)

运营者指出焦煤 2701 机构早已全面做多而策略不做多。核实:机构组合计 6/10 转多、
8/5 起稳定 +6~9 万手,引擎 `side=net_long` 却 `long_enabled=False`。
「只做空」来自 2023-08 起三年基本熊市的样本 —— 做多那条腿没有历史可以验证自己,
是「没有证据」不是「证明不该做多」。

回放说明即便开了做多,现行信号在这波里也只吃到 +6.7%(8/10→8/17 后被交割纪律
强平;成本信号 8/4 被止损打出去后价格跑离成本再没进)。
出场体检(REPORT_EXIT_CAMPAIGN_v1)查明根因是「机构一轮」的测量在四个品种上
两三天一翻,拿不住不是规则问题是锚点问题;焦煤是唯一机构行为干净、
每个机构出场变体都赢的品种。

**运营者拍板两件**:
1. 焦煤开做多、不要 dip —— 零新代码,只改 `VARIETIES["JM"]`。回测上比只做空差
   (0.73 vs 0.91,回撤 −14.7% → −32.9%),**知情破例**,页面 risk_flags 照挂。
   引擎路径对拍 46 笔 +69.8%/0.73/−32.9% 与重扫一致;其余四品种不动。
2. 「焦煤机构出场」立项,预注册在 `research/PLAN_JM_INST_EXIT_v1.md`:主规格 B =
   机构翻向或卸仓>30% 才走(止损/交割保留,关散户翻向与持满),进场用新配置,
   五道闸门;来历是出场体检里挑出来的,记账标事后假设复验。


## DEC-117 焦煤出场换成「机构出场」;仓位动作文案按净持仓方向说(2026-08-23)

### 焦煤机构出场(运营者拍板)

PLAN_JM_INST_EXIT_v1 预注册后五关全过(REPORT_JM_INST_EXIT_v1):进场 = 流量共振双向
(DEC-116),出场 = 机构组方向翻转或本轮卸仓 >30% 就走,止损/临近交割保留,
**关**散户翻向与持满。72 笔 +244.0%/胜率 59.7%/回撤 −13.9%/夏普 1.79/t +3.13;
逐年 3/4、walk-forward +195% vs +70%、阈值 50% 仍 1.14、去抖 2 日仍 1.47。

**必须记住它赢的方式**:不是「拿得更久」,是「机构一松手立刻走、再上手立刻跟」的
高换手跟随 —— 均持有 9→4 日、笔数 46→72,回撤因此砍半。出场体检
(REPORT_EXIT_CAMPAIGN_v1)说明同一套出场在其余四品种全输,所以它**只能按品种开**。

实现:`RULES["exit_mode"]`(retail/inst)由 use() 按品种注入;`inst_exit_flags` 与研究脚本
的 flip | u30 逐字同构;`attach_inst_exit` 把触发列挂上 sig;replay 在 inst 模式下把研究钩子
(extra_exit / disable_reverse)接到生产配置上、跳过持满、理由记「机构出场」,缺列直接
ValueError 不许静默退化。对拍:引擎路径与研究 B **逐笔一致**(72 笔);其余四品种逐笔不变。
测试 54(+3),前端策略方案页出场文案按 exit_mode 分支。

丑话同报告:三年 72 笔、2025 一年贡献 +120%、换手翻倍实盘摩擦更多、事后假设复验。
DEC-116 开做多单看是让夏普 0.91→0.73、回撤翻倍的,**加上本条才成立** —— 两条是一套。

### 仓位动作文案(运营者当场指出)

页面「散户在减多 → 反向看涨」「机构在加空」此前都按 z 的正负说动作。焦煤实况:
散户净空 −12,454、5 日 −3,018(在**加空**)写成「减多」;机构净多 66,360、5 日 −294
(在**减多**)写成「加空」。改为按**净持仓方向 + 变化方向**说动作(加多/减多/加空/减空),
z 只管「反向看涨/看跌」那半句;无变化或缺数据退回老话术。前端测试钉住焦煤这组实况。


## DEC-118 生猪加做多腿:只由「机构净空且本轮卸仓≥50%」触发(知情破例,2026-08-23)

运营者假设「机构空了之后开始减仓,就会翻多」。实测(REPORT_LH_LONG_v1):
机构减空之后**有一周反弹**(卸仓首越 50% 后 5 日 +1.55%,t 2.4,对照无条件 −0.5%),
但 20 日归零、40 日回到下跌;机构真翻净多之后 40 日反而 −3.3% —— 三年单边熊里
「减空」是跌得慢不是会涨。三年只有 3~4 次事件。

做多腿回测**必须隔离**:第一版把流量 z≥1 的多单混了进去(多单均值 +0.61%);
隔离后只由「净空且卸仓≥50%」触发:14 笔均值 +1.06%/t 1.18、胜率 43%,
整体 32 笔 +117.4%/夏普 2.14/回撤 −8.4%,对只做空 18 笔 +87.4%/2.34/−4.2%。
**按预注册判据不过**(夏普降、回撤翻倍)。流量 z≥1 做多仍是 +0.02%/t 0.02,确实没东西。

**运营者知情拍板打开**,理由不在单边收益:它联动**生猪向上套利**(博合约反弹,
向上套利更安全),做多腿是给套利一个触发背景。记账:知情破例,不是验证通过。

实现:`RULES["long_mode"]`(flow / unload_bounce)与 `long_unload_min` 由 use() 按品种注入;
`attach_bounce_long` 把触发列挂上 sig(与 research/run_lh_long2.py 逐字同构);
`_apply_long_mode` 在进场信号上**压掉正值、注入反弹、做空优先**,缺列 ValueError;
挡单原因补「做多需机构净空且本轮卸掉≥50%(现状)」。生猪 `long_enabled` 开、dip 关。
对拍:引擎路径与研究隔离版逐笔一致(32 笔,胜率 56.2%);FG/SA/JD/JM 逐笔不变。
测试 57(+3);通用夹具基线钉 long_mode=flow / exit_mode=retail,品种专属模式各自测。

丑话:三年样本、做多 14 笔、t 1.18;2024 两笔(+9.1%/+3.9%)占多单利润大头。


## DEC-119 套利监控页生猪跨月组合挂「卸仓反弹窗口」背景(2026-08-23)

运营者问 DEC-118 那条做多信号对生猪套利「到底有没有用」。实测
(REPORT_LH_SPREAD_SIGNAL_v1,向上套利按牛市价差=买近卖远):
生猪跨月牛市价差**无条件是亏的**(20 日 −123 元/吨,t −7.2,涨的比例 37%);
在「机构净空且本轮卸仓 ≥50%」的日子里 20 日 +107 元/吨、涨 53%(t 2.4,106 天 / 25 段,
独立观察十几个);40 日归零。有用 —— 用法是**窗口过滤**,不是「越线就做」
(首次越线那一天与做多腿实际进场日都没信号)。窗口内价差波动约为单边的三分之二,
方向也更清楚;窗口外相反。

**做法:只显示,不进套利的任何判据**(与 DEC-087 FG-SA 资金流向同一性质)。
引擎 payload 新增 `bounce_long` 块(active / unload / side / min / note,只在
long_mode=unload_bounce 的品种上有);套利监控页生猪跨月组合旁挂「反弹窗口:开/关 + 原因」,
文案抽成 `bounce-hint.ts`(可测,掉榜不编数字)。

进场逻辑(给运营者看套利时用,页面不下单):窗口开才考虑生猪牛市价差,持有一个月内;
窗口关不做 —— 那时牛市价差是逆势。


## DEC-120 反弹窗口按所选交易日显示;套利页日期选择器点击即弹(2026-08-23)

运营者两条反馈:①套利监控页的日期选择器「有时候不弹」;②选了日期之后,反弹窗口
还显示机构**最新**状态 —— 他要看的是**那天**的机构状态,拿来手动验证信号。

①日期选择器原来 `editable` 且在「可选日期列表为空」时整体禁用:点进输入框是在文字里
放光标而不是弹面板,列表没回来那一瞬又是禁用态。改成 `:editable="false"`(点击即弹)、
只在历史信号模式下禁用(那个模式不按单日看);列表没回来之前面板能开、日子不可选。

②引擎 payload 新增 `bounce_history`:逐日 {d, active, unload, side},PIT 口径(当天可见),
728 行、106 天活跃(与 REPORT_LH_SPREAD_SIGNAL_v1 一致)。前端 `pickBounceDay(history, date)`
取最后一个 d ≤ 所选日的行 —— **每一行按它自己的交易日取**(历史信号模式下各行日期不同);
早于历史起点不编数据、不拿最新的顶;周末/节假日退到前一天时把「按 X 日机构状态」写出来。
测试 +3。


## DEC-121 套利监控页:生猪跨月价差低位拐头 10% 直接亮「⚡ 反弹进场」,不过合格门(2026-08-23)

运营者的大局判断:2026 是生猪磨底年,要做的是移仓换月带来的反弹(择时平仓,不拿到期),
五年样本不是这个问题的对照组。他要求先查「价差拐头」与「机构/散户减仓」有无关联,
没有就直接按拐头触发。

查了(REPORT_LH_TURN_ASSOC_v1):关联弱 —— 拐头日机构卸仓 5 日变化与结果相关 +0.24,
散户 +0.10;LH2611−LH2705 价差 7/24 触底、7/27 拐头,机构大规模卸仓(27%→61%)与散户
减多是 8/4~8/6 的事,**拐头领先减仓约 7 个交易日**,拿减仓当条件只会晚进。
2026 年 14 次低位拐头「直接进」20 日均 +22.8%、胜 93%;被「持到期 > 0」合格门拦掉的
10 次均 +24.4% 全胜,放过的 4 次里反而有一次 −25.5% —— 在磨底年,合格门在反向筛选。

**改动(只动前端,存事实不存结论的纪律不变)**:`isLhBounceEntry` = 生猪跨月 × 低位 ×
今天刚拐头 × 本轮首次穿线 × 未进红线,**不看合格门**;`isEntry = 合格进场 || 生猪反弹进场`,
计数/筛选/排序一并生效;徽标「⚡ 反弹进场 · 做多价差 · 择时平仓」用橙色与正规蓝 ⚡ 区分,
提示写明依据与出场口径(移动止盈 1/3,不拿到期)。

**如实记三点**:①这是按「2026 磨底年」的市场判断开的门,2025 同类事件 28 次好坏参半
(−114% ~ +97%),不是全样本验证 —— **年份一换要回头关掉或重验**;②14 次里同日同波的
多笔合并后独立观察约 8 个;③运营者三次指出我把「磨底年专用」理解成「全样本策略」,
这条的教训是:先问清问题的样本边界,再决定用什么数据回答。


## DEC-122 生猪席位组改固定 5 家(国泰君安/东证/东吴/永安/浙商),不再滚动重选(2026-08-23,知情破例)

运营者看到兴证期货(择时收益 1.61 亿、第 5 名压线)入选,质疑择时收益准则对重仓席位不公平
(小席位容易跑),要求「挑赚钱多、又能影响价格的大席位」。

**先核再测**(REPORT_LH_SEAT_PICK_v1):①恒定仓位取各家自己的平均仓位,择时收益是绝对金额,
小席位凑不出 1.6 亿,现行组平均净持仓 900~2100 手;最大三家总盈亏 9~10 亿但择时 ≈0,
赚的是熊市方向钱。②走前半年重选:择时收益合计 t 5.93,「大席位里挑赚钱多」4.15(K=5)/5.15(K=8),
2025 年后者强、2026 年打平,**无一准则在所有窗口占优,差距噪音级**。③运营者点名的固定名单用
引擎现行策略回放,近一年:滚动组 +69.1%/夏普 3.22/回撤 −8.2%(16 笔),固定 5 家 +64.2%/**3.94/−3.2%**
(10 笔 80%),固定 4 家(去永安)+57.0%/2.95/−11.7%;全样本固定名单只有 +29.5%/0.72/−28.0%
(滚动 +117.4%/2.14/−8.4%)。

**我的判定是不换**(近一年累计现行最高、全样本远胜、不靠后视挑人)。**运营者拍板用固定 5 家,
取回撤小的那版** —— 知情破例。

**改动**:`VARIETIES["LH"]["fixed_members"]` + `fixed_since`;`use()` 注入 `RULES["fixed_members"]`
(其余品种 None 照旧滚动);新函数 `fixed_groups()` 返回与 `rolling_groups()` 同形状
(逐日同一组、log 一条写拍板日、cuts 空);产物加 `group_mode: fixed|rolling`;前端席位组页/
怎么算的/席位持仓表三处文案按 group_mode 切换,固定名单页面上写明依据与全样本代价。
测试:引擎 +2(TestFixedGroup)、前端 +1。

**如实记三点**:①名单是按今天的认知挑的,回到 2024 年并不灵 —— **磨底年过去要回头重验**;
②换名单当天生猪状态从「做多中 11 日」变「观察中」:永安 1.6 万手空单未卸,组合计卸仓 37.5% < 50%,
8/6 那笔反弹做多在固定名单下不触发,套利页反弹窗口背景同灭 —— 部署前已告知;
③`backtest` 文案改为固定名单口径,旧的 +117.4% 保留在同一句里作对照。


## DEC-123 生猪页加「换月反弹提示」:主力剩 ≤22 日且近 20 日跌 ≥5% → 提示买次主力 X+2(2026-08-23,知情破例)

运营者框定只看 2026(磨底年):席位规则在 2026 单独评全负(REPORT_LH_2026_ROLL_v1 ②),
现行「卸仓≥50%」一次没触发;不一样的地方在**合约周期**——年内 −25.9% 几乎全在每个主力最后一个月,
到期前被砸狠(近 20 日 −11%)的 3~4 月、7 月周期,次主力 20 日 +3.9%/+4.2%;砸得温和的 5 月周期次主力 −4.2%。
规则「剩 ≤22 日 & 近 20 日跌 ≥5%」2026 触发 3 次 2 对 1 错(1 月 −5.2% 刚过线那次次主力 −4.4%)。
**运营者拍板「直接做 1」**:挂页面当提示,不进持仓、不进回测。

**改动**:`VARIETIES["LH"]["roll_bounce"]` = {since 2026-01-01, dleft_max 22, drop_min 0.05};`use()` 注入
`RULES["roll_bounce"]`(其余品种 None);新函数 `next_main_contract()`(月份 +2 跨年进位)与
`roll_bounce_payload()`(当前是否触发 + since 起每个主力首次触发的历史,带次主力结算价与后 20 日涨跌);
产物 `roll_bounce`;前端今日信号页首屏加灰/绿提示卡,写明条件、差多少、历史逐条结果与「不是全样本验证」。
测试:引擎 +3(TestRollBounce)、前端 +1。

**如实记**:①依据只有 2026 三次,其中一次错;门槛提到 −8% 可避开那次,但那是在 3 个点上调参,没做;
②与 DEC-121 同属按磨底年判断开的门,**年份一换要回头关掉或重验**;③研究脚本入库 `run_lh_2026_lows.py`、
`run_lh_2026_roll.py`;前一步全样本低点研究(REPORT_LH_LOWS_v1)结论不变:席位数据没有领先低点的指标。


## DEC-124 生猪做多腿只从 2026-01-01 起开,之前年份只做空(2026-08-23,运营者拍板)

运营者看历史信号表 13 笔做多 −24.6% 没变(DEC-123 只是提示卡,不进回测),问引擎有没有重跑;
我指出「卸仓≥50%」做多腿仍开着,关掉则只做空 20 笔 +74.0%/夏普 1.83/回撤 −8.0%(固定 5 家),
开着全程 33 笔 +29.5%/0.72/−28.0%。**运营者拍板:只开 2026 年的做多开关,前面年份不开** ——
2026 是磨底年,之前单边熊市;13 笔做多全在 2023~2025,2026 年这条腿一次没触发。

**改动**:`VARIETIES["LH"]["long_since"] = "2026-01-01"`;`use()` 注入 `RULES["long_since"]`
(其余品种 None = 全程);新函数 `long_allowed(d)`;`replay()` 进场处 want==1 且 d < long_since 则压成 0。
只挡做多进场;bounce_long 背景(套利页反弹窗口)、做空腿、出场四件套不变。前端策略方案页
做多段加一句「做多腿只从 2026-01-01 起开」。测试:引擎 +2(TestLongSince)。

**如实记**:①这是用起始日切掉历史上的亏损腿,**回测里的 20 笔 +77.5% 不含做多腿的任何历史表现**,
做多腿在 2026 的成色完全未知(0 次触发);②顶部 5 条「曲线不好拿住」警告随之消失,是因为亏的腿被
日期切掉,不是策略变好了;③与 DEC-121/123 同属磨底年专用,年份一换要回头看。


## DEC-125 焦煤席位组改固定 5 家(国泰君安/东证/永安/浙商/东吴),不滚动重选(2026-08-23,知情破例)

运营者要求焦煤也按生猪那套重筛、列「大席位里挑赚钱多」前 8(REPORT_JM_SEAT_PICK_v1):前 8 与现行
择时前 5 重叠 4 家,只差浙商(总盈亏第 3、择时第 6);中信期货 0.74 亿/中信建投 −4.95 亿不上榜(2025 起方向做反)。
**运营者拍板固定 国泰君安/东证/永安/浙商/东吴**(换华泰为浙商)。

同策略只换席位组回放:近一年 固定 +41.7%/夏普 1.85/回撤 −7.1% 对 滚动 +68.0%/2.35/−7.6%;2026 +23.1% 对 +30.8%;
全样本 +114.4%/1.23/−20.7% 对 +244.0%/1.79/−13.9%。**三窗口固定名单都弱**,按拍板上,如实记。

**改动**:`VARIETIES["JM"]["fixed_members"]/fixed_since`(复用 DEC-122 的 `fixed_groups` 通道,无新代码路径);
`backtest` 文案改固定口径并保留滚动数字对照;前端固定名单文案从 group_log 取成员、通用化(生猪/焦煤共用);
研究脚本通用化入库 `run_seat_big8.py`、`run_fixed_groups.py`。测试:引擎 64、前端 26 全绿。

**勘误(同日)**:上面「三窗口固定都弱」的对照里滚动组 2025-05~2026-04 是另一批人(含浙商、海通),
不是华泰换浙商的干净对照。同段同 4 家只换一人:近一年 含华泰 +46.4% vs 含浙商 +41.7%,34 笔相同,
差在 3 笔机构出场晚 1~2 天;浙商的 5 日变化无择时信息(t 0.19,华泰 2.32)但量级小。
**固定名单弱于滚动组的主因是失去「按年重选」,不是浙商。**(REPORT_JM_SEAT_PICK_v1 勘误节)


## DEC-126 焦煤席位组改回滚动按年重选(撤销 DEC-125,2026-08-23,运营者拍板)

DEC-125 勘误查明固定名单弱于滚动的主因是失去「按年重选」本身(同段同 4 家只换华泰→浙商仅差 5 个点、34 笔相同),
运营者「还是改回去吧」。`VARIETIES["JM"]` 去掉 `fixed_members/fixed_since`,backtest 文案还原并附一句试过又改回;
固定通道代码与测试保留(生猪仍用)。生产焦煤回到 东证/国泰君安/华泰/永安/东吴(2026-05-01 重选组)。

顺带查明**生猪 2026 做多腿为何一次没触发**:做多条件是机构净空且本轮卸仓 ≥50%;固定 5 家里永安持 −15,592 手
基本不卸,组峰值净空 −56,961(03-26)至今只卸 38%、年内最高 46%(08-07);原滚动组(含兴证、无永安)2026 卸仓
最高 81%(04-17)、bounce_long 为真 26 天。**是永安这一席把做多腿压住了**,不是引擎没跑。运营者知情,暂未改。


## DEC-127 生猪做多卸仓门槛 50% → 30%;套利页反弹窗口在固定名单下已失去数据支撑;日历弹不出来修复(2026-08-23)

**门槛**:固定 5 家里永安不卸,组 2026 卸仓最高 46%,50% 把做多腿关死。扫 25%~50%(run_lh_unload_min.py):
曲面不单调、噪音级 —— 45% 只多 8/6 一笔(一个点上调出来的数),40/35% 多放进 4 月亏的那笔,30% 有 6 笔
(2026 做多累计 +2.3%、胜 50%,整体回撤 −3.2%→−11.0%),25% 多出的是同一波拆单。**运营者拍板 30%**。
如实记:2026 做多腿在任何门槛下都在盈亏平衡附近。

**反弹窗口**:运营者问套利页这个信号是不是有问题。复验(REPORT_LH_SPREAD_SIGNAL_v1 勘误):原验证是滚动组+50%
(窗口内 20 日 +107 元/吨、涨 53%);**固定 5 家下 50% 窗口内 −315、涨 13%;30% −177、涨 29%,都不如无条件
(−123、37%)** —— 永安不卸使「卸仓比例」不再代表机构减空。引擎 `bounce_long.note` 改为如实文案(门槛用
配置值,写明已不优于无条件、待重验或下线),前端注释同步;**是否改按滚动组算或下线,待运营者定。**

**日历弹不出来**(两次报):Chrome 实测复现 —— 双击/连点输入框时 popper 的 el-zoom-in-top 进场过渡被打断,
卡在 enter-active、opacity≈0,面板 display:block 却看不见,几秒后才慢慢显出来。修法:`popper-class="spread-date-popper"`
+ 非 scoped CSS 关掉该 popper 的过渡(页内注入同样 CSS 实测立刻显示)。上一版 DEC-120 的 `:editable=false` 没治到点。


## DEC-128 套利页生猪反弹窗口改写:「已卸掉 xx%,反弹参考区间 10~26%,处于价差高位/低位」(2026 回测,2026-08-23,运营者拍板)

运营者要求回测 2026 套利组合,页面按「窗口开:机构净空且本轮已卸掉 xx%,反弹参考区间 xx~xx,处于价差高位/低位」写;
**我第一版把「参考区间」理解成「卸仓在哪个区间时之后价差涨」,运营者纠正:区间 = 一般卸仓多少时价差到最小**
(历次触底那天的卸仓比例范围)。REPORT_LH_UNLOAD_SPREAD_2026_v1(固定 5 家、17 次独立触底日):
最小 0% / 四分位 **10%~26%** / 中位 15% / 最大 33%;见顶 11 次中位 10%。**如实记:与 2026 机构净空日无条件分布
(8%~25%)几乎一样,卸仓比例标不出底部**(价差拐头领先机构减仓约 7 日,DEC-121);页面写区间是给人参考,不是信号。

**改动**:`bounce-hint.ts` `BOUNCE_REF`(10~26%、低位 <30%、高位 >70%)与 `pairPosBand`;`bounceHint(b, position)`
写「已卸掉 xx%,反弹参考区间 10~26%(在区间内/已超过/还没到历次触底时的卸仓比例),处于价差低位/中位/高位」,
低位 = 窗口开、其余关;套利页传 `item.pair.position`;引擎 `bounce_long.note` 同步;文案不再依赖引擎 `active/min`
(那是做多腿的门,DEC-127 30%)。第一版(10~20% 口径)提交过 5372f95 但未部署,本版覆盖。
**只有 2026 一年、约 5 个独立波段;磨底年过去要回头重验。**


## DEC-129 玻璃席位组点名换人:华泰 → 国泰君安,只管到下次重选;引擎加 group_overrides 机制(2026-08-23,运营者拍板)

运营者问国泰君安为何不在玻璃席位组:2025-10-01 重选时择时第 6(7.83 亿,差华泰 3 亿,2023 年亏 9.93 亿所致),
仓位全场第 2;截至今天已升到第 5(13.77 亿)。运营者要求换人并看 2026 收益变化,随后拍板「席位要换」。

**回放**(run_fg_swap_gtja.py):华泰→国泰君安(权重 1/0.5/0.3)**2026 都是 0 笔,与现行一致** —— 玻璃成本进场门
(价 ≥ 机构空头成本 & 卸仓 ≤30%)自 2025-10-01 起一次没开,而玻璃主力这段 −42%(2026 −28%):机构空单一直在赚钱、
价格始终在其成本之下,「不追」把整段下跌挡在门外。共振进场同期 +25.1%/2026 +11.6%(两组一样)。
**这一条比换谁更重要,已告知运营者:玻璃(大概率纯碱同理)的空头成本门在这轮趋势里是瞎的,待立项。**

**机制**:不固定名单(DEC-126 教训:固定弱的主因是失去重选),加 `VARIETIES[*]["group_overrides"]` =
[{since, replace:{旧:新}}],`apply_group_overrides()` 在滚动组之上替换,**只管到下一次重选切点**,新人已在组里则不动;
log 写一条 manual=True(括号里当时的择时收益),前端标「手动换人(华泰→国泰君安)· 到下次重选为止」。
玻璃 since 写 2026-08-21(最近交易日;拍板日周日)。测试 +3。

**如实记**:换人那天引擎把它当「机构本轮刚翻向」,卸仓从 0% 重新计、成本从当日重新推 —— 成本进场门很可能在两三天内
在 ~917 开一笔空(与每年重选切点的行为一致,不是 bug),已告知运营者。


## DEC-130 席位页:交易所未公布排名的日子补画 K 线、标「未公布」,与掉榜分开说(2026-08-23,运营者拍板两件一起做)

运营者看生猪 LH2607 席位页:6/23 之后 K 线与持仓整个消失,问是不是全掉榜了。查实:**大商所只对持仓量 ≥ 2 万手的合约
公布成交持仓排名**,LH2607 6/24 持仓跌到 19,583 手(<2 万)排名停发,席位数据断在 6/23;行情有到 7/22、散户窗口止点 6/30,
最后一周在页面上看不见。历史上每个合约都一样(LH2603 在 16,629 手断、LH2605 在 1,112 手断)。
K 线之所以跟着断,是 `build_net_position_series` 只补席位序列首尾之间的日历、尾巴之后一天不补(防无中生有)。

**改动**:domain 新 `mark_unpublished_and_extend_tail()`:席位序列末尾之后仍有行情的交易日补行(手数 0、missing=全部选中),
并把「库里任何席位都没有这个合约行」的日子标 `unpublished=true`;database 新 `seat_published_dates()`;API
`NetPositionDayItem.unpublished`。前端:未公布日净持仓留空(不画 0)、灰色底色带(与掉榜警示色分开)、小窗写
「交易所未公布(持仓量 <2 万手不发排名)· 持仓未知,不是掉榜」、掉榜天数不把它们算进去、页头加一段 2 万手规则说明。
测试:domain +2(17)、前端 +1(145)。

**口径**:「掉榜」= 榜在人不在(该席位不在前二十);「未公布」= 整张榜不存在。两者都不许画 0 当清仓,但要分开说。
**补记(同日)**:摘要与各家分腿的 as-of 日**跳过未公布的尾巴**,退到最后一个有榜的交易日(LH2607 → 06-23),
日期标签写出那一天;否则默认落在 07-22 显示「净持仓 0 手、计入 0 家、全员掉榜」,误导。domain +1 测试(18)。


## DEC-131 临近交割强平后不许原地续仓,除非触发新的进场信号(2026-08-23,运营者拍板,全品种)

运营者问生猪 8/17 为何触发做多:查实是**换月续仓** —— 8/4 按 30% 门槛开多 LH2609,8/14 临近交割强平(+6.89%),
同日做多条件仍成立(卸仓 38%、净空),引擎在新主力 LH2611 @12,425 续多。状态型进场信号(卸仓反弹、成本进场)
在强平那天多半还成立,原逻辑必然续上。运营者:不希望这样,除非触发新的进场信号。

**改动**:`RULES["rearm_after_delivery"]=True`(全局);`replay()` 在「临近交割」平仓后记住方向 `rearm`,
该方向的进场信号**至少消失一天**再出现才算新信号,期间同方向不进、反方向不受限。前端策略方案页加一条说明。
测试 +4(71)。回放影响:生猪 26→25 笔(去掉 8/17 那笔续多,当前持仓消失→观察中),玻璃 120→119 笔 +218.1%→+209.2%,
焦煤 72 笔 +244.0%→+226.5%,纯碱/鸡蛋无变化 —— 都是一两笔续仓的差别,如实记。


## DEC-132 席位净持仓小窗加逐家两条腿:悬停哪天看哪天各家多空单与成本推算(2026-08-24,运营者拍板)

净持仓页此前小窗只有合计;顶上「最新一天」那两行逐家明细只有 as-of 一天。运营者:把这两行也放进小窗,移到哪天显示哪天。

**改动**:API `SeatNetPositionResponse.member_series` = 逐家 {{member, legs[]}},legs 与 days **按下标对齐**,
字段名故意短(l/lc/s/sc —— 十家×几千天,长名字白白翻倍体积);掉榜/未公布日为 null。数据源就是已算好的
`member_days`(与摘要那排同一个 `build_variety_series`,不另起口径)。前端小窗「计入席位」行下逐家一行:
多 X 手 均价 Y · 空 Z 手 均价 W;掉榜写「掉榜,持仓未知」;交易所未公布的日子整段不列(那天谁都未知)。
前端 145 测试全绿;小窗 HTML 无组件级测试(ECharts formatter,挂载测试够不到),如实记。


## DEC-133 生猪策略整体切换为「逐合约战役」:左侧批次进场 + 聪明钱份额资格 + 卸仓30%快出(2026-08-24,运营者拍板)

缘起:运营者重述聪明钱框架(方向为王、左侧分批、成本为锚、跟随聪明钱仓位),连续七轮研究后在生猪上验证成立
(全程 `research/REPORT_DIP_COST_v1.md`,含焦煤先行与生猪纯验证)。运营者:「生猪明显超过了原来的策略,先上线」。

**策略**(`engine/campaign.py`,逐笔对拍门禁 `research/run_campaign_parity.py`,51/51 全等):
- 进场:每届合约每方向独立 —— 阵营(席位组内该合约净多/净空成员,可见口径 net_off)逢跌/亏着加仓,
  区间(隔≤3日并段)累计净加 ≥800 手确认;当日结算不劣于**批次成本**(区间加仓×结算加权,PIT);
  **份额资格**:该方向人格在本品种全部历史合约的累计战役盈亏 ≥ 对侧×25%(多头 +1.4 亿 对 空头 +35.7 亿,
  被它挡掉 —— 生猪多头侧是套保/接盘,逐届接飞刀)。次日开盘成交;一区间一笔;多仓并行。
- 出场:阵营|净|跌破自进场峰值 70%(机构出货第一脚)→ 次日开盘;或交割纪律。**无价格止损。**
- 手数阈值按品种规模缩放(阵营峰值中位 10,644/64,800 = ×0.164):add_min 150 / confirm 800。

**回测**(2023-08 起,51 笔):简单加总 +118.8pp / 逐笔复利 +200.5% / 逐年全正(2026 +5.9%×12)/
最差单笔 −4.1% / 出场分布 卸仓49+交割1。**超过组内最赚钱席位**(东吴 简单 +95.9%/复利 +122.7%)。
**丑话如实**:安慰剂 p=0.159 —— 做空方向本身贡献大头,进场择时的增量在 51 笔上不显著;实际效果等前向数据裁决。

**工程**:build_payload 按 `strategy="campaign"` 覆盖进出场/历史/统计/对比;机构流向卡、散户维度(降级纯展示)、
卸仓反弹、换月提示、席位组等展示维度全保留;payload 新增 `campaign{positions,watch,qual}`(多仓并行,
状态条仍单笔取最新);前端战役持仓/逐合约观察卡 + entry-gate campaign 分支 + 策略方案页文案整套换。
引擎测试 +7(78);前端 +3(148);顺手修掉生猪页 fetch 错误文案与 fetch-hint 正则不匹配的 bug
(「读取失败 403」→「HTTP 403」,否则状态码分流永远走不到)。其余四品种 payload 逐字段冒烟无变化。

**研究裁决留档**(同日,细节在 REPORT_DIP_COST_v1):散户到峰止盈(焦煤最优)在生猪 −132pp,判死,不迁移;
对手浮盈止血/换月反弹(焦煤四测)/资格过滤v1(≥0)均否;焦煤维持现行引擎不动,运营者说「等下再调整焦煤」。


## DEC-134 机构资金页加「一排合约小窗」:逐合约看各家持仓与成本,恒 5 个;组内各家撤成本列(2026-08-24,运营者拍板)

运营者看 DEC-133 上线后的生猪页:「成本不用显示了,没什么意义,不同合约价差比较大;下面做一排窗口,
显示不同月份合约这些主力席位的持仓和不同合约的持仓成本 —— 我们是多合约开战。到期看不到持仓就下掉,
换上最新的,一直保持 5 个。除了黄金,其它品种全部这样做。」

**改动**:引擎 `contracts_panel(seat, grp, d, prev_d)` —— 近 7 日内组内有行、未过窗口止点的合约,
按月份升序取前 5;逐合约给各家 net/change/on_board(与「组内各家」卡同口径)。挂进 payload
`contracts_panel`(可选字段,五个合计流向品种都有;金银另一组件天然不受影响)。
前端:组内各家撤成本列;新增 `.panel-row` 五窗一排(合约+剩N日+各家净持仓/变化/该合约成本),
成本按合约并发走 `seats/net-position`(Rust 成本引擎,一个事实一处维护),失败静默单窗缺成本。
引擎测试 79(+1)、前端 149(+2,旧「组内各家显示成本」测试按新形态重写)。

**顺带回答的运营者提问**(卸仓30%出场是否贴峰):49 笔卸仓出场里 55% 落在该笔峰值后 3 个交易日内
(43% 两日内、20% 当天),距峰中位 3 日、中位少吃 1.6pp;拖 8 日以上的两成几乎全是峰值 <1% 的
无行情笔。**赚钱的笔都贴峰出**,机构出货与价格拐头基本同步(`research/out/lh_exit_vs_peak.txt`)。


## DEC-135 生猪 campaign 加「跟批加仓」;历史表改按进场日期排序(2026-08-24,运营者拍板)

缘起:运营者看 LH2611 实盘——首枪 8/10 信号 @12,155 成交后,机构 8/14 @12,300、8/17 @12,415 又加两批,
其全仓均价抬到 12,3xx,我们的一枪被钉在**他们位置最差的第一批**上,成本反而劣于机构。生产数据核实:
阵营净空 8/10 12,484 → 8/11 峰 14,724 → 8/13 回吐 21.7%(未到30%)→ 8/14/8/17 两批加回 → 8/24 大跌里
再加 3,802 手创新高 15,333。**「分批」是运营者框架的固有环节,一枪版没实现它。**

**规则**(campaign cfg 加 `max_units: 3`):同一建仓区间内,机构每多攒够一个 confirm(800手)台阶算新一批;
新批出现时若价格仍不劣于批次成本、**且优于我们当前持仓均价**(做空=更高,只摊好不摊坏)→ 跟加 1 单位;
每战役最多 3 单位;出场不变(卸30%/交割全平)。运营者原话:「后续入场全是这种方式」——**先只上生猪,
其余品种他自己慢慢测方案再定**(金银永远不动)。

**回测**(引擎口径,逐单位连乘取均):50 战役 13 场多单位,加总 +131.7→+140.4pp、复利 +231.4%→+261.5%、
t 2.66→2.86、中位 +0.87→+1.38%、最差单笔 −4.1% 不变。**对拍:引擎与研究实现 51 场逐批成交日/出场原因
完全一致**;研究首版报的「15 场/+129.1pp」是脚本陈旧变量 bug(加仓分支引用了前一日的 ok 残值),作废勘误。
LH2611 重放:8/11@12,155 + 8/17@12,400 + 8/18@12,425,均价 12,327 与机构齐平——正是运营者要的形态。

**工程**:trades 加 `units/entries[]`,entry_px=均价;逐日等权矩阵按单位铺列;逐单位连乘均值==记账断言;
前端历史表/战役持仓表带「×N批」与逐批明细;**历史表排序改为按进场日期从先到后**(运营者指定;原来按流序
反转,多合约并行后看起来像按出场日乱排)。引擎测试 81、前端 149。


## DEC-136 生猪「移仓强制流压力表」上线:散户多头剩仓 → 近月承压,只显示不进判据(2026-08-24,运营者拍板)

缘起:运营者给出完整交易模型七点,点名先补第 5 点(流动性与移仓:谁必须交易/还有多少仓/剩多少时间/
能否承接)。预注册 `PLAN_ROLL_PRESSURE_v1` + 报告 `REPORT_ROLL_PRESSURE_v1`。

**研究结论(16 届,2311~2609)**:
- **机构版被否**:空头拥挤下交割前价差(近−次主力)平均在跌(dleft≤30:−1.69%,仅 25% 届上涨),
  机构剩仓与价差变动秩相关 ±0.1 —— 机构能买平近月+卖开次月同步移仓,不构成单边强制流。
- **散户版成立(主断言被否后的第二枪,证据等级降一档)**:真正"必须交易且无承接"的是散户多头
  (窗口止点强制离场)。dleft≤20 锚点:散户净多剩仓 vs 其后价差变动**秩相关 −0.53**;
  高剩仓组价差 **−3.14%(88% 届在跌)** vs 低剩仓组 −0.36%;散户没剩仓的两届(2409/2411)价差反涨。
  剩仓前六的届:−7.76/−5.30/−4.55/−3.88/−3.48%(反例 2609 +2.24%,磨底年反弹)。
  这正是换月反弹(DEC-123)从近月腿看的同一现象;载体天然是**空近月多次主力的跨期**(第 6 点的入口)。

**落地(只显示)**:引擎 `roll_pressure_payload`(散户剩仓/历届锚点四分位实算不写死/机构主导侧/
5:20 量比/价差现值/逐届历史),挂 hog_signals.json;前端 `roll-pressure-hint.ts`(统计数字只住引擎
note,不抄第二份),生猪页压力卡 + 套利监控页生猪跨月组合行内提示(只挂含近月主力那条腿的组合,
窗口内才显示)。**不进判据**:16 届样本、事后探索、2609 刚出反例;攒实盘再议升级为跨期信号。
引擎首版 bug 自查抓获:历届锚点误用"还是主力的时段"找 dleft≤20(那时主力早换人),16 届只剩 6 届,
改按合约自己的行情序列算。引擎测试 81、前端 152(+3)。当前读数:LH2611 剩 53 日未到窗口,
散户剩仓 5,392 手处历届中位。


## DEC-137 压力表升级为判据(⚡压力进场·做空价差);跨期表达=结构识别层,资格豁免版否决(2026-08-24,运营者拍板)

**① 压力进场判据**(运营者问「怎么进套利判据」后拍板):窗口内(近月剩 5~20 日)且散户净多剩仓
≥ 历届锚点 75 分位 → 亮「⚡ 压力进场 · 做空价差(空近月多次主力)」,每届一次,持到交割纪律日;
同窗口该组合**做多价差**信号挂 ⚠「散户强制卖压窗口」降级(不删只警示)。
**PIT 回测**(expanding 分位):触发 7 届 **+2.93%/胜 86%/最差 −2.24%**;未触发 5 届 −0.88%;
无条件基线 +1.34%。出场铃(泄压 30% 提前出)+2.77% 打平且 4/7 届不响,**不加**。
方向天然单边:被迫方永远是散户多头(散户天然站多,历史零净空样本,对称分支不做——运营者问过,已答)。
判据布尔在引擎(`entry_flag/suppress_long`),前端只渲染(DEC-104 规矩)。
丑话:7 触发 6 次集中 2025-26(时代效应)、上一届 2609 亏过;性质同 DEC-121 知情上。

**② 跨期表达(完整模型第 6 点)**:「资格豁免对冲腿」回测否决(`run_pair_hedge.py`:对冲腿 15 笔
−1.84%/笔,组合夏普 1.95→1.72,回撤未降)——**对冲腿也必须是聪明钱,份额资格不许豁免**。
落地为**结构识别层**:合格反向战役共存(空近+多远)时 campaign payload 标 `pairs`,战役持仓表
显示「跨期对冲结构」;生猪多头人格资格转正时自动生效,现为空集。逐腿仍各按自己阵营出场。
引擎 81、前端 152 测试全绿。


## DEC-138 散户接盘确认:进 campaign 展示当仓位分级,不当开关(2026-08-24,运营者拍板"改进三")

完整模型第 3 点缺口(「聪明钱加仓 + 散户站反方向 + 价格确认,才逐步加仓」)。
**回测(LH 50 笔战役,按进场信号日)**:D2 = 散户三家在该合约 5 日向对面加仓 ——
**有确认 25 笔 +4.96%/胜 68%,无确认 25 笔 +0.65%/胜 52%,差 +4.31pp/笔,t=2.29,逐年 4/4 同向**
(2026 年 +11.70% vs +2.39%);D1(散户净持仓在对面)46/50 笔为真,判别力弱。
**当硬门测过不划算**:首枪需 D2 → 26 战役均值 +4.11% 但加总 127.9→106.9pp、t 3.14→2.87
(被滤掉的仍 +0.65%/笔为正);跟批加 D2 门无差别(跟批时散户几乎都在接)。
**落地 = 展示 + 仓位分级**(运营者手动定仓位):watch 逐合约逐侧带 散户净持仓/5日变化/接盘✓;
战役持仓表带「进场✓/✗ · 现仍在接/未接」;trades 记 `retail_confirm`(25/50 与回测全等);
说明文案带实测数字。判定函数在引擎(campaign.retail_state),前端只渲染。
当前活体:LH2611 空单进场时散户未接(✗)——8/10 散户正在减多;今日(8/24)散户
大跌接多 +5,080 手,空头侧"现仍在接 ✓"预计亮起。


## DEC-139 焦煤第二引擎「跟华泰」上线:与现行引擎并列各管各仓(2026-08-24,运营者拍板)

缘起:运营者问「焦煤只跟一家席位来得及吗,比如东证」。五家全测(REPORT_JM_SINGLE_SEAT_v1)
→ 闸门验收(PLAN/REPORT_JM_HUATAI_v1)→ 运营者拍板上。

**为什么是华泰**:五家 T+1 方向跟随唯一逐年 4/4 全正(+167.7%/夏普 1.05);独立旁证 =
五家里唯一 5 日流向 IC 显著者(t=2.32,REPORT_JM_SEAT_PICK_v1 勘误**先于**本轮回测指认);
节奏在甜点上(同方向中位 3 天,T+2 跟随几乎不衰减 1.05→1.02)。东证 2 天一翻 T+1 跟不上
(+18.9%,2026 −28.5%),永安是仓位户(−36%)。

**定位 = 第二引擎,不是替换**:与现行引擎日收益相关 −0.07,50/50 组合夏普 1.82 >
现行单跑 1.73 > 华泰单跑 0.99——分散价值,不是更强的信号。两台并列、各管各仓,
分歧时天然净敞口≈0(降风险机制),不互相否决。

**闸门**:安慰剂 p=0.034;T+2 不衰减;品种合计口径同号;扣成本 +150.6%/0.99;逐年 4/4。
**三条丑话(页面照挂)**:①肥尾——66 段胜率 41%、利润的 130% 在 5 个长趋势段;
②五选一 Bonferroni p≈0.17,先验押在 IC 旁证;③2026 靠 6 月一波(+29.4%),5/8 个月为负。

**工程**:引擎 `seat_follow_payload`(零参数,统计逐次全量重算不写死),VARIETIES JM 配
`follow_seat`;前端第二引擎卡(方向/第N日/本段进场与浮动/扣成本回放/逐年/最近翻转/
翻向日 ⚡ 高亮"次日开盘反手")。规则文本同步:操作频率约每月 2 次。
**七点模型 × 焦煤对账**(运营者问):现状用到 3(共振进场)/4(机构出场快出)/7(机构撤即撤)
+ 部分 2(卸仓出场);本条补 1 的"识别可跟随聪明钱"维;空白 = 2 批次成本进场(当时 p 边缘
运营者暂停)、5 焦煤版压力表(未测,散户结构先验存疑)、6(相关路径已测否)。


## DEC-140:焦煤移仓压力表上展示级(只显示不进判据)(2026-08-24)

**运营者拍板**:「压力表上展示级吧」(REPORT_JM_THREE_GAPS_v1 汇报后)。

- **规则**:焦煤页出压力表卡(同生猪 DEC-136 形态):近月散户带符号净剩仓、
  历届锚点四分位、剩余日、level 高/中/低照标——但 `criterion=False`,
  **entry_flag 恒假,⚡永不亮**,窗口内不压制任何信号。
- **为什么只到展示级**(REPORT_JM_THREE_GAPS_v1):9 届样本(生猪 16 届),
  高剩仓组 100% 届在跌(−1.86%)方向同生猪机制,但秩相关仅 −0.28、≤10 日锚点
  翻号、PIT 判据无区分度(未触发届也 −1.04%,无条件做空价差全样本 +1.14%/届,
  赢的是焦煤近月交割前普跌的基流,不是筛选)。
- **焦煤差异两处**:次主力 = +4 个月(主力 1/5/9 序列,配置 step=4,别共用生猪
  +2 默认);散户在焦煤锚点历届全净多,但 **JM2701 当前净空 −8,921 手** ——
  若 11/12 月锚点仍净空即镜像分支(净空剩仓托近月→做多价差)首个活体,
  到时是攒样本,不是改规则。
- **实现**:引擎 `roll_pressure` 配置加 step/criterion 两键(生猪不受影响,
  默认 +2/True);前端 hint ⚡ 改认 `entry_flag`(DEC-104:判据在引擎算),
  展示级高位文案「近月对次主力承压(展示级,不进判据)」。
  引擎 81 测试过,前端 153(新增展示级用例)。

## DEC-141:玻璃第二引擎「跟永安」上线(2026-08-24)

**运营者拍板**:「上"玻璃跟永安"第二引擎」(REPORT_FGSA_MODEL_v1 汇报后)。

- **规则**(与 DEC-139 焦煤华泰同构,零参数):永安期货在当日主力合约上的可见
  净持仓(net_off,按合约 ffill)方向;收盘定、次日开盘反手;约 15 翻/年。
- **定位 = 第二引擎非替换**:与现行 FG 引擎(成本进场,DEC-114)日收益相关 +0.31,
  50/50 组合夏普 0.84 > 现行 0.64 > 永安 0.73,回撤 −26.4% 比两台单跑都浅。
- **为什么永安**:五家全测唯一过初筛(0.78/正年 11/14);14 年样本安慰剂 p=0.000,
  Bonferroni ×5 仍成立(强于焦煤华泰的 0.17);T+2 0.77 不衰减;扣成本 +760.7%。
- **三条丑话(note 照挂)**:①流向 IC t=1.72 不显著,信息在仓位水平,缺独立旁证,
  证据形态=超长样本回测极强、旁证弱;②2016 −34%、2022-23 连亏两年;③单跑回撤 −46%。
- **实现**:VARIETIES["FG"]["follow_seat"] 配 member+note(seat_follow_payload 的
  note 改为 cfg 可覆盖,焦煤不配沿用华泰默认文案;统计全实算);前端零改动
  (卡片本就动态读 member)。引擎 81 测试过。
- 上线时状态:永安**多头**,自 2026-08-17 @924,净 +65,544 手。

## DEC-142:玻纯「永安对冲簿」状态卡上线(展示级)(2026-08-24)

**运营者拍板**:「要上,套利监控的 FG-SA 组合页挂一张"永安对冲簿"状态卡」
(REPORT_FGSA_LINK_v2 汇报后;此前运营者纠正"价差不能直接交易"——可以买
郑商所套利指令 SP FG-SA,玻璃在前纯碱在后)。

- **规则(零参数)**:永安在 FG 主力与 SA 主力净持仓**反向**时(历史约 31% 天数)
  跟它的方向持价差(多玻空碱→做多价差,反之做空);同向→不在场。**展示级,
  不进判据**;与 pair_fgsa 资金流向并排(连续读数 vs 离散状态)。
- **证据(REPORT_FGSA_LINK_v2)**:+124.3%/夏普 0.77/回撤 −22.3%,安慰剂 p=0.020
  (两选一 ×2=0.040),**T+2 零衰减**,扣成本(0.2%/翻转)+51.5%/0.44。
  阵营版同规则已否(状态内 3bp vs 永安 20bp)——信号在单席位对冲簿不在阵营。
- **丑话(note 照挂)**:跨 v1+v2 五候选全族校正 p=0.10 = 知情上一档;利润前重
  后轻(2020-22 赚大头,近三年只是没亏);2023/2025 小亏年。
- **实现**:引擎 PAIR_EXTRA 小缓存(run_one 跑 FG/SA 时存永安主力净持仓/ret_open)
  + fgsa_hedge_book()(统计/在场段全实算,扣成本口径),挂 pair_fgsa.json 的
  hedge_book 键,算不出置 None 不拖垮 pair 信号;前端 SpreadMonitorView 只挂
  FG-SA 组合行,腿序守法同 fundFlowFor(是走扩还是做缩按 instrument_1 是否 FG 翻),
  老 JSON 无键不显示。引擎 81/前端 153。
- 上线时状态:**反向态(两边吃)第 2 日**,永安 FG2701 +65,544 / SA2701 −18,666
  → 做多价差,与 FG 第二引擎(DEC-141)同源同向。

## DEC-143:对冲簿/资金流向术语修正 + 两腿成本引净持仓引擎(2026-08-25)

**运营者拍板**(截图纠错后「改掉…效果图先给我看看」→「部署吧」):

- **多空词不是扩缩词**:多玻空碱=**做多价差**、空玻多碱=**做空价差**(玻璃恒前腿);
  "走扩/收窄"是价格相对 0 轴的位置词——**收窄=向 0 轴靠近**。原对冲簿卡写"做扩"、
  资金流向在价差 −132 时写"倾向走扩",两处方向感都错。
- **资金流向文案按 0 轴翻**:前端看当前价差在 0 轴哪侧决定收窄/走扩(fundFlowFor);
  引擎 direction 字段只说数值方向,note 写明定义。运营者问过"这行还有没有用",
  建议删(C2 测死过+权重感与过闸的对冲簿等同),**本轮保留修正版**,没明确拍板不删。
- **两腿 @成本引净持仓成本引擎**(运营者:「成本直接引用净持仓的成本,不需要你
  单独算」):前端带登录态调 /seats/net-position(contract=当前主力,members=永安),
  取 latest_members 对应腿 long_cost/short_cost——与净持仓页逐家那排**同源同数**
  (DEC-132 推算口径)。引擎不算成本(我先写了个"段内同向加仓×结算加权"的自算版,
  被运营者当场打断废弃——平台已有的口径不许另起炉灶,两处实现必然漂移)。
  取不到(掉榜/接口错)只不显示 @那截。
- **生猪移仓压力小字跟判据状态走**:⚡ 触发时"⚡已升判据·每届一次,持到交割纪律日",
  未触发"散户强制流,背景参考"——原句"非进场信号"与 DEC-137 自相矛盾(DEC-104 同款:
  升级判据忘了改周边旧文案,这是第二次,改判据时要 grep 周边所有引用它性质的文案)。
- payload 契约变更:hedge_book.direction widen/narrow→**long/short**,history.side 同;
  前端类型同步。引擎 81/前端 153/vue-tsc 零错;效果图经运营者过目后部署。

## DEC-144:「资金流向」行从套利监控删除(2026-08-25)

**运营者拍板**:「资金流向——删掉」(DEC-143 效果图确认后追加)。

- 删的只是 FG-SA 组合行上那条显示;**引擎照算 pair_fgsa.json 的 z/direction/note
  留档**(研究连续性),要挂回来只动 SpreadMonitorView 一个文件。
- 为什么删:它当仓位规则被测死过(REPORT_FGSA_LINK_v1 C2:91 翻/年会被磨死),
  与过闸的对冲簿并排都是红字、权重感等同,会稀释真信号;对冲簿不在场的时段
  本来也不做价差。恢复时记住 0 轴口径(DEC-143):收窄=向 0 轴,不能拿 z 符号直译。

## DEC-145:鸡蛋压力表上判据级 + 镜像分支(全平台首个双向 ⚡)(2026-08-25)

**运营者拍板**:「鸡蛋上压力表,这是价差的入场信号吗」——是,跨月价差入场判据,
按判据级上,双向。

- **规则(REPORT_JD_MODEL_v1,PIT expanding)**:窗口内(剩≤25 日)散户净多剩仓
  ≥历届 Q3 → **⚡做空价差**(空近月多次主力);净空剩仓≤历届 Q1 → **⚡做多价差**
  (多近月空次主力,镜像分支)。每届一次,持到交割纪律日。
- **证据**:27 届(样本最大);≤20/≤10 锚点秩相关同号 −0.32;触发 11 届
  +3.04%/胜 82%,未触发届仅 +0.41%(区分度真,焦煤的基流问题不存在);
  镜像 4 触发 3 胜(鸡蛋散户 19% 的届净空,生猪 0%——双向机制全平台首证)。
- **丑话(note 照挂)**:单轮测试;+10.2/+9.4 两届贡献大头;镜像仅 4 例;
  证据等级同 DEC-137 知情上。
- **工程差异**:鸡蛋主力序列不规则(28 届/3 年,+1/+2 月混)——历届次主力改用
  **真实主力序列继任**(生猪/焦煤下与月份算术等价,回归冒烟 LH/JM 分位与生产
  逐位一致);当前届不配 step,按 20 日均量在更远月选。window 25(≤30 档锚点
  实测秩相关 −0.04,开太早全是噪音)。
- payload 新增 entry_flag_long/suppress_short(老 JSON 无此键,前端可选读);
  note 改 cfg 可覆盖(同 DEC-141 模式)。前端:hint 加镜像 ⚡ 文案;套利监控
  rollAt 泛化为 LH+JD(焦煤展示级不进套利监控——行内位留给可操作信号)。
- 上线时状态:JD2610 剩 30 日未进窗口,散户净多 15,013 已超 Q3 8,419 ——
  **数日后进窗口若维持,⚡做空价差首亮**。引擎 81/前端 153。
- 附:研究途中发现 JD 行情 7/31、8/3、8/5 三天采集坏行(只有结算价+重复 3 行),
  引擎 main_series 静默跳过,信号那三天缺失;修复已开任务卡(spawn_task)。

## DEC-146:五窗面板括号变化改「较上一交易日」(2026-08-25)

**运营者拍板**:「要改成相对昨天变化,不要5日变化,鸡蛋那边测一下」。
起因:玻璃永安 FG2701 8/24 单日砍 13,390 手,五窗 5 日口径却显 +22,715
(8/17→8/24),当日动作被窗口平均掉,运营者以为与净持仓页对不上。

- contracts_panel 的 prev 从 sig_win(5)日前改为 mkt.index[-2](上一交易日);
  **「组内各家」摘要卡保持 sig_win 日口径,两卡自此有意不同**(摘要看趋势,
  五窗看当日动作),前端注释已写明别改回一致。
- 全品种统一(五窗是共用组件);鸡蛋数据冒烟:5 窗 change 与「今−昨」手算
  逐项对拍 0 不一致。掉榜日照旧 change=None 不显示。
- 引擎 81/前端 153。

## DEC-147:焦煤换月接力——交割纪律出场且机构无出货信号时,新主力同向接回(2026-08-25)

**运营者指出**:「关于移仓换月,临近交割,平仓趋势还没走完的单,有非常大的问题,
当时 8.17 日出场 09 合约,应该评估 2701 合约是否买入…当时机构完全没有出货信号。
你看看要怎么修复这个问题」。

- **机制诊断**:交割纪律是**日历事件不是观点**,但出场后引擎的进场端要等全新触发
  (机构仓早建完、没有新增量流),趋势没走完的仓接不回来。8/17 案:JM2609 多单
  纪律出场 +6.71%,当天机构零出货信号,2701 随后主升。
- **规则(replay 新增 roll_continue,默认关,焦煤开)**:交割纪律出场当天,若
  **没有任何真实出场理由**(机构未触发/未止损/未反向/未消退)且新主力可进有价 →
  次日开盘同向接回,记 rolled_from,视作同一轮持仓延续。
- **与 DEC-131 的分工(不冲突,131 原样保留)**:131 管**进场端**——状态型信号
  没断不算新进场信号;接力是**出场端**的延续,由"出场条件未触发"驱动。
  一句话:131 挡的是"信号还亮着就再开一笔新仓",147 修的是"机构没撤却因日历丢仓"
  ——七点第 7 条(机构撤=我们撤)的对偶命题:机构没撤=不该被日历撤。
- **回放证据**:三年仅触发 2 次(2025-04 一笔 −0.72% 很快被机构出场;8/17 案接力
  JM2701 多 @1528.5 持有中),+226.5%→+228.2%,夏普 1.73/回撤 −13.9% 不变——
  对历史近中性,属**机制修正**非信号主张,证据形态=逻辑正确+活体案例,如实记。
- 实现:默认关生产其余品种零变化(不变式测试钉住);前端历史表合约列加「接力」
  徽章(悬停注来源)。引擎 81/前端 153。仅焦煤开;鸡蛋(月月换、纪律出场更频)
  是否也开,等运营者看焦煤活体表现后另议。

## DEC-147 追加:鸡蛋也开换月接力(2026-08-25,运营者拍板)

回放:34→40 笔,+48.9%→+68.8%,夏普 1.23→**1.48**,回撤 −8.9%→−12.8%(加深一档,
来自 2025-11 连环接力那笔 −3.49%,如实记);8 笔接力 43% 胜率但大头 +4.74/+3.21/
+2.66 —— 鸡蛋月月换主力、纪律出场最频,是接力机制受益最大的品种。
活体:8/17 JD2609 空接力 JD2610 @3903,持有中。

## DEC-148:净持仓查询缓存——数据不动不重算(2026-08-25,运营者拍板)

**运营者**:「这个成本显示有个问题,刷新一遍就要重新计算,应该跟总览的那个图一样,
每天数据采集完成之后算完一遍,后续采集数据之前就不用重算了」。

- 总览图的机制就是 DEC-078 的 report_cache(版本没变就一直用这份)。本决定把同一
  设计套到 /seats/net-position:键=(工作区, instrument|members|contract|trade_date
  规范化查询串),值=(数据版本, 序列化响应);版本锚=最新席位交易日的
  report_data_version,采集落库版本即变、逐键失效重算。
- 五窗/品种页每天敲的是同一批键(恒定合约×席位组合),天然全命中——刷新不再逐次
  跑「席位×合约」成本推算。手工写端点清 report_cache 的两处一并清这份(统一失效,
  省一次"两个缓存为什么行为不一样"的排查)。条目量级几十(单人面板),不设淘汰。
- 序列化失败降级为原样直出,不拖垮请求。clippy 全绿。

## DEC-149:五窗括号改逐腿变化(多±X 空±X)(2026-08-26)

**运营者拍板**:「这后面的增减要改一下,写详细,比如+多单多少,减空单多少,
不然空单后面又是 −xx,实际是加了空单,会理解错」。中信 FG2701 净空 −103,508
括号 "(−11,988)" 实为**加空 11,988**,净数口径把加空读成"在减"。

- 引擎 contracts_panel 每家加 change_long/change_short(该合约多单/空单较昨日
  增减;某腿任一天掉榜或帧缺腿列 = 不可知给 None);change(净)保留兼容。
- 前端 panelChg:有腿数据显示「多+X 空+Y」(为零的腿不写);两腿都不可知才退回
  净数并标「净」前缀;颜色仍按净变化。底注同步。恒等式 净变=多变−空变 冒烟
  FG 全窗零差,测试夹具补腿列断言钉住。引擎 81/前端 153。

## DEC-149 二改:单数字随方向表述(净多±X/净空±X),两腿版否(2026-08-26)

一改的两腿版(多+X 空+Y)效果图给运营者过目后被否:「显示净多+4790就行了,
不要两腿显示」。改为:净多席位「净多±X」、净空席位「净空±X」——净空+11,988 =
空头仓位增了 11,988,方向词补上歧义即除,信息密度不增。change_long/change_short
留在 payload(引擎不回退),只是不显示。底注同步。

## DEC-150:历史信号页加第二引擎切换(2026-08-26)

**运营者拍板**(效果图过目后「可以」):「把华泰的历史信号放进去,标注华泰,
标清楚,单独做个第二引擎按钮,两个历史信号就不重叠,按钮放在(对比卡标题右侧)」。

- 主引擎历史页原样;「与恒定满仓做空比」卡标题右侧加 `第二引擎 · 跟{member} ▸`
  按钮(只有配 seat_follow 的品种出现:焦煤华泰/玻璃永安自动同款)。
- 点开整页切换到第二引擎历史:回放统计(累计/夏普/回撤/翻转+逐年,扣成本)+
  note 丑话原文 + **翻转段表**(方向/翻向日/出场=下一次翻向日/合约/进场价/段收益/
  持有,倒序最新在上;段收益为毛收益,口径差在脚注写明)。`◂ 返回主引擎` 切回;
  换品种归位主引擎视图。
- 引擎 seat_follow.history 12→40 段(今日卡仍取尾 8)。引擎 81/前端 153。

## DEC-150 二改:第二引擎历史页与主引擎全同规格(2026-08-26,运营者拍板)

「不要少做,要做得和第一引擎一样,出场价和进场价,所有的信号,还有底下的翻页,
全部一样」。改动:①引擎段记录加 exit_date/exit_px(=下一段翻向日的同一口开盘,
反手语义;尾段持有中为 None,链式恒等冒烟全过);history 40 段→**全量**;
②前端第二引擎视图补齐:全部/做空/做多三张统计卡(只算已平段,毛收益口径标明)、
出场价列、与主引擎同款 el-pagination(独立分页状态,换品种归位)。

## DEC-151:合约窗改版——全合约开窗 + 机构 vs 五大散户对照 + 沉淀资金(2026-08-28)

**运营者拍板**(样式效果图确认,追加「散户净多净空也要跟机构一样显示」):
「放到这里(五窗),把所有合约的这5大跟踪席位全部列出来,不只列这5个;界面要改,
每个月份合约小窗做长一点,席位后面直接跟手数成本,中间vs,这5大散户席位的持仓和成本」。

- **全部活跃合约开窗**(不再截 5 个),一行一窗做宽;活跃=近 7 日机构组**或**散户
  五家有行且未过窗口止点。
- 每窗行:机构席位 净持仓+(净多/净空±X,DEC-149 口径)+@成本 | vs | 散户席位同款
  三列。**五大散户席位 = RULES["retail_panel"]**(东方财富/方正中期/徽商/平安/
  中信建投,=运营者席位页「散户席位」收藏组);与 retail_seed(三家,进判据)是
  两份名单,判据名单不许动。
- **窗头沉淀资金** = 全市场持仓×结算×点值×保证金率;费率进品种配置
  (VARIETIES["FG"]["sink"]={18%,临近交割20%,near_days 22},与运营者行情软件
  2026-08-27 截图逐格对过);未配费率的品种显示名义市值并标明。量价缺任一不硬算。
- 成本仍前端引净持仓引擎(DEC-143),名单扩为机构+散户十家(DEC-148 缓存扛量)。
- build_payload 增 oi 参数(clean 后行情表 pivot);测试改断全窗+散户列+无量价
  sink=None。引擎 81/前端 153。上线后由 Claude 用 Chrome 亲眼验收(运营者指定)。

## DEC-151 补齐:四品种沉淀资金费率(2026-08-28,运营者供图「你看看」)

从运营者行情软件截图逐格反推(公式=持仓×现价×点值×费率,费率=交易所最低保证金
×双边):**生猪 16%/近月 20%、鸡蛋 14%/近月 20%、焦煤 24%(近月不抬,jm2609 剩
12 日实测仍 24%)、纯碱 16%/近月 20%**;每品种 2~3 个合约验证命中(LH2611 68.4亿/
jd2610 12.68亿/jm2701 139.4亿/SA2701 40.17亿 等)。至此五品种合约窗全部显示
「沉淀资金」同款口径,名义市值退场。

## DEC-152:合约窗加筹码地图(展示级)(2026-08-28)

**运营者定的规则**:「观察每天的波动,看机构席位的空单成本以及多单成本,不管多单
和空单,筹码要优于机构,尽量买到最优筹码位置,提高容错率」。五问五答:①锚取机构
最优成本 ②区间看 5 日最高最低价 ③不标仓位层数(自己定)④展示级 ⑤多空两套都显示。

- **带**(引擎 zone_band):低带 = 近 5 日**最低两天的低点**,高带 = **最高两天的高点**;
  高带=空进场/多出场,低带=多进场/空出场。两端都是盘面真出现过的价。
  **第一版用"高低差×20%"凑带宽被运营者当场否**(「筹码位置有点怪…可以看 5 日的
  最低最高价」)——教训:他说看某个价就用那个价,别拿百分比造中间值。
  两天同高/同低时前端只显示一个价。
- **锚**(前端 chipAnchor,成本走净持仓引擎 DEC-143):机构侧五家中,做空取成本
  **最高**者、做多取成本**最低**者;散户列只在 vs 表作对手参考,不当锚。
- **✓/✗**:现价与锚比 —— 做空现价≥锚=筹码优于机构,做多≤锚=优。锚缺失(该向全掉榜
  或成本不可知)不下判断。
- **展示级**:不进任何判据、不标层数。趋势市里带会离现价较远(如实反映那 5 天的
  真实价位),横盘时带贴近现价。
- build_payload 增 hi/lo/close 参数。引擎 81/前端 153。

## DEC-153:「组内各家」卡也用方向表述(2026-08-28)

**运营者**:「这里也标记净空或者净多」(截图指组内各家卡的 `-113,848 手 (-2,489)`)。
与 DEC-149 五窗同一个歧义:净空席位的括号负数其实是**减空**、正数是**加空**,
净数口径读反方向。改用同一个 panelChg:净多席位「净多±X」、净空席位「净空±X」。

**两卡口径仍有意不同**(DEC-146):组内各家=较 sig_win(5)日前,五窗=较昨日;
现在文案格式一样了,更要写明口径 —— 底注补「括号=较 N 日前变化…下面那排小窗是
**较昨日**,口径有意不同」。前端 153。

## DEC-152 二改:筹码带取"经常能到的位置",并列出逐日明细(2026-08-28)

**运营者**:「改成"经常能到的位置"(≈900~905)」。原口径取最低/最高的**两天**
(极值),FG2701 给 895~896,他手算是 900~905。

- **新口径**:去掉最极端那天,低带 = 第 2、3 低的低点,高带 = 第 2、3 高的高点。
  理由:最低/最高那天多半是插针,一周只摸一次,挂单等于不成交;次低次高才是
  「大部分时间都能到」的价位,容错率也高(运营者原话「基本都能到 900 附近」)。
  只有 2 天数据时退回极值两带,不硬造。
- **同时把 5 日逐日高低列在页面上**(zones.lows/highs,低点升序·高点降序):
  口径要不要再挪一档,他一眼就能指定是哪两天 —— 上一轮的返工正是因为我拿编的
  数字画效果图(DEC-152 教训),这次把原料直接摆出来。
- 引擎 81/前端 153。
