# 备份与恢复

## 范围

备份必须覆盖：

- PostgreSQL。
- 对象文件。
- 加密密文元数据。

主密钥恢复副本必须与数据库备份分开保存。

## Phase 1 状态

Phase 1 仅创建基础迁移和系统版本表，不创建大量业务表。

## 恢复检查

恢复后必须检查：

- 数据库迁移记录。
- 对象引用一致性。
- `kek_version` 元数据。
- Workspace 隔离。
- 审计完整性。

RPO、RTO、保留周期和异地位置仍记录在 `docs/OPEN_QUESTIONS.md`。
